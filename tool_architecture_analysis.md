结论：这个方向值得做，但真正降低数据清洗成本的不是“每个工具都是类”，而是“每个工具拥有唯一、机器可验证的输入/输出协议，并由运行时强制校验”。

如果只是把调用范式写进提示词，要求 AI“严格照着输出”，可靠性仍然有限；如果升级为 JSON Schema/Pydantic + 中央校验器 + 原生工具调用优先，这套架构会很稳。

## 当前项目状态

项目已经实现本文建议的主要协议，并分为三层：

- `core`：工具模型、注册表、单文件插件发现器、解析器、SQLite 元数据仓库、目录工具和统一执行运行时。
- `tool`：`BaseTool` 兼容导出、单文件工具模板与 `SearchTool` 具体实现。
- `agents`：OpenAI 兼容客户端以及模型—工具多轮编排。

当前实现以 Pydantic 输入/输出模型作为唯一 Schema 来源，执行前检查注册代次、版本、哈希、权限和副作用确认，支持依赖分层、并发限制及同步/异步超时。Agent 默认扫描受信任的 `tool/` 包，根据 `TOOL_ENABLED` 和 `create_tool()` 协议自动注册单文件工具，并保留结构化发现报告。Agent 还支持原生工具调用、严格函数 Schema、结构化错误恢复，以及可选的 `defer_tool_loading=True` 按需目录加载。

SQLite Repository 可由 Agent 注入并随工具注册同步；Catalog 只返回当前 Registry 中确实可执行且调用者有权限看到的条目。`.env`、`.venv`、缓存和构建产物均已被 `.gitignore` 排除。

## 你的方案的优点

### 1. 工具内聚性很好

一个工具类集中保存：

- 名称和用途
- 输入规则
- 输出规则
- 调用示例
- 执行逻辑
- 权限及副作用
- 超时和重试策略

新增工具时只需要增加一个类并注册，而不必同时修改 Agent、解析器和大量条件分支。

### 2. 能显著减少格式清洗

假设搜索调用统一为：

```json
{
  "type": "tool_call",
  "tool": "search",
  "arguments": {
    "query": "Python 3.12 新特性",
    "limit": 5
  }
}
```

运行时就不需要从“我觉得应该搜索一下……”这样的自然语言中猜参数，只需要：

1. 解析 JSON。
2. 验证 `tool` 是否存在。
3. 用对应 Schema 验证 `arguments`。
4. 调用工具。

这会消除大量正则匹配、关键词判断和特殊分支。

### 3. 可测试性明显提高

每个工具可以分别测试：

- 合法参数是否通过。
- 缺少字段是否拒绝。
- 多余字段是否拒绝。
- 类型错误是否拒绝。
- 执行异常是否转换成统一错误。
- 输出是否符合结果 Schema。

测试不需要真的调用模型。

### 4. 有利于观测、审计和权限控制

统一调用协议后，可以稳定记录：

- 哪个模型选择了哪个工具。
- 参数是什么。
- 执行耗时。
- 是否成功。
- 是否发生重试。
- 是否包含写文件、执行命令等副作用。

未来增加高风险工具时，还可以在执行前统一进行授权检查。

### 5. 有利于适配不同模型提供商

工具内部只关心规范化参数，模型提供商的差异由适配器处理：

```text
不同模型接口
      ↓
统一 ToolCall
      ↓
Schema 校验
      ↓
统一工具类
```

这样不会让每个工具分别适配 OpenAI、其他兼容接口或本地模型。

## 主要缺点和风险

### 1. “提示 AI 严格输出”不等于真正严格

即使给出完美范式，模型仍可能输出：

- JSON 前后的解释文本。
- Markdown 代码块。
- 单引号、尾随逗号等无效 JSON。
- 错误字段名。
- 字符串形式的数字。
- 不存在的工具名。
- 正确格式但语义错误的参数。

因此，严格性必须由程序保证，不能由提示词保证。

建议采用：

```text
模型生成
  ↓
JSON 解析
  ↓
Schema 严格校验
  ↓
权限/语义校验
  ↓
执行
```

校验失败时可以把结构化错误返回模型，允许有限次数修正，但绝不能带着未经验证的参数直接执行。

### 2. 工具类容易变成“万能类”

如果一个类同时负责：

- 提示词生成
- Markdown/JSON 解析
- 参数修复
- 权限判断
- 网络调用
- 重试
- 日志
- 输出格式化

工具类会迅速膨胀。

更合理的职责分离是：

- `ToolSpec`：工具的静态协议和元数据。
- `BaseTool`：执行经过验证的参数。
- `ToolRegistry`：注册、查找工具。
- `ToolCallParser`：解析统一调用信封。
- `Validator`：输入输出校验。
- `AgentRuntime`：负责完整调用循环和权限策略。

尤其不建议保留现在这种 `parse_args(query: str)`：工具不应该自己解析整段模型文本。统一协议应该只解析一次，然后把 `arguments` 交给工具对应的数据模型验证。

### 3. Schema、范式和函数签名可能形成三份真相

如果同时维护：

- `args_schema`
- 提示词里的调用范式
- Python 函数参数

三者很容易不一致。

例如 Schema 把 `limit` 定义为整数，但范式示例使用 `"5"`，函数默认值又是 10。

最好让类型模型成为唯一真相，并自动生成：

- JSON Schema
- 调用说明
- 示例文档
- 运行时验证逻辑

可以使用 Pydantic 严格模式，或者 JSON Schema 验证库。不要手工维护重复协议。

### 4. 工具很多时，上下文成本会上升

如果每次请求都把所有工具的完整说明、Schema 和示例发给模型：

- Token 消耗增加。
- 工具之间更容易混淆。
- 选择准确率可能下降。
- 首次响应变慢。

你提到“AI 想调用某个工具时再读取范式”，思路是对的，但存在一个先有鸡还是先有蛋的问题：AI 必须先知道有哪些工具及其大致用途，才能决定读取哪个范式。

可以采用两级暴露：

1. 首先只提供工具名、简短描述、类别和风险等级。
2. 选中候选工具后，再加载完整输入 Schema 和示例。

工具不多时则可以直接全部提供。

### 5. 多工具调用和连续调用会让协议复杂化

以后很可能出现：

- 同时搜索多个关键词。
- 先搜索，再抓取页面。
- 一个工具的结果作为另一个工具的输入。
- 并行调用多个工具。
- 工具调用后发现参数不足，需要询问用户。

协议需要提前考虑：

- `call_id`
- 并行调用列表
- 调用结果关联
- 最大调用轮数
- 循环检测
- 超时
- 幂等性

这些不应该由各个工具类自行实现，而应交给 Agent 运行时。

### 6. 工具结果本身仍然可能很脏

严格的调用格式只解决“AI 到工具”的参数清洗，没有解决“外部服务到工具”的结果清洗。

例如搜索 API 可能返回：

- 缺少标题。
- URL 格式异常。
- 不同类型的时间字段。
- HTML 片段。
- 不可信网页指令。
- 服务商特有字段。

因此工具还应定义 `output_schema`，把外部原始结果转换成稳定的内部格式后再返回 Agent。

## 推荐的数据结构

建议把静态定义与执行实例分开：

```python
class ToolSpec:
    name: str
    description: str
    version: str
    input_schema: dict
    output_schema: dict
    examples: list[dict]
    side_effect: str
    timeout_seconds: int


class BaseTool(ABC):
    spec: ToolSpec

    @abstractmethod
    async def execute(self, arguments: dict) -> dict: ...
```

搜索工具的输入协议可以是：

```json
{
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "minLength": 1
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 20
    }
  },
  "required": ["query"],
  "additionalProperties": false
}
```

`additionalProperties: false` 很重要，它能阻止模型随意添加字段。

工具结果也使用统一信封：

```json
{
  "type": "tool_result",
  "call_id": "call_123",
  "tool": "search",
  "ok": true,
  "data": {
    "items": []
  },
  "error": null
}
```

`call_id` 最好由运行时生成，而不是依赖模型生成。

## 推荐调用流程

```text
用户请求
   ↓
Agent 获取工具目录
   ↓
模型决定直接回答或选择工具
   ↓
读取选中工具的完整协议
   ↓
生成结构化 ToolCall
   ↓
中央解析器解析 JSON
   ↓
输入 Schema + 语义 + 权限校验
   ↓
执行 Tool.execute(arguments)
   ↓
输出 Schema 校验及规范化
   ↓
ToolResult 返回模型
   ↓
模型生成最终回答
```

如果模型提供商支持原生工具调用或受约束的结构化输出，应优先使用；普通 JSON 文本协议可以作为兼容本地模型和其他提供商的后备方案。当前 OpenAI 适配遵循官方 function-calling 循环，并为函数定义启用严格 Schema。

## 最终评价

可以把这个方案拆成两个判断：

- “所有工具都实现统一的类接口”：非常合理，适合这个项目。
- “只让 AI 阅读范式并承诺严格输出”：不够可靠，不应作为执行安全边界。

我会推荐采用“类封装 + Schema 唯一真相 + 中央校验 + 统一调用信封 + 原生工具调用优先”的版本。这样数据清洗不会完全消失，但会从脆弱的自然语言清洗，转化为确定、可测试、可审计的结构化验证。

对 Agent 系统而言，这是非常值得的转换。

可以，这本质上是“动态工具发现 + 按需加载工具协议”。当工具数量较大、协议较长或工具经常变化时，比把所有 Schema 一次性塞进 Prompt 更合适。

但建议把“数据库查询工具”设计成受控的工具目录服务，而不是允许模型直接生成 SQL。同时，调用顺序必须由程序状态机强制执行，不能只在 Prompt 里要求模型“先查询再调用”。

## 它解决了什么问题

### 1. 降低初始 Prompt 大小

原方案每次请求的工具成本近似为：

```text
基础 Prompt + 所有工具描述 + 所有工具 Schema
```

动态查询后变成：

```text
基础 Prompt + 工具目录查询协议
          + 本次实际使用工具的 Schema
```

假设有 100 个工具，而一次任务只需要 2 个工具，那么剩余 98 个工具的完整协议不必进入上下文。

不过它降低的是“不相关工具的 Token”，并不意味着总 Token 一定更低，因为会额外增加：

- 一次工具目录查询。
- 一轮或多轮模型请求。
- 查询结果。
- 最终仍然需要加入所选工具的完整 Schema。

因此，工具越多、单个 Schema 越大、每次实际使用的工具越少，这个方案越划算。只有十来个简单工具时，直接提供全部工具通常更简单、延迟也更低。

### 2. 降低工具选择混乱

一次给模型几十个结构相似的工具，容易出现：

- 选错工具。
- 混淆相似参数。
- 把 A 工具的字段用于 B 工具。
- 注意力被大量无关说明分散。

按需返回少量候选工具，可以让模型集中处理当前相关的工具。

### 3. 支持工具动态更新

工具信息进入数据库后，可以集中管理：

- 启用和禁用。
- Schema 版本。
- 描述更新。
- 权限要求。
- 工具分类。
- 所属插件或服务。
- 调用统计。
- 更新时间。

新增工具后不必修改基础 Prompt。只要注册成功，目录查询就可以发现它。

### 4. 可以实现最小权限暴露

工具目录可以根据当前用户、Agent、租户或运行环境过滤结果。例如普通用户根本查询不到文件删除、命令执行等高风险工具。

这不仅节省 Prompt，也减少模型误用高风险工具的机会。

### 5. 有利于审计和治理

数据库可以记录：

- 谁注册了工具。
- 哪个版本覆盖了哪个版本。
- 哪个 Agent 查询过工具。
- 模型拿到了哪个版本的 Schema。
- 调用了多少次。
- 成功率和平均耗时。

这比单纯的内存字典更适合规模化运行。

## 最重要的设计问题：模型怎么知道要查询什么

如果 Prompt 中只有数据库查询工具，却完全没有工具名称和用途，模型不知道应该查询 `search`、`weather` 还是 `read_file`。

因此，不能要求模型必须知道准确的 `tool_name`。查询工具至少应支持按自然语言意图搜索：

```json
{
  "action": "search",
  "intent": "搜索互联网上关于 Python 3.12 的资料",
  "limit": 5
}
```

返回简短候选：

```json
{
  "candidates": [
    {
      "tool_name": "web.search",
      "summary": "搜索互联网公开内容",
      "schema_version": "2.1"
    },
    {
      "tool_name": "docs.search",
      "summary": "搜索内部技术文档",
      "schema_version": "1.4"
    }
  ]
}
```

选中后再查询完整协议：

```json
{
  "action": "get_spec",
  "tool_name": "web.search",
  "schema_version": "2.1"
}
```

也可以将两步合并为一个 `resolve` 操作，直接根据意图返回最相关工具的完整协议，减少一次模型往返：

```json
{
  "action": "resolve",
  "intent": "搜索互联网公开资料",
  "limit": 3
}
```

对于当前项目，我更推荐一个 `ToolCatalogTool`，通过 `action` 区分 `search`、`get_spec` 和 `resolve`，而不是建立多个数据库工具。

## 推荐调用流程

```text
用户请求
   ↓
   LLM 只看到 ToolCatalogTool（启用 defer_tool_loading=True）
   ↓
LLM 按意图查询工具目录
   ↓
目录返回候选工具及简短描述
   ↓
选中工具并取得完整 Schema
   ↓
运行时把该工具标记为“本会话已加载”
   ↓
LLM 生成正式工具调用
   ↓
运行时检查：是否加载、版本是否一致、参数是否合法
   ↓
执行工具
   ↓
验证并规范化工具结果
   ↓
结果返回 LLM
```

这里必须特别强调：“是否先查询过工具”要由运行时记录和检查，而不是相信模型遵守 Prompt。

可以把运行时设计为严格状态机：

```text
DISCOVERED
    ↓
SPEC_LOADED
    ↓
CALL_VALIDATED
    ↓
AUTHORIZED
    ↓
EXECUTED
```

模型如果跳过 `SPEC_LOADED` 直接调用工具，运行时应拒绝执行，并要求重新查询协议。

## 数据库中应该存什么

“调用范式”最好不要只存一段自然语言或 JSON 示例。示例不能完整表达必填字段、类型、范围和互斥关系。

建议至少存储：

```text
tool_name
namespace
summary
description
schema_version
schema_hash
input_schema
output_schema
examples
tags
aliases
enabled
side_effect
permissions
timeout_seconds
idempotent
implementation_ref
owner
updated_at
```

其中最重要的是：

- `input_schema`：输入规则。
- `output_schema`：工具输出规则。
- `schema_version`：版本判断。
- `schema_hash`：防止查询后被悄悄替换。
- `side_effect`：只读、写入、删除、外部提交等。
- `permissions`：哪些 Agent 或用户可以调用。

示例只能帮助模型理解，Schema 才是校验依据。

## 不建议数据库保存可执行代码

数据库适合保存工具协议和索引，不适合直接保存 Python 代码并动态执行。

建议保留两个部分：

```text
数据库目录：
tool_name → ToolSpec、版本、权限、描述

运行时注册表：
tool_name → BaseTool 实例或执行端点
```

执行时需要同时满足：

1. 数据库中存在并启用。
2. 当前进程中存在对应执行器。
3. 执行器声明的 Schema 哈希与数据库一致。
4. 当前调用者拥有权限。

否则可能出现“数据库里有工具，但实际代码没有加载”或者“数据库 Schema 是新版，运行中的代码还是旧版”。

## 重复工具直接覆盖的风险

“重复则覆盖”可以实现，但不建议静默覆盖。

例如一个工具已经注册为：

```text
web.search 2.0
```

另一个插件错误地使用同名工具，就可能把真实搜索工具替换掉。更严重时，这可能成为工具劫持入口。

推荐：

```python
registry.register(tool, replace=False)
```

只有明确更新时才使用：

```python
registry.register(tool, replace=True)
```

即使允许自动覆盖，也至少应做到：

- 数据库 `tool_name` 唯一约束。
- 使用事务完成更新。
- 记录旧版本、新版本和注册来源。
- 计算 Schema 哈希。
- 覆盖后清除缓存。
- 已经加载旧版本的会话继续使用旧版本，或者被要求重新查询。
- 不允许低权限插件覆盖系统工具。
- 使用命名空间，例如 `web.search`、`filesystem.search`。

仅以 `search`、`read`、`send` 作为全局名称，后期很容易冲突。

## 解决 Prompt 大小后仍然存在的问题

### 1. 额外延迟

一次普通工具调用可能由原来的一轮变成：

```text
LLM 查询目录
→ LLM 获取协议
→ LLM 调用目标工具
→ LLM 处理结果
```

多工具任务会进一步放大延迟。

因此，不建议“每次调用都重新查询同一个工具”。更合理的是：

> 每个会话在第一次调用某个 Schema 版本前查询一次，之后缓存；版本变化时重新查询。

缓存键可以使用：

```text
conversation_id + tool_name + schema_version + schema_hash
```

### 2. 工具目录会成为单点故障

如果数据库不可用，模型就无法发现任何工具。

需要：

- 本地缓存。
- 超时控制。
- 只读副本。
- 基础工具的降级配置。
- 数据库异常时明确返回结构化错误。

单进程原型阶段其实不必马上使用复杂数据库。SQLite 加内存缓存已经足够；需要多实例部署时再换成中心数据库。

### 3. Schema 和执行代码可能漂移

如果开发者修改了 Python 参数却没有更新数据库，模型获取的范式会与实际工具不一致。

不要手工同时维护两份定义。应当以工具类中的类型模型为唯一真相，在注册时自动生成并写入数据库：

```text
Python 输入模型
    ↓ 自动生成
JSON Schema
    ↓ 注册
数据库目录
```

### 4. 严格输出问题仍然没有消失

数据库只能让模型“拿到正确的范式”，不能保证模型一定遵守。

仍然需要：

- JSON 解析。
- Schema 严格校验。
- `additionalProperties: false`。
- 工具名白名单。
- 字段范围校验。
- 语义校验。
- 有限次数纠错。
- 执行前权限判断。

当前实现已经使用原生工具调用或标准 JSON 解析，并通过 Pydantic Schema 做中央校验；不再依赖正则提取 Markdown JSON。

### 5. 工具结果仍然需要清洗

查询协议只规范了：

```text
LLM → 工具
```

没有规范：

```text
外部 API → 工具 → LLM
```

每个工具还需要 `output_schema`，由工具把第三方原始结果转换成统一内部结果。否则搜索、网页、数据库等工具仍然会返回格式不稳定的数据。

### 6. 数据库内容也可能成为提示注入来源

工具描述、示例和外部结果最终都会进入模型上下文。如果任何用户都能注册工具，就可能写入类似“忽略之前指令”这样的恶意描述。

因此：

- 只有可信主体可以注册工具。
- 描述字段长度受限。
- 不允许任意 Prompt 指令。
- 注册来源需要签名或权限验证。
- 不向模型开放任意 SQL。
- 查询工具只提供固定的参数化操作。

### 7. 旧协议和覆盖竞态

可能发生：

1. 模型查询到 `web.search 2.0`。
2. 管理员覆盖为 `web.search 3.0`。
3. 模型仍按 2.0 生成调用。
4. 运行时按 3.0 执行。

所以正式调用应携带版本：

```json
{
  "type": "tool_call",
  "tool_name": "web.search",
  "schema_version": "2.0",
  "schema_hash": "abc123",
  "arguments": {
    "query": "Python 3.12"
  }
}
```

运行时发现版本不一致时，不执行，要求重新取得协议。

## 对当前项目的具体建议

上述基础能力已经落地。后续建议集中在生产化能力：

1. 增加持久化审计事件和调用指标。
2. 为 Registry/Repository 同步失败增加事务式补偿。
3. 按部署方式明确多进程、跨事件循环的全局限流策略。
4. 增加更多真实 OpenAI 兼容服务的契约测试。
5. 对外部搜索结果增加 URL、内容类型和提示注入边界策略。

整体上，这套数据库目录方案能够很好地解决“大规模工具的上下文膨胀、工具发现、动态更新和集中治理”。但它没有自动解决格式可靠性、安全、版本一致性和输出清洗。最稳妥的方案是：

> 数据库负责发现，Schema 负责定义，运行时负责强制约束，工具类只负责执行。

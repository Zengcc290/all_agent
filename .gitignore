.env
.env.*
!.env.example
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.db
*.sqlite3
!update_log.sqlite3
dist/
build/
*.egg-info/
.vscode/
.idea/
*.log
.DS_Store
Thumbs.db

# Local provider profile may contain plaintext API keys. Publish only the
# sanitized provider.example.toml template.
config/provider.toml
config/providers.toml
!config/provider.example.toml

# Local services profile (embedding/search/Qdrant/Neo4j) may contain plaintext
# credentials. Publish only the sanitized services.example.toml template.
config/services.toml
!config/services.example.toml

# One-off migration backups (scripts/migrate_storage.py, scripts/migrate_to_cloud.py)
*.bak.pre_migration

# Editor metadata / attachments
.opensquilla/

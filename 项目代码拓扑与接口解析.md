# all-agent / 知识星云：项目代码拓扑与逐函数接口解析

> ⚠️ **本文档已部分过期（快照于技能包/文件工具簇删除之前）。**
>
> 以下内容描述的模块**已从仓库删除**，对应章节只作历史参考，不要据此定位代码：
>
> - **技能包子系统**：`core/skill_catalog.py`、`core/skill_discovery.py`、
>   `core/skill_models.py`、`core/skill_registry.py`、`skills/*.md`（6 个文件），
>   以及 `agents/agent.py` / `agents/react.py` / `core/__init__.py` / `constants.py`
>   里的相关接线（`Agent.skills`、`discover_skills`、`_with_registered_skill_names`、
>   `ReActAgent._skill_directory_lines`、`ReActAgent.SKILL_VIEW_REACT_INSTRUCTIONS`、
>   `system.skill_catalog`、`DEFAULT_SKILLS_ROOT` 等 10 个常量）。
> - **`fs.*` 文件工具簇**：`tool/fs_read_text.py`、`tool/fs_read_dir.py`、
>   `tool/fs_read_file_glob.py`、`tool/fs_write_text.py`、`tool/fs_edit_text.py`，
>   以及 `tool/_shared.py` 中的 `read_text_file` / `atomic_write_text` /
>   `run_process` / `run_git` / `cap_text` / `normalize_encoding` 等助手
>   （`tool/_shared.py` 现仅保留 `workspace_root` / `allow_outside_workspace` /
>   `resolve_path` / `_is_within`，供 `tool/rag_tool.py` 使用）。
>
> 同时被删除的还有 `memory/` 的一批死代码（`MemoryItem.from_dict`/`event_time`、
> `WorkingMemory.set/get_by_key/get_value`、`PerceptualMemory.store/get_data`、
> 各存储的 `clear()`、`DocumentProcessor.chunks`、若干死常量）。
> 工具面板因此从 17 个收敛到 11 个。统计数字（69 个实现文件、626 个函数）也已随之变化。
> 需要精确现状请重新生成本文档。

> 本文档按**真实导入依赖**推导出的拓扑顺序，逐层、逐文件、逐函数解析项目实现代码。
> 范围：仅项目实现代码（`constants.py`、`core/`、`agents/`、`tool/`、`memory/`、`web/`）；前端单文件 `web/static/index.html` 按结构描述（非 Python，不逐函数解析）。
> 不含测试（`tests/`，36 个文件）、开发工具脚本（`scripts/`）、文档与资源文件、第三方依赖（`.venv/`）。
>
> 生成方式：对全部 106 个 Python 文件做 AST 结构抽取 + 导入图分析，再按强连通分量（SCC）压缩后拓扑排序；
> 函数签名来自 AST（不是人工誊抄），函数语义来自源码阅读。
>
> 统计：实现代码文件 **69** 个（61 个实现模块 + 8 个包门面 `__init__.py`）；函数/方法/嵌套函数 **626** 个，类 **133** 个，合计 **759** 个可命名定义，全部在本文档中有对应条目。

---

## 0. 怎么读这份文档

1. **先读第 1 章拓扑总览**：理解 12 个依赖层与每一层为什么在那一层。
2. **再读第 2 章跨层调用链**：把「一次 Agent 请求」「一次工具执行」「一次文档入库」「一次 Web 问答」四条主线看懂，后面各文件的函数就不再是散点。
3. **最后按第 3 章逐层逐文件读**：每一节对应一个文件，函数级条目给出签名、参数、返回值、副作用与调用关系。

约定：
- 「依赖」= 该模块 import 的项目内部模块；「被依赖」= 谁 import 它。
- 拓扑层号越小越底层（越先读）。同一层内的模块互不依赖，可以任意顺序读。
- `→` 表示依赖方向（调用/数据流）。

---

## 1. 拓扑总览

### 1.1 分层结果（SCC 压缩后）

| 层 | 模块 | 说明 |
|---|---|---|
| L1 | `constants`、`core.models`、`core.activity_log`、`agents.message_utils`、`agents.providers`、`memory.storage.graph`、`tool._shared` | 零内部依赖的地基：常量、数据模型、日志、消息转换、配置解析、图存储、工具公共路径工具 |
| L2 | `core.registry`、`core.repository`、`core.update_log`、`core.skill_models`、`core.tool_loop`、`agents.llm`、`memory.embedding`、`memory.ids`、`memory.rag.document`、`web.domain_classifier` | 只依赖 L1 的基础设施：工具注册表、仓储、更新日志库、技能模型、工具循环、LLM 客户端、嵌入、ID 派生、文档解析、领域分类 |
| L3 | `core.catalog`、`core.discovery`、`core.parser`、`core.runtime`、`core.skill_discovery`、`core.skill_registry`、`tool.base`、`memory.storage.document_repo`、**[环] `memory.base` ↔ `memory.storage.{document,qdrant,vector}`** | 运行时骨架：工具目录、自动发现、模型输出解析、并发调度、技能发现/注册，以及记忆实体基类与存储实现（互为环）；`document_repo` 单向依赖 `memory.base`，不加入环 |
| L4 | `core.skill_catalog`、`memory.types.{working,episodic,semantic,perceptual}`、`tool.current_time`、`tool.fs_*`、`tool.search`、`tool.tool_template`、`tool.update_log` | 四层记忆类型 + 第一批具体工具（时间、文件系统、联网搜索、更新日志） |
| L5 | `agents.agent`、`memory.manager`、`tool.read_update_log` | Agent 基类与执行主体；记忆总协调器；更新日志读取工具 |
| L6 | `agents.react`、`memory.rag.{graph_rag,knowledge}`、`web.graph_builder`、`tool.read_update_logs` | ReAct 协议层、知识抽取与图检索、记忆→星云图映射 |
| L7 | `memory.rag.pipeline` | RAG 总管道（解析→切块→documents/chunks 真值源→嵌入→抽取→图补丁；检索为向量×FTS5 RRF 混合），依赖 L3 的 `document_repo` 与 L6 的抽取和图检索 |
| L8 | `tool._memory` | 记忆工具的公共后端装配（默认管理器/管道/LLM 抽取器） |
| L9 | `tool.memory_query`、`tool.memory_add`、`tool.memory_tool`、`tool.rag_search`、`tool.rag_tool` | 五个记忆/知识工具本体 |
| L10 | `web.support` | Web 层单例装配：管理器、管道、知识管家 Agent、嵌入与聊天可用性 |
| L11 | `web.seed` | 种子数据播种（依赖 support 的单例） |
| L12 | `web.app` | FastAPI 应用与全部 HTTP 路由（最上层，无人依赖）；前端单文件 `web/static/index.html` 由它挂载为静态首页 |

### 1.2 唯一的真实依赖环

```
memory.base  ──>  memory.storage.document
             ──>  memory.storage.qdrant
             ──>  memory.storage.vector
memory.storage.document ──> memory.base
memory.storage.qdrant   ──> memory.base, memory.storage.vector
memory.storage.vector   ──> memory.base
```

- 成因：`memory/base.py` 需要构造/引用存储实现（SQLite 文档库、内存/Qdrant 向量库），而每个存储实现又需要 `memory.base` 里的 `MemoryItem`/`MemorySearchResult` 等实体与协议。
- 运行时为什么不出错：`memory/base.py` 在**模块顶层**只从 `memory.storage` 包门面导入协议/实现，而这些存储模块从 `memory.base` 取的名称在导入顺序中已先定义；`memory/__init__.py` 又先导入 `base` 再导入 `storage`，因此实际加载顺序保证了名称可用。
- 文档处理方式：把它作为**一个拓扑组件**（L3）处理，读法是「`memory.base` 的实体与协议 → 三个存储实现 → 回到 `memory.base` 里使用它们的管理逻辑」。
- `memory.storage.document_repo` 不在此环内：它只**单向**从 `memory.base` 取 `utc_now`，`base` 不反向引用它；但 `memory.rag.pipeline` 会直接依赖它做 documents/chunks 真值源，所以它位于 L3、被 L7 消费。

> 另外，8 个 `__init__.py` 是**聚合门面**，只做再导出（`memory/__init__.py` 甚至聚合了整个 memory 子系统）。它们不属于任何实现层，统一在第 4 章列出：门面 → 被聚合模块的清单。

### 1.3 包门面再导出关系

| 门面 | 再导出自 | 作用 |
|---|---|---|
| `agents/__init__.py` | `agents.agent`、`agents.llm`、`agents.providers`、`agents.react` | 对外暴露 `Agent`/`ReActAgent`/`LLM`/`ProviderRegistry` 等 |
| `core/__init__.py` | `core.catalog`、`core.discovery`、`core.models`、`core.parser`、`core.registry`、`core.repository`、`core.runtime`、`core.skill_catalog`、`core.skill_discovery`、`core.skill_models`、`core.skill_registry`、`core.tool_loop`、`core.update_log` | 工具框架的唯一公共入口（工具插件只 `from core import ...`） |
| `memory/__init__.py` | `memory.base`、`memory.embedding`、`memory.manager`、`memory.rag.document`、`memory.rag.pipeline`、`memory.storage.*`、`memory.types.*` | 四层记忆系统的公共入口 |
| `memory.rag/__init__.py` | `memory.rag.document`、`memory.rag.graph_rag`、`memory.rag.knowledge`、`memory.rag.pipeline` | RAG 子系统入口 |
| `memory.storage/__init__.py` | `memory.storage.document`、`memory.storage.document_repo`、`memory.storage.graph`、`memory.storage.qdrant`、`memory.storage.vector` | 存储后端入口（新增 document_repo 真值源） |
| `memory.types/__init__.py` | `memory.types.episodic`、`memory.types.perceptual`、`memory.types.semantic`、`memory.types.working` | 四层记忆类型入口 |
| `tool/__init__.py` | `tool.base` | 工具包门面（真实工具靠目录发现，不在此登记） |
| `web/__init__.py` | `web.app` | 让 `python -m web.app` 与 `from web import app` 都成立 |

---

## 2. 跨层关键调用链

### 2.1 一次 Agent 请求（含工具调用）

```
调用方 (web.app.chat / 脚本)
  └─ agents.agent.Agent.run(query, tool_names=..., context=ExecutionContext)
       ├─ agents.agent.Agent._resolve_llm()            解析 provider profile → agents.llm.LLM
       ├─ (text_react) agents.react.ReActAgent.run_with_react(...)
       │     ├─ 组装提示：system prompt + 工具 schema + 历史
       │     ├─ agents.llm.LLM.complete(messages)      一次模型往返
       │     ├─ agents.react.parse_react_response(...) 解析 Thought/Action/Action Input
       │     ├─ core.runtime.ToolRuntime.execute(...)  真正的工具执行
       │     │     ├─ core.registry.ToolRegistry.resolve(name)
       │     │     ├─ 写确认门：context.confirmed_side_effects 是否含 confirmation_key
       │     │     ├─ 并发调度：parallel_safe / max_concurrency / global_semaphore
       │     │     └─ 工具实例 execute(input_model)
       │     └─ 把 Observation 追加回对话，直到 Final Answer
       └─ 返回最终答案字符串
```

关键约束：**所有副作用工具在执行前都要通过 `core.runtime` 的写确认门**；确认键由 `ExecutionContext.confirmed_side_effects` 提供，`web.app` 只给聊天放行 `memory.add`。

### 2.2 一次工具执行（框架内部）

```
core.runtime.ToolRuntime.execute(name, raw_arguments, context)
  ├─ ToolRegistry.resolve()            取工具实例 + 世代号（防止旧 schema 调用）
  ├─ ToolSpec.input_model.model_validate(...)   Pydantic 严格校验
  ├─ 若 spec.side_effect != "read"：检查 ctx.confirmed_side_effects
  ├─ 取工具/全局信号量 → 执行（同步工具丢线程池）
  ├─ 异常归一：ToolError(code=EXECUTION_ERROR, message="tool execution failed")
  └─ 成功：ToolResult(ok=True, output=..., ...)；失败：按 spec.idempotent 决定可否重试
```

### 2.3 一次文档入库（RAG）

```
memory.rag.pipeline.RAGPipeline.ingest_source(file/dir, base_dir=...)
  ├─ memory.rag.document.DocumentProcessor.parse()     解析 txt/md/json/html（PDF 走可选依赖）
  ├─ DocumentProcessor.normalized_text()               统一归一化正文（documents.raw_text 与切块共用）
  ├─ DocumentProcessor.chunks_with_spans()             定长+重叠切块，保留每块在归一化正文里的字符区间
  ├─ memory.storage.document_repo                      先写真值源：
  │     ├─ upsert_document(DocumentRecord)             documents 行（原文/tags/permission/status 状态机）
  │     └─ upsert_chunk(ChunkRecord) + set_chunk_vector_status
  │           ├─ memory.base / memory.types.*          正文写入 semantic 层（块 id 与真值源一致）
  │           ├─ memory.embedding.*                    向量化（本机网关 / DashScope API / 离线 Hash）
  │           ├─ memory.storage.*                      向量库 upsert（内存/Qdrant，轻量 payload）
  │           ├─ memory.rag.knowledge.LLMKnowledgeExtractor   抽出 domain/实体/三元组
  │           └─ memory.rag.knowledge.materialize_extraction  落 semantic 记忆 + 图边（含 supersede/retract）
  │           └─ 抽取失败时 set_status(document, "failed", error=...)（源文本已在真值源，不丢失）
  └─ 返回 ingest 报告（chunks/domains/entities/relations/errors）
```

检索侧（`RAGPipeline.retrieve` / `hybrid_retrieve`）：查回向量层后以 `chunks` 真值源回查正文（同一 id 的内容以真值源为准）；`HELLOAGENTS_MEMORY_HYBRID` 开启（默认）时走「向量 × FTS5 关键词」两路、RRF 融合；嵌入网关不可达时自动降级为纯关键词 FTS5 并记录 `last_retrieval_note`。

### 2.4 一次 Web 问答

```
POST /api/chat
  ├─ web.support.chat_ready()              是否有可用 provider key
  ├─ web.support.get_agent()               知识管家（ReActAgent + 4 个记忆工具）
  ├─ web.support.chat_confirmed_side_effects(agent)  只放行 memory.add
  ├─ Agent.run(...)                        （见 2.1）
  ├─ web.support.record_qa(...)            写入 episodic 记忆
  ├─ web.support.schedule_qa_extraction(...) 后台抽成图补丁（也 bump 全局 revision）
  ├─ app.invalidate_graph()                本地写入与后台抽取共用 bump_graph_revision
  ├─ pipeline.graph_retrieve(...)          图多跳检索 → answer/sources/paths
  └─ pipeline.hybrid_retrieve(...)         混合检索（向量×FTS5 RRF）→ retrieval 溯源报告（U4）
```

其余端点：`/api/graph`（支持 `?since=<revision>` 增量）、`/api/ingest`（网关不可达时 503 并给隧道命令）、`/api/knowledge`、`/api/facts`、`/api/export`、`/api/import`、`/api/health`（新增 `embedding_reachable/store_modes/degraded`），以及 Phase 6/7 新增的 `/api/documents`、`/api/documents/{id}`、`/api/documents/{id}/revectorize`、`/api/stats`、`/api/reconcile`（GET 报告 / POST 自愈），都复用 `web.support` 的单例与 `memory`/`memory.rag` 的公开 API。

---

## 3. 逐层逐文件解析

> 下面按拓扑层从上到下展开。同一层内模块互不依赖，可任意顺序阅读；每个模块小节都给出它在本层的角色、依赖方向与逐个函数的接口说明。

## L1 零依赖地基（常量 / 数据模型 / 日志 / 消息 / 配置 / 图存储 / 路径工具）

### `agents/message_utils.py` — OpenAI 兼容 SDK 返回值的统一 JSON 结构转换层

- **拓扑层**：L1 ｜ **依赖**：Python 标准库 `json`、`collections.abc.Mapping`、`typing.Any`；不依赖项目内其他模块 ｜ **被依赖**：`agents.llm`、`agents.agent`；`agents.react` 通过 `agents.agent` 的兼容导入使用这些转换函数；测试也直接覆盖。
- **规模**：99 行 / 6 个 def
- **职责**：屏蔽 Pydantic 模型、dataclass、普通 mapping 与 SDK 对象之间的字段访问差异。统一把 assistant message、tool call 和 tool result 转成可放入会话历史的 JSON 兼容结构，避免 `agent.py`、`react.py`、`llm.py` 各自复制 provider 兼容逻辑。
- **关键模块级对象**：
  - `DEFAULT_TOOL_NAME = "unknown.tool"`：工具名缺失时用于日志和错误占位。
  - `MAX_TOOL_NAME_CHARS = 200`：安全工具名截断上限。
  - `MAX_TOOL_ERROR_CHARS = 1000`：安全错误文本截断上限。
  - `__all__`：导出 9 个公共对象，其中实际函数为 6 个。

#### 函数 `field(value: Any, key: str, default: Any = None) -> Any` — 兼容读取 mapping 或对象字段

- **参数 / 返回**：`value` 是 mapping 或任意对象；`key` 是字段名；`default` 是缺省值。mapping 使用 `get`，其他对象使用 `getattr`，返回字段值或默认值。
- **关键实现 / 调用关系**：这是同文件其余转换函数及 `agents.llm._field` 的基础。它不捕获对象属性描述符抛出的异常，也不递归访问嵌套结构。
- **副作用 / 异常**：无副作用；对象不是 mapping 且属性不存在时由 `getattr(..., default)` 返回默认值，描述符自身异常会向外传播。

#### 函数 `safe_tool_name(value: Any) -> str` — 生成有长度上限的安全工具名

- **参数 / 返回**：仅当 `value` 是字符串时执行 `strip()`；其他类型使用 `DEFAULT_TOOL_NAME`。空字符串回退到默认名，最终最多保留 200 字符。
- **关键实现 / 调用关系**：被 `agents.agent`、`agents.react` 用于把未知或异常工具名写进 `ToolResult` 和错误文本。
- **副作用 / 异常**：无。

#### 函数 `safe_tool_call_error(error: Exception) -> str` — 生成有长度上限的工具调用错误文本

- **参数 / 返回**：返回 `str(error)`；若结果为空则返回异常类名；最多保留 1000 字符。
- **关键实现 / 调用关系**：被 native tool call 解析失败和 ReAct native 兼容路径用于构造 `ToolError.message`。
- **副作用 / 异常**：无；它本身不吞掉构造字符串时的异常，但调用点通常已处于异常处理分支。

#### 函数 `tool_call_dict(item: Any) -> dict[str, Any]` — 把单个 SDK tool-call 对象转成消息 part

- **参数 / 返回**：读取 `id`、`type`、`function.name`、`function.arguments`；`type` 默认 `"function"`，参数默认 `"{}"`。返回顶层不含 `None` 的字典。
- **关键实现 / 调用关系**：由 `message_dict` 对 assistant message 的 `tool_calls` 列表逐项调用。只过滤顶层空字段；嵌套 `function` 的 `name`、`arguments` 若显式为 `None` 仍可能保留为 `None`。
- **副作用 / 异常**：无。

#### 函数 `message_dict(message: Any) -> dict[str, Any]` — 把 assistant message 标准化为可保存字典

- **参数 / 返回**：优先调用对象的 `model_dump(exclude_none=True)`；mapping 做浅复制；其他对象只提取 `role`、`content`、`tool_calls` 三个非 `None` 字段。最终确保 `role` 默认为 `"assistant"`。
- **关键实现 / 调用关系**：若存在非空 `tool_calls`，逐项经 `tool_call_dict` 转换。它被 `Agent.run_with_tools` 和 `ReActAgent.run_with_react` 写入会话历史前调用。
- **副作用 / 异常**：无自身副作用；`model_dump` 或属性访问异常不在此处兜底。

#### 函数 `result_json(result: Any) -> str` — 将工具结果序列化为 provider 可接收的 JSON 字符串

- **参数 / 返回**：先尝试 `result.model_dump(mode="json")`；失败后尝试 `result.model_dump()`；最后使用 `json.dumps(..., ensure_ascii=False, default=str)` 返回字符串。
- **关键实现 / 调用关系**：被 native 工具结果回灌、ReAct Observation 回灌调用；本项目实际传入 `ToolResult`。
- **副作用 / 异常**：无副作用；若第二次 `model_dump()` 也失败，异常向外传播；不可 JSON 化的叶子值通过 `default=str` 兜底。

---

### `agents/providers.py` — 命名 provider profile 的 TOML 加载、校验与密钥解析层

- **拓扑层**：L1 ｜ **依赖**：标准库 `os`、`re`、`tomllib`、`Mapping`、`dataclass`、`Path`、`MappingProxyType`、`urlsplit` ｜ **被依赖**：`agents.agent`、`agents.__init__`；`web.support`、`tool.memory` 及测试直接导入。
- **规模**：276 行 / 14 个 def
- **职责**：把 `config/provider.toml` 等文件中的 provider 条目解析成不可变 `ProviderProfile`。校验名称、HTTP(S) URL、密钥或环境变量、默认模型、模型列表、adapter 和 `tool_mode`，并为 Agent 提供按名称解析和临时注册能力。
- **关键模块级对象**：
  - `_PROFILE_NAME_RE`：profile 名必须以字母/数字开头，只允许字母、数字、下划线和连字符，最长 64。
  - `_ENV_NAME_RE`：判断旧字段 `api_key_env` 中是否是可用的环境变量名。
  - `_TOOL_MODES = frozenset({"native_strict", "native_loose", "text_react", "none"})`：允许的工具协议。
  - `ProviderProfile`：冻结 dataclass，profile 值对象。
  - `__all__`：导出 `ProviderProfile`、`ProviderRegistry`。

#### 类 `ProviderProfile` — 一个不可变的 OpenAI 兼容 provider 配置

- **构造**：由 dataclass 自动生成，字段依次为 `name: str`、`base_url: str`、`api_key: str`、`default_model: str`、`models: tuple[str, ...]`、`api_key_env: str | None = None`、`adapter: str = "openai_compatible"`、`tool_mode: str = "native_strict"`。构造本身不执行 `_parse_profile` 的 URL、模型和密钥校验。
- **关键属性**：`name` 是 profile 标识；`base_url` 是已由解析器去掉末尾 `/` 的 endpoint；`api_key` 是内联密钥或空串；`api_key_env` 是环境变量后备；`default_model` 默认模型；`models` 可用模型元组；`adapter` 当前只支持 `openai_compatible`；`tool_mode` 决定 native/text_react/none。
- **方法**：
  - `api_url(self) -> str` — **property**；返回 `self.base_url`，对外提供旧接口名 `api_url`；无参数、无副作用。
  - `public_info(self) -> dict[str, Any]` — 返回可检查的 profile 元数据；非空密钥脱敏为 `"***"`，空密钥返回 `""`；包含 `name`、`adapter`、`base_url`、`api_url`、`api_key`、`api_key_env`、`default_model`、`models` 列表和 `tool_mode`。不解析环境变量，也不发起网络请求。

#### 类 `ProviderRegistry` — TOML profile 注册表与密钥解析器

- **构造**：`__init__(self, config_path: str | Path | None = None) -> None`。`config_path` 为 `None` 时调用 `default_config_path()`；否则展开 `~` 并 resolve。初始化为空 `MappingProxyType`、空 active profile，随后立即 `reload()`。
- **关键属性**：`config_path` 是已解析路径；`_profiles` 是不可变映射；`active_profile` 是当前默认 profile 名；这些属性在 reload 后整体替换。
- **方法**：
  - `default_config_path() -> Path` — **staticmethod**；定位 `<项目根>/config`，依次优先选择存在文件 `provider.toml`、`providers.toml`、`provider.example.toml`，都不存在时返回 `provider.toml` 路径；无副作用。
  - `profiles(self) -> Mapping[str, ProviderProfile]` — **property**；暴露不可变 profile 映射；调用方不能直接修改注册表。
  - `reload(self) -> None` — 检查文件存在，二进制打开并用 `tomllib.load` 解析；TOML 解析错误转成 `ValueError`，文件缺失抛 `FileNotFoundError`；随后用 `_parse_document` 原子替换映射和 active profile。
  - `get(self, name: str) -> ProviderProfile` — 校验名称是非空字符串；查不到时抛出含可用 profile 列表的 `ValueError`；不解析密钥。
  - `resolve_api_key(self, profile_name: str) -> str` — 先使用 profile 内联 `api_key`；为空且配置了 `api_key_env` 时读取并 strip 环境变量；环境变量缺失或内联密钥为空抛 `ValueError`。
  - `register_ephemeral(self, name: str, *, api_key: str, base_url: str, default_model: str) -> None` — 供旧接口/测试注入内存 profile。校验 `api_key` 是非空字符串，用 `_parse_profile` 校验 URL、模型和协议，再复制映射并拒绝重复名称；不改变 `active_profile`，也不写 TOML。注意此路径由注册表直接调用 `_parse_profile`，没有复用 `_parse_document` 中的 profile 名正则校验。
- **调用关系 / 副作用 / 异常**：`Agent.__init__`、`Agent.reload_provider_profiles`、`Agent._completion_target` 使用它。`reload` 会替换内存映射；`register_ephemeral` 会更新内存映射，均不直接修改文件。

#### 函数 `_parse_document(document: Mapping[str, Any], config_path: Path) -> tuple[dict[str, ProviderProfile], str]` — 校验顶层 TOML 文档

- **参数 / 返回**：`document` 是 TOML 顶层表，`config_path` 仅用于错误定位；返回 `(profiles, active_profile)`。
- **关键实现 / 调用关系**：要求 `defaults` 是 mapping，`profiles` 是非空 mapping，`active_profile` 是非空字符串；逐项校验 profile 名并调用 `_parse_profile`；最后确认 active profile 确实存在。由 `ProviderRegistry.reload` 调用。
- **副作用 / 异常**：无副作用；文档结构、名称或 active profile 不合法时抛 `TypeError` 或 `ValueError`。

#### 函数 `_parse_profile(name: str, raw_profile: Mapping[str, Any]) -> ProviderProfile` — 校验并构造单个 profile

- **参数 / 返回**：`name` 是 profile 名，`raw_profile` 是条目 mapping；返回 `ProviderProfile`。
- **关键实现 / 调用关系**：只接受 adapter `openai_compatible`；读取 `api_url`（优先）或 `base_url`，要求绝对 `http`/`https` URL；兼容旧 `api_key_env` 字段：若值符合环境变量名则作为变量名，否则视作旧版误写的内联 key；要求默认模型非空，`models` 是非空数组、无重复且包含默认模型；校验 tool_mode；最后去掉 base URL 末尾 `/` 并 strip key。
- **副作用 / 异常**：无；类型、URL、模型、adapter、tool_mode 不合法时抛 `TypeError` 或 `ValueError`。该函数本身不校验 profile 名格式。

#### 函数 `_required_string(source: Mapping[str, Any], key: str, location: str) -> str` — 读取必填非空字符串

- **参数 / 返回**：从 `source.get(key)` 取值；类型必须是字符串且 strip 后非空；返回 strip 后的字符串。
- **关键实现 / 调用关系**：由 `_parse_document` 和 `_parse_profile` 调用。
- **副作用 / 异常**：无；失败抛 `ValueError`，错误中包含 `location` 和 `key`。

#### 函数 `_optional_string(source: Mapping[str, Any], key: str, default: str) -> str` — 读取可选非空字符串

- **参数 / 返回**：缺省时返回 `default`；显式配置时必须是非空字符串；返回 strip 后值。
- **关键实现 / 调用关系**：用于 `adapter` 和 `tool_mode` 默认值。
- **副作用 / 异常**：无；显式空值或非字符串抛 `ValueError`。

#### 函数 `_validate_model(profile_name: str, value: Any) -> str` — 校验单个模型名

- **参数 / 返回**：模型值必须是非空字符串；返回 strip 后模型名。
- **关键实现 / 调用关系**：`_parse_profile` 对 `models` 数组逐项调用。
- **副作用 / 异常**：无；失败抛 `ValueError`。

---

### `constants.py` — 全项目统一的常量与默认值单一事实来源

- **拓扑层**：L1 ｜ **依赖**：Python 标准库 `re` ｜ **被依赖**：`core.repository`、`core.update_log`、`core.skill_models`、`core.tool_loop`、`core.skill_catalog`、`core.skill_discovery`，以及 `agents.*`、`memory.*`、`web.*`、`tool.*`
- **规模**：320 行 / 0 个 def
- **职责**：集中定义跨模块共享的请求参数、安全上限、数据库文件名、技能校验规则、记忆/RAG 参数、Web 输入限制和星图展示参数，以及「连接与端点」一节的**服务连哪里**事实来源（本机回环地址、各端口、Qdrant/Neo4j/嵌入网关端点）。它避免同一默认值在多个模块出现不同副本；工具模块自己的 `TOOL_ENABLED` 被刻意排除，因为该布尔值属于每个工具模块的发现协议开关。
- **关键模块级对象**：
  - `DEFAULT_MAX_RETRIES = 3`、`DEFAULT_TEMPERATURE = 0.7`、`DEFAULT_TIMEOUT = 60`、`PROMPT_CACHE_KEY_VERSION = "pc-v1"`、`DEFAULT_SKILLS_ROOT = "skills"`：Agent/LLM 请求、缓存命名空间和技能根目录默认值。
  - `TOOL_LOOP_SAFETY_LIMIT = 64`：无显式轮数上限时的工具循环安全上限。
  - `OBSERVATION_COMPRESS_THRESHOLD = 12_000`、`OBSERVATION_STUB_PREFIX`、`OBSERVATION_PREVIEW_CHARS = 400`、`HISTORY_MAX_MESSAGES = 60`：历史压缩与裁剪参数。
  - `SKILL_NAME_PATTERN`、`MAX_DESCRIPTION_CHARS = 2000`、`MAX_VERSION_CHARS = 32`、`MAX_TRIGGER_CHARS = 200`、`CONTENT_HASH_LENGTH = 64`、`SKILL_FILE_SUFFIX`、`LEGACY_SKILL_ENTRY_FILENAME`、`README_FILENAME`、`ENABLED_FIELD`：技能名称、前端元数据、content hash 和文件布局协议。
  - `DEFAULT_TOOLS_DB_FILENAME`、`DEFAULT_UPDATE_LOG_FILENAME`、`DEFAULT_MEMORY_DB_FILENAME`：SQLite 默认文件名。
  - `LOCALHOST = "127.0.0.1"`（本机回环地址，用字面 IP 避免 Windows 上 `localhost` 解析到 `::1`）；`DEFAULT_EMBEDDING_GATEWAY_PORT = 10800`、`DEFAULT_QDRANT_PORT = 6333`、`DEFAULT_NEO4J_BOLT_PORT = 7687`、`DEFAULT_WEB_PORT = 8765`、`DEFAULT_EMBEDDING_TUNNEL_REMOTE_PORT = 18000`：本机回环端口分配表（改端口只改这里）。
  - `DEFAULT_QDRANT_URL = f"http://{LOCALHOST}:6333"`、`DEFAULT_NEO4J_URI = f"bolt://{LOCALHOST}:7687"`、`DEFAULT_NEO4J_USERNAME = "neo4j"`、`DEFAULT_NEO4J_PASSWORD = "neo4j"`：Qdrant/Neo4j 的本机文档化端点。**只是默认值，不是默认启用**——`MemoryConfig.qdrant_url/neo4j_uri` 出厂仍为 `None`（走内存回退），要连真服务必须显式配置（.env 的 `HELLOAGENTS_MEMORY_*` 或构造参数）。
  - `DEFAULT_EMBEDDING_BASE_URL`：**已从公网 DashScope 端点改为本机隧道** `http://127.0.0.1:10800`（网关是唯一嵌入来源；隧道命令由 `.env` 的 `EMBEDDING_TUNNEL_HINT` 下发，不写进仓库）。`DEFAULT_EMBEDDING_MODEL`、`DEFAULT_EMBEDDING_BATCH_SIZE`：嵌入模型与批大小。
  - `MEMORY_DEFAULT_TTL_SECONDS`、`MEMORY_WORKING_CAPACITY`、`MEMORY_SEARCH_LIMIT`、`MEMORY_SIMILARITY_THRESHOLD`、`MEMORY_EMBEDDING_DIMENSION`、`MEMORY_EMBEDDING_TIMEOUT`、`MEMORY_SQLITE_DEFAULT`、`MEMORY_QDRANT_COLLECTION`：记忆层默认配置。
  - `RAG_CHUNK_SIZE`、`RAG_CHUNK_OVERLAP`、`RAG_RETRIEVE_LIMIT`、`RAG_GRAPH_HOPS`、`RAG_GRAPH_MAX_HOPS`、`RAG_GRAPH_PATH_LIMIT`、`RAG_CONTEXT_MAX_CHARS`、`RAG_CHUNK_MAX`：文档切块、检索和图检索上限。
  - `ENTITY_SIMILARITY_THRESHOLD`、`ENTITY_DEFAULT_CONFIDENCE`、`ENTITY_DEFAULT_TYPE`、`ENTITY_NAME_MAX_LENGTH`、`ENTITY_CLEAN_MAX_LENGTH`、`ENTITY_PREFIX_MIN_LENGTH`、`GRAPH_CONTEXT_MAX_RELATIONS`：知识抽取与实体合并参数。
  - `REACT_UNMARKED_ANSWER_RETRY_LIMIT`、`REACT_MALFORMED_ANSWER_RETRY_LIMIT`：ReAct 文本协议容错重试上限。
  - `MAX_UPLOAD_BYTES`、`WEB_CHAT_MAX_CHARS`、`WEB_FACT_*`、`WEB_GRAPH_RAG_QUERY_MAX`、`WEB_GRAPH_RAG_LIMIT_MAX`、`WEB_KNOWLEDGE_MAX_CHARS`、`WEB_IMPORT_ERRORS_MAX`、`WEB_INGEST_CHUNK_SIZE`、`QA_EXTRACT_CHUNK_SIZE`：Web API 的输入、输出和切块限制。
  - `NEBULA_PALETTE`、`DEFAULT_DOMAIN`、`TIMELINE_DOMAIN`、`TIMELINE_ENTITY`、`TIMELINE_ID`、`DOC_DOMAIN`、`NEBULA_CONTENT_PREVIEW_CHARS`、`NEBULA_EVENT_TITLE_CHARS`、`NEBULA_DATE_CHARS`、`DOMAIN_TITLE_WEIGHT`：星图展示和领域分类参数。

本文件没有类和函数定义，因此没有需要逐项展开的 `def`。

### `core/activity_log.py` — 工具注册和 ReAct 运行过程的控制台活动日志

- **拓扑层**：L1 ｜ **依赖**：标准库 `logging`、`collections.abc.Iterable` ｜ **被依赖**：`core.registry`、`core.discovery`，以及 `agents.react`
- **规模**：123 行 / 13 个 def
- **职责**：提供一个专用 logger 和一组稳定的日志接口，让注册、发现和 ReAct 调用方不需要直接处理格式。部分接口故意为空，以便调用点保留统一的 API，同时不把内部轮次信息输出给用户。logger 使用 `print` 风格 handler，便于交互环境立即看到进度。
- **关键模块级对象**：`LOGGER = logging.getLogger("all_agent.activity")`，设置 INFO 级别、禁用向上传播，并只添加一个 `_ConsoleLogHandler`；`__all__` 暴露 logger 和公开日志函数。

#### 类 `_ConsoleLogHandler(logging.Handler)` — 通过 `print(..., flush=True)` 输出日志记录

- **构造**：继承 `logging.Handler`，源码未定义自定义 `__init__`，因此使用父类构造流程。
- **关键属性**：由父类维护 `formatter`、`level` 等 handler 状态；模块在创建实例后设置 `[%(asctime)s] %(message)s` 格式。
- **方法**：
  - `emit(self, record: logging.LogRecord) -> None` — 格式化并立即打印日志；打印自身发生异常时调用 `self.handleError(record)`，避免日志 handler 反向中断业务。

#### 函数 `log_tool_registration(tool_name: str, generation: int | None, registered_names: Iterable[str]) -> None` — 保留工具注册调用点但不输出

- **参数**：`tool_name` 为工具名；`generation` 为注册代次；`registered_names` 为当前注册名集合。
- **返回**：`None`。
- **关键实现**：函数体只有说明性 docstring，没有日志、状态变更或显式异常处理。
- **调用关系**：由 `ToolRegistry.register` 调用；不调用其他模块函数。

#### 函数 `log_discovery_summary(package: str, registered_names: Iterable[str]) -> None` — 保留发现汇总调用点但不输出

- **参数**：`package` 为扫描包名；`registered_names` 为发现并注册的工具名。
- **返回**：`None`。
- **关键实现**：空实现，发现明细不会进入面向用户的活动流。
- **调用关系**：由 `discover_tools` 在扫描结束后调用。

#### 函数 `log_react_round_started(round_number: int, max_rounds: int | None) -> None` — 静默忽略轮次开始

- **参数**：`round_number` 为当前轮；`max_rounds` 为上限，`None` 表示未显式限制。
- **返回**：`None`。
- **关键实现**：空实现，轮次被视作内部控制流。
- **调用关系**：由 ReAct 循环调用。

#### 函数 `log_model_completed(round_number: int, elapsed_seconds: float) -> None` — 输出模型轮次耗时

- **参数**：轮次编号和耗时秒数。
- **返回**：`None`。
- **关键实现**：使用 `max(0.0, elapsed_seconds)` 防止负数；以三位小数输出。
- **调用关系**：由 Agent/ReAct 模型请求流程调用，写入模块 `LOGGER`。

#### 函数 `log_model_first_chunk(round_number: int, elapsed_seconds: float) -> None` — 输出流式首字延迟

- **参数**：轮次编号和从请求到首个流式数据块的耗时。
- **返回**：`None`。
- **关键实现**：同样对耗时做零下限保护，日志文案说明流式连接已建立。
- **调用关系**：由支持流式请求的 Agent 调用。

#### 函数 `log_react_thought(round_number: int, thought: str) -> None` — 输出当前模型思考文本

- **参数**：轮次编号和待展示思考。
- **返回**：`None`。
- **关键实现**：先经 `_clean_text` 规范化空白并截断，再写 INFO 日志。
- **调用关系**：调用 `_clean_text`；由 ReAct 流程调用。

#### 函数 `log_tool_call_started(round_number: int, tool_names: Iterable[str]) -> None` — 输出准备执行的工具名

- **参数**：轮次编号和工具名可迭代对象。
- **返回**：`None`。
- **关键实现**：通过 `_tool_names` 去重、过滤和排序；空集合显示为“无”。
- **调用关系**：调用 `_tool_names`；由 ReAct 工具调用路径调用。

#### 函数 `log_tool_call_completed(round_number: int, tool_names: Iterable[str], elapsed_seconds: float) -> None` — 输出工具调用耗时

- **参数**：轮次编号、工具名集合和耗时。
- **返回**：`None`。
- **关键实现**：不输出工具结果正文，避免日志泄露或膨胀；耗时做零下限保护。
- **调用关系**：调用 `_tool_names`；由 ReAct 工具调用 `finally` 路径调用。

#### 函数 `log_react_final_answer(round_number: int, answer: str) -> None` — 静默忽略最终答案日志

- **参数**：轮次编号和最终答案。
- **返回**：`None`。
- **关键实现**：空实现，最终答案已由调用方向外返回。
- **调用关系**：由 ReAct 结束流程调用。

#### 函数 `log_react_parse_issue(round_number: int, issue: str) -> None` — 输出工具调用协议问题

- **参数**：轮次编号和问题描述。
- **返回**：`None`。
- **关键实现**：问题文本经 `_clean_text` 清洗后以 WARNING 输出，便于诊断卡住的工具调用。
- **调用关系**：调用 `_clean_text`；由 ReAct 解析失败路径调用。

#### 函数 `_tool_names(names: Iterable[str]) -> list[str]` — 规范化工具名集合

- **参数**：任意工具名可迭代对象。
- **返回**：排序后的字符串列表。
- **关键实现**：只保留非空字符串，并通过 set 去重。
- **调用关系**：由两个工具调用日志函数调用。

#### 函数 `_clean_text(value: str, limit: int = 2000) -> str` — 压缩文本并限制长度

- **参数**：`value` 为待清洗文本；`limit` 为最大字符数，默认 2000。
- **返回**：清洗后的字符串。
- **关键实现**：用 `" ".join(str(value).split())` 合并所有空白；长度超过 limit 时保留 `limit - 3` 个字符并追加 `...`。源码未对非正 limit 做显式校验。
- **调用关系**：由思考、协议问题和若干日志函数调用。

### `core/models.py` — 工具调用、结果和契约的核心 Pydantic/dataclass 值模型

- **拓扑层**：L1 ｜ **依赖**：标准库 `hashlib`、`json`、`math`、`re`、`collections.defaultdict`、`dataclasses`，以及 `pydantic` ｜ **被依赖**：`core.registry`、`core.repository`、`core.catalog`、`core.discovery`、`core.parser`、`core.runtime`、`core.skill_catalog` 和 `core.__init__`
- **规模**：268 行 / 10 个 def
- **职责**：定义工具协议的无歧义边界：模型产生的 `ToolCall`、运行时返回的 `ToolResult`/`BatchToolResult`、内部错误 `ToolError`，以及描述工具静态契约的 `ToolSpec`。所有输入输出模型都要求禁止额外字段和严格类型；`ToolSpec` 额外计算 input/output schema 的稳定哈希，作为版本和写确认绑定键。
- **关键模块级对象**：`StrictModel` 是所有严格 Pydantic 模型的基类；`ToolCall`、`ToolError`、`ToolResult`、`BatchToolResult`、`ToolSpec`、`ExecutionContext` 是核心契约类型。

#### 类 `StrictModel(BaseModel)` — 所有内部严格模型的统一 Pydantic 配置基类

- **构造**：继承 Pydantic `BaseModel`，由子类字段生成构造参数；本类没有源码级 `__init__`。
- **关键属性**：`model_config = ConfigDict(extra="forbid", strict=True, validate_assignment=True)`，要求禁止额外字段、严格类型并在赋值时重新校验。
- **方法**：无自定义方法定义。

#### 类 `ToolCall(StrictModel)` — 一次模型请求执行某个注册工具版本的调用信封

- **构造**：字段包括 `type: Literal["tool_call"] = "tool_call"`、`call_id`、`tool_name`、`schema_version`、`schema_hash`、可选 `registry_generation`、`arguments: dict[str, Any]`、`depends_on: list[str] = []`。各字符串字段有最小/最大长度约束；`registry_generation` 若存在必须至少为 1。
- **关键属性**：`call_id` 在批内唯一；`schema_version`/`schema_hash`/`registry_generation` 用于防止过期调用；`arguments` 保留原始字典，由运行时再按具体工具输入模型严格验证；`depends_on` 表示同批内依赖的 call ID。
- **方法**：无自定义方法定义。

#### 类 `ToolError(StrictModel)` — 面向调用方的结构化工具错误

- **构造**：`code: str`、`message: str`、`retryable: bool = False`；code 最长 64，message 最长 2000。
- **关键属性**：`retryable` 表示调用方是否可重试。
- **方法**：无自定义方法定义。

#### 类 `ToolResult(StrictModel)` — 单个工具调用的成功或失败结果

- **构造**：`type: Literal["tool_result"] = "tool_result"`、`call_id`、`tool_name`、`ok: bool`、`data: Any | None = None`、`error: ToolError | None = None`。
- **关键属性**：成功时数据在 `data`；失败时错误在 `error`。
- **方法**：
  - `validate_consistency(self) -> ToolResult` — `@model_validator(mode="after")`；成功结果不得带 error，失败结果必须带 error，失败结果不得带 data；不满足时抛出 `ValueError`。

#### 类 `BatchToolResult(StrictModel)` — 一次批量执行的聚合结果

- **构造**：`type: Literal["batch_tool_result"] = "batch_tool_result"`、`results: list[ToolResult]`。
- **关键属性**：`results` 与输入调用通常按位置或 call ID 关联。
- **方法**：
  - `validate_unique_call_ids(self) -> BatchToolResult` — `@model_validator(mode="after")`；按 call ID 分组，重复 ID 只有在该组全部是 `DUPLICATE_CALL_ID` 诊断结果时才允许，否则抛出 `ValueError`。

#### 类 `ToolSpec` — 工具静态契约和 schema 指纹

- **构造**：冻结 dataclass，字段为 `name: str`、`description: str`、`version: str`、`input_model: type[BaseModel]`、`output_model: type[BaseModel]`、`side_effect: str = "read"`、`permissions: tuple[str, ...] = ()`、`timeout_seconds: float = 30.0`、`idempotent: bool = True`、`parallel_safe: bool = True`、`max_concurrency: int | None = None`、`tags: tuple[str, ...] = ()`、`recommended_before_tools: tuple[str, ...] = ()`；`_schema_hash` 由 `field(init=False, repr=False)` 生成，不参与调用方构造。
- **关键属性**：`name` 必须是至少含一个点的命名空间；`side_effect` 默认只读但推荐值未被枚举限制；`timeout_seconds` 必须有限且大于 0；`max_concurrency` 若存在必须为正整数；`recommended_before_tools` 只属于提示词建议，不形成强制执行图。
- **方法**：
  - `__post_init__(self) -> None` — 校验名称正则、描述和版本长度、Pydantic 输入/输出模型类型及 `extra="forbid"`、副作用字符串、超时、布尔语义、权限/tags 元组、推荐工具命名空间和自引用、并发限制；最后对 `{"input": ..., "output": ...}` 的 JSON schema 做规范序列化并 SHA-256，写入 `_schema_hash`。
  - `schema_hash(self) -> str` — `@property`；返回计算出的十六进制摘要。
  - `confirmation_key(self) -> str` — `@property`；返回 `name@version#schema_hash`，将写确认绑定到具体可执行契约。
  - `input_schema(self) -> dict[str, Any]` — `@property`；实时返回输入模型的 JSON schema。
  - `output_schema(self) -> dict[str, Any]` — `@property`；实时返回输出模型的 JSON schema。
  - `summary(self) -> dict[str, Any]` — 返回目录展示所需摘要，不含完整 input/output schema；tags 和推荐工具转换为 list。
  - `model_description(self) -> str` — `@property`；无推荐前置工具时返回原描述；否则追加明确标注为 advisory only、not enforced 的推荐工具行。

#### 类 `ExecutionContext` — 单次工具请求的主体、权限和写确认上下文

- **构造**：冻结 dataclass，`subject: str = "default"`、`permissions: frozenset[str] = frozenset()`、`confirmed_side_effects: frozenset[str] = frozenset()`。
- **关键属性**：`confirmed_side_effects` 应保存注册表生成的代次绑定确认键；`permissions` 目前是可审计元数据，源码中的 runtime 和 agents 不执行权限门禁。
- **方法**：
  - `__post_init__(self) -> None` — 校验 subject 非空，permissions 和 confirmed_side_effects 都是非空字符串 frozen set；类型不符抛出 `TypeError`，subject 不符抛出 `ValueError`。

### `memory/storage/graph.py` — Neo4j 图关系存储与本地回退

- **拓扑层 / 依赖 / 被依赖**：图存储实现层；只依赖标准库；被 `storage` 门面、`manager` 和 `types.semantic` 依赖。它不在 `base` 的循环依赖组中，也没有抽象基类或独立接口。
- **规模**：363 行 / 13 个 def。
- **职责**：保存 semantic 层的实体关系边。给定 driver 或 URI 时写入 Neo4j；没有 driver/URI 时使用两个进程内字典实现相同 API 的内存回退。边属性可携带 `memory_id`，用于删除某条 semantic 记录对应的关系。Phase 4（F6）起同时维护**实体属性**（domain/aliases/importance）：写边时一并写入两端实体属性，并提供 `entity()/entity_aliases()/path_query()/relation_memory_ids()` 供侧栏、多跳路径与对账使用。
- **关键模块级对象/常量**：`__all__ = ["Neo4jGraphStore"]`；类内别名 `upsert_relation = add_relation`、`related = get_relations`；本地数据结构为 `_local: dict[source, list[edge]]`、`_reverse: dict[target, list[(source, edge)]]` 和 `_entities: dict[name, {domain, aliases, importance}]`（内存回退下的实体属性镜像）。

#### 类 `Neo4jGraphStore` — Neo4j/内存图关系存储

- **构造**：`def __init__(self, uri: str | None = None, username: str | None = None, password: str | None = None, *, driver: Any = None, database: str | None = None) -> None`。
  - 记录 `database`、注入的 `driver`，初始化本地正向索引、反向索引和空 `_entities`。
  - 当 `driver is None and uri` 时导入 `neo4j.GraphDatabase`；缺少依赖抛 `RuntimeError`；用户名或密码缺失抛 `ValueError("username and password are required for Neo4j")`；否则创建 driver。
  - `driver is None` 且 `uri` 为空时保留内存回退。
- **关键属性**：`driver`、`database`、`_local`、`_reverse`、`_entities`；本地边的单条结构为 `{"source": ..., "relation": ..., "target": ..., "properties": {...}}`。
- **方法**：
  - `add_relation(self, source: str, relation: str, target: str, *, properties: Mapping[str, Any] | None = None, source_domain: str = "", target_domain: str = "", source_aliases: list[str] | None = None, target_aliases: list[str] | None = None, source_importance: float = 0.5, target_importance: float = 0.5) -> None` — 校验三个名字均为非空字符串，否则 `ValueError`；复制 properties。内存模式在 `_local[source]` 按 `(relation,target)` 找到已有边，存在则 `properties.update(props)`，不存在则追加边并写反向索引；随后把两端实体属性并入 `_entities`（`_merge_entity`，已有实体只在别名非空时更新别名）。Neo4j 模式用一条带 `ON CREATE/ON MATCH` 的 Cypher 完成：`MERGE` 两端 `MemoryEntity`（`ON CREATE` 写 domain/aliases/importance；`ON MATCH` 只更新非空别名，避免后续边把首建实体的领域/重要度覆盖成空）和 `RELATED {kind: ...}`，并 `SET r += $properties` 合并边属性。返回 `None`；Neo4j 异常向上抛。该查询的边合并键是 source/relation/target，不把 `memory_id` 纳入图关系主键。
  - `_merge_entity(self, name: str, domain: str, aliases: list[str] | None, importance: float) -> None` — 内存模式下 Cypher `ON CREATE/ON MATCH` 的镜像：无该实体时写入 `{domain, aliases, importance}`；已存在且别名非空时只更新别名。
  - `entity(self, name: str) -> dict[str, Any]` — 读取单实体的 `domain/aliases/importance`。无 driver 时返回 `_entities` 镜像；有 driver 时查询 Neo4j（启用 Neo4j 时**以它为准**，`_entities` 只是本进程写过的镜像，新进程里是空的）。查询不到返回空字典。
  - `entity_aliases(self) -> dict[str, list[str]]` — `{实体名: 别名}` 一次取回（避免 N+1）；无 driver 读内存镜像，有 driver 一条 Cypher 查全部 `MemoryEntity` 的 name/aliases。供星云图侧栏（`web.graph_builder._entity_aliases`）使用。
  - `relation_memory_ids(self) -> list[str]` — 所有边的 `memory_id`（内存模式取 `properties.memory_id`，Neo4j 模式查 `r.memory_id`）；对账（`/api/reconcile`）用它和 SQLite 事实集比较缺口。
  - `get_relations(self, entity: str, *, relation: str | None = None, direction: str = "both") -> list[dict[str, Any]]` — `direction` 只能是 `in`、`out`、`both`，否则 `ValueError`。内存模式按方向合并正向和反向边，构造统一的 `source/relation/target/properties` 字典，再按精确 relation 过滤；返回的字典是新包装，但 `properties` 仍是原对象引用。Neo4j 模式根据方向构造 MATCH/WHERE，可选 `r.kind = $relation`，返回 record 转为 dict；异常向上抛。别名 `related` 指向该方法。
  - `path_query(self, start: str, target: str, *, max_depth: int = 3, limit: int = 50) -> list[dict[str, Any]]` — 两实体间的简单路径，每条为 `{"entities": [...], "relations": [{source, relation, target}, ...]}`。校验 start/target 非空、max_depth/limit 为正整数；无 driver 时用 `_local_paths`（BFS、队列保证最短路径优先），有 driver 时执行变长区间 `[*1..max_depth]` Cypher（上界用 `int()` 收敛后拼入，避免注入）。
  - `_neighbours(self, entity: str) -> list[tuple[str, str, bool]]` — 内存 BFS 用：返回与 `entity` 相连的 `(另一端, relation, 是否出边)` 列表。
  - `_local_paths(self, start: str, target: str, *, max_depth: int, limit: int) -> list[dict[str, Any]]` — 内存回退的简单路径 BFS；简单路径不重复经过同一实体（天然无环）。
  - `delete_relation(self, source: str, relation: str, target: str) -> bool` — 内存模式过滤掉匹配边，清理反向索引并替换 `_local[source]`，返回源出边数量是否减少；Neo4j 模式按 source、relation、target 删除 `RELATED`，返回 `relationships_deleted` 是否非零。无显式输入校验；Neo4j session 异常向上抛。
  - `delete_memory_relation(self, memory_id: str) -> bool` — 校验非空字符串；内存模式遍历所有源边，删除 `properties.memory_id == memory_id` 的边并同步清理反向索引，返回是否移除过边；Neo4j 模式执行按 `r.memory_id` 删除并返回是否删除了关系。该方法是 semantic 层按记录 ID 清理图边的契约入口。
  - `close(self) -> None` — 若 driver 存在且提供可调用 `close`，调用并返回 `None`；内存回退无操作。源码没有把 `driver` 置空，重复调用可能再次调用底层 close 或导致客户端自定义行为。
- **本地回退语义**：同一 source/relation/target 只保留一条边，新属性合并；查询返回边的浅层复制；没有容量/TTL/持久化；`clear` 方法未定义；`_entities` 同样只存活于本进程。

---

### `tool/_shared.py` — 工作区路径、文本处理与子进程通用辅助

- **拓扑层**：L1 ｜ **依赖**：仅标准库（`codecs`、`locale`、`os`、`signal`、`subprocess`、`sys`、`tempfile`、`time`、`collections.abc.Mapping`、`dataclasses`、`pathlib`） ｜ **被依赖**：`fs_read_text.py`、`fs_read_dir.py`、`fs_read_file_glob.py`、`fs_write_text.py`、`fs_edit_text.py`；其中 `fs_read_file_glob.py` 还直接使用私有判定 `_is_within`。
- **规模**：349 行 / 14 个 def。
- **工具名 / 副作用**：无；以 `_` 开头且没有插件协议，因此不是可发现工具。
- **职责**：集中实现工作区根目录和越界判断；编码规范化与文本截断；有上限的文本文件读取；同目录临时文件 + `os.replace` 的原子写；带超时和输出上限的子进程及 Git 调用；子进程超时后的进程树终止。
- **关键常量**：`WORKSPACE_ROOT_ENV = "WORKSPACE_ROOT"`、`ALLOW_OUTSIDE_ENV = "WORKSPACE_ALLOW_OUTSIDE"`、`DEFAULT_MAX_OUTPUT_CHARS = 200_000`、`TRUNCATION_SUFFIX = "\n...[output truncated]"`、`MAX_TEXT_FILE_BYTES = 32 * 1024 * 1024`。

##### 路径解析与工作区沙箱

- `workspace_root()` 读取环境变量 `WORKSPACE_ROOT`；仅当变量值存在且去空白后非空时，使用 `Path(raw.strip()).expanduser().resolve()`；否则使用 `Path.cwd().resolve()`。因此发现器创建的默认 FS 工具以进程当前目录为工作区根。
- `allow_outside_workspace()` 读取 `WORKSPACE_ALLOW_OUTSIDE`；未设置时为 `False`；设置后仅下列大小写不敏感值视为开启：`1`、`true`、`yes`、`on`。
- `resolve_path()` 先要求 `base_dir` 是 `str` 或 `Path`，路径是去空白后非空的字符串。相对路径以已解析的 `base_dir` 为锚点；绝对路径直接使用；最终使用 `Path.resolve()` 解析符号链接和规范化路径。
- 当 `allow_outside` 为 `None` 时，`resolve_path()` 才读取环境开关；显式传入 `False` 会覆盖环境变量。未允许越界时，解析结果必须与 `base_dir` 相等或以 `base_dir + os.sep` 开头，否则抛出 `ValueError`。
- `_is_within()` 使用 `os.path.normcase(str(...))` 后做前缀比较，适合 Windows 的大小写不敏感语义；比较包含路径分隔符，避免把同前缀的兄弟目录误判为内部路径。
- 重要边界：`resolve_path()` 的 `base_dir` 是可注入的沙箱根。所有默认 `create_tool()` 都把 `base_dir` 设为 `workspace_root()`；但构造函数接受自定义 `base_dir` 时，沙箱根就是该自定义目录。源码未见“自定义 `base_dir` 必须仍位于 `workspace_root()` 内”的额外检查。

#### 函数 `workspace_root() -> Path`

```python
def workspace_root() -> Path
```

- **参数**：无。
- **返回**：配置的、展开用户目录并解析后的工作区根目录；无配置时为当前进程工作目录。
- **关键实现 / 调用关系**：读取 `WORKSPACE_ROOT`；各 FS 工具的构造函数在未注入 `base_dir` 时调用。

#### 函数 `allow_outside_workspace() -> bool`

```python
def allow_outside_workspace() -> bool
```

- **参数**：无。
- **返回**：环境变量 `WORKSPACE_ALLOW_OUTSIDE` 是否命中 `{"1", "true", "yes", "on"}`（`casefold()` 后比较）。
- **关键实现 / 调用关系**：只在 `resolve_path()` 的 `allow_outside is None` 分支间接调用；它是部署层开关，不是 LLM 输入字段。

#### 函数 `resolve_path(base_dir: str | Path, path: str, *, allow_outside: bool | None = None) -> Path`

```python
def resolve_path(
    base_dir: str | Path,
    path: str,
    *,
    allow_outside: bool | None = None,
) -> Path
```

- **参数**：`base_dir` 为锚点沙箱目录；`path` 为用户提供的相对或绝对路径；`allow_outside` 为可选部署开关，`None` 表示读取环境变量。
- **返回**：已展开、解析和规范化后的绝对 `Path`。
- **关键实现**：检查参数类型和非空；相对路径拼接 `base`；调用 `candidate.resolve()`；未允许越界时用 `_is_within()` 拒绝；错误消息同时包含原路径和解析后的工作区根。
- **调用关系**：被五个 FS 工具的 `execute()` 调用；异常由 Runtime 统一转为工具执行错误。

#### 函数 `_is_within(base: Path, candidate: Path) -> bool` — 判断路径是否位于基准目录内

```python
def _is_within(base: Path, candidate: Path) -> bool
```

- **参数**：`base`、`candidate` 均为已解析或可字符串化的路径。
- **返回**：`True` 当两者规范大小写后相等，或 `candidate` 以 `base + os.sep` 开头。
- **关键实现 / 调用关系**：`resolve_path()` 和 `fs_read_file_glob.py` 使用。

#### 函数 `normalize_encoding(encoding: str) -> str`

```python
def normalize_encoding(encoding: str) -> str
```

- **参数**：编码名称字符串，去除首尾空白后不可为空。
- **返回**：`codecs.lookup(...).name` 得到的规范 codec 名称。
- **关键实现 / 调用关系**：未知编码抛 `ValueError("unknown text encoding ...")`；被 `read_text_file()` 和 `atomic_write_text()` 调用，写工具还用其生成输出中的规范编码名。

#### 函数 `_sanitize_surrogates(text: str) -> str`

```python
def _sanitize_surrogates(text: str) -> str
```

- **参数**：任意文本。
- **返回**：若不含代理码位则原样返回；否则把 `U+D800` 至 `U+DFFF` 的孤立代理替换为 `U+FFFD`。
- **关键实现 / 调用关系**：`cap_text()` 在截断前调用，避免输出非法 Unicode 代理。

#### 函数 `cap_text(text: str, max_chars: int) -> tuple[str, bool]`

```python
def cap_text(text: str, max_chars: int) -> tuple[str, bool]
```

- **参数**：`text` 为文本；`max_chars` 为字符上限。
- **返回**：`(结果文本, 是否截断)`。未超限时返回原文和 `False`；超限时返回 `text[:cut] + TRUNCATION_SUFFIX` 和 `True`。
- **关键实现**：先清理代理码位；截断点若落在代理码位上则向前回退；**截断后缀不计入 `max_chars`，所以返回文本长度可能比 `max_chars` 多出后缀长度**。
- **调用关系**：`read_text_file` 的调用方、`_decode_capped()` 和 `fs_read_text` 使用。

#### 函数 `read_text_file(path: Path, encoding: str) -> tuple[str, str]`

```python
def read_text_file(path: Path, encoding: str) -> tuple[str, str]
```

- **参数**：`path` 为待读文件；`encoding` 为显式编码名。
- **返回**：`(解码文本, 规范编码名)`。
- **关键实现**：先用 `stat()` 检查文件字节数不超过 `MAX_TEXT_FILE_BYTES`（32 MiB），再一次性 `read_bytes()`；若原始字节含 NUL 且规范编码名不含 `"16"`/`"32"`，判定为二进制并拒绝；解码使用严格模式，`UnicodeDecodeError` 归一为带文件路径和编码名的 `ValueError`。
- **异常**：文件缺失、权限错误等由 `path.stat()`/`read_bytes()` 原样抛出；超限和二进制判断抛 `ValueError`。
- **调用关系**：`fs_read_text.execute()`、`fs_edit_text.execute()`。

#### 函数 `atomic_write_text(path: Path, text: str, encoding: str, *, create_parents: bool) -> int`

```python
def atomic_write_text(
    path: Path,
    text: str,
    encoding: str,
    *,
    create_parents: bool,
) -> int
```

- **参数**：`path` 为目标文件；`text` 为完整内容；`encoding` 为编码；`create_parents` 决定缺失父目录是否自动创建。
- **返回**：成功写入的编码后字节数。
- **关键实现**：规范编码；检查父路径是目录或按开关创建；拒绝目标是目录；在触碰目标文件前先 `text.encode(canonical)`，编码失败即抛 `ValueError`；用 `tempfile.mkstemp` 在目标同目录创建临时文件，写入后 `flush()`、`os.fsync()`，再 `os.replace()` 原子替换；异常时清理临时文件。
- **副作用 / 异常**：可能创建父目录和临时文件，可能替换目标文件；父路径不是目录、父目录不存在、目标是目录、编码失败分别抛 `NotADirectoryError`、`FileNotFoundError`、`IsADirectoryError`、`ValueError`。
- **调用关系**：`fs_write_text.execute()` 和 `fs_edit_text.execute()`。源码未显式处理目录项的 `fsync`，也未显式保留既有目标文件的权限位/ACL。

#### 类 `ProcOutcome` — 子进程结果数据类

```python
@dataclass
class ProcOutcome
```

- **字段**：`exit_code: int`；`stdout: str`；`stderr: str`；`timed_out: bool = False`；`duration_seconds: float = 0.0`；`stdout_truncated: bool = False`；`stderr_truncated: bool = False`。
- **方法**：该类没有定义 `def`；字段由 `run_process()` 构造。

#### 函数 `run_process(command: str | list[str], *, cwd: str | Path | None = None, timeout_seconds: float = 60.0, env: Mapping[str, str] | None = None, shell: bool = False, max_output_chars: int = DEFAULT_MAX_OUTPUT_CHARS) -> ProcOutcome`

```python
def run_process(
    command: str | list[str],
    *,
    cwd: str | Path | None = None,
    timeout_seconds: float = 60.0,
    env: Mapping[str, str] | None = None,
    shell: bool = False,
    max_output_chars: int = DEFAULT_MAX_OUTPUT_CHARS,
) -> ProcOutcome
```

- **参数**：命令或参数列表；工作目录；正的有限超时且必须小于 3600 秒；字符串到字符串的环境增量映射；是否使用 shell；每个输出流最大字符数（必须为正整数）。
- **返回**：`ProcOutcome`；超时时 `exit_code=-1`、`timed_out=True`，并仍返回已捕获的部分输出。
- **关键实现**：验证参数后合并 `os.environ`；Windows 使用 `CREATE_NEW_PROCESS_GROUP`，非 Windows 使用 `start_new_session=True`；用 `subprocess.PIPE` 捕获 stdout/stderr；正常完成调用 `communicate(timeout=...)`，超时调用 `_kill_process_tree()` 后再等待 10 秒，二次超时则 `kill()`；输出分别经 `_decode_capped()`。
- **异常**：参数类型或范围错误在启动前抛出；启动和管道错误由 `subprocess.Popen`/`communicate` 抛出。
- **调用关系**：`run_git()` 使用；本任务列出的 FS 工具不直接调用它，但它属于 `_shared` 的通用工具能力。

#### 函数 `git_env() -> dict[str, str]`

```python
def git_env() -> dict[str, str]
```

- **参数**：无。
- **返回**：`{"GIT_TERMINAL_PROMPT": "0"}`。
- **关键实现**：只关闭终端提示，故意不覆盖 `GIT_ASKPASS` 或设置 `GIT_CONFIG_NOSYSTEM`，以保留系统级凭据管理器等正常能力；无凭据时让 Git 快速失败而不是挂起。
- **调用关系**：`run_git()` 使用。

#### 函数 `run_git(args: list[str], *, cwd: str | Path | None = None, timeout_seconds: float = 60.0) -> ProcOutcome`

```python
def run_git(
    args: list[str],
    *,
    cwd: str | Path | None = None,
    timeout_seconds: float = 60.0,
) -> ProcOutcome
```

- **参数**：Git 子命令参数列表、工作目录、超时秒数。
- **返回**：`run_process(["git", *args], ...)` 的 `ProcOutcome`。
- **关键实现 / 调用关系**：`shell=False`，并注入 `git_env()`；用于不会等待交互式终端提示的 Git 调用。

#### 函数 `_kill_process_tree(process: subprocess.Popen) -> None`

```python
def _kill_process_tree(process: subprocess.Popen) -> None
```

- **参数**：待终止的 `Popen` 对象。
- **返回**：无返回值。
- **关键实现**：若进程已结束则直接返回；Windows 调用 `taskkill /pid <pid> /T /F`，异常时回退 `process.kill()`；非 Windows 对该进程组发送 `SIGKILL`，若进程组不可用则回退 `process.kill()`。
- **调用关系**：仅在 `run_process()` 超时分支调用。

#### 函数 `_decode_capped(data: bytes | None, max_chars: int) -> tuple[str, bool]`

```python
def _decode_capped(data: bytes | None, max_chars: int) -> tuple[str, bool]
```

- **参数**：原始输出字节或 `None`；最大字符数。
- **返回**：解码并截断后的文本及截断标志；空输入返回 `("", False)`。
- **关键实现**：编码顺序为 UTF-8、系统 preferred encoding（若不与 UTF-8 相同）；全部失败时用 UTF-8 `errors="replace"`；最终调用 `cap_text()`。
- **调用关系**：`run_process()` 对 stdout/stderr 各调用一次。

## L2 基础实施（注册表 / 仓储 / 更新日志 / 技能模型 / 工具循环 / LLM / 嵌入 / 文档解析 / 领域分类）

### `agents/llm.py` — OpenAI 兼容 Chat Completions 客户端与流式响应重组层

- **拓扑层**：L2 ｜ **依赖**：`constants.DEFAULT_MAX_RETRIES/DEFAULT_TEMPERATURE/DEFAULT_TIMEOUT`、`agents.message_utils.field`；可选依赖第三方 `openai`（在 `LLM.__init__` 中延迟导入）；标准库 `logging`、`math`、`re`、`sys`、`Callable`、`Iterable`、`Mapping` ｜ **被依赖**：`agents.agent`、`agents.react`、`agents.__init__`；`web.support`、`tool.memory` 和测试直接导入。
- **规模**：455 行 / 14 个 def
- **职责**：封装一个已解析 profile 的 OpenAI SDK 客户端，负责请求参数校验、普通/流式 completion、prompt cache 参数透传。流式路径把 OpenAI 分片的 `content` 和 `tool_calls` 片段重组成非流式响应形状，并可把最终答案实时回显终端。
- **关键模块级对象**：
  - `LOGGER`：模块 logger；回显写失败、流关闭失败和首次分片回调失败通常降级为 debug 日志。
  - `_FINAL_ANSWER_MARKER_RE`：匹配行首 `Final Answer:`、中文“最终答案/最终回答/最终回复”及可选 Markdown 粗体、全角/半角冒号，用于 `react_final` 模式。
  - `EchoMode = Literal["react_final", "content"]`：两种回显模式。
  - `_FinalAnswerEchoer`：单次流式请求的回显控制器。
  - `_assemble_streaming_response`：流分片重组函数。
  - `LLM`：对外客户端包装。
  - `_field`：`message_utils.field` 的向后兼容别名。

#### 函数 `_default_echo_write(text: str) -> None` — 默认 stdout 回显

- **参数 / 返回**：写入 `text` 后立即 flush；无返回值。
- **关键实现 / 调用关系**：仅当 `_FinalAnswerEchoer` 没有注入 `write` 时使用。
- **副作用 / 异常**：写 stdout；底层写入异常向外抛，但 `_FinalAnswerEchoer._emit` 会捕获并记录。

#### 类 `_FinalAnswerEchoer` — 控制一次流式 completion 的终端回显

- **构造**：`__init__(self, mode: EchoMode, write: Callable[[str], None] | None, prefix: str = "\nAI：") -> None`。`mode` 决定立即回显 content 或等待 final marker；`write` 缺省用 `_default_echo_write`；`prefix` 是首次实际输出前加的前缀。初始化内部 buffer、`_live` 和 `echoed`。
- **关键属性**：`_mode`、`_write`、`_prefix`、`_buffer` 用于延迟匹配；`_live` 表示之后是否直接转发；`echoed` 记录是否已经写过任何用户可见文本。
- **方法**：
  - `feed(self, piece: str) -> None` — 空片段直接忽略。`content` 模式立即 `_emit`；`react_final` 模式先累积 buffer，出现 `_FINAL_ANSWER_MARKER_RE` 后丢弃 marker 并进入 live，将 marker 后 tail 写出。跨 chunk 的 marker 可由持续累积的 buffer 识别。
  - `flush(self) -> None` — 若已经输出过内容则补一个换行；没有输出时不打印。用于流结束或异常退出。
  - `_emit(self, text: str) -> None` — 空文本忽略；首次输出时尝试写 prefix，随后写正文；两处写异常都只记录 `LOGGER.debug`。即使写入失败，`echoed` 仍会在尝试前置为 `True`。

#### 函数 `_assemble_streaming_response(stream: Iterable[Any], *, on_first_chunk: Callable[[], None] | None = None, echo_mode: EchoMode | None = None, echo_write: Callable[[str], None] | None = None) -> dict[str, Any]` — 重组流式 completion

- **参数 / 返回**：`stream` 是 SDK chunk 可迭代对象；`on_first_chunk` 在首个含非空 `choices` 的 chunk 上最多调用一次；`echo_mode`/`echo_write` 可选控制终端回显。返回含单个 `choices[0].message`、`finish_reason` 以及可选 `stream_echoed` 的字典。
- **关键实现 / 调用关系**：按 chunk 读取 `choices[0]`、`finish_reason`、`delta.content` 和 `delta.tool_calls`。content 分片按顺序拼接；tool calls 按 `index` 建条目，分别拼接 id、function.name、function.arguments，最后按 index 排序。`delta.tool_calls` 缺失或空时跳过。
- **副作用 / 异常**：会消耗并关闭 stream，关闭异常只记日志；会调用回调并吞掉回调异常；流迭代自身异常向外传播。返回值把无内容表示为 `content=None`，只带 tool calls 时也可复用普通响应解析。

#### 类 `LLM` — 单个 OpenAI 兼容 provider 目标的客户端包装

- **构造**：`__init__(self, *, api_key: str, base_url: str, model: str, max_retries: int = DEFAULT_MAX_RETRIES) -> None`。`api_key`、`base_url`、`model` 必须是字符串且 strip 后非空；`max_retries` 必须是非 bool 的非负整数。延迟导入 `openai.OpenAI`，创建 `self.client`；缺包时转成 `RuntimeError`。
- **关键属性**：`api_key`、`base_url`、`model` 是规范化配置；`max_retries` 传给 SDK；`client` 是 OpenAI SDK 客户端。
- **方法**：
  - `get_query() -> str` — **staticmethod**；阻塞调用 `input("请输入你的问题：")` 并返回用户输入。
  - `complete_streaming(self, messages: list[dict[str, Any]], *, model: str | None = None, temperature: float = DEFAULT_TEMPERATURE, timeout: float = DEFAULT_TIMEOUT, on_first_chunk: Callable[[], None] | None = None, echo_mode: EchoMode | None = None, echo_write: Callable[[str], None] | None = None, prompt_cache_key: str | None = None, prompt_cache_retention: str | None = None, **kwargs: Any) -> dict[str, Any]` — 固定 `stream=True` 发起 Chat Completions 请求，禁止 kwargs 覆盖 `messages/model/stream`；只在显式非 None 时传入 prompt cache 字段；随后交给 `_assemble_streaming_response`。本函数自身不复用 `complete` 的参数校验。
  - `think(self, messages: list[dict[str, Any]], temperature: float = DEFAULT_TEMPERATURE, timeout: float = DEFAULT_TIMEOUT, stream_response_bool: bool = True, prompt_cache_key: str | None = None, prompt_cache_retention: str | None = None, **kwargs: Any) -> str` — 调用 `complete`；流式时 `"".join(self.stream_response(response))`，非流式时取 `_message_content`。它同时保留打印流内容的历史行为。
  - `complete(self, messages: list[dict[str, Any]], *, model: str | None = None, temperature: float = DEFAULT_TEMPERATURE, timeout: float = DEFAULT_TIMEOUT, stream: bool = False, prompt_cache_key: str | None = None, prompt_cache_retention: str | None = None, **kwargs: Any) -> Any` — 校验 timeout 为有限正数、temperature 为有限数、stream 为 bool；prompt cache key 非空且不超过 64 字符，retention 只允许 `in_memory`/`24h`/None；禁止 kwargs 覆盖保留字段；最终调用 SDK 并原样返回 SDK 响应。`model or self.model` 决定模型。
  - `_message_content(response: Any) -> str` — **staticmethod**；兼容 mapping/对象响应，要求存在 choices 和 message，返回 `message.content`，空值归一为 `""`；缺少 choices/message 抛 `ValueError`。
  - `stream_response(self, response: Iterable[Any])` — 无返回类型注解的生成器；迭代 chunk，取 choices、delta、content，打印并 yield 每个非空 content。`finally` 尽力关闭同步 stream 并吞掉关闭异常。它是 `think(stream_response_bool=True)` 的文本路径。

#### 函数 `_field(value: Any, key: str, default: Any = None) -> Any` — 兼容别名

- **参数 / 返回**：完全转发到 `message_utils.field`，返回 mapping 或对象字段。
- **关键实现 / 调用关系**：`_assemble_streaming_response` 和 `LLM.stream_response` 使用该别名，避免同文件重复实现。
- **副作用 / 异常**：无额外行为；异常语义与 `field` 相同。

---

### `core/registry.py` — 线程安全的可执行工具注册表

- **拓扑层**：L2 ｜ **依赖**：标准库 `inspect`、`threading`、`abc`、`typing`，以及 `core.activity_log`、`core.models` ｜ **被依赖**：`core.catalog`、`core.discovery`、`core.parser`、`core.runtime`、`core.__init__`；`tool.base` 和大量插件经 `core` 使用
- **规模**：183 行 / 16 个 def
- **职责**：把 `ToolSpec` 与真正可执行的 `BaseTool` 实例绑定，并提供原子查找、快照、契约匹配和注册代次。注册代次在替换或重新注册时递增，使调用方能够识别旧确认和旧调用。所有读写在 `RLock` 下串行化，返回的 snapshot 是某个时点的稳定字典视图。
- **关键模块级对象**：`BaseTool` 是工具执行抽象基类；`ToolRegistry` 是默认注册表实现。

#### 类 `BaseTool(ABC)` — 定义工具插件必须实现的执行接口

- **构造**：抽象基类，无源码级 `__init__`；子类自行构造。
- **关键属性**：类属性 `spec: ToolSpec`，约定由子类提供。
- **方法**：
  - `execute(self, arguments: BaseModel) -> BaseModel | Any` — `@abstractmethod`；接收已按 input_model 校验的参数并返回符合 output_model 的数据或对象；源码直接 `raise NotImplementedError`，具体实现由 `execute` 的返回值经 runtime 再做严格输出校验。

#### 类 `ToolRegistry` — 保存工具实例并维护每工具代次

- **构造**：`__init__(self) -> None`，初始化 `_tools`、`_generations` 和 `threading.RLock`。
- **关键属性**：`_tools: dict[str, BaseTool]`、`_generations: dict[str, int]`、`_lock: threading.RLock`。
- **方法**：
  - `register(self, tool: BaseTool, *, replace: bool = False) -> None` — 校验工具、replace 和 `ToolSpec`，并额外确认 `spec.input_model`/`output_model` 是类；同名校验在锁内进行；未允许 replace 时抛 `ValueError`；写入工具并使该名字的代次加一；最后在锁外记录注册日志。
  - `unregister(self, name: str) -> BaseTool` — 校验非空字符串；锁内移除并返回实例，不存在时抛 `KeyError`；代次映射保留不删除，使序列跨注销/重注册保持单调。
  - `confirmation_key(self, name: str) -> str` — 解析工具后返回 `ToolSpec.confirmation_key + ":" + 当前代次`，用于把写确认绑定到实现实例，而不只是 schema。
  - `resolve(self, name: str) -> tuple[BaseTool, int]` — 校验非空字符串；锁内原子返回工具和当前代次，未注册时转换为带工具名的 `KeyError`。
  - `maybe_resolve(self, name: str) -> tuple[BaseTool, int] | None` — 无效名或未注册时返回 `None`，否则锁内返回工具和代次。
  - `snapshot(self, names: list[str] | tuple[str, ...] | None = None) -> dict[str, tuple[BaseTool, int]]` — 返回稳定视图；`names=None` 返回全部，显式 names 会校验类型并在任一缺失时抛 `KeyError`；输出去重依赖于字典键。
  - `get(self, name: str) -> BaseTool` — 转发 `resolve(name)[0]`。
  - `maybe_get(self, name: str) -> BaseTool | None` — 转发 `maybe_resolve` 并取工具。
  - `is_registered(self, name: str, *, version: str | None = None, schema_hash: str | None = None) -> bool` — 校验 name/version/schema_hash；解析不到返回 False；否则按已提供的版本和哈希条件匹配。
  - `registration_status(self, name: str) -> dict[str, Any]` — 返回 JSON 友好状态；未注册时各详情为 None；已注册时包含 version、schema_hash、generation 和实现模块/qualname。
  - `summaries(self) -> list[dict[str, Any]]` — 返回所有工具 `spec.summary()`。
  - `specs(self) -> list[ToolSpec]` — 返回所有工具规格。
  - `__contains__(self, name: str) -> bool` — 锁内判断名字是否存在。
  - `__len__(self) -> int` — 锁内返回注册表条目数。

### `core/repository.py` — 工具元数据的 SQLite 持久化层

- **拓扑层**：L2 ｜ **依赖**：标准库 `json`、`re`、`sqlite3`、`threading`、`contextlib`、`pathlib`、`typing`，以及 `constants`、`core.models` ｜ **被依赖**：`core.catalog`、`core.discovery`、`core.__init__`；Agent 通过 repository 同步工具目录
- **规模**：254 行 / 11 个 def
- **职责**：只持久化可发现的工具元数据，不持久化可执行代码或插件实例。目录工具可在实现尚未加载时读取元数据，支持 catalog-first/lazy execution；`save` 默认拒绝同主键重复，`replace` 时才覆盖。
- **关键模块级对象**：`DEFAULT_TOOLS_DB_FILENAME` 从 constants 导入并作为默认数据库名；`ToolSpecRepository` 是唯一的持久化实现。

#### 类 `ToolSpecRepository` — SQLite 工具规格仓库

- **构造**：`__init__(self, path: str | Path = DEFAULT_TOOLS_DB_FILENAME) -> None`。`path` 可为普通文件或 `":memory:"`；`~` 会先展开；非 memory 路径会按需创建父目录。
- **关键属性**：`path` 是规范化路径；`_lock` 是 `RLock`；`_shared_connection` 只用于 `":memory:"`，并配置 `check_same_thread=False` 与 `sqlite3.Row`；调用构造时立即执行 `_initialize()`。
- **方法**：
  - `_connect(self) -> sqlite3.Connection` — memory 模式返回共享连接；文件模式每次新建连接并设置 row factory；若 memory 连接已关闭则抛 `RuntimeError("repository is closed")`。
  - `_connection(self) -> Iterator[sqlite3.Connection]` — `@contextmanager`；锁内获取连接，用 `with connection` 提交/回滚事务，finally 只关闭非共享的文件连接。
  - `close(self) -> None` — 关闭 memory 共享连接并置空；文件模式通常没有持久共享连接可关。
  - `_initialize(self) -> None` — 创建 `tool_specs` 表；主键为 `(tool_name, version)`；字段覆盖描述、schema、风险属性、并发、tags、推荐工具、enabled 和实现引用；通过 `PRAGMA table_info` 对旧表缺失的 `max_concurrency`、`recommended_before_tools` 列做增量迁移。
  - `save(self, spec: ToolSpec, *, implementation_ref: str | None = None, replace: bool = False) -> None` — 校验参数；`replace=False` 使用普通 INSERT，重复主键由 SQLite 报错；`replace=True` 使用 INSERT OR REPLACE；枚举/元组转为 JSON，布尔转 0/1，保存时默认 `enabled=1`。
  - `get(self, tool_name: str, version: str | None = None) -> dict[str, Any] | None` — 校验名称和版本长度；只查 enabled 行；指定版本返回单行或 None，不指定版本则对同工具所有版本按 `_version_key` 选最高版本。
  - `search(self, intent: str, limit: int = 5) -> list[dict[str, Any]]` — 校验 intent 和 1..20 的 limit；读取所有 enabled 行；先保留每个工具的最高版本；再把 intent 按空白切词，在名称、描述和 tags 上做 `term in haystack` 子串计分；无词时返回名称排序的前 limit 项，有词时只返回得分大于 0 的项目并按得分降序、名称升序稳定排序。
  - `active_tool_names(self) -> list[str]` — 返回所有 enabled 的工具名，去重并按名称排序，不受 search 的 limit 限制。
  - `_version_key(version: str) -> tuple[object, object]` — `@staticmethod`；用 `-` 分出 core 和 pre-release suffix，对数字/非数字片段生成可比较 tuple，使 `2` 小于 `10`；无 `-` 的发布版本标记为 1，有后缀的标记为 0，因此同 core 时正式版大于预发布版。
  - `_decode(row: sqlite3.Row) -> dict[str, Any]` — `@staticmethod`；把 SQLite 行转字典，反序列化 input_schema、output_schema、permissions、tags、recommended_before_tools，并把 enabled、idempotent、parallel_safe 转回 bool。

### `core/skill_models.py` — 技能包的静态元数据模型

- **拓扑层**：L2 ｜ **依赖**：标准库 `re`、`dataclasses`、`typing`，以及 `constants` ｜ **被依赖**：`core.skill_discovery`、`core.skill_registry`、`core.__init__`
- **规模**：100 行 / 2 个 def
- **职责**：描述一个按需加载的只读 Markdown 指令包。模型只保存元数据和文件路径/hash，不保存正文；正文由技能目录工具在 `view` 时从磁盘读取，保证读到当前版本。
- **关键模块级对象**：`SkillSpec` 是唯一公开值模型。

#### 类 `SkillSpec` — 单个技能文件的不可变元数据

- **构造**：生成构造参数 `name: str`、`description: str`、`version: str = "1.0.0"`、`triggers: tuple[str, ...] = ()`、`file_path: str | None = None`、`content_hash: str = ""`。空 content_hash 专用于未从文件加载的合成 spec；发现流程提供真实 SHA-256。
- **关键属性**：`name` 必须匹配 kebab-case ASCII；`version` 默认 `1.0.0`；`triggers` 是字符串元组；`file_path` 可为 None；`content_hash` 为空或 64 位小写十六进制。
- **方法**：
  - `__post_init__(self) -> None` — 校验名称、描述、版本、triggers、file_path 和 content_hash；内容非法时抛出 `ValueError` 或 `TypeError`。
  - `summary(self) -> dict[str, Any]` — 返回技能名、描述、版本、triggers、content_hash、file_path 的 JSON 友好视图。

### `core/tool_loop.py` — 模型/工具多轮循环的统一轮数与重复调用控制

- **拓扑层**：L2 ｜ **依赖**：标准库 `json`、`collections.abc`、`typing`，以及 `constants` ｜ **被依赖**：`core.__init__`；`agents.agent`、`agents.react` 实例化并使用
- **规模**：62 行 / 3 个 def
- **职责**：不感知 ReAct 文本或原生 function call，只提供共同生命周期能力：按上限生成一基轮次、在 `max_rounds=None` 时使用安全上限、检测同一调用的无限重复。
- **关键模块级对象**：`ToolLoop.DEFAULT_SAFETY_LIMIT = TOOL_LOOP_SAFETY_LIMIT`，是兼容别名；真实单一来源仍是 constants。

#### 类 `ToolLoop` — 有界轮次生成器和重复调用守卫

- **构造**：`__init__(self, max_rounds: int | None = None, *, safety_limit: int = DEFAULT_SAFETY_LIMIT) -> None`。`max_rounds=None` 表示使用 safety_limit；safety_limit 必须为正整数；初始化 `_calls` 记录调用签名次数。
- **关键属性**：`max_rounds`、`safety_limit`、`_calls`。
- **方法**：
  - `rounds(self) -> Iterator[int]` — 生成从 1 开始的轮次；显式 max_rounds 优先，否则在 safety_limit 停止。
  - `record_call(self, name: str, arguments: Mapping[str, Any] | Any) -> None` — 用稳定 JSON（`sort_keys=True`、紧凑分隔符、`default=str`）把名称和参数编码为签名；同一签名超过 3 次时抛出 `RuntimeError`。

### `core/update_log.py` — 追加式项目更新日志的 SQLite 持久化与索引协议

- **拓扑层**：L2 ｜ **依赖**：标准库 `json`、`os`、`platform`、`sqlite3`、`threading`、`contextlib`、`datetime`、`pathlib`、`typing`，以及 `constants` ｜ **被依赖**：`tool.update_log`、`tool.read_update_log`、`tool.read_update_logs`、`core.__init__`
- **规模**：246 行 / 10 个 def
- **职责**：为项目变更提供只追加的历史表。每次写入只向调用方返回新 `update_id`、时间戳、系统名和 `next_update_id`，历史正文保留在 SQLite 中而不进入模型上下文；读取按显式 ID 或连续 ID 区间进行。
- **关键模块级对象**：`DEFAULT_UPDATE_LOG_FILENAME` 是默认文件名；`UpdateLogRepository` 是持久化类；`__all__` 暴露该默认名和类。

#### 类 `UpdateLogRepository` — 线程安全的更新日志仓库

- **构造**：`__init__(self, path: str | Path | None = None) -> None`。`None` 时优先读环境变量 `UPDATE_LOG_DB_PATH`，否则使用 `DEFAULT_UPDATE_LOG_FILENAME`；`~` 会展开；`":memory:"` 使用共享连接并立即初始化表。
- **关键属性**：`path`、`_lock`、`_shared_connection`；memory 连接设置 `check_same_thread=False` 和 10 秒 timeout。
- **方法**：
  - `_connect(self) -> sqlite3.Connection` — 共享连接非空时直接返回；否则新建连接，设置 row factory 和 10 秒 timeout。源码没有像 `ToolSpecRepository` 那样在 memory 连接关闭后显式拒绝重连，因此 close 后再调用可能创建新的连接对象。
  - `_connection(self) -> Iterator[sqlite3.Connection]` — `@contextmanager`；串行化连接，设置 `busy_timeout=10000` 和 `foreign_keys=ON`，事务上下文退出时提交或回滚，finally 关闭非共享连接。
  - `close(self) -> None` — 关闭 memory 共享连接；文件模式按调用连接，构造器不保留长期连接。
  - `_initialize(self) -> None` — 文件路径先创建父目录；创建 `update_logs` 表，`update_id INTEGER PRIMARY KEY AUTOINCREMENT` 是唯一索引/主键，其余字段均为 NOT NULL，`files_json` 保存文件变更数组。
  - `append(self, *, executor: str, update_type: str, title: str, task_background: str, update_details: str, added_features: str, files: Sequence[Mapping[str, str]], behavior_impact: str, validation: str, risks: str, follow_up: str, system_name: str | None = None, timestamp: str | None = None) -> dict[str, Any]` — 校验所有文本字段非空；`files` 不能是字符串/bytes，每项必须是 Mapping 且 path/action/description 非空，至少一项文件变更；`system_name` 默认为 `platform.system()`，空值回退 `"Unknown"`；`timestamp` 默认当前 UTC 秒精度 ISO 字符串；单次事务 INSERT，并返回 `update_id`、`timestamp`、`system_name`、`next_update_id = update_id + 1`。
  - `get(self, update_id: int) -> dict[str, Any] | None` — 校验正整数且排除 bool；按主键查询；找到时 `_decode`，否则 None。
  - `latest_id(self) -> int` — 查询 `MAX(update_id)`；空表返回 0。
  - `get_range(self, start_id: int, end_id: int) -> list[dict[str, Any]]` — 校验正整数且 `end_id >= start_id`；单条 SQL 使用 `BETWEEN ... AND ... ORDER BY update_id ASC`，返回值解码后列表。区间中缺失的 ID 不会被补齐，调用方需自行检查连续性。
  - `_decode(row: sqlite3.Row) -> dict[str, Any]` — `@staticmethod`；行转字典并把 `files_json` 反序列化后改名为 `files`。

**索引协议要点**：`update_id` 由 SQLite AUTOINCREMENT 单调生成；`append` 返回的 `next_update_id` 是写入后的提示值，实际权威最新 ID 应通过 `latest_id()` 获取。`get_range` 是包含端点的连续区间查询，但 SQL 不生成缺失记录；读取工具会检查返回数量并报告缺口。源码未提供 update/delete API，符合追加式日志设计。

### `memory/embedding.py` — API/离线嵌入服务接口与实现

- **拓扑层 / 依赖 / 被依赖**：嵌入基础层；只依赖标准库、`constants`，不依赖包内其他业务模块。被 `base`、`manager` 和包门面依赖，RAG/工具通过 `MemoryManager` 间接使用。
- **规模**：490 行 / 26 个 def。
- **职责**：提供统一的 `BaseEmbedding` 接口、OpenAI 兼容 HTTP 的 `APIEmbedding`、本机 `qwen-embed` 网关客户端 `EmbedServerEmbedding` 和离线确定性的 `HashEmbedding`。`APIEmbedding` 支持批量请求、按响应 `index/text_index` 排序、数量/维度/有限性校验；`gateway_reachable()` 只做 TCP 可达性探测，用于**报告降级**与**选择检索路径**，绝不用于切换嵌入实现（三套向量空间互不兼容）。无 key/网关时 `make_default_embedding` 会选择 `HashEmbedding`，但其向量空间与真实嵌入不兼容。
- **关键模块级对象/常量**：默认模型、base URL、批大小常量；`EMBED_GATEWAY_HINT`（网关不可达时的提示文案，刻意不含地址，隧道命令由部署文档下发）；`__all__` 包含 `DEFAULT_EMBEDDING_BASE_URL`、`DEFAULT_EMBEDDING_BATCH_SIZE`、`DEFAULT_EMBEDDING_MODEL`、`APIEmbedding`、`BaseEmbedding`、`EmbedServerEmbedding`、`load_dotenv_once`，但未把 `HashEmbedding` 列入 `__all__`（包门面仍显式导入它）。旧别名 `EmbeddingService = BaseEmbedding` 已删除，由 `EmbedServerEmbedding` 取代。

#### 函数 `load_dotenv_once() -> None` — 可选加载项目 `.env`

- **签名**：`def load_dotenv_once() -> None:`
- **参数**：无。
- **返回**：`None`。
- **副作用/异常**：尝试导入 `dotenv.load_dotenv`；不可用时直接返回；成功时调用 `load_dotenv(override=False)`，已有环境变量优先。导入或加载异常未统一包装。
- **调用关系**：`APIEmbedding.__init__`、`MemoryConfig.from_env`、`make_default_embedding`。

#### 类 `BaseEmbedding(ABC)` — 嵌入服务最小接口

- **构造**：抽象基类，无显式 `__init__`；类属性 `dimension: int = 0`。
- **关键属性**：`dimension` 为 0 时表示尚未确定或不做固定维度校验。
- **方法**：
  - `embed(self, text: str) -> list[float]` — `@abstractmethod`；要求实现把单条文本转为有限数值向量；基类实现抛出 `NotImplementedError`。
  - `embed_batch(self, texts: Iterable[str]) -> list[list[float]]` — 默认实现先把 iterable 物化为列表，拒绝非字符串元素（`TypeError`），再逐条调用 `embed`；返回与输入同序的向量列表；不提供空列表特判，空输入返回空列表。

#### 类 `HashEmbedding(BaseEmbedding)` — 无网络依赖的确定性哈希袋词嵌入

- **构造**：`def __init__(self, dimension: int = MEMORY_EMBEDDING_DIMENSION) -> None`；`dimension` 必须是正整数且不能是 bool，否则抛 `ValueError`。
- **关键属性**：`dimension` 为固定桶数；向量是稀疏计数经 L2 归一化后的稠密列表。
- **方法**：
  - `tokenize(text: str) -> list[str]` — `@staticmethod`；用 `re.findall(r"\w+", text.casefold(), flags=re.UNICODE)` 提取 Unicode 词/字符 token；返回 token 列表；非字符串会在调用 `casefold()` 时抛 `AttributeError`。
  - `_index(self, token: str) -> int` — 私有方法；对 token 的 UTF-8 bytes 做 BLAKE2b 8 字节摘要，取大端整数后对 `dimension` 取模；返回桶下标。
  - `embed(self, text: str) -> list[float]` — 参数必须是 `str`，否则抛 `TypeError`；`Counter` 统计 token，桶中累加 `1.0 + log(count)`，最后按 L2 范数归一化；空文本或没有任何 token 时返回全零向量。无外部副作用。
  - `to_dict(self) -> dict[str, Any]` — 返回类型名和维度；不包含任何权重/密钥。
  - `__repr__(self) -> str` — 返回可读构造摘要。
- **注意**：哈希桶只有同号累加，没有带符号散列；不同 token 可能冲突。该实现只适合离线/本地回退，不保证与 API 模型的语义空间兼容。

#### 类 `APIEmbedding(BaseEmbedding)` — OpenAI 兼容 `/embeddings` 客户端

- **构造**：`def __init__(self, api_key: str | None = None, *, model: str = DEFAULT_EMBEDDING_MODEL, base_url: str = DEFAULT_EMBEDDING_BASE_URL, dimension: int | None = None, timeout: float = 30.0, batch_size: int = DEFAULT_EMBEDDING_BATCH_SIZE, client: Any = None, api_key_env: str = "DASHSCOPE_API_KEY") -> None`。
  - `model`、`base_url`、`api_key_env` 必须为非空字符串；`dimension` 若给出必须为正整数；`timeout`、`batch_size` 必须为正。
  - `api_key=None` 时调用 `load_dotenv_once()` 并从 `api_key_env` 读取；仍无有效 key 抛 `RuntimeError`。
  - `client` 可用于测试注入，不改变核心批处理和校验。
- **关键属性**：`api_key`（去除首尾空白）、`model`、`base_url`（去尾部 `/`）、`timeout`、`batch_size`、`client`、`api_key_env`、`dimension`（未配置时为 0）。
- **方法**：
  - `embed(self, text: str) -> list[float]` — 校验字符串，然后调用 `embed_batch([text])[0]`；非字符串抛 `TypeError`；返回单向量。
  - `embed_batch(self, texts: Iterable[str]) -> list[list[float]]` — 物化 iterable，拒绝非字符串元素；空输入返回 `[]`；按 `batch_size` 切片，逐批调用模块函数 `_embed_batch_once`；按输入批次顺序扩展结果。API/校验错误向上传递；不自动重试。
  - `to_dict(self) -> dict[str, Any]` — 返回类型、模型、base URL、维度、批大小；不返回 API key。
  - `__repr__(self) -> str` — 返回模型、维度和 base URL 摘要；不暴露 key。
- **调用关系**：`embed_batch` 调用 `_embed_batch_once`，后者调用 `_request` 和 `_extract_embedding_vectors`。

#### 函数 `_embed_batch_once(embedding: APIEmbedding, values: list[str]) -> list[list[float]]` — 执行单批嵌入并规范化响应

- **签名**：`def _embed_batch_once(embedding: APIEmbedding, values: list[str]) -> list[list[float]]:`
- **参数**：所属 `APIEmbedding` 和一批非空/已类型校验的字符串。
- **返回**：统一为 `list[list[float]]` 的向量，长度与输入一致，且全部长度一致、数值有限。
- **副作用/异常**：请求后检查数量、空向量、维度一致性、`math.isfinite` 和预期维度；失败抛 `RuntimeError`；成功后把 `embedding.dimension` 设置为实际向量维度。无重试/退避。
- **调用关系**：由 `APIEmbedding.embed_batch` 按批调用。

#### 函数 `_request(embedding: APIEmbedding, values: list[str]) -> Any` — 调 HTTP 或注入测试客户端并解码 JSON

- **签名**：`def _request(embedding: APIEmbedding, values: list[str]) -> Any:`
- **参数**：`embedding` 提供模型、base URL、key、超时和可选 client；`values` 为本批文本。
- **返回**：调用方 `client`、`client.embed(...)` 或 `client.embeddings.create(...)` 的返回值，或 HTTP JSON 解码结果。
- **副作用/异常**：若注入 client 可调用则调用 `client(payload, model=...)`；否则尝试 `embed(values, model=...)` 或 `embeddings.create(input=values, model=...)`，无匹配接口抛 `TypeError`。无 client 时向 `{base_url}/embeddings` POST JSON，异常映射：HTTPError 转 `RuntimeError` 并截取响应体 500 字符；URLError 转 `RuntimeError`；JSON `ValueError` 转 `RuntimeError`。注入 client 自身异常不包装。
- **调用关系**：`_embed_batch_once`。

#### 函数 `_extract_embedding_vectors(response: Any) -> list[list[float]]` — 从 OpenAI/DashScope 响应提取有序向量

- **签名**：`def _extract_embedding_vectors(response: Any) -> list[list[float]]:`
- **参数**：dict 或带属性的响应对象。
- **返回**：按显式 `index`（或 `text_index`）排序后的向量列表；没有显式位置时按枚举顺序；若位置不是连续 `0..n-1` 抛 `RuntimeError("embedding response indices were incomplete")`。
- **副作用/异常**：支持 `data[].embedding` 和 `output.embeddings`；缺数据、缺向量、向量不能转 float、空向量均抛 `RuntimeError`。显式位置 `int(explicit)` 的转换异常未被该函数捕获，会原样冒出 `ValueError`/`TypeError`；这点属于源码中可见的行为边界。
- **调用关系**：`_embed_batch_once`。

#### 函数 `gateway_reachable(base_url: str, *, timeout: float = 1.0) -> bool` — 探测 `/embed` 网关端口是否可连

- **签名**：`def gateway_reachable(base_url: str, *, timeout: float = 1.0) -> bool:`
- **参数 / 返回**：`base_url` 为网关地址；`timeout` 为 TCP 连接超时。返回该地址的 host:port 是否能建立 TCP 连接。
- **关键实现 / 调用关系**：用 `urllib.parse.urlsplit` 取 host/port（默认 80/443）；`socket.create_connection` 探测，`OSError` 一律返回 `False`。用途是**报告降级**（`/api/health` 的 `embedding_reachable`）和 Phase 3 起的**检索路径选择**（`pipeline._vector_hits`）。它**绝不能**用来换嵌入实现：网关/公网 API/HashEmbedding 三套向量空间互不兼容，配置了但不可达的网关必须保持配置，让调用方降级到关键词检索而不是混入哈希向量。
- **副作用/异常**：只做网络探测；空或非字符串 `base_url` 返回 `False`。

#### 类 `EmbedServerEmbedding(BaseEmbedding)` — 本机 `qwen-embed` 网关（自定义 `/embed` 协议）客户端

- **构造**：`def __init__(self, api_key: str | None = None, *, base_url: str = DEFAULT_EMBEDDING_BASE_URL, dimension: int | None = None, timeout: float = 60.0, batch_size: int = DEFAULT_EMBEDDING_BATCH_SIZE, client: Any = None) -> None`。
  - `base_url` 必须非空字符串；`dimension` 若给出必须为正整数；`timeout`、`batch_size` 必须为正，否则 `ValueError`。
  - `base_url` 默认即 constants 的 `DEFAULT_EMBEDDING_BASE_URL`（本机隧道 `http://127.0.0.1:10800`）。`client` 用于测试注入。
- **关键属性**：`api_key`（可选，用于 `Authorization: Bearer`）、`base_url`（去尾部 `/`）、`timeout`、`batch_size`、`client`、`dimension`（未配置时为 0，首批后由响应维度确定）。
- **方法**：
  - `embed(self, text: str) -> list[float]` — 校验字符串后 `embed_batch([text])[0]`；非字符串抛 `TypeError`。
  - `embed_batch(self, texts: Iterable[str]) -> list[list[float]]` — 物化 iterable、拒绝非字符串；空输入返回 `[]`；按 `batch_size` 切片逐批调 `_embed_server_batch_once`。
  - `to_dict(self) -> dict[str, Any]` — 返回 `type/base_url/dimension/batch_size`；不含 key。
  - `__repr__(self) -> str` — 维度与 base URL 摘要。
- **协议说明**：请求 `{"texts": [...]}` POST 到 `{base_url}/embed`，响应 `{"embeddings": [[...], ...]}` 加可选 `dim`；数量、维度、有限性校验与 `APIEmbedding` 一致。网关自带模型时无需 key，但可传可选 Bearer。若网关讲 OpenAI `/embeddings` 形状则应改用 `APIEmbedding`。

#### 函数 `_embed_server_batch_once(embedding: EmbedServerEmbedding, values: list[str]) -> list[list[float]]` — 网关单批嵌入与校验

- **签名**：`def _embed_server_batch_once(embedding: EmbedServerEmbedding, values: list[str]) -> list[list[float]]:`
- **参数 / 返回**：所属实例和一批字符串；返回与输入同序、同维度、全有限的向量。
- **副作用/异常**：请求后检查数量一致、非空向量、维度一致、`math.isfinite` 与预期维度；失败抛 `RuntimeError`；成功后把 `embedding.dimension` 设为实际维度。无重试。
- **调用关系**：`EmbedServerEmbedding.embed_batch` 按批调用。

#### 函数 `_embed_server_request(embedding: EmbedServerEmbedding, values: list[str]) -> Any` — 发 `POST {base_url}/embed` 并解析 JSON

- **签名**：`def _embed_server_request(embedding: EmbedServerEmbedding, values: list[str]) -> Any:`
- **参数 / 返回**：返回注入 client 的返回值或 HTTP JSON 解码结果。
- **关键实现 / 副作用/异常**：有注入 client 时按可调用/`embed()`/`embeddings.create()` 顺序适配；无 client 时 POST JSON，可选带 `Authorization: Bearer`；`HTTPError` 转 `RuntimeError`（截取响应体 500 字符）、`URLError` 转 `RuntimeError`（附 `EMBED_GATEWAY_HINT`）、JSON `ValueError` 转 `RuntimeError`。
- **调用关系**：`_embed_server_batch_once`。

#### 函数 `_extract_embed_server_vectors(response: Any) -> list[list[float]]` — 提取网关裸 `embeddings` 列表

- **签名**：`def _extract_embed_server_vectors(response: Any) -> list[list[float]]:`
- **参数 / 返回**：dict（取 `embeddings`）或带属性对象（取 `.embeddings`）；返回向量列表。
- **副作用/异常**：缺列表、单项含不可转 float 值或空向量均抛 `RuntimeError`。
- **调用关系**：`_embed_server_batch_once`。

---

### `memory/ids.py` — 实体、关系和事实主键派生

- **拓扑层 / 依赖 / 被依赖**：叶子工具模块；只依赖标准库和 `constants`。被 `memory.types.semantic` 与 `memory.rag.knowledge` 依赖，避免 `manager -> types.semantic -> rag.knowledge -> manager` 的导入环。
- **规模**：81 行 / 6 个 def。
- **职责**：提供统一的实体名规范化、实体 ID、关系 ID、单值槽位谓词 key 和旧版事实 ID。规范化和哈希规则保证 semantic 层与 RAG 抽取对同一三元组派生相同主键，也保留旧 `fact:<s>|<p>|<o>` 行的原地更新兼容路径。
- **关键模块级对象/常量**：`ENTITY_CLEAN_MAX_LENGTH = 500`、`ENTITY_NAME_MAX_LENGTH = 200`；`__all__` 包含 `entity_id_for`、`legacy_fact_id_for`、`normalize_entity_name`、`predicate_key_for`、`relation_id_for`，不包含私有 `_clean_text`。

#### 函数 `_clean_text(value: Any, *, max_length: int = ENTITY_CLEAN_MAX_LENGTH) -> str` — 私有文本清理

- **签名**：`def _clean_text(value: Any, *, max_length: int = ENTITY_CLEAN_MAX_LENGTH) -> str:`
- **参数**：任意值和非关键字默认上限；非字符串返回空字符串。
- **返回**：用任意空白拆分后以单个空格连接，截断到 `max_length`，再 `strip()`。
- **副作用/异常**：无 I/O；无显式异常处理。
- **调用关系**：`normalize_entity_name`。

#### 函数 `normalize_entity_name(value: str) -> str` — 生成稳定比较键

- **签名**：`def normalize_entity_name(value: str) -> str:`
- **参数**：实体显示名。
- **返回**：先清理并 `casefold()`，再删除所有非 `\w`/非中文字符范围字符；返回可用于哈希的比较键，不返回原显示标签。
- **副作用/异常**：非字符串经 `_clean_text` 变成空字符串，最终返回空字符串；无其他显式异常。
- **调用关系**：`entity_id_for`、`relation_id_for`、`predicate_key_for`、RAG 知识物化层。

#### 函数 `entity_id_for(name: str) -> str` — 派生实体主键

- **签名**：`def entity_id_for(name: str) -> str:`
- **参数**：实体名称。
- **返回**：规范化 key 的 SHA-256 十六进制摘要前 20 字符，格式 `entity:<20 hex>`。
- **副作用/异常**：规范化后为空时抛 `ValueError("entity name must contain at least one searchable character")`；无 I/O。
- **调用关系**：供 semantic/RAG 实体统一；本模块外主调用方见 RAG 知识抽取。

#### 函数 `relation_id_for(subject: str, predicate: str, object: str) -> str` — 派生关系/事实主键

- **签名**：`def relation_id_for(subject: str, predicate: str, object: str) -> str:`
- **参数**：三元组三个端点/谓词。
- **返回**：三个端点分别规范化为比较键，以 `|` 连接后取 SHA-256 前 24 字符，格式 `relation:<24 hex>`。
- **副作用/异常**：源码未显式校验空值；空字符串也会参与 hash，因此可能生成稳定但语义无效的 ID。
- **调用关系**：`SemanticMemory.add_fact` 和 RAG 知识物化层共同使用，从而避免同一三元组双主键。

#### 函数 `predicate_key_for(subject: str, predicate: str) -> str` — 派生单值槽位键

- **签名**：`def predicate_key_for(subject: str, predicate: str) -> str:`
- **参数**：主语和谓词，不包含 object。
- **返回**：规范化后的 `subject|predicate` 的 SHA-256 前 24 字符；用于识别 `余额`、`当前版本` 等单值槽位并支持新值淘汰旧值。
- **副作用/异常**：源码未显式校验空值。
- **调用关系**：RAG 知识物化层写入 semantic metadata；semantic 模块本身只透传该字段到图属性。

#### 函数 `legacy_fact_id_for(subject: str, predicate: str, object: str) -> str` — 生成旧版事实 ID

- **签名**：`def legacy_fact_id_for(subject: str, predicate: str, object: str) -> str:`
- **参数**：三元的原文值。
- **返回**：`f"fact:{subject.strip()}|{predicate.strip()}|{object.strip()}"`；保留旧数据库可读形式。
- **副作用/异常**：非字符串会因 `.strip()` 抛 `AttributeError`；不做大小写/标点归一化。
- **调用关系**：`SemanticMemory.add_fact` 在 canonical ID 不存在时回退查找旧记录，更新旧行而不是创建重复行。

---

### `memory/rag/document.py` — 文档归一化、格式解析、切块与路径收容

- **拓扑层 / 依赖 / 被依赖**：L2；依赖 `constants`、标准库 `csv/io/json/os/re/pathlib/dataclasses/typing/uuid`；被 `memory.rag.__init__`、`pipeline` 以及 Web 上传摄取链依赖。
- **规模**：162 行 / 7 个 def
- **职责**：定义不可变文档值与 `txt/md/json/jsonl/csv/html/pdf` 等本地内容的解析器，并把文档按字符窗口切成带重叠的语义记忆块。解析明确区分“字面文本”和“文件路径”，切块补齐 `document_id` 与 `chunk_index`，`resolve_within` 提供目录收容边界。Phase 2 起提供 `normalized_text()`/`chunks_with_spans()`：**归一化正文是 `documents.raw_text` 与切块偏移的唯一共同来源**，`ChunkSpan` 携带每块在归一化正文里的 `char_start/char_end`，供重切与 UI 高亮复用。
- **关键模块级对象/常量/单例**：`__all__ = ["ChunkSpan", "Document", "DocumentProcessor", "resolve_within"]`；默认块参数来自 `RAG_CHUNK_SIZE=1000`、`RAG_CHUNK_OVERLAP=100`。可选 PDF 解析依赖运行时 `pypdf`；未安装时抛 `RuntimeError`。模块无单例。

#### 类 `ChunkSpan` — 块 + 它在归一化正文中的字符区间

- **构造**：`@dataclass(frozen=True)`，字段 `chunk: Document`、`char_start: int`、`char_end: int`。
- **关键属性**：`char_start/char_end` 索引到 `DocumentProcessor.normalized_text` 的输出（即 `documents.raw_text` 存的同一份归一化文本），因此偏移可用于重新切片与前端高亮（方案 2.3）。
- **调用关系**：`DocumentProcessor.chunks_with_spans` 返回；`RAGPipeline.ingest` 用它把偏移写入 `ChunkRecord`。

#### 类 `Document` — 已验证的非空文档数据单元

- **构造**：dataclass 字段为 `content: str`、`id: str = field(default_factory=lambda: str(uuid4()))`、`metadata: Mapping[str, Any] = field(default_factory=dict)`；`frozen=True` 不允许常规属性赋值。
- **关键属性**：`content` 是正文；`id` 是文档标识，默认 UUID 字符串；`metadata` 在构造后转为普通 `dict`。
- **方法**：`__post_init__(self) -> None` — 校验 `content` 为非空字符串、`id` 为非空字符串；把 `metadata or {}` 复制为 `dict` 并通过 `object.__setattr__` 写回 frozen 实例。参数仅为自身；返回 `None`；正文/标识非法时抛 `ValueError`；上下文是 dataclass 自动构造后调用，Web/RAG 创建 `Document` 时都会经过该校验。

#### 类 `DocumentProcessor` — 多格式文档解析与重叠切块

- **构造**：源码未定义 `__init__`，使用无状态默认构造。
- **关键属性**：无实例属性。
- **方法**：

##### `parse(self, source: str | Path | io.TextIOBase | bytes, *, metadata: Mapping[str, Any] | None = None) -> Document`

作用：把一个输入源归一为 `Document`。参数 `source` 可为 `Path`/任意 `__fspath__` 对象、`bytes`、文本流或 `str`；参数 `metadata` 为可选元数据映射。返回新 `Document`。

分支与副作用：`Path`/`os.PathLike` 会真正读取文件字节，并自动加入 `source`、`filename`、`extension`，随后调用 `_parse_bytes`；`bytes` 按 UTF-8 容错解码；`io.TextIOBase` 调用 `read()`；`str` 始终视为字面文本，绝不会探测文件系统。路径读取、格式解析或 `Document` 校验异常原样向上抛；不支持的输入类型抛 `TypeError("source must be text, bytes, a path, or a text stream")`。调用关系：`RAGPipeline.ingest_source` 传入 `Path`；直接调用方若传 `str` 得到的是文本而非文件内容。

##### `normalized_text(self, document: Document) -> str`

作用：返回文档的唯一归一化正文——把 `document.content` 中连续空白折叠为单个空格并 `strip()`。**归一化只发生在这里**：`documents.raw_text` 与 `chunks_with_spans` 的偏移都消费同一份文本，若两处各归一次就会让 `char_start/char_end` 指向错误的正文。参数为源文档；返回归一化字符串。调用关系：`chunks_with_spans` 与 `RAGPipeline.ingest`（写 `DocumentRecord.raw_text`）。

##### `chunks(self, document: Document, *, chunk_size: int = RAG_CHUNK_SIZE, overlap: int = RAG_CHUNK_OVERLAP) -> list[Document]`

作用：`[span.chunk for span in self.chunks_with_spans(...)]` 的便捷封装——沿用旧调用点的返回形状。参数、校验与异常和 `chunks_with_spans` 完全一致。调用关系：既有调用方（`RAGPipeline.ingest` 已改用 `chunks_with_spans`）。

##### `chunks_with_spans(self, document: Document, *, chunk_size: int = RAG_CHUNK_SIZE, overlap: int = RAG_CHUNK_OVERLAP) -> list[ChunkSpan]`

作用：按 `step = chunk_size - overlap` 滑动切块；每块元数据复制原文并加入 `document_id` 和从 0 开始的 `chunk_index`，块 id 为 `f"{document.id}:{index}"`；同时记录每块在归一化正文中的 `[char_start, char_end)`（末块取 `min(start+chunk_size, len(text))`）。参数分别是源文档、块字符数和重叠字符数。返回 `list[ChunkSpan]`；空文本返回空列表。

校验与异常：`chunk_size` 必须是正 `int`，`bool` 被明确拒绝；`overlap` 必须是大于等于 0 且小于 `chunk_size` 的 `int`，`bool` 同样被拒绝，否则抛 `ValueError`。副作用：只创建新对象，不修改源 `Document`。调用关系：`RAGPipeline.ingest` 对每个输入文档调用它，并把 `char_start/char_end` 写入 `ChunkRecord`。

##### `@staticmethod _parse_bytes(raw: bytes, extension: str) -> str`

作用：按扩展名把字节转为文本。`.jsonl` 逐行 `json.loads` 后再以 UTF-8 JSON 行连接；`.json` 解析后缩进格式化；`.csv` 转为以 ` | ` 分列的文本；`.html/.htm` 用正则剥离标签；`.pdf` 延迟导入 `pypdf.PdfReader` 并拼接每页文本；其他扩展名按 UTF-8 容错解码。参数为文件和已小写扩展名；返回字符串。

异常与副作用：JSON 解码错误、JSON 结构错误、PDF 解析错误会向上抛；缺少 `pypdf` 时抛 `RuntimeError("PDF parsing requires pypdf")`。不直接写持久化状态。调用关系：仅由 `DocumentProcessor.parse` 调用。

#### 函数 `resolve_within(base_dir: str | Path, path: str | Path) -> Path`

作用：把相对路径解析到 `base_dir` 下，并拒绝越界结果，供接收调用方路径的摄取入口做沙箱边界。参数 `base_dir` 必须是 `str` 或 `Path`，`path` 可为字符串或路径。返回解析后的绝对 `Path`。

行为与异常：展开 `~`、拼接相对路径、调用 `resolve()`，再用 `os.path.normcase` 后比较“等于基目录或以 `base + os.sep` 开头”。`base_dir` 类型错误抛 `TypeError`；越界抛 `ValueError`。副作用是文件系统路径解析，不读写文件。调用关系：`RAGPipeline.ingest_source(base_dir=...)` 调用它；直接依赖 `DocumentProcessor.parse` 的调用方并不会自动获得这层保护。

### `web/domain_classifier.py` — 零依赖、确定性的本地领域分类器

- **拓扑层 / 依赖 / 被依赖**：L2；只依赖 `constants.DEFAULT_DOMAIN` 与 `DOMAIN_TITLE_WEIGHT`；被 `web.graph_builder` 用于知识块、文档实体和领域恒星分类。
- **规模**：111 行 / 2 个 def
- **职责**：用人工关键词表把文本归入编程开发、数学、物理、化学、生物医学、历史人文、经济管理、文学艺术之一；标题命中权重为 3，正文命中权重为 1。无命中回退“未分类”，同样的输入稳定得到同样结果。
- **关键模块级对象/常量/单例**：`DOMAIN_KEYWORDS` 是 `dict[str, tuple[str, ...]]`；`KNOWN_DOMAINS` 从键构造并保持插入顺序；`DEFAULT` 是 `DEFAULT_DOMAIN` 的别名。无单例、无 I/O、无外部依赖。

#### 函数 `classify_domain(text: str, *, title: str = "", default: str = DEFAULT) -> str`

作用：把文本和标题转为小写，对每个领域逐关键词打分；关键词在标题中加 `DOMAIN_TITLE_WEIGHT`，否则在正文中加 1；取严格更高分者，全部为 0 返回 `default`。参数为正文、标题、兜底领域；返回领域名。函数对空正文/标题、`None` 入参有 `(text or "")`/`(title or "")` 容错；无写入副作用。调用关系：`build_graph.build_graph` 对每个 RAG 块调用。

#### 函数 `majority_domain(domains: list[str], *, default: str = DEFAULT) -> str`

作用：统计数据列表的众数；空列表返回 `default`。参数为领域列表和兜底值；返回领域名。实现依赖 `max` 的稳定性，因此并列时返回先插入 `counts` 的领域；无副作用。调用关系：`build_graph` 在文档所有知识块分类完后，把文档实体重新挂到多数领域。

## L3 运行时骨架（工具目录 / 发现 / 解析 / 调度 / 技能发现注册 / 记忆实体与存储环）

### `core/catalog.py` — 工具目录查询、契约解析和可执行惰性加载门面

- **拓扑层**：L3 ｜ **依赖**：标准库 `re`、`typing`，`pydantic`，以及 `core.models`、`core.registry`、`core.repository` ｜ **被依赖**：`core.__init__`；`agents.agent` 构造并注册为 `system.tool_catalog`
- **规模**：287 行 / 10 个 def
- **职责**：为模型提供受限的搜索、获取规格和解析工具接口。它不接收或执行原始 SQL，可同时查看当前注册表和持久化目录；当实现未加载时会返回持久化元数据，支持先发现目录、后续再执行的工作流。
- **关键模块级对象**：`CatalogInput`、`CatalogOutput`、`ToolCatalogTool`。

#### 类 `CatalogInput(BaseModel)` — 目录工具输入

- **构造**：Pydantic 字段 `action: Literal["search", "get_spec", "resolve"]`、`intent: str | None = None`、`tool_name: str | None = None`、`version: str | None = None`、`limit: int = 20`；字符串有长度限制，limit 在 1..20。
- **关键属性**：`model_config = ConfigDict(extra="forbid", strict=True)`。
- **方法**：
  - `validate_action_arguments(self) -> CatalogInput` — `@model_validator(mode="after")`；`get_spec` 必须有非空 `tool_name`，`resolve` 必须有非空 `intent`，否则抛 `ValueError`。

#### 类 `CatalogOutput(BaseModel)` — 目录工具输出

- **构造**：`candidates: list[dict[str, Any]] = []`、`spec: dict[str, Any] | None = None`、`specs: list[dict[str, Any]] = []`。
- **关键属性**：`candidates` 用于搜索结果；`spec` 为兼容单工具调用保留；`specs` 可返回多个完整契约。
- **方法**：无自定义方法定义。

#### 类 `ToolCatalogTool(BaseTool)` — 只读的工具目录工具实现

- **构造**：`__init__(self, registry: ToolRegistry, repository: ToolSpecRepository | None = None, *, repository_only: bool = False) -> None`。`registry` 是当前可执行注册表；`repository` 是可选的持久化目录；`repository_only` 决定搜索/resolve 是否始终走持久化层。
- **关键属性**：`registry`、`repository`、`repository_only`；类级 `spec` 为 `system.tool_catalog` 1.0，只读，tags 为 catalog/discovery。
- **方法**：
  - `execute(self, arguments: CatalogInput, context: ExecutionContext | None = None) -> CatalogOutput` — 校验参数和 context；`get_spec` 先查活动注册表，未加载但 repository 有记录时返回 stored full spec，若活动实现存在则校验版本及 repository schema 同步；`search`/`resolve` 在 repository_only 或当前没有其他已加载工具时走 repository；否则搜索活动 specs；resolve 返回一个或多个完整契约，无匹配时抛 `ValueError`；search 返回候选摘要和代次。context 目前不影响搜索行为。
  - `_active_generation(self, spec: ToolSpec) -> int` — 解析当前 spec，若期间注册表对象与传入 spec 不再相等则抛 `ValueError`；否则返回代次。
  - `_has_loaded_tools(self) -> bool` — 判断注册表中除 catalog 自身外是否还有工具。
  - `_stored_generation(self, stored: dict[str, Any]) -> int` — 若持久化工具已在注册表中且版本和 schema_hash 一致，返回真实代次；否则返回 0（catalog-only）。
  - `_search_specs(self, intent: str, limit: int, _context: ExecutionContext) -> list[ToolSpec]` — 先取活动 specs；有 repository 时只保留版本和 schema_hash 与 stored 一致的项；用 intent terms 对名称、描述和 tags 计分；空词返回按名称排序的前 limit 项，否则只返回得分大于 0 的项。
  - `_intent_terms(intent: str) -> set[str]` — `@staticmethod`；小写化并把点转为空格，提取 ASCII 词；对常见中文能力词做固定别名扩展（日志、记录、读取、查看、查询、参数、工具、列表、全部、时间、搜索、网页、记忆）；不使用 LLM 或动态 SQL。
  - `_full_stored_spec(stored: dict[str, Any], generation: int) -> dict[str, Any]` — `@staticmethod`；把 SQLite 解码行转换为与 `_full_spec` 同形的完整契约字典。
  - `_full_spec(spec: ToolSpec, generation: int) -> dict[str, Any]` — `@staticmethod`；把活动 spec 的 summary 扩展为完整 schema、权限、超时、幂等/并行属性和代次。

### `core/discovery.py` — 单文件工具插件发现、导入、验证和注册

- **拓扑层**：L3 ｜ **依赖**：标准库 `importlib`、`inspect`、`pkgutil`、`sys`、`dataclasses`、`pathlib`、`types`、`typing`，以及 `core.activity_log`、`core.models`、`core.registry`、`core.repository` ｜ **被依赖**：`core.__init__`；`agents.agent`、`agents.react` 通过 discover 包装器调用
- **规模**：511 行 / 15 个 def
- **职责**：扫描一个受信任包的直接子模块，执行每个模块的 `TOOL_ENABLED` 和零参数 `create_tool()` 协议，把实例注册到 `ToolRegistry`，并可选同步持久化元数据。单个插件异常被记录在报告里而不会阻断其他独立插件；严格模式最后统一抛错。`metadata_only=True` 只保存静态契约，不调用工厂、不保留执行实例，服务于 catalog-first 惰性加载。
- **关键模块级对象**：`DiscoveryStatus` 是状态字面量类型；`ToolDiscoveryRecord`、`ToolDiscoveryReport` 是报告值对象；`ToolDiscoveryError` 是严格模式错误；`discover_tools` 是主入口。

#### 类 `ToolDiscoveryRecord` — 一个模块的发现结果

- **构造**：生成字段 `module: str`、`path: str | None`、`enabled: bool | None`、`status: DiscoveryStatus`、`tool_name: str | None = None`、`version: str | None = None`、`generation: int | None = None`、`error: str | None = None`。
- **关键属性**：记录忽略、禁用、注册、已注册或错误状态及可选诊断。
- **方法**：
  - `as_dict(self) -> dict[str, Any]` — 使用 `dataclasses.asdict` 返回 JSON 友好字典。

#### 类 `ToolDiscoveryReport` — 一次包扫描的不可变报告

- **构造**：生成字段 `package: str`、`records: tuple[ToolDiscoveryRecord, ...]`。
- **关键属性**：`records` 包含全部模块结果；调用方不应在报告生成后修改。
- **方法**：
  - `errors(self) -> tuple[ToolDiscoveryRecord, ...]` — `@property`；筛出 status 为 `"error"` 的记录。
  - `registered(self) -> tuple[ToolDiscoveryRecord, ...]` — `@property`；同时包含 `"registered"` 和 `"already_registered"`。
  - `ok(self) -> bool` — `@property`；没有 error 记录时为 True。
  - `for_tool(self, name: str) -> ToolDiscoveryRecord | None` — 校验非空工具名，返回第一条 tool_name 匹配记录或 None。
  - `as_dict(self) -> dict[str, Any]` — 返回 package、ok 和逐条序列化 records。

#### 类 `ToolDiscoveryError(RuntimeError)` — 严格扫描失败异常

- **构造**：`__init__(self, report: ToolDiscoveryReport) -> None`，保存 report，并把所有 error 记录的 module/error 拼进异常消息。
- **关键属性**：`report`。
- **方法**：仅有 `__init__`，无额外方法。

#### 函数 `discover_tools(registry: ToolRegistry, *, package: str | ModuleType = "tool", repository: ToolSpecRepository | None = None, replace: bool = False, strict: bool = False, reload_modules: bool = False, metadata_only: bool = False) -> ToolDiscoveryReport` — 扫描并注册直接子模块

- **参数**：`registry` 为目标注册表；`package` 是包名或已导入模块；`repository` 是可选元数据仓库；`replace` 允许替换不同实现；`strict` 在有 error 时抛异常；`reload_modules` 对已导入模块调用 reload；`metadata_only` 要求 repository 且只保存契约不创建实例。
- **返回**：`ToolDiscoveryReport`。
- **关键实现**：
  1. 校验 registry、repository 和四个布尔参数；metadata_only 无 repository 直接 `ValueError`。
  2. `_load_package` 导入包并要求有 `__path__`；包导入失败时记录 error，严格模式立即抛 `ToolDiscoveryError`。
  3. 清导入缓存，按模块名排序扫描直接子模块；以下划线开头、名为 `base` 或子包记录为 ignored。
  4. 普通模块 import/reload；异常被记录为 module import failed 并继续。
  5. 每个启用模块必须有 bool `TOOL_ENABLED` 和可无参调用 `create_tool`，否则生成 error。
  6. 常规模式调用 factory 并注册；metadata_only 模式调用 `_register_discovered_metadata`。单模块异常转换为 error 记录，尽量保留工具名、版本和已有代次。
  7. 汇总报告、调用发现日志；strict 且有 error 时抛异常，否则返回报告。
- **调用关系**：调用 `_load_package`、`_candidate_path`、`_error_record`、`_register_discovered_tool`、`_register_discovered_metadata`、`registry` 和 `log_discovery_summary`；由 Agent 的 `discover_tools` 和 core 门面调用。

#### 函数 `_load_package(package: str | ModuleType) -> tuple[str, ModuleType | None, ToolDiscoveryRecord | None]` — 导入并验证工具包

- **参数**：包名或 `ModuleType`。
- **返回**：`(package_name, package_module_or_None, error_record_or_None)`。
- **关键实现**：模块对象直接使用 `__name__`；字符串必须非空并 import；其他类型抛 `TypeError`；包导入异常转 error record；缺少 `__path__` 时返回 error record。
- **调用关系**：仅由 `discover_tools` 调用；内部调用 `_error_record`。

#### 函数 `_register_discovered_tool(registry: ToolRegistry, module: ModuleType, module_path: str | None, tool: object, *, repository: ToolSpecRepository | None, replace: bool) -> ToolDiscoveryRecord` — 注册已创建工具并同步元数据

- **参数**：目标 registry、模块、路径、factory 返回对象、可选 repository 和 replace 标志。
- **返回**：`ToolDiscoveryRecord`。
- **关键实现**：验证 tool 是 BaseTool 且有 ToolSpec；若同名实现类型和 spec 完全相同则保存元数据并返回 already_registered，不 bump 代次；若不同且 replace=False 抛 `ValueError`；否则注册（存在同名时 replace=True）、解析当前代次并保存 implementation_ref。
- **调用关系**：由 `discover_tools` 调用；调用 `registry.register/resolve` 和 `_save_spec`。

#### 函数 `_register_discovered_metadata(module: ModuleType, module_path: str | None, registry: ToolRegistry, *, repository: ToolSpecRepository | None, replace: bool) -> ToolDiscoveryRecord` — 不调用工厂地保存静态 ToolSpec

- **参数**：模块、路径、当前注册表、可选仓库和 replace。
- **返回**：`ToolDiscoveryRecord`。
- **关键实现**：遍历模块变量，找出定义在本模块且同时是 BaseTool 子类、带 ToolSpec 的类；必须恰好一个，否则抛 `TypeError`；若同名活动实例 spec 相同则保存并返回 already_registered；若持久化 schema 不同且 replace=False 抛 `ValueError`；最后保存元数据但不注册执行实例，generation 保持 None。
- **调用关系**：由 `discover_tools(metadata_only=True)` 调用；调用 `_save_metadata`。

#### 函数 `_save_spec(repository: ToolSpecRepository | None, spec: ToolSpec, module: ModuleType, tool: BaseTool) -> None` — 保存可执行工具的元数据

- **参数**：repository、spec、模块、实例。
- **返回**：None。
- **关键实现**：repository 为 None 直接返回；implementation_ref 为 `模块名:类qualname`；使用 `replace=True` 写入。
- **调用关系**：由 `_register_discovered_tool` 调用。

#### 函数 `_save_metadata(repository: ToolSpecRepository | None, spec: ToolSpec, module: ModuleType, tool_class: type[BaseTool], *, replace: bool) -> None` — 保存类级工具元数据

- **参数**：repository、spec、模块、工具类和 replace。
- **返回**：None。
- **关键实现**：repository 为 None 直接返回；implementation_ref 来自类 qualname；按传入 replace 写入。
- **调用关系**：由 `_register_discovered_metadata` 调用。

#### 函数 `_candidate_path(module_info: pkgutil.ModuleInfo) -> str | None` — 推导未导入模块的候选路径

- **参数**：`pkgutil.ModuleInfo`。
- **返回**：字符串路径或 None。
- **关键实现**：finder 的 `path` 不是 str 时返回 None；子包推为 `root/short_name/__init__.py`，普通模块推为 `root/short_name.py`。
- **调用关系**：由 `discover_tools` 为 ignored 和错误记录提供路径。

#### 函数 `_error_record(module: str, path: str | None, enabled: bool | None, error: Exception, prefix: str, *, tool_name: str | None = None, version: str | None = None, generation: int | None = None) -> ToolDiscoveryRecord` — 构造统一错误记录

- **参数**：模块、路径、enabled 状态、异常、错误前缀，以及可选工具身份/代次。
- **返回**：status 为 error 的 ToolDiscoveryRecord。
- **关键实现**：error 文本格式为 `prefix: ExceptionType: message`；不抛异常，只封装错误。
- **调用关系**：被包导入、模块导入、协议校验和注册失败路径调用。

### `core/parser.py` — 原生工具调用与模型 JSON 的容错解析

- **拓扑层**：L3 ｜ **依赖**：标准库 `ast`、`json`、`collections.abc`、`typing`，以及 `core.models`、`core.registry` ｜ **被依赖**：`core.__init__`；`agents.agent`、`agents.react`、`tool.search` 和测试
- **规模**：168 行 / 7 个 def
- **职责**：把文本 JSON、字典/列表以及 OpenAI 兼容原生调用转换成内部 `ToolCall`，只负责解析和契约绑定，不执行工具。`loads_model_json` 对模型包装噪声提供确定的三段式修复，同时拒绝 NaN/Infinity 等非有限 JSON 常量。
- **关键模块级对象**：无额外模块级常量或类；公开入口是解析函数。

#### 函数 `parse_tool_calls(payload: str | dict[str, Any] | list[dict[str, Any]]) -> list[ToolCall]` — 解析内部或回退格式调用

- **参数**：字符串 JSON、单对象字典或对象列表。
- **返回**：严格校验后的 `ToolCall` 列表。
- **关键实现**：字符串先走 `loads_model_json`；字典若有 `tool_calls` 键则展开该值，否则把自身作为单调用；最终必须是 list，否则 `TypeError`；每个项目用 `ToolCall.model_validate(..., strict=True)`。
- **调用关系**：调用 `loads_model_json`；由文本协议和外部调用方使用。

#### 函数 `parse_openai_tool_calls(tool_calls: list[Any], registry: ToolRegistry, name_map: dict[str, str] | None = None, registrations: Mapping[str, tuple[BaseTool, int]] | None = None) -> list[ToolCall]` — 转换 OpenAI 兼容原生调用

- **参数**：native call 序列、注册表、可选 provider 名到 canonical 名映射、可选固定注册快照。
- **返回**：内部 `ToolCall` 列表。
- **关键实现**：接受 list/tuple；逐项用 `_get` 兼容 Mapping 和对象字段；缺失 call ID 生成 `native-call-{position+1}`，超长 ID 抛 `ValueError`；解析 function name 和 arguments；显式 registrations 优先，否则 registry.resolve；字符串参数经 `loads_model_json`，其他参数必须最终为 dict；调用中的版本、schema_hash 和 generation 从解析到的 tool 填充。
- **调用关系**：调用 `_get`、`loads_model_json`、`registry.resolve`；由 Agent 原生 function-call 路径调用。

#### 函数 `_get(value: Any, key: str, default: Any = None) -> Any` — 兼容字典和对象字段读取

- **参数**：目标值、字段名和默认值。
- **返回**：Mapping 用 `.get`，其他对象用 `getattr`；缺失时返回 default。
- **关键实现**：不抛属性错误。
- **调用关系**：由 `parse_openai_tool_calls` 调用。

#### 函数 `loads_model_json(value: str, *, object_only: bool = False) -> Any` — 三段式模型 JSON 解码

- **参数**：`value` 为原始文本；`object_only=True` 时 balanced 扫描只找 `{...}`。
- **返回**：解码出的 JSON 值。
- **关键实现**：
  1. `text = value.strip()`，先尝试完整文本。
  2. 用 `_balanced_json_substring` 找第一个平衡 JSON 对象或数组，并尝试该子串。
  3. 用 `_repair_single_quoted_object` 将单引号 Python 字面量转为 JSON，再尝试一次。
  - `json.loads` 使用 `reject_json_constant` 拒绝 NaN/Infinity；所有候选失败时抛出统一的 `json.JSONDecodeError`，并带最后一次原因。
- **调用关系**：调用 `_balanced_json_substring`、`_repair_single_quoted_object`、`reject_json_constant`；由 `parse_tool_calls`、`parse_openai_tool_calls` 和 ReAct Action Input 解析调用。

#### 函数 `_balanced_json_substring(text: str, *, object_only: bool = False) -> str` — 提取首个平衡 JSON 块

- **参数**：文本和是否只接受对象。
- **返回**：首个平衡块或空字符串。
- **关键实现**：选择最早的 `{` 或 `[` 起点；扫描时跟踪字符串、反斜杠转义和嵌套深度；遇到同一深度闭合符号返回切片；未闭合返回空。源码没有做完整的 JSON 语法校验，后续仍交给 `json.loads`。
- **调用关系**：由 `loads_model_json` 调用。

#### 函数 `_repair_single_quoted_object(candidate: str) -> str` — 把 Python 风格单引号字典转为 JSON

- **参数**：候选文本。
- **返回**：JSON 字符串或空字符串。
- **关键实现**：无单引号直接返回空；用 `ast.literal_eval` 安全解析，只接受 dict；对 value/syntax/memory/recursion 错误或不可序列化结果返回空；`json.dumps` 使用 `ensure_ascii=False`。
- **调用关系**：由 `loads_model_json` 调用。

#### 函数 `reject_json_constant(value: str) -> None` — 拒绝非标准 JSON 常量

- **参数**：`json.loads` 遇到的常量名。
- **返回**：从不正常返回；始终抛 `ValueError`。
- **关键实现**：拒绝 NaN、Infinity、-Infinity，使模型输入保持严格有限 JSON。
- **调用关系**：传入 `json.loads(parse_constant=...)`；也被 `tool.search` 直接导入复用。

### `core/runtime.py` — 工具批次的验证、依赖调度、并发执行和结果聚合

- **拓扑层**：L3 ｜ **依赖**：标准库 `asyncio`、`inspect`、`logging`、`threading`、`dataclasses`、`typing`，`pydantic`，以及 `core.models`、`core.registry` ｜ **被依赖**：`core.__init__`；`agents.agent` 通过 `execute_tool_calls` 使用，测试覆盖较多
- **规模**：457 行 / 11 个 def
- **职责**：接收一批 `ToolCall`，先做注册、版本、写确认、参数和依赖校验，再按依赖图和并发属性调度。同步工具在线程池执行，异步工具在当前事件循环执行，统一套超时和输出模型校验；错误转换成结构化 `ToolResult`，不会把工具异常直接冒泡到批次调用方。
- **关键模块级对象**：`LOGGER`；`_ExecutionTimedOut` 是内部超时标记；`_LoopExecutionState` 保存每个事件循环的全局和工具级 semaphore；`ToolExecutionManager` 是公开管理器。

#### 类 `_ExecutionTimedOut(Exception)` — runtime 内部超时标记

- **构造**：无自定义 `__init__`，使用 `Exception` 默认构造。
- **关键属性**：无。
- **方法**：无自定义方法。

#### 类 `_LoopExecutionState` — 绑定单个事件循环的异步并发原语

- **构造**：生成字段 `global_semaphore: asyncio.Semaphore` 和 `tool_semaphores: dict[tuple[object, ...], asyncio.Semaphore] = {}`。
- **关键属性**：state 只能由一个事件循环使用；工具 semaphore 按工具契约键缓存。
- **方法**：无自定义方法。

#### 类 `ToolExecutionManager` — 一个批次的工具执行管理器

- **构造**：`__init__(self, registry: ToolRegistry, *, max_concurrency: int = 8) -> None`。`max_concurrency` 必须为正整数；初始化 registry、并发上限、每事件循环状态字典和线程锁。
- **关键属性**：`registry`、`max_concurrency`、`_loop_states`、`_loop_states_lock`。
- **方法**：
  - `async execute_batch(self, calls: list[ToolCall], context: ExecutionContext | None = None, *, failure_policy: Literal["continue", "fail_fast"] = "continue") -> BatchToolResult` — 批次总入口；校验策略、调用序列和 context；空批次直接返回。为每个 call 做以下预检：重复 call_id 标记 `DUPLICATE_CALL_ID`；未注册标记 `UNKNOWN_TOOL`；schema 版本/hash/generation 不一致标记 `SCHEMA_MISMATCH`；非 read 副作用要求 `context.confirmed_side_effects` 包含 `registry.confirmation_key`，否则 `CONFIRMATION_REQUIRED`；输入用 `spec.input_model.model_validate(..., strict=True)`，失败为 `INVALID_ARGUMENTS`。随后建立依赖索引，未知依赖为 `UNKNOWN_DEPENDENCY`，重复依赖为 `AMBIGUOUS_DEPENDENCY`，自依赖为 `DEPENDENCY_CYCLE`。调度器按已完成依赖找 ready 集；只要 ready 中有非 parallel_safe 工具就一次执行一个，否则并发执行 ready；`fail_fast` 在已有预检失败或批内失败后把剩余调用标记 `ABORTED`。没有 ready 的剩余图标记 `DEPENDENCY_CYCLE`。最后保证每项都有结果并返回 `BatchToolResult`。
  - `_abort_remaining(self, calls: list[ToolCall], results: list[ToolResult | None], remaining: set[int]) -> None` — 把 remaining 中所有调用填为 `ABORTED` 并清空集合；原地修改 results 和 remaining。
  - `_error_result(call: ToolCall, error: ToolError) -> ToolResult` — `@staticmethod`；构造 ok=False、data 为空的 ToolResult。
  - `_loop_state(self) -> _LoopExecutionState` — 获取当前事件循环；锁内清理已关闭且非当前 loop 的旧 state；按需创建当前 loop 的全局 semaphore；返回 state。
  - `_tool_semaphore(self, loop_state: _LoopExecutionState, tool: BaseTool) -> asyncio.Semaphore` — 非 parallel_safe 容量固定 1；并行安全工具使用 spec.max_concurrency 或 manager 上限；以名称、版本、schema_hash、parallel_safe 和容量组成缓存键，按需创建/复用 semaphore。
  - `async _run_one(self, index: int, call: ToolCall, tool: BaseTool | None, arguments: BaseModel | None, dependency_indices: list[int], results: list[ToolResult | None], context: ExecutionContext, loop_state: _LoopExecutionState) -> tuple[int, ToolResult]` — 单调用执行核心。先检查依赖结果，失败依赖返回 `DEPENDENCY_FAILED`。先获取工具 slot，再获取全局 slot，避免排队工具占用全局名额。通过 `inspect.signature(tool.execute)` 判断是否注入 context：keyword-only 用关键字，positional-only/positional-or-keyword 用位置参数。协程函数创建 task 并 await 超时；同步函数用 `asyncio.to_thread`，同步调用返回 awaitable 时再 ensure_future。成功结果必须用 output_model 严格校验并 model_dump。超时返回 `TIMEOUT`，输出校验失败返回 `INVALID_OUTPUT`，其他异常返回 `EXECUTION_ERROR`。finally 释放 slots；若线程任务在超时后仍未结束，则保留两个 slot 并在任务结束时通过 callback 释放，避免隐藏并发重叠；已完成未取消任务会读取 exception，避免未观察异常。
  - `async _await_until(self, task: asyncio.Task[Any], deadline: float, *, cancel_on_timeout: bool) -> Any` — 计算剩余 timeout，等待 task；未完成时按 `cancel_on_timeout` 决定取消，然后抛 `_ExecutionTimedOut`；完成时返回 `task.result()`，因此任务异常会自然冒泡给 `_run_one`。
  - `_release_after_background_work(self, loop_state: _LoopExecutionState, tool_semaphore: asyncio.Semaphore)` — 返回一个 done callback；源码没有为返回类型写注解。
  - `release(task: asyncio.Task[Any]) -> None` — 嵌套函数；若 task 未取消则读取 exception，随后释放全局和工具 semaphore。
  - `_limited_message(error: Exception, limit: int = 2000) -> str` — `@staticmethod`；若是 Pydantic ValidationError，只拼接 loc 和 msg，不包含 input_value，避免回显不可信参数或秘密；其他异常用 `str(error)`；空消息回退类型名；最终按 limit 截断。

**调度与写确认要点**：权限集合目前是信息性元数据，runtime 不做权限门禁；非 `read` 的 side_effect 必须提供代次绑定的 confirmation key。依赖只解决同批内顺序和失败传播，`ToolSpec.recommended_before_tools` 不会被 runtime 强制执行。

### `core/skill_discovery.py` — `skills/<name>.md` 技能文件的平面发现、frontmatter 校验和正文读取

- **拓扑层**：L3 ｜ **依赖**：标准库 `hashlib`、`dataclasses`、`pathlib`、`typing`，以及 `constants`、`core.skill_models` ｜ **被依赖**：`core.skill_catalog`、`core.__init__`；`agents.agent` 和测试调用
- **规模**：335 行 / 11 个 def
- **职责**：扫描技能根目录下的单个 Markdown 文件，不导入或执行任何 Python。文件名 stem 是技能名，根 README 被跳过，旧式 `<name>/SKILL.md` 报告迁移错误，单个坏文件不会阻断其他技能。frontmatter 使用简化 `key: value` 协议，未知字段直接报错。
- **关键模块级对象**：`DiscoveryStatus`；`SkillDiscoveryRecord`、`SkillDiscoveryReport`、`SkillDiscoveryError`；`discover_skills` 和 `read_skill_content`。

#### 类 `SkillDiscoveryRecord` — 单个技能候选的发现结果

- **构造**：生成字段 `name: str`、`path: str | None`、`status: DiscoveryStatus`、`version: str | None = None`、`generation: int | None = None`、`error: str | None = None`。
- **关键属性**：记录候选状态、版本、代次和错误。
- **方法**：
  - `as_dict(self) -> dict[str, Any]` — 用 `asdict` 返回 JSON 友好字典。

#### 类 `SkillDiscoveryReport` — 一次技能根扫描的不可变报告

- **构造**：生成字段 `root: str`、`records: tuple[SkillDiscoveryRecord, ...]`。
- **关键属性**：`records` 是本次扫描的全部候选结果。
- **方法**：
  - `errors(self) -> tuple[SkillDiscoveryRecord, ...]` — `@property`；筛出 error 记录。
  - `registered(self) -> tuple[SkillDiscoveryRecord, ...]` — `@property`；只筛 status 为 `"registered"` 的记录，源码没有把 `already_registered` 纳入该属性。
  - `ok(self) -> bool` — `@property`；没有 error 时为 True。
  - `for_skill(self, name: str) -> SkillDiscoveryRecord | None` — 校验名称非空，返回首个同名记录或 None。
  - `as_dict(self) -> dict[str, Any]` — 返回 root、ok 和逐条记录。

#### 类 `SkillDiscoveryError(RuntimeError)` — 严格技能扫描失败异常

- **构造**：`__init__(self, report: SkillDiscoveryReport) -> None`；保存 report，并把 error 记录的名称和错误拼入异常消息。
- **关键属性**：`report`。
- **方法**：仅有 `__init__`。

#### 函数 `discover_skills(registry, *, root: str | Path = DEFAULT_SKILLS_ROOT, replace: bool = False, strict: bool = False) -> SkillDiscoveryReport` — 扫描技能根并注册有效技能

- **参数**：`registry` 为技能注册表；`root` 为技能根；`replace` 控制不同 spec 替换；`strict` 决定 error 是否抛异常。源码未给 registry 写类型注解，也未显式做 isinstance 校验。
- **返回**：`SkillDiscoveryReport`。
- **关键实现**：
  1. 校验 root 为 str/Path，replace/strict 为 bool；root resolve 后必须是目录，否则生成 root error，严格模式抛 `SkillDiscoveryError`。
  2. 按文件名排序遍历直接子项；目录若有 `SKILL.md` 记录迁移错误，否则 ignored；非 `.md` 记录 ignored；根 README 静默跳过。
  3. `_load_spec` 返回 spec 时注册；返回 None 时记录 disabled；注册前用 registry.maybe_resolve 判断是否 already_registered。异常转换为带类型名的 error 记录并继续。
  4. 汇总报告，strict 且有 error 时抛异常，否则返回。
- **调用关系**：调用 `_load_spec` 和 `registry.maybe_resolve/register`；由 Agent 的 `discover_skills` 和 core 门面调用。

#### 函数 `_load_spec(name: str, skill_file: Path) -> SkillSpec | None` — 解析单个技能文件

- **参数**：文件 stem 名称和路径。
- **返回**：有效 `SkillSpec`，禁用时返回 None。
- **关键实现**：先校验 skill name pattern；读取 UTF-8 全文并计算 SHA-256；`_parse_frontmatter` 返回 None 表示 disabled；要求非空 description；version 默认 `1.0.0`；triggers 支持逗号字符串或 list/tuple；逐项限制 description/version/trigger 长度和 trigger 非空；最后构造包含 file_path 和 content_hash 的 spec。
- **调用关系**：由 `discover_skills` 调用；调用 `_parse_frontmatter` 和 `SkillSpec`。

#### 函数 `_parse_frontmatter(raw: str, skill_file: Path) -> dict[str, Any] | None` — 解析简化的 frontmatter

- **参数**：原始文本和路径（用于错误消息）。
- **返回**：字段字典；`enabled: false` 时返回 None。
- **关键实现**：首行去空白必须是 `---`；寻找下一个 `---` 结束；空行和 `#` 开头行跳过；每行必须含 `:`；key 小写化；只允许 `enabled`、`description`、`version`、`triggers`；enabled 只接受 true/false；未知 key 抛 `ValueError`。
- **调用关系**：仅由 `_load_spec` 调用；不执行 YAML，不使用第三方解析器。

#### 函数 `read_skill_content(spec: SkillSpec) -> str` — 读取技能当前正文

- **参数**：技能 spec。
- **返回**：文件完整 UTF-8 文本。
- **关键实现**：file_path 为 None 时抛 `ValueError`；路径不是文件时抛 `FileNotFoundError`；每次调用都重新读磁盘，因此不缓存正文，也不会在读取时重新计算或校验 content_hash。
- **调用关系**：由 `SkillCatalogTool.execute` 和外部调用方调用。

### `core/skill_registry.py` — 技能元数据的线程安全注册表

- **拓扑层**：L3 ｜ **依赖**：标准库 `threading`，以及 `core.skill_models` ｜ **被依赖**：`core.skill_catalog`、`core.__init__`；`agents.agent` 持有实例
- **规模**：91 行 / 9 个 def
- **职责**：只保存 `SkillSpec` 元数据和代次，不保存正文。目录的 `view` 每次从磁盘读取内容，保证已有注册不会掩盖文件更新；相同 spec 重复注册幂等，避免重启或重复扫描导致 prompt-cache 材料变化。
- **关键模块级对象**：`SkillRegistry` 是唯一注册表类。

#### 类 `SkillRegistry` — 技能 spec 的锁保护注册表

- **构造**：`__init__(self) -> None`；初始化 `_specs`、`_generations` 和 `RLock`。
- **关键属性**：`_specs: dict[str, SkillSpec]`、`_generations: dict[str, int]`、`_lock`。
- **方法**：
  - `register(self, spec: SkillSpec, *, replace: bool = False) -> int` — 校验参数；同 spec 完全相等时返回原代次；不同且 replace=False 抛 `ValueError`；不同且允许替换时更新 spec 并 bump 代次；返回当前代次。
  - `resolve(self, name: str) -> tuple[SkillSpec, int]` — 校验非空名；锁内返回 spec 和代次，缺失时抛 `KeyError`。
  - `maybe_resolve(self, name: str) -> tuple[SkillSpec, int] | None` — 无效名或未注册返回 None，否则返回二元组。
  - `get(self, name: str) -> SkillSpec` — 转发 `resolve(name)[0]`。
  - `maybe_get(self, name: str) -> SkillSpec | None` — 转发 `maybe_resolve` 并取 spec。
  - `snapshot(self) -> dict[str, tuple[SkillSpec, int]]` — 锁内按名称排序构造稳定快照。
  - `__contains__(self, name: str) -> bool` — 锁内判断名称是否存在。
  - `__len__(self) -> int` — 锁内返回条目数。

### `memory/base.py` — 核心数据结构、配置与共享 CRUD/语义检索实现

- **拓扑层 / 依赖 / 被依赖**：核心类型层兼公共实现层；运行时依赖 `memory.embedding` 和 `constants`，通过 `TYPE_CHECKING` 及 `BaseMemory.__init__` 局部导入 `memory.storage`。被 `manager`、全部 `types`、全部 `storage` 实现、`rag.pipeline`、`rag.knowledge` 以及包门面依赖。
- **规模**：524 行 / 23 个 def。
- **职责**：定义四层共享的 `MemoryType`、`MemoryItem`、`MemorySearchResult`、`MemoryConfig` 和 `BaseMemory`。`MemoryItem` 是跨存储后端移动的标准记录；`BaseMemory` 提供统一的增删查、TTL/过期过滤、向量检索、回滚和向量索引清理。该文件不直接绑定具体厂商，默认 SQLite/内存向量实现通过惰性导入选择，以避免与存储模块在导入期形成直接环。`MemoryConfig` 的连接字段（`qdrant_url`/`neo4j_uri`）出厂为 `None`（= 不连接、走内存回退），端点默认值统一收在 `constants.py` 的「连接与端点」小节（`DEFAULT_QDRANT_URL`/`DEFAULT_NEO4J_URI`），`.env` 的 `HELLOAGENTS_MEMORY_*` 负责覆盖。
- **关键模块级对象/常量**：
  - 从 `constants` 引入的默认值：`DEFAULT_EMBEDDING_BASE_URL`、`DEFAULT_EMBEDDING_BATCH_SIZE`、`DEFAULT_EMBEDDING_MODEL`、`DEFAULT_MEMORY_DB_FILENAME`、`MEMORY_DEFAULT_TTL_SECONDS`、`MEMORY_EMBEDDING_DIMENSION`、`MEMORY_EMBEDDING_TIMEOUT`、`MEMORY_QDRANT_COLLECTION`、`MEMORY_SEARCH_LIMIT`、`MEMORY_SIMILARITY_THRESHOLD`、`MEMORY_SQLITE_DEFAULT`、`MEMORY_WORKING_CAPACITY`。
  - `MemoryType` 枚举值：`WORKING="working"`、`EPISODIC="episodic"`、`SEMANTIC="semantic"`、`PERCEPTUAL="perceptual"`。
  - `__all__`：`DEFAULT_MEMORY_DB_FILENAME`、`BaseMemory`、`MemoryConfig`、`MemoryItem`、`MemorySearchResult`、`MemoryType`、`default_sqlite_path`、`ensure_datetime`、`make_default_embedding`、`utc_now`。
  - `BaseMemory` 的别名：`remember = add`、`store = add`、`remove = delete`、`retrieve = get`、`query = search`。

#### 函数 `default_sqlite_path()` — 计算默认 SQLite 路径

- **签名**：`def default_sqlite_path() -> str:`
- **参数**：无。
- **返回**：环境变量 `MEMORY_DB_PATH` 的非空值；若未设置，返回 `DEFAULT_MEMORY_DB_FILENAME`，当前常量值为 `"memory.sqlite3"`。
- **副作用/异常**：只读取环境变量；源码中未见显式异常处理。
- **调用关系**：供 Agent 工具/Web 入口构造默认 `MemoryConfig` 使用；不属于 `MemoryConfig.__post_init__` 的默认计算链条（dataclass 默认直接使用 `MEMORY_SQLITE_DEFAULT`）。

#### 函数 `make_default_embedding(config: MemoryConfig | None = None) -> BaseEmbedding` — 按优先级选择网关 / API / 离线嵌入

- **签名**：`def make_default_embedding(config: MemoryConfig | None = None) -> BaseEmbedding:`
- **参数**：`config` 为可选的 `MemoryConfig`；为 `None` 时内部调用 `MemoryConfig.from_env()`。
- **返回**：`EMBEDDING_BASE_URL`（`os.getenv`，去空白）非空时返回 `EmbedServerEmbedding(base_url=..., dimension=config.embedding_dimension, timeout=..., batch_size=...)`（本机转发网关，优先级最高）；否则有有效 API key 时返回 `APIEmbedding`；都没有时返回 `HashEmbedding(dimension=config.embedding_dimension)`。
- **副作用/异常**：调用 `load_dotenv_once()`；读取 `config.embedding_api_key`，再回退到 `DASHSCOPE_API_KEY`。三套向量空间互不兼容——切换 provider 必须重索引已有条目；`EmbedServerEmbedding`/`APIEmbedding` 构造校验失败会抛出异常。
- **调用关系**：被 `BaseMemory.__init__`、`MemoryManager.__init__` 和 `web.support.build_embedding` 调用。

#### 函数 `utc_now() -> datetime` — 返回带 UTC 时区的当前时间

- **签名**：`def utc_now() -> datetime:`
- **参数**：无。
- **返回**：`datetime.now(UTC)`。
- **副作用/异常**：无显式副作用；无显式异常处理。
- **调用关系**：`MemoryItem` 默认时间工厂、TTL 计算、过期判断。

#### 函数 `ensure_datetime(value: datetime | str | None) -> datetime | None` — 规范化时间值到 UTC datetime

- **签名**：`def ensure_datetime(value: datetime | str | None) -> datetime | None:`
- **参数**：`None` 原样返回；`datetime` 直接使用；`str` 通过 `datetime.fromisoformat` 解析。
- **返回**：若结果无时区，补 `UTC`；统一 `astimezone(UTC)` 后返回。
- **副作用/异常**：非以上三种类型抛出 `TypeError("datetime values must be datetime, ISO string, or None")`；字符串解析失败抛出 `ValueError`。
- **调用关系**：`MemoryItem.__post_init__`、`EpisodicMemory.timeline`、外部 API 的时间输入。

#### 类 `MemoryItem` — 跨存储后端的标准记忆记录

- **构造**：dataclass 自动构造。参数与默认值依次为：
  - `content: str`：必填，搜索文本；`__post_init__` 中非字符串会转成 `str`。
  - `memory_type: MemoryType | str = MemoryType.WORKING`。
  - `id: str = field(default_factory=lambda: str(uuid4()))`。
  - `metadata: dict[str, Any] = field(default_factory=dict)`。
  - `importance: float = 0.5`。
  - `created_at: datetime = field(default_factory=utc_now)`。
  - `updated_at: datetime = field(default_factory=utc_now)`。
  - `expires_at: datetime | None = None`。
  - `timestamp: datetime | None = None`。
  - `embedding: list[float] | None = None`。
  - `payload: Any = None`。
  - `modality: str | None = None`。
  - `relations: list[dict[str, Any]] = field(default_factory=list)`。
- **关键属性**：
  - `content` 面向文本嵌入和全文展示；`payload` 保留图片字节、URI 或其他多模态原始值。
  - `metadata` 承载层特有协议字段，例如 semantic 的 `subject`、`predicate`、`object`、`active`、`superseded_by`。
  - `created_at`/`updated_at` 是记录时间；`timestamp` 是事件时间；`event_time` 属性统一取 `timestamp or created_at`。
  - `expires_at` 控制 TTL；`embedding` 是当前向量空间中的向量；`relations` 是记录级关系描述，不是 Neo4j 图对象本身。
- **方法**：
  - `__post_init__(self) -> None` — 校验并规范化字段；参数是自动传入的实例字段；返回 `None`；将 `content` 字符串化、`memory_type` 转 `MemoryType`、非空 `id` 必须为非空字符串、`importance` 必须为有限且位于 `[0,1]` 的数值、时间字段调用 `ensure_datetime`、`modality` 非空校验、embedding 转 `list[float]` 并拒绝空/非有限/字符串或 bytes，`relations` 转 list 且元素必须是 `Mapping`。失败抛 `TypeError`/`ValueError`。
  - `is_expired(self) -> bool` — `@property`；返回 `expires_at is not None and expires_at <= utc_now()`；无副作用、无显式异常；被 `BaseMemory.get`、文档存储 `list`、管理器检索/清理调用。
  - `event_time(self) -> datetime` — `@property`；返回 `timestamp or created_at`；用于 episodic 时间线排序。
  - `to_dict(self, *, include_payload: bool = True) -> dict[str, Any]` — 生成可持久化字典；`include_payload=False` 时省略 `payload` 键；元数据/关系/负载经 `_json_safe`，时间转 ISO 字符串，`memory_type` 输出枚举值字符串，embedding 直接作为列表输出。无正式异常处理，嵌入已由构造校验。
  - `from_dict(cls, value: Mapping[str, Any]) -> MemoryItem` — `@classmethod`；复制输入、先对 `payload` 执行 `_json_restore`，再 `cls(**data)`；因此不接受额外无关键，缺少字段使用 dataclass 默认值；构造校验异常会继续向上抛出。
- **关键模块级对象/常量**：字段默认值依赖 `uuid4`、`utc_now`；`payload` 的 bytes 编码协议由 `_json_safe`/`_json_restore` 定义。

#### 函数 `_json_safe(value: Any) -> Any` — 把任意值递归转换为 JSON 可保存形态

- **签名**：`def _json_safe(value: Any) -> Any:`
- **参数**：任意 Python 值。
- **返回**：`bytes` 转为 `{"__bytes__": "<base64>"}`；`Mapping` 的键转字符串并递归；`list/tuple/set` 转为列表并递归；其余先尝试 `json.dumps`，失败则转 `str`。
- **副作用/异常**：无显式抛错；`json.dumps` 的 `TypeError`/`ValueError` 被捕获并退化为字符串。
- **调用关系**：`MemoryItem.to_dict` 处理 metadata、relations、payload。

#### 函数 `_json_restore(value: Any) -> Any` — 恢复 `_json_safe` 编码的 bytes

- **签名**：`def _json_restore(value: Any) -> Any:`
- **参数**：任意已解码 JSON 值。
- **返回**：仅当 dict 的键集合严格等于 `{"__bytes__"}` 时解码 base64；普通 dict/list 递归恢复；其他值原样返回。
- **副作用/异常**：base64 解码失败时捕获所有异常并返回原 dict；其他递归不抛出显式异常。
- **调用关系**：`MemoryItem.from_dict`、`SQLiteDocumentStore._decode`。

#### 类 `MemorySearchResult` — 带分数的冻结检索结果

- **构造**：dataclass `frozen=True`，字段 `item: MemoryItem`、`score: float`；没有显式 `__post_init__` 校验。
- **关键属性**：`item` 是命中的记忆记录，`score` 是向量库返回的相似度/距离分数。
- **方法**：
  - `to_dict(self) -> dict[str, Any]` — 调用 `item.to_dict()` 后增加 `"score"` 键；返回普通字典；不修改原 item；无显式异常。

#### 类 `MemoryConfig` — 运行时配置与默认值集合

- **构造**：dataclass 自动构造。参数与默认值依次为：
  - `sqlite_path: str | Path = MEMORY_SQLITE_DEFAULT`，当前值为 `":memory:"`。
  - `default_ttl_seconds: float | None = MEMORY_DEFAULT_TTL_SECONDS`，当前值为 `3600.0`。
  - `working_memory_capacity: int = MEMORY_WORKING_CAPACITY`，当前值为 `100`。
  - `search_limit: int = MEMORY_SEARCH_LIMIT`，当前值为 `10`。
  - `similarity_threshold: float = MEMORY_SIMILARITY_THRESHOLD`，当前值为 `0.0`。
  - `embedding_dimension: int = MEMORY_EMBEDDING_DIMENSION`，当前值为 `1024`。
  - `embedding_model: str = DEFAULT_EMBEDDING_MODEL`，当前值为 `"qwen3-embedding-0.6b"`。
  - `embedding_base_url: str = DEFAULT_EMBEDDING_BASE_URL`，当前为 DashScope 兼容模式地址。
  - `embedding_api_key: str | None = None`。
  - `embedding_timeout: float = MEMORY_EMBEDDING_TIMEOUT`，当前值为 `30.0`。
  - `embedding_batch_size: int = DEFAULT_EMBEDDING_BATCH_SIZE`，当前值为 `10`。
  - `qdrant_url: str | None = None`。
  - `qdrant_collection: str = MEMORY_QDRANT_COLLECTION`，当前值为 `"helloagents_memory"`。
  - `neo4j_uri: str | None = None`、`neo4j_username: str | None = None`、`neo4j_password: str | None = None`。
  - `extra: dict[str, object] = field(default_factory=dict)`。
- **关键属性**：配置对象同时驱动存储选择、默认 TTL、容量、检索阈值、嵌入维度和远端 Qdrant/Neo4j 连接参数；`extra` 仅承载扩展配置，未在源码中进一步解释。
- **方法**：
  - `__post_init__(self) -> None` — 校验工作容量、搜索数量、相似度阈值、嵌入维度、模型/base URL 非空、API key 类型及非空规则、timeout/batch size 为正且有限、TTL 为正且有限或 `None`、Qdrant collection 非空，并把非 dict 的 `extra` 尝试转为 dict。异常类型主要是 `ValueError`；`int()`/`float()` 转换异常会按原状向上传递。未显式校验 `qdrant_url`、Neo4j 三个连接字段、`sqlite_path` 类型。
  - `from_env(cls, prefix: str = "HELLOAGENTS_MEMORY_") -> MemoryConfig` — `@classmethod`；遍历 `__dataclass_fields__`，把 `prefix + FIELD_NAME` 环境变量映射到字段；整数字段用 `int`，浮点/TTL 字段用 `None`（文本 `none`）或 `float`，`extra` 跳过，其余用原始字符串；调用 `load_dotenv_once`，`embedding_api_key` 为空时回退 `DASHSCOPE_API_KEY`；返回 `cls(**values)`。**空值（`VAR=`）等同未设置**——`.env` 里最常见的占位写法不会再透传给 `embedding_api_key`/`embedding_model`/`embedding_base_url` 这类「非空字符串」字段进而在 `__post_init__` 抛 `ValueError`，使整个 `from_env` 不可用。类型转换或 `__post_init__` 异常会向上抛出。
  - `to_dict(self) -> dict[str, object]` — 遍历 `self.__dict__`；`Path` 转字符串，其他值保持原样；返回浅拷贝式字典，不递归复制嵌套对象；无显式异常。

#### 类 `BaseMemory` — 四层共享的 CRUD、TTL 与向量检索实现

- **构造**：`def __init__(self, *, document_store: BaseDocumentStore | None = None, vector_store: BaseVectorStore | None = None, embedding: BaseEmbedding | None = None, config: MemoryConfig | None = None, memory_type: MemoryType | str | None = None) -> None`。
  - `document_store` 为空时局部导入并使用 `SQLiteDocumentStore(self.config.sqlite_path)`。
  - `vector_store` 为空时使用 `InMemoryVectorStore()`。
  - `embedding` 为空时调用 `make_default_embedding(self.config)`。
  - `config` 为空时创建 `MemoryConfig()`。
  - `memory_type` 为空时读取子类类属性 `self.memory_type`，再转成 `MemoryType`。
- **关键属性**：`config`、`document_store`、`vector_store`、`embedding`、`memory_type`；具体层通过继承该类并设置类属性 `memory_type` 复用 CRUD。
- **方法**：
  - `_validate_embedding_dimension(self, vector: list[float]) -> None` — 私有校验；从嵌入实例读取 `dimension`，仅在非零时要求长度一致；不匹配抛 `ValueError`；被 `add` 和 `search` 调用。
  - `add(self, content: str, *, metadata: Mapping[str, Any] | None = None, importance: float = 0.5, ttl_seconds: float | None = None, ttl: float | None = None, expires_at: datetime | str | None = None, timestamp: datetime | str | None = None, item_id: str | None = None, payload: Any = None, modality: str | None = None, relations: list[dict[str, Any]] | None = None) -> MemoryItem` — 创建并持久化记录。`ttl` 是 `ttl_seconds` 的别名但两者不能同时给出；`expires_at` 与 TTL 互斥；只有 working 层在未显式给 TTL 时自动使用 `config.default_ttl_seconds`；源码校验 TTL 的类型为 `int/float`、不能是 bool 且 `<= 0` 时拒绝，但未显式检查 `NaN/inf`，随后以 `expires_at = utc_now() + timedelta(...)` 计算并可能由 datetime/timedelta 自身抛错。若给出了 `item_id`，先读取已有记录并禁止跨记忆类型复用；随后构造 `MemoryItem`、嵌入 `content`、校验维度，先 `document_store.upsert`，再 `vector_store.upsert`。向量写入失败时：新记录删除文档记录；已有记录恢复旧文档并尝试恢复/删除旧向量，之后重新抛出异常。返回最终 `MemoryItem`；可能抛出 `ValueError`、嵌入/存储异常。该函数不做内容级去重，语义事实的去重由 `SemanticMemory.add_fact` 负责。
  - `get(self, item_id: str) -> MemoryItem | None` — 从文档库读取；只有记录类型属于当前层时才参与返回。若属于当前层且已过期，调用 `self.delete(item_id)` 并返回 `None`；类型不匹配返回 `None`；不修改向量库以外的存储状态。无显式异常转换。
  - `delete(self, item_id: str) -> bool` — 读取文档；不存在或类型不匹配返回 `False`；否则先删向量库条目，再删除文档记录并返回文档删除结果。向量删除结果被忽略，因此向量条目不存在不阻止文档删除；无显式异常包装。
  - `search(self, query: str, *, limit: int | None = None, threshold: float | None = None, metadata: Mapping[str, Any] | None = None) -> list[MemorySearchResult]` — 查询字符串必须为 `str` 且非空白才继续；`limit` 默认配置值且必须为正整数；`threshold` 默认配置值且必须位于 `[0,1]`；嵌入查询并校验维度；向向量库请求 `max(limit*4, limit)` 条候选并按 `memory_type` 过滤。逐条通过 `get` 重新读取，以剔除过期/错误类型，过滤 `score < threshold` 和 `score <= 0`，再按 metadata 的键值精确过滤，达到 `limit` 即返回。返回 `MemorySearchResult` 列表；嵌入、维度或向量库异常按原状向上传递。注意源码把非正分数一律视为无意义匹配，即使调用方把 threshold 设为 0。
  - `list(self, *, include_expired: bool = False) -> list[MemoryItem]` — 直接委托 `document_store.list(memory_type=self.memory_type, include_expired=include_expired)`；默认不返回过期项；源码注释明确列表读取不触发向量索引重建。
  - `clear(self) -> int` — 先列出当前层包括过期项，逐条删除向量条目和文档记录，返回实际删除的文档数；只清理当前层的向量和文档，不清理图关系。
- **别名调用关系**：`remember`/`store` 指向 `add`，`remove` 指向 `delete`，`retrieve` 指向 `get`，`query` 指向 `search`。

---

### `memory/storage/document.py` — SQLite 文档持久化与文档存储抽象

- **拓扑层 / 依赖 / 被依赖**：存储实现层；顶层依赖 `memory.base` 的 `MemoryItem`、`MemoryType`、`_json_restore`，因此属于真实循环依赖组。被 `storage` 门面、`base` 的局部默认构造和 `manager` 依赖。
- **规模**：176 行 / 18 个 def。
- **职责**：定义文档库抽象接口，并用 SQLite 实现记忆记录的持久化、反序列化和按类型/过期状态列表。SQLite 表保存所有层的公共字段；`metadata`、`payload`、`relations` 以 JSON 文本存储，bytes payload 通过 `__bytes__` 标记恢复。
- **关键模块级对象/常量**：`__all__ = ["BaseDocumentStore", "SQLiteDocumentStore"]`；表名 `memories`，索引 `idx_memories_type`、`idx_memories_timestamp`。

#### 类 `BaseDocumentStore(ABC)` — 文档存储抽象契约

- **构造**：抽象基类，无显式构造函数。
- **关键属性**：无实例属性要求；实现必须提供四个抽象操作。
- **方法**：
  - `upsert(self, item: MemoryItem) -> None` — `@abstractmethod`；插入或按 `item.id` 覆盖记录；返回 `None`；具体实现的异常语义由实现决定。
  - `get(self, item_id: str) -> MemoryItem | None` — `@abstractmethod`；按 ID 返回记录或 `None`。
  - `delete(self, item_id: str) -> bool` — `@abstractmethod`；删除并返回是否实际删除。
  - `list(self, *, memory_type: MemoryType | str | None = None, include_expired: bool = False) -> list[MemoryItem]` — `@abstractmethod`；按可选记忆类型过滤并决定是否带过期项返回。
  - `save(self, item: MemoryItem) -> None` — 默认把 `save` 委托 `upsert`；返回 `None`；无显式异常处理。
  - `clear(self, memory_type: MemoryType | str | None = None) -> int` — 为自定义存储提供可移植兜底：先 `list(include_expired=True)`，逐条删除并计数；副作用是逐条删除；不保证原子性。
  - `close(self) -> None` — 默认空操作，表示自定义存储可能没有需要释放的资源。

#### 类 `SQLiteDocumentStore(BaseDocumentStore)` — 基于 SQLite 的默认文档库

- **构造**：`def __init__(self, path: str | Path = ":memory:") -> None`。
  - `path=":memory:"` 使用单连接模式；其他路径将 `Path(path).expanduser()` 转字符串，并递归创建父目录。
  - 初始化 `threading.RLock`；内存路径立即建立 `sqlite3.connect(..., check_same_thread=False)`，设置 `row_factory = sqlite3.Row`；调用 `_initialize()` 建表建索引。
- **关键属性**：`path`、`_lock`、`_connection`；`_connection` 仅对 `:memory:` 持久保留。
- **存储格式**：列 `id TEXT PRIMARY KEY`、`content TEXT NOT NULL`、`memory_type TEXT NOT NULL`、`metadata TEXT NOT NULL`、`importance REAL NOT NULL`、`created_at/updated_at TEXT NOT NULL`、`expires_at/timestamp TEXT NULL`、`embedding TEXT NULL`、`payload TEXT NULL`、`modality TEXT NULL`、`relations TEXT NOT NULL`；时间以 ISO 字符串存储，embedding 为 JSON 数组，metadata/payload/relations 为 JSON 文本。
- **方法**：
  - `_connect(self) -> sqlite3.Connection` — 私有；若已有持久连接直接返回；否则 `sqlite3.connect(self.path)` 并设置行工厂；返回新连接。该方法本身不关闭新连接，由调用方 `_connection_scope` 管理。
  - `_connection_scope(self) -> Iterator[sqlite3.Connection]` — `@contextmanager`；在 `RLock` 内取连接，使用 `with connection:` 提供事务提交/回滚；若连接不是持久 `_connection`，退出时关闭。参数无；返回 contextmanager/yield 连接；SQLite 异常原样传递。
  - `_initialize(self) -> None` — 创建 `memories` 表及按 `memory_type`、`timestamp` 的索引；返回 `None`；数据库错误向上抛。
  - `upsert(self, item: MemoryItem) -> None` — 校验必须是 `MemoryItem`，否则 `TypeError`；调用 `item.to_dict()`，执行 `INSERT ... ON CONFLICT(id) DO UPDATE` 更新全部业务列；metadata/payload/relations 用 `json.dumps(..., ensure_ascii=False)`，embedding 用 `json.dumps`；返回 `None`；SQLite 异常向上抛。
  - `get(self, item_id: str) -> MemoryItem | None` — 按主键查询，命中后调用 `_decode`；未命中返回 `None`；只读取，不处理过期。
  - `delete(self, item_id: str) -> bool` — 执行按 ID 删除并返回 `cursor.rowcount > 0`；删除在事务作用域内提交。
  - `list(self, *, memory_type: MemoryType | str | None = None, include_expired: bool = False) -> list[MemoryItem]` — 可选按 `MemoryType(memory_type).value` 过滤；按 `COALESCE(timestamp, created_at) DESC` 排序；解码全部行后，若 `include_expired=False` 过滤 `is_expired`。非法 memory_type 会由 `MemoryType(...)` 抛 `ValueError`。
  - `clear(self, memory_type: MemoryType | str | None = None) -> int` — 全表删除或按类型删除，返回 SQLite `rowcount`；不重建索引；无显式异常包装。
  - `_decode(row: sqlite3.Row) -> MemoryItem` — `@staticmethod`；解析 metadata/relations/embedding JSON，payload 先 `json.loads` 再 `_json_restore`；构造 `MemoryItem`，因此会再次走字段校验和时间规范化；无效 JSON 或字段异常向上抛。
  - `close(self) -> None` — 持锁关闭已有连接并置 `_connection=None`；返回 `None`。对 `:memory:` 连接关闭后再次访问会通过 `_connect` 创建新的空内存数据库，源码没有把原数据迁移回来；对文件路径则后续操作重新开连接。

---

### `memory/storage/document_repo.py` — `documents`/`chunks` 真值源（原文 + 分块边界）

- **拓扑层 / 依赖 / 被依赖**：L3 存储实现层；顶层依赖 `constants`、标准库 `json/sqlite3/threading/pathlib/dataclasses` 和 `memory.base` 的 `utc_now`（**单向**依赖，不在 `memory.base` 的循环依赖组里）。被 `memory.storage` 门面、`memory.rag.pipeline`（摄取/检索/删除同步）与 `web.app`（文档中心/对账/重嵌入路由）依赖。
- **规模**：442 行 / 26 个 def / 3 个类。
- **职责**：在同一份记忆 SQLite 文件上维护 `documents`（归一化原文、title、source、tags、permission、status 状态机、error）与 `chunks`（正文、`chunk_index`、`char_start/char_end` 偏移、`vector_status`）两张表，作为**向量层与图层的投影真值源**（方案 2.1/2.2）。`raw_text` 与偏移都来自 `DocumentProcessor.normalized_text` 的同一份归一化文本。同时提供 FTS5 关键词检索（`trigram` 优先、`unicode61` 回退），这是嵌入网关不可达时检索的降级路径（D8）。连接/加锁模型与 `SQLiteDocumentStore` 一致：文件库每调用一个连接、`RLock` 包住每个作用域、`:memory:` 钉住单连接。
- **关键模块级对象/常量**：
  - `DOCUMENT_STATUSES = ("uploaded", "parsed", "vectorized", "extracted", "failed")`、`CHUNK_VECTOR_STATUSES = ("pending", "indexed", "failed")`、`PERMISSIONS = ("private", "shared", "public")`、`FTS_TOKENIZERS = ("trigram", "unicode61")`——状态机与权限枚举刻意留在此文件（与同文件 DDL 同源，constants.py 不重复收）。
  - `__all__` 未显式定义；通过 `memory.storage.__init__` 导出 `ChunkRecord`、`DocumentRecord`、`DocumentRepository`。

#### 类 `DocumentRecord` — 一份已入库文档的行值对象

- **构造**：`@dataclass`，字段 `document_id: str`、`title: str = ""`、`raw_text: str = ""`、`source: str = ""`、`tags: list[str] = field(default_factory=list)`、`permission: str = "private"`、`status: str = "uploaded"`、`error: str | None = None`、`created_at: str = ""`、`updated_at: str = ""`。
- **关键属性**：`raw_text` 是归一化全文；`status` 走 `DOCUMENT_STATUSES` 状态机；`permission` 决定是否允许离开本机（D9，当前仅元数据，未做门禁）。无自定义方法。

#### 类 `ChunkRecord` — 一个分块的行值对象

- **构造**：`@dataclass`，字段 `chunk_id: str`、`document_id: str`、`chunk_index: int`、`char_start: int`、`char_end: int`、`text: str`、`vector_status: str = "pending"`。
- **关键属性**：`char_start/char_end` 索引到 `documents.raw_text`；`vector_status` 走 `CHUNK_VECTOR_STATUSES`。无自定义方法。

#### 类 `DocumentRepository` — `documents`/`chunks` 的 CRUD + FTS5 检索

- **构造**：`def __init__(self, path: str | Path = ":memory:") -> None` — `:memory:` 钉住单连接（`check_same_thread=False`），文件路径展开 `~` 并创建父目录；初始化 `RLock`、可选持久连接、`fts_tokenizer`；立即 `_initialize()` 建表、索引、FTS 触发器。
- **关键属性**：`path`、`_lock`、`_connection`（仅 `:memory:` 保留）、`fts_tokenizer`（`"trigram"`/`"unicode61"`/`None`）。
- **方法**：
  - `_connect(self) -> sqlite3.Connection` — 持久连接存在直接返回，否则新建文件连接并设 row factory；不负责关闭。
  - `_connection_scope(self) -> Iterator[sqlite3.Connection]` — `@contextmanager`；`RLock` 内取连接，`with connection:` 提供事务提交/回滚，非持久连接在 finally 关闭。
  - `_initialize(self) -> None` — 创建 `documents`/`chunks` 表与 `idx_documents_status`/`idx_chunks_document` 索引，然后调 `_initialize_fts`。
  - `_initialize_fts(self, connection) -> None` — 建 `chunks_fts` 外部内容表（`content='chunks'`，rowid 关联）与 INSERT/DELETE/UPDATE 三个同步触发器；`trigram` 建失败时删表换 `unicode61`；表已存在时按 SQL 文本判断沿用原分词器；新表在触发器建好后 `'rebuild'` 回填一次。FTS5 不可用时 `fts_tokenizer=None`，检索退化为纯向量，但不阻止仓储打开。
  - `upsert_document(self, doc: DocumentRecord) -> None` — 类型/枚举/非空 document_id 校验；`INSERT ... ON CONFLICT(document_id) DO UPDATE` 更新业务列；`created_at` 只在首次插入落库，重跑 ingest 不刷掉。
  - `get_document(self, document_id: str) -> DocumentRecord | None` — 按主键查询并解码。
  - `list_documents(self, *, tag: str = "", status: str = "", page: int = 1, page_size: int = 20) -> tuple[list[DocumentRecord], int]` — 校验 page/page_size 为正整数；`tag` 用 `json_each(documents.tags)` 精确匹配，`status` 精确匹配；返回 `(本页 items, 过滤后总数)`，按 `updated_at DESC` 排序。
  - `count_documents(self) -> int` — 全表计数。
  - `set_status(self, document_id: str, status: str, *, error: str | None = None) -> None` — 校验状态枚举后更新 `status/error/updated_at`。
  - `delete_document(self, document_id: str) -> int` — 先删该文档全部 chunks（FTS 触发器同步清理），再删 documents 行；返回删除的 chunk 数。
  - `upsert_chunk(self, chunk: ChunkRecord) -> None` — 校验类型与 `vector_status` 后单条 `_write_chunk`。
  - `upsert_chunks(self, chunks: list[ChunkRecord]) -> None` — 先整体校验，再同一连接作用域内批量写入（单事务）。
  - `_write_chunk(connection, chunk) -> None` — `@staticmethod`；`INSERT ... ON CONFLICT(chunk_id) DO UPDATE` 更新全部列。
  - `get_chunk(self, chunk_id: str) -> ChunkRecord | None` — 按主键查询并解码。
  - `list_chunks(self, document_id: str) -> list[ChunkRecord]` — 按 `chunk_index` 升序返回该文档全部分块。
  - `set_chunk_vector_status(self, chunk_id: str, status: str) -> None` — 校验枚举后更新该块向量状态。
  - `_fts_query(query: str) -> str` — `@staticmethod`；把用户查询转成 FTS5 匹配串（对 `trigram` 用 `"..."` 短语形式），空/非法输入返回空串。
  - `search_keywords(self, query: str, *, limit: int = 10) -> list[tuple[str, float]]` — FTS5 关键词检索；`fts_tokenizer` 为 `None` 时返回空表；用 `bm25(chunks_fts)` 作为分数，返回 `(chunk_id, score)`。
  - `chunk_counts(self) -> dict[str, int]` — `{document_id: chunk 数}`，供文档列表带计数。
  - `chunk_ids(self, *, vector_status: str = "") -> list[str]` — 全部 chunk id，或仅某 `vector_status` 的 id（对账用）。
  - `stats(self) -> dict[str, int]` — `documents/chunks/chunks_indexed` 计数（`/api/stats` 用）。
  - `close(self) -> None` — 关闭持久（`:memory:`）连接并置空；文件库无持久连接。
  - `_check_choice(field_name, value, allowed) -> None` — `@staticmethod`；值不在 allowed 元组内抛 `ValueError`。
  - `_decode_document(row) -> DocumentRecord` — `@staticmethod`；把 SQLite 行转 `DocumentRecord`（tags 从 JSON 还原）。
  - `_decode_chunk(row) -> ChunkRecord` — `@staticmethod`；行转 `ChunkRecord`。

---

### `memory/storage/qdrant.py` — Qdrant 向量数据库适配器

- **拓扑层 / 依赖 / 被依赖**：远端/本地 Qdrant 向量实现；顶层依赖 `memory.base` 和 `BaseVectorStore`，属于真实循环依赖组。被 `storage` 门面和 `manager` 通过存储选择依赖。
- **规模**：187 行 / 11 个 def。
- **职责**：把 `MemoryItem` 的向量写入 Qdrant collection。Phase 3（Qdrant 轻量化）起 payload 改为**投影式瘦身**：向量与正文的真值在 SQLite（`document_repo` 的 `chunks` 与 memories），Qdrant 只保留回查/过滤需要的少量字段，避免同一份内容在两个库漂移。collection 延迟到首次 upsert 时按实际维度创建，支持注入兼容 client；检索使用 namespace 和可选的 memory_type 过滤；`list_ids()`/`_ensure_ready_for_read()` 支持只读进程的对账与读路径。
- **关键模块级对象/常量**：`__all__ = ["QdrantVectorStore"]`；默认 collection 来自 `constants.MEMORY_QDRANT_COLLECTION`（值仍为 `"helloagents_memory"`）；payload 额外字段 `"namespace"`。

#### 类 `QdrantVectorStore(BaseVectorStore)` — Qdrant 适配器

- **构造**：`def __init__(self, url: str | None = None, collection_name: str = MEMORY_QDRANT_COLLECTION, *, api_key: str | None = None, client: Any = None, dimension: int | None = None, namespace: str = "memory") -> None`。
  - `namespace` 必须非空；`collection_name` 必须非空；`dimension` 若给出必须为正整数。
  - `client=None` 时导入 `qdrant_client.QdrantClient`；有 URL 时连接 URL，无 URL 时使用 `path=":memory:"`。
  - 缺少 qdrant-client 抛 `RuntimeError("QdrantVectorStore requires qdrant-client")`。
- **关键属性**：`client`、`collection_name`、`dimension`（可为 `None`，表示等待首次向量确定）、`namespace`、`_ready`。
- **方法**：
  - `_ensure_collection(self, dimension: int) -> None` — 私有懒初始化；已 `_ready` 直接返回。导入 `Distance`/`VectorParams`，查询 collection 是否存在；不存在则创建 COSINE collection；存在则先检查构造时维度，再尽量通过 `get_collection(...).config.params.size` 校验远端维度。成功后记录 `dimension` 并设 `_ready=True`；维度不匹配抛 `ValueError`，其他客户端异常包成 `RuntimeError`。
  - `_light_payload(item: MemoryItem) -> dict[str, Any]` — `@staticmethod`；从 `item.to_dict()` 只取 `id/memory_type/created_at/expires_at` 四个字段（正文与其余元数据在 SQLite 真值源）。`upsert` 用它，再补 `namespace`。
  - `upsert(self, item: MemoryItem) -> None` — `item.embedding` 为空时静默返回；否则用向量长度初始化 collection，构造 `payload = self._light_payload(item)` 并追加 `namespace`，调用 `client.upsert` 写入一个 `PointStruct`；Point ID 由 `_point_id` 转换。无本地锁；Qdrant/依赖异常向上抛。
  - `upsert_chunk(self, chunk_id: str, vector: list[float], *, document_id: str = "", chunk_index: int = 0, source: str = "", memory_type: str = "semantic") -> None` — 重索引路径（`revectorize`/对账自愈）的窄 payload 写入：`chunk_id/document_id/chunk_index/source/memory_type/namespace`。向量为空直接返回；collection 按向量维度懒初始化。与 `upsert` 共用同一 `chunk_id`，因此 SQLite 回查路径一致。
  - `delete(self, item_id: str) -> bool` — collection 尚未 ready 时直接返回 `False`；否则用 `PointIdsList` 删除对应点并返回 `True`。它不查询点数，无法区分点原本是否存在；Qdrant 客户端异常向上抛。
  - `_ensure_ready_for_read(self) -> bool` — 读路径的 ready 探测：`_ready` 为真直接返回；否则用 `client.collection_exists` 探测，集合存在才置 `_ready=True`。探测异常按「读不到」返回 `False`（由调用方降级）。避免只读进程把空集合误报为「没有向量」。
  - `list_ids(self, *, limit: int = 10000) -> list[str]` — 用 `client.scroll` 取回全部点的 app 级 id（payload 里 `chunk_id` 优先、否则 `id`、最后回落点 id）；对账（`/api/reconcile`）需要全集。未 ready 或无 `scroll` 时返回空表。
  - `search(self, vector: list[float], *, limit: int = 10, memory_type: MemoryType | str | None = None) -> list[tuple[str, float]]` — 校验 limit；未 ready（经 `_ensure_ready_for_read`）返回 `[]`；构造必须匹配 `namespace`、可选匹配 `memory_type` 的 Filter。优先 `client.search(...)`，捕获 `AttributeError` 后回退 `client.query_points(...).points`；每条命中取 payload 的 `chunk_id`（优先）或 `id`（老点），都缺失才回落 Qdrant 点 id。注意 `except AttributeError` 会同时覆盖 client 内部误用带来的 AttributeError，源码没有更窄的异常区分。
  - `_point_id(item_id: str) -> str` — `@staticmethod`；若 `uuid.UUID(item_id)` 可解析则原样返回，否则返回基于 `uuid.NAMESPACE_URL` 和 `f"helloagents-memory:{item_id}"` 的确定性 UUID5 字符串。捕获 `ValueError`/`AttributeError`；其他异常不捕获。
  - `clear(self) -> None` — 仅当 `_ready` 为真时删除整个 collection，并把 `_ready=False`；返回 `None`。源码没有 `close()` 方法，因此 `MemoryManager.close()` 的可选 close 调用不会关闭 Qdrant client。

---

### `memory/storage/vector.py` — 向量索引抽象、余弦相似度与内存实现

- **拓扑层 / 依赖 / 被依赖**：存储实现层；顶层依赖 `memory.base` 的 `MemoryItem`、`MemoryType`，属于真实循环依赖组。被 `storage` 门面、`qdrant`（继承接口）、`base` 的惰性默认构造和 `manager` 依赖。
- **规模**：73 行 / 10 个 def。
- **职责**：定义向量存储的最小接口，提供与实现无关的 `cosine_similarity`，并实现进程内可重建的 `InMemoryVectorStore`。内存实现按记录 ID 保存向量副本和记忆类型，检索时按类型过滤后计算相似度，按分数降序、ID 升序稳定排序。
- **关键模块级对象/常量**：`__all__ = ["BaseVectorStore", "InMemoryVectorStore", "cosine_similarity"]`；内部字典 `_vectors: dict[str, tuple[list[float], MemoryType]]`。

#### 类 `BaseVectorStore(ABC)` — 向量索引抽象契约

- **构造**：抽象基类，无显式构造函数。
- **关键属性**：无强制属性；`search` 返回 `(item_id, score)` 对，score 越大通常表示越相似（Qdrant COSINE 与本地余弦一致）。
- **方法**：
  - `upsert(self, item: MemoryItem) -> None` — `@abstractmethod`；按 `item.id` 写入/覆盖该记录的向量和类型元数据；无嵌入时的处理由实现决定。
  - `delete(self, item_id: str) -> bool` — `@abstractmethod`；按 ID 删除，返回是否存在并删除。
  - `search(self, vector: list[float], *, limit: int = 10, memory_type: MemoryType | str | None = None) -> list[tuple[str, float]]` — `@abstractmethod`；按查询向量检索，可选按层过滤，返回最多 `limit` 个 `(id, score)`。
  - `clear(self) -> None` — 默认空操作；实现可覆盖为清空索引；无显式异常。

#### 函数 `cosine_similarity(left: list[float], right: list[float]) -> float` — 计算余弦相似度

- **签名**：`def cosine_similarity(left: list[float], right: list[float]) -> float:`
- **参数**：两个向量。
- **返回**：长度为空、长度不一致、存在非有限值或任一范数为 0 时返回 `0.0`；否则返回点积除以两范数乘积。
- **副作用/异常**：无 I/O；数值转换异常未包装，但 `float(value)` 对不可转值可能抛 `TypeError`/`ValueError`。
- **调用关系**：`InMemoryVectorStore.search` 对每个候选调用。

#### 类 `InMemoryVectorStore(BaseVectorStore)` — 本地进程内向量索引

- **构造**：`def __init__(self) -> None`；初始化 `_vectors` 空字典和 `threading.RLock`。
- **关键属性**：`_vectors` 以记录 ID 为键，值为 `(list[float], MemoryType)`；向量会复制存储，避免调用方后续修改原列表；`_lock` 保证 upsert/delete/search/clear 互斥。
- **方法**：
  - `upsert(self, item: MemoryItem) -> None` — `item.embedding is None` 时直接返回，不删除旧条目；否则拒绝空向量或含非有限值（`ValueError`），持锁写入 `(list(item.embedding), item.memory_type)`。不给返回；无 I/O。注意：已有 ID 若新 item 无 embedding，旧向量会保留，这是源码未显式清理的行为。
  - `delete(self, item_id: str) -> bool` — 持锁 `pop`，返回是否真的移除。
  - `search(self, vector: list[float], *, limit: int = 10, memory_type: MemoryType | str | None = None) -> list[tuple[str, float]]` — 校验 `limit` 正整数；把可选 `memory_type` 转枚举；在锁内为匹配类型的条目计算 `cosine_similarity`；按 `(-score, item_id)` 排序后截取 `limit`，返回 `(id, score)`。查询向量本身不显式校验有限性，非有限/零向量会由相似度函数返回 0。
  - `clear(self) -> None` — 持锁清空 `_vectors`；返回 `None`；不影响文档库。

---

### `tool/base.py` — 新版 tool 协议基类的兼容导出层

- **拓扑层**：L3 ｜ **依赖**：`core.registry.BaseTool`、`core.registry.ToolRegistry` ｜ **被依赖**：`tool/__init__.py` 以及直接执行 `from tool.base import BaseTool, ToolRegistry` 的代码。
- **规模**：5 行 / 0 个 def。
- **工具名 / 副作用**：无 ｜ **职责**：本项目有旧路径 `tool.base` 和新核心路径 `core.registry`，本文件仅保持前者可用；不复制实现、不改变行为。
- **关键常量**：`__all__ = ["BaseTool", "ToolRegistry"]`。

##### 类

本文件未定义类。导出的 `BaseTool` 是抽象基类，源码约定其具有 `spec: ToolSpec` 属性，并声明抽象方法 `execute(self, arguments: BaseModel) -> BaseModel | Any`；`ToolRegistry` 负责注册、解析、快照、代次和确认键。以上契约定义在 `core.registry`，不是本文件中的新实现。

##### 函数

本文件未定义函数。因此本文件的 def 计数为 0，不能把导入名称误计为本文件函数。

## L4 第一批具体工具与四层记忆类型（时间 / 文件系统 / 搜索 / 更新日志 / working·episodic·semantic·perceptual）

### `core/skill_catalog.py` — `system.skill_catalog` 技能列表与按需查看工具

- **拓扑层**：L4 ｜ **依赖**：标准库 `pathlib`、`typing`，`pydantic`，`constants`，以及 `core.models`、`core.registry`、`core.skill_discovery`、`core.skill_registry` ｜ **被依赖**：`core.__init__`；`agents.agent` 构造并注册
- **规模**：104 行 / 3 个 def
- **职责**：先只把技能摘要放入稳定的系统前缀，只有模型显式 `view` 时才返回完整 Markdown，从而减少 prompt token 并保持缓存友好。技能是不可执行说明书，不是工具调用目标。
- **关键模块级对象**：`SkillCatalogInput`、`SkillCatalogOutput`、`SkillCatalogTool`。

#### 类 `SkillCatalogInput(BaseModel)` — 技能目录输入

- **构造**：`action: Literal["list", "view"]`、`skill_name: str | None = None`；skill_name 最长 64；配置 `extra="forbid", strict=True`。
- **关键属性**：view 需要 skill_name。
- **方法**：
  - `validate_action_arguments(self) -> SkillCatalogInput` — `@model_validator(mode="after")`；view 缺少非空 skill_name 时抛 `ValueError`。

#### 类 `SkillCatalogOutput(BaseModel)` — 技能目录输出

- **构造**：`skills: list[dict[str, Any]] = []`、`content: str | None = None`、`version: str | None = None`、`content_hash: str | None = None`。
- **关键属性**：list 填充 skills；view 填充正文、版本和 hash。
- **方法**：无自定义方法定义。

#### 类 `SkillCatalogTool(BaseTool)` — 技能摘要和正文的只读门面

- **构造**：`__init__(self, registry: SkillRegistry, *, root: str | Path = DEFAULT_SKILLS_ROOT) -> None`；校验 registry 类型；保存 registry，并把 root resolve 为绝对 Path。源码当前未在 execute 中使用 self.root。
- **关键属性**：`registry`、`root`；类级 spec 为 `system.skill_catalog` 1.1，只读，tags 为 catalog/skills/instructions。
- **方法**：
  - `execute(self, arguments: SkillCatalogInput, context: ExecutionContext | None = None) -> SkillCatalogOutput` — 校验 arguments；显式 `del context`，因为这是只读工具且仅为签名兼容接收 context；list 从 registry snapshot 生成 summaries；view 用去空白 skill_name 调 `maybe_resolve`，缺失时给出调用 action=list 的错误，存在时调用 `read_skill_content` 并返回正文、version 和 content_hash。

### `memory/types/episodic.py` — 带时间线视图的事件记忆

- **拓扑层 / 依赖 / 被依赖**：类型实现层；依赖 `BaseMemory`、`MemoryItem`、`MemoryType` 和 `ensure_datetime`。被 `types` 门面和 `manager` 依赖。
- **规模**：29 行 / 2 个 def。
- **职责**：提供事件写入 `record` 和按时间窗口读取 `timeline`。通用 `add` 负责 embedding、TTL 和持久化；episodic 本身不额外附加 TTL 策略。事件时间使用 `timestamp`，缺少时由 `MemoryItem.event_time` 回退到 `created_at`。
- **关键模块级对象/常量**：类属性 `memory_type = MemoryType.EPISODIC`。

#### 类 `EpisodicMemory(BaseMemory)` — 事件/问答留痕实现

- **构造**：无显式 `__init__`；继承 `BaseMemory` 的构造，类属性固定为 `MemoryType.EPISODIC`。调用方通常由 `MemoryManager` 注入共享 document/vector/embedding/config。
- **关键属性**：继承的全部属性；事件所需的时间信息放在 `timestamp`、`created_at`、`updated_at` 等字段。
- **方法**：
  - `record(self, event: str, *, timestamp: datetime | str | None = None, metadata: Mapping[str, Any] | None = None, importance: float = 0.5) -> MemoryItem` — 直接调用 `self.add(event, timestamp=timestamp, metadata=metadata, importance=importance)`；返回新 `MemoryItem`；不单独限制 TTL，父类异常原样传播。
  - `timeline(self, *, start: datetime | str | None = None, end: datetime | str | None = None, limit: int | None = None) -> list[MemoryItem]` — 将 start/end 通过 `ensure_datetime` 规范化；start 晚于 end 抛 `ValueError`；limit 非 None 时必须是正整数；使用 `self.list()`（默认排除过期），按 `item.event_time` 同时过滤边界（闭区间），然后按 `event_time` 升序排序，最后截取 limit。返回列表；无时间时使用 created_at，和源码 `event_time` 一致。

---

### `memory/types/perceptual.py` — 多模态感知条目存储

- **拓扑层 / 依赖 / 被依赖**：类型实现层；依赖 `BaseMemory`、`MemoryItem`、`MemoryType`。被 `types` 门面和 `manager` 依赖。
- **规模**：27 行 / 2 个 def。
- **职责**：把原始多模态数据放入 `payload`，把可供向量检索的文本放入 `content`，并单独保存非空 `modality`。当调用方没有提供 `content` 时，字符串数据直接作为文本，非字符串数据使用 `"{modality} perceptual memory"` 作为占位文本。
- **关键模块级对象/常量**：类属性 `memory_type = MemoryType.PERCEPTUAL`；类内别名 `remember = store`。

#### 类 `PerceptualMemory(BaseMemory)` — 多模态感知记忆实现

- **构造**：无显式 `__init__`；继承 `BaseMemory`，类属性固定为 `MemoryType.PERCEPTUAL`。
- **关键属性**：继承的 document/vector/embedding/config；`MemoryItem.payload` 保存原始数据，`modality` 保存模态标签。
- **方法**：
  - `store(self, data: Any, *, modality: str, content: str | None = None, metadata: Mapping[str, Any] | None = None, importance: float = 0.5, item_id: str | None = None) -> MemoryItem` — 校验 `modality` 为非空字符串；`content` 非 None 时使用它，否则字符串 `data` 作为 content，非字符串使用模态占位文本；调用 `self.add(str(text), metadata=metadata, importance=importance, payload=data, modality=modality, item_id=item_id)` 并返回 item。嵌入只针对 text/payload 分离后的文本内容；异常来自父类或校验。
  - `get_data(self, item_id: str, default: Any = None) -> Any` — 通过 `self.get(item_id)` 读取感知记录，存在则返回 `item.payload`，否则返回 default；过期记录由 `BaseMemory.get` 删除并返回 None。

---

### `memory/types/semantic.py` — 事实、实体关系与图边协调

- **拓扑层 / 依赖 / 被依赖**：类型实现层；依赖 `BaseMemory`、`MemoryItem`、`MemoryType` 和 `ids`（含 `entity_id_for`），运行时延迟导入 `Neo4jGraphStore`。被 `types` 门面和 `manager` 依赖；RAG 知识物化层通过 `manager.semantic` 写入事实。
- **规模**：201 行 / 9 个 def。
- **职责**：把三元组事实写入文档库并通过 `relation_id_for` 统一主键，同时将关系写入图存储。它支持旧 `fact:` ID 的原地更新，并保留调用方传入的 `active/superseded_by/superseded_at/supersedes` 元数据；不过 supersede/retract 的实际多策略退役逻辑在 RAG 知识物化层完成，本类没有独立的 supersede/retract 方法。删除记录时同步调用图存储的 `delete_memory_relation`。Phase 4（F6）起所有的边写入都收敛到 `_write_edge`：新事实、**合并/撤回更新**都会重新刷图边属性（旧的 active/memory_id 不再残留在图中），并把两端实体属性（domain/aliases/importance）随边一起写入图。
- **关键模块级对象/常量**：类属性 `memory_type = MemoryType.SEMANTIC`；`__all__ = ["SemanticMemory"]`；图属性白名单包括 `evidence`、`source`、`source_document`、`chunk_id`、`predicate_key`、`action`、`cardinality`、`active`、`superseded_by`、`superseded_at`、`supersedes`。

#### 类 `SemanticMemory(BaseMemory)` — 语义事实 Memory + graph 边实现

- **构造**：`def __init__(self, *, graph_store: Neo4jGraphStore | None = None, **kwargs: Any) -> None`。
  - 先 `super().__init__(memory_type=self.memory_type, **kwargs)`；`kwargs` 可传文档/向量/嵌入/config。
  - `graph_store=None` 时延迟导入并实例化 `Neo4jGraphStore()`；无 URI/driver 时该对象使用内存回退。
- **关键属性**：`graph_store`、继承的公共存储；事实的文档记录通过 canonical relation ID 关联图属性 `memory_id`。
- **方法**：
  - `add_fact(self, subject: str, predicate: str, object: str, *, metadata: Mapping[str, Any] | None = None, confidence: float = 1.0, item_id: str | None = None) -> MemoryItem` — 校验三个字段为非空字符串；confidence 必须为 `[0,1]` 数字；canonical ID 默认为 `relation_id_for(subject,predicate,object)`；若 canonical ID 不存在且调用方未传 `item_id`，回退查 `legacy_fact_id_for` 的旧行。已有同层事实时：合并旧 metadata 和新 metadata；当新 metadata 为 `None` 或不含 `"active"` 时，重置 `active=True`、`superseded_by=[]`、`superseded_at=""`，否则保留显式 `active=False` 等退役状态；然后以 `importance=max(existing.importance, confidence)` 和原 fact_id 调 `self.add(...)`，**随后调用 `_write_edge(...)` 用合并后的 metadata 重刷图边**（F6：合并/撤回也会更新边的 active/memory_id，保证图中边与 SQLite 事实条目一致）。新事实时，合并传入 metadata 并在覆盖 `subject`、`predicate`、`object`、`confidence` 后调用 `self.add(...)`，重要性为 confidence，再走同一 `_write_edge`。返回 item；校验失败抛 `ValueError`，文档/嵌入/图存储异常向上抛。
  - `_graph_properties(self, item: MemoryItem, metadata: Mapping[str, Any]) -> dict[str, Any]` — 构造一条边的图属性：固定 `memory_id`（=item.id）与 `confidence`（来自 metadata，默认 1.0），再按白名单（`evidence/source/source_document/chunk_id/predicate_key/action/cardinality/active/superseded_by/superseded_at/supersedes`）复制 metadata 字段。所有写边路径共用同一属性集合，避免各分支漂移。
  - `_endpoint_attributes(self, name: str) -> dict[str, Any]` — 取端点实体的 `{domain, aliases, importance}`：用 `entity_id_for(name)` 从文档库读 `kind=entity` 项，缺失时返回空字典（图节点保持默认属性）。抽取管道先写实体再写关系，因此实体项通常已存在。
  - `_write_edge(self, subject: str, predicate: str, object: str, item: MemoryItem, metadata: Mapping[str, Any]) -> None` — 汇总 `_graph_properties(item, metadata)` 与两端 `_endpoint_attributes`，调用 `graph_store.add_relation(..., source_domain=..., target_domain=..., source_aliases=..., target_aliases=..., source_importance=..., target_importance=...)`。新事实与合并/撤回更新都经它落边。
  - `add_relation(self, source: str, relation: str, target: str, *, metadata: Mapping[str, Any] | None = None) -> MemoryItem` — 参数只是 `add_fact` 的别名语义；直接 `return self.add_fact(source, relation, target, metadata=metadata)`；返回事实 item；没有 graph 操作的第二套路径。
  - `delete(self, item_id: str) -> bool` — 先查文档，记录不存在或类型不匹配返回 `False`；调用父类 `delete` 删除向量和文档；仅当父类返回真时，尝试取得 `graph_store.delete_memory_relation`，可调用则执行并忽略其返回值；返回父类删除结果。图方法缺失时仍可成功返回；图删除异常会向上传播。
  - `related(self, entity: str, *, relation: str | None = None) -> list[dict[str, Any]]` — 直接委托 `graph_store.get_relations(entity, relation=relation)`；返回图边字典列表；异常由图实现决定。
  - `facts(self, entity: str | None = None) -> list[MemoryItem]` — 调用 `self.list()`（默认排除过期）；`entity=None` 返回全部事实；否则用精确值比较 `metadata["subject"]` 或 `metadata["object"]`，不进行 `normalize_entity_name` 等价匹配。返回过滤后的列表。
- **supersede/retract 语义说明**：本类只保存和合并 `active`、`superseded_by`、`superseded_at`、`supersedes`，以及图属性白名单中的 `action`/`cardinality`；RAG 知识物化层负责先退役旧事实，再写入新事实，retract 写入 `active=false` 且保留历史记录。因此从本文件单独看，没有自动扫描同槽位并退役旧值的方法。

---

### `memory/types/working.py` — 带 TTL 和容量淘汰的会话内工作记忆

- **拓扑层 / 依赖 / 被依赖**：类型实现层；依赖 `BaseMemory`、`MemoryItem`、`MemoryType`。被 `types` 门面和 `manager` 依赖。
- **规模**：58 行 / 6 个 def。
- **职责**：实现工作记忆的键值式 `set/get_by_key/get_value` 接口，同时保留通用 `add`。TTL 语义复用 `BaseMemory.add`：working 在未显式提供 TTL 或 `expires_at` 时自动使用 `MemoryConfig.default_ttl_seconds`。每次 `add` 后按重要性升序、更新时间升序淘汰超额项。
- **关键模块级对象/常量**：类属性 `memory_type = MemoryType.WORKING`；没有模块级常量。

#### 类 `WorkingMemory(BaseMemory)` — 工作记忆实现

- **构造**：`def __init__(self, *, capacity: int | None = None, **kwargs: Any) -> None`。
  - 先调用 `super().__init__(memory_type=self.memory_type, **kwargs)`，因此 `kwargs` 可传 `document_store`、`vector_store`、`embedding`、`config`。
  - `capacity=None` 时使用 `config.working_memory_capacity`；容量必须是正整数且不能是 bool，否则 `ValueError`。
- **关键属性**：`capacity`、继承的 `config` 与三类存储、`memory_type` 固定为 working。
- **方法**：
  - `add(self, content: str, **kwargs: Any) -> MemoryItem` — 调用父类 `add` 完成 TTL、嵌入和持久化，再调用 `_evict_if_needed()`，返回新 item。任何父类异常原样传播；容量淘汰发生在写入之后。
  - `set(self, key: str, value: Any, *, ttl_seconds: float | None = None, importance: float = 0.5, metadata: Mapping[str, Any] | None = None) -> MemoryItem` — 键必须为非空字符串；先查 `get_by_key(key)`，存在则 `delete(existing.id)`；合并 metadata 并写入 `"key" = key`；最后调用 `add(str(value), metadata=merged, payload=value, ttl_seconds=ttl_seconds, importance=importance, item_id=key)`。返回新记录；键值本身的原始类型只存在 `payload`。如果旧记录的 metadata 有相同 key 但 ID 不等于 key，会删除旧记录再创建以 key 为 ID 的新记录。
  - `get_by_key(self, key: str) -> MemoryItem | None` — 先按 ID 直接 `document_store.get(key)`，若类型正确且未过期则返回；若直接 ID 不存在/过期/类型不符，则扫描 `self.list()` 找 `metadata["key"] == key` 的未过期项；找不到返回 `None`。该函数不通过 `self.get`，因此直接命中过期项时不会触发 `BaseMemory.get` 的删除副作用；列表扫描默认排除过期项。
  - `get_value(self, key: str, default: Any = None) -> Any` — 调用 `get_by_key`，存在时返回 `item.payload`，否则返回 `default`；不区分 payload 为 `None` 与缺失。
  - `_evict_if_needed(self) -> None` — 私有淘汰；调用 `self.list()`（默认排除过期），若数量不超过容量直接返回；否则按 `(importance, updated_at)` 升序选出超出容量的 victim 并逐条 `delete`。返回 `None`；删除异常原样传播。排序先牺牲低重要性，再牺牲更新时间较早者。

---

### `tool/current_time.py` — 获取当前系统本地时间

- **拓扑层**：L4 ｜ **依赖**：`datetime.UTC`/`datetime`、`pydantic`、`core.BaseTool`/`ToolSpec` ｜ **被依赖**：`core.discovery` 扫描 `tool` 包时导入并调用工厂。
- **规模**：76 行 / 2 个 def。
- **工具名 / 副作用**：`system.current_time`、`side_effect="read"`。
- **职责**：读取一次 Agent 进程所在系统的本地时间；返回带 UTC 偏移的 ISO 8601 秒级时间和 Unix 秒时间戳；不写任何文件或外部状态。
- **关键常量**：`TOOL_ENABLED = True`。

#### 类 `CurrentTimeInput(BaseModel)` — 输入契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- **字段**：无。调用方不应传参数；多余字段会被严格模型拒绝。

#### 类 `CurrentTimeOutput(BaseModel)` — 输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `local_time: str`：无默认；`min_length=1`；系统本地时间，ISO 8601 且包含 UTC 偏移量。
- `timezone_name: str`：无默认；`min_length=1`；系统本地时区名，无法取得名称时返回 `"UTC"`。
- `unix_timestamp: int`：无默认；`ge=0`；自 1970-01-01 UTC 起的整数秒。
- 类内没有额外 `def`。

#### 类 `CurrentTimeTool(BaseTool)` — 工具实现

- `spec`：`name="system.current_time"`、`description` 指明需要真实当前时间且无参数无写入、`version="1.0.0"`、输入输出为上述模型、`side_effect="read"`、`permissions=()`、`timeout_seconds=5.0`、`idempotent=True`、`parallel_safe=True`、`max_concurrency=None`、`tags=("system", "time", "clock", "datetime")`；`recommended_before_tools` 未显式传入，使用默认空元组。
- **声明理由**：读取系统时钟无本地副作用；重复调用安全，因此 `idempotent=True`（这里的“幂等”指无副作用/可安全重试，不表示两次返回相同时间）；并发安全，未设置工具专属并发上限而使用 Runtime 全局上限。

##### 方法 `execute(self, arguments: CurrentTimeInput) -> CurrentTimeOutput`

```python
def execute(self, arguments: CurrentTimeInput) -> CurrentTimeOutput
```

- **作用**：读取一次本地时间并生成稳定输出。
- **参数**：`arguments` 应是 `CurrentTimeInput`；方法首行 `del arguments`，源码未做运行时类型检查。
- **返回**：`CurrentTimeOutput`，`local_time=local_now.isoformat(timespec="seconds")`、`timezone_name=local_now.tzname() or "UTC"`、`unix_timestamp=int(local_now.astimezone(UTC).timestamp())`。
- **副作用 / 异常**：无写副作用；系统时钟/时区环境异常由 `datetime` 调用自然抛出，源码中未见额外捕获。
- **调用关系**：Runtime 调用；`create_tool()` 只创建实例。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`CurrentTimeTool()`。
- **调用关系**：`core.discovery` 检查 `TOOL_ENABLED` 后调用。

### `tool/fs_edit_text.py` — 在读改写事务内替换字面文本

- **拓扑层**：L4 ｜ **依赖**：`pathlib.Path`、`pydantic`、`core.BaseTool`/`ToolSpec`、`_shared.atomic_write_text`/`read_text_file`/`resolve_path`/`workspace_root` ｜ **被依赖**：`core.discovery` 扫描并注册 `fs.edit_text`。
- **规模**：179 行 / 3 个 def。
- **工具名 / 副作用**：`fs.edit_text`、`side_effect="write"`。
- **职责**：在一次 `execute()` 内读取文件、按字面量查找 `old_string`、做替换并用规范编码原子写回；默认要求唯一匹配，`replace_all=True` 时才替换全部；避免“先前读取后文件改变”的窗口。
- **关键常量**：`TOOL_ENABLED = True`；`MAX_OLD_CHARS = 100_000`；`MAX_NEW_CHARS = 1_000_000`。两个常量目前没有被字段定义直接引用，字段使用等价字面量。

#### 类 `EditTextInput(BaseModel)` — 输入契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `path: str`：无默认；`min_length=1`、`max_length=2000`；待编辑文件路径；相对工作区根或工作区内的绝对路径。
- `old_string: str`：无默认；`min_length=1`、`max_length=100_000`；精确字面量；默认必须唯一出现。
- `new_string: str`：无默认；`max_length=1_000_000`；替换文本；空字符串会删除 `old_string`；换行和缩进按字面保留。
- `replace_all: bool = False`：为 `True` 时替换所有出现；为 `False` 时非唯一匹配报错。
- `encoding: str = "utf-8"`：`min_length=1`、`max_length=32`；文件编码。
- 类内没有额外 `def`。

#### 类 `EditTextOutput(BaseModel)` — 输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `resolved_path: str`：无默认；`min_length=1`；被编辑的绝对规范化路径。
- `replacements: int`：无默认；`ge=0`；实际替换的出现次数。
- `changed: bool`：无默认；成功编辑时源码固定返回 `True`。
- `character_count: int`：无默认；`ge=0`；编辑后文件的字符数。
- `byte_count: int`：无默认；`ge=0`；编辑后编码内容的字节数。
- 类内没有额外 `def`。

#### 类 `EditTextTool(BaseTool)` — 工具实现

- `spec`：`name="fs.edit_text"`、`description` 说明在既有 UTF-8 文本中精确替换块、原子写回、与 `fs.write_text` 的分工、默认要求唯一匹配、`version="1.1.0"`、输入输出模型、`side_effect="write"`、`permissions=()`、`timeout_seconds=15.0`、`idempotent=False`、`parallel_safe=False`、`max_concurrency=1`、`tags=("fs", "file", "edit", "replace", "text")`、`recommended_before_tools=("fs.read_text",)`。
- **声明理由**：这是读-改-写操作；重复执行时 `old_string` 已被替换，第二次通常找不到目标而失败或产生不同结果，所以不把重试标为安全，`idempotent=False`。并发编辑同一文件会丢更新，所以 `parallel_safe=False` 并把并发压到 1。建议先读文件确认精确内容，但不强制。

##### 方法 `__init__(self, base_dir: str | Path | None = None, *, allow_outside: bool | None = None) -> None`

```python
def __init__(
    self,
    base_dir: str | Path | None = None,
    *,
    allow_outside: bool | None = None,
) -> None
```

- **作用 / 参数 / 实现**：注入沙箱根和越界开关；默认 `workspace_root()`。

##### 方法 `execute(self, arguments: EditTextInput) -> EditTextOutput`

```python
def execute(self, arguments: EditTextInput) -> EditTextOutput
```

- **作用**：验证类型，读取既有文件，检查匹配次数，执行替换并原子写回。
- **参数**：`arguments` 必须是 `EditTextInput`，否则抛 `TypeError`。
- **返回**：`EditTextOutput`；`replacements` 为 `occurrences`（全替换）或 1（单次替换）；`character_count=len(replacement)`，`byte_count` 为原子写返回值。
- **副作用**：替换文件内容和修改文件元数据。
- **异常**：路径越界/编码非法/匹配 0 次/非唯一匹配且未开启 `replace_all` 均为 `ValueError`；文件不存在为 `FileNotFoundError`；不是文件为 `IsADirectoryError`；父目录异常和编码失败由 `atomic_write_text()` 抛出。
- **关键实现**：`text.count(old_string)` 决定匹配次数；`old_string` 为空被模型限制为不可能；`replace_all=True` 使用 `str.replace(..., new_string)`，否则使用次数参数 1；写回调用 `create_parents=False`，因此父目录必须已存在。源码没有在 `new_string == old_string` 时提前返回；只要找到匹配就会执行写回并报告 `changed=True`。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`EditTextTool()`。

### `tool/fs_read_dir.py` — 稳定列出工作区目录的一级子项

- **拓扑层**：L4 ｜ **依赖**：`os`、`datetime`、`pathlib.Path`、`pydantic`、`core.BaseTool`/`ToolSpec`、`_shared.resolve_path`/`workspace_root` ｜ **被依赖**：`core.discovery` 扫描并注册 `fs.read_dir`。
- **规模**：183 行 / 3 个 def。
- **工具名 / 副作用**：`fs.read_dir`、`side_effect="read"`。
- **职责**：列出指定目录的直接子项；默认隐藏点号开头的条目；返回名称、绝对路径、类型、大小、修改时间和截断状态；按目录优先、再按名称大小写不敏感排序，供模型稳定浏览项目布局。
- **关键常量**：`TOOL_ENABLED = True`；`MAX_ENTRIES = 5_000`；`ReadDirInput.max_entries` 允许 1..5000，默认 500。源码中 `MAX_ENTRIES` 目前没有被 `execute()` 直接引用。

#### 类 `ReadDirInput(BaseModel)` — 输入契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `path: str`：无默认；`min_length=1`、`max_length=2000`；待列目录；相对工作区根或工作区内的绝对路径。
- `include_hidden: bool = False`：是否包含名称以点开头的条目（如 `.git`、`.venv`）。
- `max_entries: int = 500`：`ge=1`、`le=5000`；返回条目上限。
- 类内没有额外 `def`。

#### 类 `DirEntry(BaseModel)` — 单个目录项

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `name: str`：无默认；`min_length=1`、`max_length=2000`；目录项名称。
- `path: str`：无默认；`min_length=1`；解析后的绝对路径。
- `is_dir: bool`：无默认；是否为目录（`item.is_dir(follow_symlinks=True)`）。
- `is_file: bool`：无默认；是否为普通文件（`item.is_file(follow_symlinks=True)`）。
- `is_symlink: bool`：无默认；是否为符号链接。
- `size: int`：无默认；`ge=0`；内容字节大小；优先跟随符号链接取 stat，失败则取链接自身 stat。
- `modified: str`：无默认；`min_length=1`；本地时区 ISO 8601 秒级字符串。
- 类内没有额外 `def`。

#### 类 `ReadDirOutput(BaseModel)` — 输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `resolved_path: str`：无默认；`min_length=1`；实际列出的绝对规范化目录。
- `entries: list[DirEntry]`：无默认；`max_length=5000`；已排序结果，目录优先。
- `entry_count: int`：无默认；`ge=0`；**本次返回的条目数**，不是目录中未截断的总数。
- `truncated: bool`：无默认；过滤后总条目数是否超过 `max_entries`。
- 类内没有额外 `def`。

#### 类 `ReadDirTool(BaseTool)` — 工具实现

- `spec`：`name="fs.read_dir"`、`description` 说明列直接子项及字段、目录优先排序、适合编辑前探索、`version="1.0.0"`、输入输出模型、`side_effect="read"`、`permissions=()`、`timeout_seconds=15.0`、`idempotent=True`、`parallel_safe=True`、`max_concurrency=8`、`tags=("fs", "directory", "list", "read")`、`recommended_before_tools=()`。
- **声明理由**：只读、可重复、并发读取安全；15 秒覆盖大目录扫描；目录工具本身不推荐额外前置工具。

##### 方法 `__init__(self, base_dir: str | Path | None = None, *, allow_outside: bool | None = None) -> None`

```python
def __init__(
    self,
    base_dir: str | Path | None = None,
    *,
    allow_outside: bool | None = None,
) -> None
```

- **作用 / 参数 / 实现**：与 `ReadTextTool.__init__` 相同；默认 `base_dir=workspace_root()`，显式传入时解析为绝对路径；`allow_outside` 保存到实例，`None` 表示交给 `resolve_path()` 读取环境变量。
- **调用关系**：`create_tool()` 无参调用；也可测试或嵌入时注入自定义根。

##### 方法 `execute(self, arguments: ReadDirInput) -> ReadDirOutput`

```python
def execute(self, arguments: ReadDirInput) -> ReadDirOutput
```

- **作用 / 参数**：验证参数类型，解析目录路径，读取一级条目，过滤隐藏项，排序并按 `max_entries` 截断。
- **返回**：`ReadDirOutput`；其中 `resolved_path` 为解析后的目录，`entries` 为有界排序结果，`entry_count=len(bounded)`，`truncated=(len(entries) > arguments.max_entries)`。
- **副作用**：无写入；调用 `os.scandir()` 读取目录。
- **异常**：路径越界为 `ValueError`；不存在为 `FileNotFoundError`；不是目录为 `NotADirectoryError`；目录项 stat 完全失败会抛 `OSError`。
- **关键实现**：`os.scandir()` 遍历；默认跳过隐藏名；对每个项目先 `item.stat(follow_symlinks=True)`，失败则回退 `follow_symlinks=False`，避免损坏符号链接中断整个列表；路径为 `Path(item.path).resolve()`；类型标志跟随符号链接；修改时间用 `datetime.fromtimestamp(st_mtime).astimezone().isoformat(timespec="seconds")`；最后按 `(not entry.is_dir, entry.name.casefold())` 排序。源码没有在达到 `max_entries` 时提前停止遍历。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`ReadDirTool()`。

### `tool/fs_read_file_glob.py` — 在沙箱根内用 glob 查找路径

- **拓扑层**：L4 ｜ **依赖**：`os`、`pathlib.Path`、`pydantic`、`core.BaseTool`/`ToolSpec`、`_shared._is_within`/`resolve_path`/`workspace_root` ｜ **被依赖**：`core.discovery` 扫描并注册 `fs.read_file_glob`。
- **规模**：177 行 / 4 个 def。
- **工具名 / 副作用**：`fs.read_file_glob`、`side_effect="read"`。
- **职责**：在指定根目录下应用 glob 模式；返回绝对、解析、稳定排序的文件和目录路径；默认排除任何隐藏路径段；限制结果数量并报告截断；拒绝 glob 解析后逃出搜索根的匹配。
- **关键常量**：`TOOL_ENABLED = True`；`MAX_MATCHES = 10_000`；输入允许 `max_matches` 为 1..10000，默认 500。源码中 `MAX_MATCHES` 目前没有被 `execute()` 直接引用。

#### 类 `ReadFileGlobInput(BaseModel)` — 输入契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `root: str`：无默认；`min_length=1`、`max_length=2000`；搜索根目录；相对工作区根或工作区内的绝对路径。
- `pattern: str`：无默认；`min_length=1`、`max_length=500`；相对根的 glob，例如 `*.py`、`tests/**/*.py`、`**/*.md`；`**` 才递归跨目录，单个 `*` 不跨目录边界。
- `include_hidden: bool = False`：是否包含路径中任一以点开头的段所对应的隐藏项。
- `max_matches: int = 500`：`ge=1`、`le=10000`；返回匹配数上限。
- 类内没有额外 `def`。

#### 类 `ReadFileGlobOutput(BaseModel)` — 输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `resolved_root: str`：无默认；`min_length=1`；实际搜索的绝对规范化目录。
- `pattern: str`：无默认；`min_length=1`；实际应用的 glob 模式。
- `matches: list[str]`：无默认；`max_length=10000`；匹配到的文件和目录绝对解析路径，按 `os.path.normcase` 后的字符串排序。
- `match_count: int`：无默认；`ge=0`；本次返回的匹配数。
- `truncated: bool`：无默认；匹配总数是否超过 `max_matches`。
- 类内没有额外 `def`。

#### 类 `ReadFileGlobTool(BaseTool)` — 工具实现

- `spec`：`name="fs.read_file_glob"`、`description` 说明递归 glob、返回绝对排序路径、用于按名称/扩展名发现文件、限制在搜索根和工作区、`version="1.0.0"`、输入输出模型、`side_effect="read"`、`permissions=()`、`timeout_seconds=15.0`、`idempotent=True`、`parallel_safe=True`、`max_concurrency=8`、`tags=("fs", "glob", "find", "search")`、`recommended_before_tools=("fs.read_dir",)`。
- **声明理由**：只读、可重复、并发安全；15 秒覆盖普通目录树匹配；建议先列目录帮助模型确定根，但不强制。

##### 方法 `__init__(self, base_dir: str | Path | None = None, *, allow_outside: bool | None = None) -> None`

```python
def __init__(
    self,
    base_dir: str | Path | None = None,
    *,
    allow_outside: bool | None = None,
) -> None
```

- **作用 / 参数 / 实现**：与其他 FS 工具一致；默认以 `workspace_root()` 为沙箱根，传入 `base_dir` 时将其 `resolve()`；`allow_outside` 原样存储。
- **调用关系**：`create_tool()` 无参调用。

##### 方法 `execute(self, arguments: ReadFileGlobInput) -> ReadFileGlobOutput`

```python
def execute(self, arguments: ReadFileGlobInput) -> ReadFileGlobOutput
```

- **作用**：解析根目录、执行 glob、过滤隐藏项、验证每个结果仍在根内、排序并截断。
- **参数**：`arguments` 必须是 `ReadFileGlobInput`，否则抛 `TypeError`。
- **返回**：`ReadFileGlobOutput`；`matches` 为最长 `max_matches` 个字符串，`match_count` 为返回数量，`truncated` 为过滤后总匹配数是否超过上限。
- **异常**：根越界为 `ValueError`；根不存在为 `FileNotFoundError`；不是目录为 `NotADirectoryError`；`root.glob()` 抛 `ValueError`/`NotImplementedError` 时会统一转成带模式名的 `ValueError`；有任一解析结果逃出根时最终抛 `ValueError`。
- **关键实现**：先 `list(root.glob(pattern))` 物化结果；每个 `match.resolve()` 后用 `_is_within(root, resolved)` 判定；若逃出则设置 `escaped=True` 并跳过，最后统一报错；隐藏过滤使用 `_has_hidden_segment(resolved.relative_to(root))`；排序键是 `os.path.normcase(str(item))`。源码没有把 `RuntimeError` 等 `Path.glob` 的其他潜在异常纳入该分支。

#### 函数 `_has_hidden_segment(relative: Path) -> bool`

```python
def _has_hidden_segment(relative: Path) -> bool
```

- **参数**：相对于搜索根、已经解析的路径。
- **返回**：路径任一段是否以点开头。
- **关键实现 / 调用关系**：只在 `ReadFileGlobTool.execute()` 的隐藏项过滤分支调用；`include_hidden=True` 时不会调用该过滤。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`ReadFileGlobTool()`。

### `tool/fs_read_text.py` — 有界读取工作区文本文件

- **拓扑层**：L4 ｜ **依赖**：`pathlib.Path`、`pydantic`、`core.BaseTool`/`ToolSpec`、`._shared.DEFAULT_MAX_OUTPUT_CHARS`/`cap_text`/`read_text_file`/`resolve_path`/`workspace_root` ｜ **被依赖**：`core.discovery` 扫描并注册 `fs.read_text`。
- **规模**：150 行 / 3 个 def。
- **工具名 / 副作用**：`fs.read_text`、`side_effect="read"`。
- **职责**：把一个工作区文本文件解码为模型可读的字符串；默认 UTF-8，也允许调用方显式指定编码；通过 32 MiB 文件上限、可配置字符上限和截断标志控制上下文；拒绝二进制或非法编码内容。
- **关键常量**：`TOOL_ENABLED = True`；默认最大返回字符数来自 `_shared.DEFAULT_MAX_OUTPUT_CHARS = 200_000`；共享层最大文件字节数为 32 MiB。

#### 类 `ReadTextInput(BaseModel)` — 输入契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `path: str`：无默认；`min_length=1`、`max_length=2000`；待读文本文件路径；相对路径锚定工作区根，绝对路径必须留在工作区内。
- `encoding: str = "utf-8"`：`min_length=1`、`max_length=32`；文本编码，例如 `utf-8`、`utf-16`、`gbk`、`latin-1`，必须与文件内容匹配。
- `max_chars: int | None = None`：`ge=1`、`le=2_000_000`；返回的最大字符数；`None` 表示使用工具默认 200,000；文件更大时在该边界截断。
- 类内没有额外 `def`。

#### 类 `ReadTextOutput(BaseModel)` — 输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `resolved_path: str`：无默认；`min_length=1`；实际读取的绝对规范化路径。
- `content: str`：无默认；`max_length=2_001_000`；解码后的文本；文件大于 `max_chars` 时截断。由于 `cap_text()` 会额外追加后缀，源码把上限留出了余量。
- `character_count: int`：无默认；`ge=0`；**完整解码文件的字符数，不是截断后长度**。
- `truncated: bool`：无默认；内容是否因 `max_chars` 被截断。
- `encoding: str`：无默认；`min_length=1`；实际解码使用的规范 codec 名称。
- 类内没有额外 `def`。

#### 类 `ReadTextTool(BaseTool)` — 工具实现

- `spec`：`name="fs.read_text"`、`description` 强调读取工作区文本、不能读二进制、不会写入、常用于编辑前检查、`version="1.0.0"`、输入输出模型、`side_effect="read"`、`permissions=()`、`timeout_seconds=15.0`、`idempotent=True`、`parallel_safe=True`、`max_concurrency=8`、`tags=("fs", "file", "read", "text")`、`recommended_before_tools=("fs.read_dir", "fs.read_file_glob")`。
- **声明理由**：只读且可重复；文件系统读取可并行，8 为保守并发上限；推荐先列目录或 glob 以便定位文件，但这是提示元数据，不是强制依赖。

##### 方法 `__init__(self, base_dir: str | Path | None = None, *, allow_outside: bool | None = None) -> None`

```python
def __init__(
    self,
    base_dir: str | Path | None = None,
    *,
    allow_outside: bool | None = None,
) -> None
```

- **作用**：注入沙箱根和越界开关。
- **参数**：`base_dir` 为自定义根；`allow_outside` 为是否允许越界，`None` 时由 `_shared` 读取环境开关。
- **关键实现**：`self.base_dir` 在传入时为 `Path(base_dir).resolve()`，否则为 `workspace_root()`；`self.allow_outside` 原样保存。**源码未校验自定义 `base_dir` 与 `workspace_root()` 的关系**。

##### 方法 `execute(self, arguments: ReadTextInput) -> ReadTextOutput`

```python
def execute(self, arguments: ReadTextInput) -> ReadTextOutput
```

- **作用**：解析路径、读取并截断文本，返回规范化结果。
- **参数**：`arguments` 必须是 `ReadTextInput`，但方法会先做 `isinstance` 检查，不满足则抛 `TypeError`。
- **返回**：`ReadTextOutput`；`content` 为 `cap_text(text, max_chars)` 的结果，`character_count` 为完整文本长度。
- **副作用**：无写入；仅读取文件和元数据。
- **异常**：路径越界/空路径/编码非法为 `ValueError`；不存在为 `FileNotFoundError`；不是普通文件为 `IsADirectoryError`；超大文件、二进制、解码失败由 `read_text_file()` 抛 `ValueError`。
- **调用关系**：`resolve_path()` → `path.exists()`/`is_file()` → `read_text_file()` → `cap_text()`；配置默认值来自共享模块。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`ReadTextTool()`。
- **调用关系**：发现器调用，未传显式构造参数，因此使用默认工作区根和环境越界开关。

### `tool/fs_write_text.py` — 原子写入完整文本文件

- **拓扑层**：L4 ｜ **依赖**：`pathlib.Path`、`pydantic`、`core.BaseTool`/`ToolSpec`、`_shared.atomic_write_text`/`normalize_encoding`/`resolve_path`/`workspace_root` ｜ **被依赖**：`core.discovery` 扫描并注册 `fs.write_text`。
- **规模**：156 行 / 3 个 def。
- **工具名 / 副作用**：`fs.write_text`、`side_effect="write"`。
- **职责**：创建一个文件或替换其全部文本内容；使用同目录临时文件、`fsync` 和 `os.replace`，使并发读取者不会看到半写状态；默认自动创建缺失父目录并允许覆盖；通过 `overwrite=False` 提供创建专用语义。
- **关键常量**：`TOOL_ENABLED = True`；`MAX_CONTENT_CHARS = 1_000_000`。字段实际使用字面量 `1_000_000`，源码中该常量目前没有被直接引用。

#### 类 `WriteTextInput(BaseModel)` — 输入契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `path: str`：无默认；`min_length=1`、`max_length=2000`；待写路径；相对工作区根或工作区内的绝对路径。
- `content: str`：无默认；`min_length=0`、`max_length=1_000_000`；完整替换内容，允许空字符串。
- `encoding: str = "utf-8"`：`min_length=1`、`max_length=32`；编码内容的文本编码。
- `create_parents: bool = True`：父目录缺失时是否自动创建。
- `overwrite: bool = True`：目标已存在时是否覆盖；`False` 表示仅创建，存在即报错。
- 类内没有额外 `def`。

#### 类 `WriteTextOutput(BaseModel)` — 输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `resolved_path: str`：无默认；`min_length=1`；已写入的绝对规范化路径。
- `existed: bool`：无默认；写入前目标是否存在。
- `created: bool`：无默认；是否由本次写入创建（等于 `not existed`）。
- `bytes_written: int`：无默认；`ge=0`；实际编码后的字节数。
- `character_count: int`：无默认；`ge=0`；写入文本的字符数。
- `encoding: str`：无默认；`min_length=1`；实际使用的规范 codec 名称。
- 类内没有额外 `def`。

#### 类 `WriteTextTool(BaseTool)` — 工具实现

- `spec`：`name="fs.write_text"`、`description` 说明完整写入/覆盖、原子性、沙箱路径、与 `fs.edit_text` 的分工、`version="1.1.0"`、输入输出模型、`side_effect="write"`、`permissions=()`、`timeout_seconds=15.0`、`idempotent=True`、`parallel_safe=False`、`max_concurrency=1`、`tags=("fs", "file", "write", "text", "create")`、`recommended_before_tools=("fs.read_text", "fs.read_dir")`。
- **声明理由**：相同参数重复写入同一完整内容会得到相同最终内容，所以 `idempotent=True`；但同一路径并发写入是“后写覆盖先写”，会丢更新，因此 `parallel_safe=False`，实际并发被限制到 1；15 秒覆盖本地编码和原子替换；建议先读原文件/列目录，但不强制。

##### 方法 `__init__(self, base_dir: str | Path | None = None, *, allow_outside: bool | None = None) -> None`

```python
def __init__(
    self,
    base_dir: str | Path | None = None,
    *,
    allow_outside: bool | None = None,
) -> None
```

- **作用 / 参数 / 实现**：注入沙箱根和越界开关；与只读 FS 工具相同，默认 `workspace_root()`。源码没有在此处做额外权限检查。

##### 方法 `execute(self, arguments: WriteTextInput) -> WriteTextOutput`

```python
def execute(self, arguments: WriteTextInput) -> WriteTextOutput
```

- **作用**：验证参数，解析并检查目标，执行原子写入并返回写后统计。
- **参数**：`arguments` 必须是 `WriteTextInput`，否则抛 `TypeError`。
- **返回**：`WriteTextOutput`；其中 `existed` 在写入前采样，`created=not existed`，`bytes_written` 来自 `atomic_write_text()`，`character_count=len(arguments.content)`，`encoding=normalize_encoding(arguments.encoding)`。
- **副作用**：写入或替换文件，可能创建父目录和临时文件；该工具的 `side_effect="write"` 使 Runtime 要求副作用确认。
- **异常**：越界/编码非法为 `ValueError`；目标已存在且 `overwrite=False` 为 `FileExistsError`；父目录缺失、父路径不是目录、目标是目录、编码失败由 `atomic_write_text()` 抛出相应异常。
- **关键实现 / 竞态边界**：`existed = path.exists()` 与后续 `os.replace()` 不是同一个原子检查；并发外部进程可能在两者之间创建/删除文件。源码没有在 `overwrite=False` 时使用独占创建来封闭该竞态。工具自身通过 `parallel_safe=False`/`max_concurrency=1` 限制自身并发，但不控制其他进程。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`WriteTextTool()`。

### `tool/search.py` — AnySearch 网页搜索工具

- **拓扑层**：L4 ｜ **依赖**：标准库 `json`、`math`、`os`、`dataclasses.replace`、`typing`、`urllib.error.HTTPError`、`urllib.parse.urlencode`/`urlsplit`/`urlunsplit`、`urllib.request.Request`/`urlopen`；`pydantic`；`core.BaseTool`/`ToolSpec`；`core.parser.reject_json_constant` ｜ **被依赖**：`core.discovery` 扫描并注册 `web.search`；`tool.__init__` 通过惰性属性导出搜索模型和工具。
- **规模**：441 行 / 17 个 def。
- **工具名 / 副作用**：`web.search`、`side_effect="read"`。这里的 read 指不写本地状态/不执行写操作，但会向外部 AnySearch 发出 HTTP 网络请求。
- **职责**：把严格的搜索输入转换为 AnySearch `POST /v1/search` 请求；通过 Bearer API Key 认证；对 HTTP 响应做大小限制、UTF-8/JSON 校验、错误码检查和字段归一；只向模型返回标题、URL、摘要的稳定子集；兼容旧版返回结构和极窄的 501 GET 回退。
- **关键常量**：`TOOL_ENABLED = True`；构造函数默认及 Spec 默认超时为 30 秒；响应体最大 2,000,000 字节；结果条数最多 10。

#### 类 `SearchInput(BaseModel)` — 搜索输入契约

- `model_config = ConfigDict(extra="forbid", strict=True, populate_by_name=True)`；严格禁止额外字段，同时允许按字段名填充 `max_results`。
- `query: str`：无默认；`min_length=1`、`max_length=500`；搜索查询；校验器额外拒绝纯空白。
- `max_results: int = 10`：`ge=1`、`le=10`；`validation_alias=AliasChoices("max_results", "limit")`；`serialization_alias="max_results"`；返回条数上限，兼容旧输入名 `limit`，但序列化仍使用提供方要求的 `max_results`。
- `tag: str | None = None`：`min_length=1`、`max_length=100`；可选 AnySearch capability tag；普通网页搜索应留空；校验器会把空值/`None`/`"null"` 归一为空，并丢弃误填的工具名别名 `web`、`web.search`、`web__search`。
- `zone: Literal["cn", "intl"] | None = None`：可选搜索区域。
- `language: str | None = None`：`min_length=1`、`max_length=32`；首选语言；校验器把空值、`None`、`"None"`、`"null"` 规整为缺席。
- `params: Annotated[dict[str, Any] | None, WithJsonSchema({...})] = None`：提供方特定参数的 JSON 对象；源码用 `WithJsonSchema` 暴露 `object additionalProperties: false` 或 `null`，避免严格函数 Schema 反对任意键；Pydantic 仍按字典校验。before 校验器仅额外接受 JSON 编码的字典字符串，以及空/`None`/`null` 空值；任意字符串保持原值，最终由严格校验拒绝。
- `format: Literal["json", "markdown"] | None = None`：可选结果格式；外层 API 响应仍是 JSON，源码说明该字段只传给提供方。
- `limit` 是 property，不是字段，作为旧参数名的读取兼容视图。

##### 方法 `normalize_tag(cls, value: Any) -> Any`

```python
@field_validator("tag", mode="before")
@classmethod
def normalize_tag(cls, value: Any) -> Any
```

- **参数**：`value` 为原始输入。
- **返回**：经 `_normalize_nullable_text()` 处理后，若为 `None` 则返回 `None`；若 `casefold()` 命中工具名别名集合则返回 `None`；否则返回去空白后的字符串。
- **关键实现 / 调用关系**：用于兼容 OpenAI 兼容模型输出的 `""` 或 `"None"`，同时避免把工具名误当 capability tag；只在 `SearchInput` 校验链调用。

##### 方法 `normalize_language(cls, value: Any) -> Any`

```python
@field_validator("language", mode="before")
@classmethod
def normalize_language(cls, value: Any) -> Any
```

- **参数 / 返回**：原始值交给 `_normalize_nullable_text()`；空字符串和 null 类文本归一为 `None`，非字符串原样返回。
- **关键实现 / 调用关系**：只接受空值归一，不执行工具名别名过滤；由 Pydantic 语言字段校验链调用。

##### 方法 `normalize_provider_params(cls, value: Any) -> Any`

```python
@field_validator("params", mode="before")
@classmethod
def normalize_provider_params(cls, value: Any) -> Any
```

- **参数 / 返回**：`None` 或非字符串原样返回；字符串去空白后为空或 `"none"`/`"null"` 时返回 `None`；否则尝试 `json.loads`，仅当解析结果是 `dict` 时返回该字典，其他情况返回原字符串。
- **关键实现 / 调用关系**：兼容弱工具调用客户端把对象序列化成字符串的情况；不会接受任意 JSON 字符串，最终仍由 Pydantic 严格类型检验拒绝非字典。

##### 方法 `reject_blank_text(cls, value: str | None) -> str | None`

```python
@field_validator("query")
@classmethod
def reject_blank_text(cls, value: str | None) -> str | None
```

- **参数 / 返回**：`query` 值；非 `None` 且 `strip()` 为空时抛 `ValueError("text values must contain a non-whitespace character")`，否则返回原值。
- **关键实现 / 调用关系**：补充 JSON Schema 长度无法表达的空白规则。

##### property `limit(self) -> int`

```python
@property
def limit(self) -> int
```

- **参数**：无（`self` 为已校验输入）。
- **返回**：`self.max_results`。
- **关键实现 / 调用关系**：只读旧参数名视图，不产生额外字段；旧调用方迁移时读取该属性。

#### 类 `SearchItem(BaseModel)` — 单条搜索结果的稳定子集

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `title: str = ""`：`max_length=1000`；标题。
- `url: str = ""`：`max_length=2000`；URL。
- `snippet: str = ""`：`max_length=5000`；摘要/描述。
- 类内没有额外 `def`。

#### 类 `SearchOutput(BaseModel)` — 搜索输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `items: list[SearchItem]`：无默认；归一化后的搜索结果列表。

##### property `results(self) -> list[SearchItem]`

```python
@property
def results(self) -> list[SearchItem]
```

- **参数**：无。
- **返回**：`self.items`，与提供方术语 `results` 对齐的只读取视图。
- **关键实现 / 调用关系**：不改变输出字段；供兼容调用方读取。

#### 类 `SearchTool(BaseTool)` — AnySearch 工具实现

- `spec`：`name="web.search"`、`description` 说明搜索公开网页并返回标题/URL/摘要，`version="1.1"`、输入输出为 `SearchInput`/`SearchOutput`、`side_effect="read"`、`permissions=()`、`timeout_seconds=30.0`、`idempotent=True`、`parallel_safe=True`、`max_concurrency=8`、`tags=("web", "search")`、`recommended_before_tools=("system.current_time",)`。
- **声明理由**：不写本地状态，也不修改外部服务状态，因此标 read；重复执行无副作用，故 `idempotent=True`（当前网页内容可能变化，不承诺结果完全相同）；每次调用使用独立 urllib 请求，未发现共享可变状态，允许并发到 8；建议需要时效性时先取当前时间，但这是提示而非依赖；权限元数据为空，当前公开工具部署不做权限过滤。

##### 方法 `__init__(self, base_url: str | None = None, api_key: str | None = None, *, timeout: float = 30.0) -> None`

```python
def __init__(
    self,
    base_url: str | None = None,
    api_key: str | None = None,
    *,
    timeout: float = 30.0,
) -> None
```

- **作用**：在构造阶段读取配置、验证 URL/API Key/超时并生成实例级 `spec`。
- **参数**：`base_url`、`api_key` 可显式注入；`timeout` 为正有限秒数。
- **关键实现**：任一关键参数为 `None` 时调用 `_load_dotenv()`；`base_url` 依次读取 `SEARCH_BASE_URL`、`ANYSEARCH_BASE_URL`；`api_key` 依次读取 `SEARCH_API`、`SEARCH_API_KEY`、`ANYSEARCH_API_KEY`；字符串去空白；非字符串 `base_url`/`api_key` 抛 `TypeError`；空 `base_url` 抛 `ValueError`；`timeout` 为 bool、非数值、非有限或 `<=0` 时抛 `ValueError`；最后用 `dataclasses.replace(type(self).spec, timeout_seconds=self.timeout)` 把执行截止时间和 urllib 超时对齐。
- **副作用 / 调用关系**：仅构造配置，不发网络请求；`create_tool()` 无参构造时自动读 `.env` 和环境变量。

##### 方法 `execute(self, arguments: SearchInput) -> SearchOutput`

```python
def execute(self, arguments: SearchInput) -> SearchOutput
```

- **作用**：构建并发送搜索请求，处理响应后归一化为 `SearchOutput`。
- **参数**：`arguments` 必须是 `SearchInput`，否则抛 `TypeError`。
- **返回**：`SearchOutput(items=...)`。
- **副作用**：向配置的 AnySearch 地址发送带 Bearer API Key 的 HTTP 请求；不写本地状态。
- **异常**：未配置 URL/Key 抛 `RuntimeError`；请求体不可 JSON 序列化抛 `ValueError`；HTTP 错误除 501 回退外由 `urlopen` 抛 `HTTPError`；网络错误由 urllib 传播；非 UTF-8、非法 JSON、NaN/Infinity 统一抛 `ValueError("search response was not valid UTF-8 JSON")`；响应结构不合法抛 `TypeError`/`RuntimeError`。
- **请求构造流程**：调用 `_search_endpoint(self.base_url)`；请求字典先放 `query` 和 `max_results`，再按 `tag`、`zone`、`language`、`params`、`format` 顺序加入非 `None` 字段；用 `json.dumps(ensure_ascii=False, separators=(",", ":"), allow_nan=False)` 编码为 UTF-8；创建 `POST` `Request`，包含 `Authorization: Bearer <api_key>`、`Content-Type: application/json`、`Accept: application/json`。
- **超时 / 回退**：`urlopen(request, timeout=self.timeout)`；若捕获到 `HTTPError` 且状态码恰为 501，关闭原异常并改用 `_legacy_search_endpoint()` 生成 GET 请求，同样使用 Bearer 和相同超时；其他 HTTP 错误不转换。
- **响应处理**：`_read_response_body()` 后调用 `json.loads(raw_body.decode("utf-8"), parse_constant=reject_json_constant)`；`reject_json_constant` 对 `NaN`/`Infinity` 抛 `ValueError`，该异常与 `UnicodeDecodeError` 一起归一；最后 `_normalize_response(payload, arguments.max_results)`。
- **调用关系**：构造函数准备配置；模块私有函数负责端点、旧端点、响应体读取、响应归一和文本转换。

#### 函数 `_load_dotenv() -> None`

```python
def _load_dotenv() -> None
```

- **参数**：无。
- **返回**：无。
- **关键实现**：尝试 `from dotenv import load_dotenv`；导入失败时静默返回，环境变量仍可用；成功则 `load_dotenv(override=False)`。
- **调用关系**：`SearchTool.__init__()` 在配置缺省时调用。

#### 函数 `_first_env(*names: str) -> str | None`

```python
def _first_env(*names: str) -> str | None
```

- **参数**：按优先级排列的环境变量名。
- **返回**：第一个值存在且去空白后非空的环境变量原值；都未命中时返回 `None`。
- **关键实现 / 调用关系**：由构造函数用于 URL 和 API Key 的别名读取。

#### 函数 `_normalize_nullable_text(value: Any) -> Any`

```python
def _normalize_nullable_text(value: Any) -> Any
```

- **参数**：任意原始值。
- **返回**：`None` 或非字符串原样返回；字符串去空白后为空或大小写后等于 `none`/`null` 时返回 `None`；否则返回去空白字符串。
- **关键实现 / 调用关系**：`normalize_tag` 与 `normalize_language` 共用。

#### 函数 `_search_endpoint(base_url: str) -> str`

```python
def _search_endpoint(base_url: str) -> str
```

- **参数**：基础 URL 字符串。
- **返回**：规范化后的搜索端点。
- **关键实现**：`urlsplit` 后要求 scheme 为 `http`/`https` 且 netloc 非空，否则抛 `ValueError("SEARCH_BASE_URL must be an absolute HTTP(S) URL")`；路径去尾部 `/`；空路径补 `/v1/search`；以 `/v1` 结尾则追加 `/search`；用 `urlunsplit` 生成结果并移除 fragment。源码没有清空已有 query。
- **调用关系**：`SearchTool.execute()` 普通 POST 路径调用。

#### 函数 `_legacy_search_endpoint(endpoint: str, arguments: SearchInput) -> str`

```python
def _legacy_search_endpoint(endpoint: str, arguments: SearchInput) -> str
```

- **参数**：已规范化的搜索端点和一个 `SearchInput`。
- **返回**：在原有查询串后追加 `q=<query>&limit=<max_results>` 的 URL。
- **关键实现 / 调用关系**：只用于 501 兼容回退；使用 `urlencode`，保留原 query，移除 fragment。

#### 函数 `_read_response_body(response: Any) -> bytes`

```python
def _read_response_body(response: Any) -> bytes
```

- **参数**：具有 `headers` 和 `read()` 的 urllib 兼容响应对象。
- **返回**：最多 2,000,000 字节的响应体。
- **关键实现**：读取 `Content-Length`（大小写两种键）；非整数或负值抛 `TypeError("invalid Content-Length header")`；大于 2,000,000 抛 `ValueError("search response is too large")`；优先 `response.read(2_000_001)`；若测试替身只支持无参 `read()`，回退为 `response.read()`，此时若没有有效的 `Content-Length`，源码没有其他硬性上限；最后再检查长度。
- **调用关系**：`SearchTool.execute()` 正常 POST 和 501 GET 两条路径共用。

#### 函数 `_normalize_response(payload: Any, max_results: int) -> SearchOutput`

```python
def _normalize_response(payload: Any, max_results: int) -> SearchOutput
```

- **参数**：已解析的 JSON `payload` 和最大结果数。
- **返回**：`SearchOutput`，其中条数不超过 `max_results`。
- **关键实现**：要求顶层是 dict；`code` 非空时必须是整数（bool 也拒绝），非 0 抛通用 `RuntimeError("AnySearch search request failed")`；`data` 若不是 dict 且非 `None` 抛 `TypeError`；当 `data is None` 且顶层没有 `items`/`results` 时抛 `TypeError`；优先从 `data["results"]` 取列表，否则取顶层 `items`/`results`；结果不是 list 抛 `TypeError`；对 `raw_items[:max_results]` 逐项处理，非 dict 项静默跳过；`title` 取 `title`，`url` 取 `url` 或 `link`，`snippet` 取 `snippet` 或 `description`，经 `_text()` 转换后分别截断到 1000/2000/5000 字符。
- **调用关系**：`SearchTool.execute()` 在 JSON 解析成功后调用。
- **边界**：若 `data` 是 dict 但缺少 `results`，源码使用默认空列表，不会报缺字段错误；这在“结果修剪/容错”中有意宽松处理。

#### 函数 `_text(value: Any) -> str`

```python
def _text(value: Any) -> str
```

- **参数**：任意响应字段值。
- **返回**：`None` 返回空字符串，否则 `str(value)`。
- **关键实现 / 调用关系**：`_normalize_response()` 使用，保证非字符串字段也能参与统一截断。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`SearchTool()`；未配置参数时由构造函数加载 `.env` 和环境变量。

### `tool/tool_template.py` — 单文件工具模板与示例实现

- **拓扑层**：L4 ｜ **依赖**：`pydantic` 的 `BaseModel`/`ConfigDict`/`Field`/`field_validator`、`core.BaseTool`/`ToolSpec` ｜ **被依赖**：复制该模板创建新工具的开发者；模块本身由发现器导入但保持关闭。
- **规模**：252 行 / 3 个 def。
- **工具名 / 副作用**：示例声明 `template.text_statistics`、`side_effect="read"`；因 `TOOL_ENABLED = False`，不会在自动发现中注册。
- **职责**：用完整、可运行的代码展示 Input/Output 严格模型、字段约束、ToolSpec、同步 `execute()`、零参数 `create_tool()` 和模块级开关；文档字符串同时是工具生成规范。示例逻辑只统计 Unicode 字符数和按空白分隔的词数。
- **关键常量**：`TOOL_ENABLED = False`。源码要求复制后按需求改成固定 `True` 或环境开关；不把模板自身当作业务工具。

#### 类 `ExampleInput(BaseModel)` — 输入契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `text: str`：无默认；`min_length=1`、`max_length=10_000`；需要统计的 UTF-8 文本，必须至少含一个非空白字符。
- 类方法 `reject_blank_text` 是额外领域校验，拒绝仅由空白组成的 `text`。

##### 方法 `reject_blank_text(cls, value: str) -> str`

```python
@field_validator("text")
@classmethod
def reject_blank_text(cls, value: str) -> str
```

- **参数**：`value` 为已通过字符串类型和长度约束的文本。
- **返回**：原值；若 `value.strip()` 为空则抛 `ValueError("text must not be blank")`。
- **调用关系**：由 Pydantic 字段校验链调用，表达 JSON Schema 长度无法覆盖的领域规则。

#### 类 `ExampleOutput(BaseModel)` — 输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `character_count: int`：无默认；`ge=0`；Python `len()` 计算的 Unicode 字符数。
- `word_count: int`：无默认；`ge=0`；按连续空白分隔后的词语数量。
- 类内没有额外 `def`。

#### 类 `ExampleTool(BaseTool)` — 示例工具实现

- 类属性 `spec`：`name="template.text_statistics"`、`description` 说明精确计数、`version="1.0.0"`、输入输出模型分别为 `ExampleInput`/`ExampleOutput`、`side_effect="read"`、`permissions=()`、`timeout_seconds=5.0`、`idempotent=True`、`parallel_safe=True`、`max_concurrency=8`、`tags=("text", "statistics", "count")`；`recommended_before_tools` 未显式传入，使用 `ToolSpec` 默认空元组。
- **声明理由**：纯本地统计且不写任何状态，所以是 read、可重复、可并发；示例客户端无速率限制，给 8 的并发上限；5 秒足够纯 CPU 文本统计。

##### 方法 `execute(self, arguments: ExampleInput) -> ExampleOutput`

```python
def execute(self, arguments: ExampleInput) -> ExampleOutput
```

- **作用**：接收已验证参数，返回严格 Output。
- **参数**：`arguments` 应为 `ExampleInput`；方法体不再做 `isinstance` 检查。
- **返回**：`ExampleOutput(character_count=len(arguments.text), word_count=len(arguments.text.split()))`。
- **副作用**：无。**异常**：若 Runtime 绕过校验传入错误对象，属性访问可能抛 `AttributeError`；源码未在此处显式归一化。
- **调用关系**：Runtime 在工具调用时调用。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`ExampleTool()`；源码注释给出未来在工厂/构造器读取配置、创建客户端和注入依赖的示例。
- **关键实现**：工厂只创建实例，不调用 `execute()`；当前返回值虽然标注为 `BaseTool`，实际类型是 `ExampleTool`。

### `tool/update_log.py` — 追加一条结构化项目更新日志

- **拓扑层**：L4 ｜ **依赖**：`platform`、`pydantic`、`core.BaseTool`/`ToolSpec`、`core.update_log.UpdateLogRepository` ｜ **被依赖**：`core.discovery` 扫描并注册 `system.update_log`；`read_update_log.py` 复用其 `UpdateLogFileChange` 模型。
- **规模**：159 行 / 3 个 def。
- **工具名 / 副作用**：`system.update_log`、`side_effect="write"`。
- **职责**：将一次项目修改的结构化事实写入 SQLite `update_logs` 表一行；只返回新的 `update_id`、`next_update_id`、UTC 时间戳和系统名，不把历史日志读入模型上下文。
- **关键常量**：`TOOL_ENABLED = True`；仓库默认路径由 `core.update_log` 根据 `UPDATE_LOG_DB_PATH` 或 `constants.DEFAULT_UPDATE_LOG_FILENAME` 决定，本模块没有定义路径常量。

#### 类 `UpdateLogFileChange(BaseModel)` — 单个文件变更

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `path: str`：无默认；`min_length=1`、`max_length=500`；项目相对文件路径。
- `action: str`：无默认；`min_length=1`、`max_length=32`；说明为 `added`、`modified`、`deleted`、`renamed` 或 `generated`，源码没有 `Literal` 硬约束。
- `description: str`：无默认；`min_length=1`、`max_length=2000`；该文件的具体变化。
- 类内没有额外 `def`。

#### 类 `UpdateLogInput(BaseModel)` — 更新日志输入契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `executor: str`：无默认；`min_length=1`、`max_length=200`；执行修改的 AI/模型或人员。
- `update_type: str`：无默认；`min_length=1`、`max_length=64`；变更类别，如 feature、fix、refactor、config、docs。
- `title: str`：无默认；`min_length=1`、`max_length=300`；简短标题。
- `task_background: str`：无默认；`min_length=1`、`max_length=4000`；需求背景和修改目标。
- `update_details: str`：无默认；`min_length=1`、`max_length=12000`；具体实现细节和关键决策。
- `added_features: str`：无默认；`min_length=1`、`max_length=6000`；新增能力，无新增时写 `none`。
- `files: list[UpdateLogFileChange]`：无默认；`min_length=1`、`max_length=100`；每个新增、修改、删除或重命名的项目文件。
- `behavior_impact: str`：无默认；`min_length=1`、`max_length=6000`；兼容性、API、配置、数据、部署或用户影响。
- `validation: str`：无默认；`min_length=1`、`max_length=6000`；实际运行的测试/检查及真实结果。
- `risks: str`：无默认；`min_length=1`、`max_length=4000`；已知风险和回滚说明，无则 `none`。
- `follow_up: str`：无默认；`min_length=1`、`max_length=4000`；后续工作，无则 `none`。
- 类内没有额外 `def`。

#### 类 `UpdateLogOutput(BaseModel)` — 更新日志输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `update_id: int`：无默认；`ge=1`；本次分配的更新 ID。
- `next_update_id: int`：无默认；`ge=1`；应写入导引索引的下一次 ID，等于 `update_id + 1`。
- `timestamp: str`：无默认；`min_length=1`；实际 UTC 写入时间。
- `system_name: str`：无默认；`min_length=1`；检测到的操作系统名。
- `recorded: bool = True`：默认 `True`；仓库返回字典没有该键，因此由 Pydantic 默认填充。
- 类内没有额外 `def`。

#### 类 `UpdateLogTool(BaseTool)` — 工具实现

- `spec`：`name="system.update_log"`、`description` 强调每次项目修改后强制执行、只写记录、不读历史、`version="1.0"`、输入输出模型、`side_effect="write"`、`permissions=()`、`timeout_seconds=10.0`、`idempotent=False`、`parallel_safe=False`、`max_concurrency=1`、`tags=("update-log", "audit", "project", "mandatory")`；`recommended_before_tools` 未显式传入，使用默认空元组。
- **声明理由**：重复调用会产生多条新记录，不是幂等操作，因此 `idempotent=False`；为避免并发追加造成顺序/索引协议混乱，限制为单调用层；写入有副作用，需要运行时确认；权限字段为空是当前公开部署的审计元数据，不是授权过滤器。

##### 方法 `__init__(self, repository: UpdateLogRepository | None = None) -> None`

```python
def __init__(self, repository: UpdateLogRepository | None = None) -> None
```

- **作用**：注入仓储；未注入时创建默认 `UpdateLogRepository()`。
- **参数**：`repository` 为可选仓储实例。
- **返回**：构造函数无返回值。
- **关键实现 / 调用关系**：默认仓储会读取环境变量或默认 SQLite 文件；测试可注入轻量替身。

##### 方法 `execute(self, arguments: UpdateLogInput) -> UpdateLogOutput`

```python
def execute(self, arguments: UpdateLogInput) -> UpdateLogOutput
```

- **作用**：把已验证的结构化参数追加到更新日志并返回紧凑确认。
- **参数**：`arguments` 必须是 `UpdateLogInput`，否则抛 `TypeError`。
- **返回**：`UpdateLogOutput(**result)`；`files` 先通过 `item.model_dump()` 转为字典列表，`system_name=platform.system()`。
- **副作用**：向 SQLite `update_logs` 插入一行；`side_effect="write"` 触发 Runtime 确认流程。
- **异常**：仓储校验失败抛 `ValueError`/`TypeError`；SQLite 打开、写入等错误由仓储传播。
- **调用关系**：`UpdateLogRepository.append()` 是实际持久化入口；工具本身不读取历史记录。

##### update_log 协议（写前读索引、返回 next_update_id）

- SQLite 仓储的 `append()` 使用自增主键插入一行，返回 `update_id`、`timestamp`、`system_name` 和 `next_update_id = update_id + 1`；仓储在连接内使用锁、事务和 `busy_timeout`。
- 项目交付协议要求调用方在此之前读取 `update_log_readme_first.md` 的“已写入”和“下一次必须写入”索引，确认预期编号；这一步由工作流/技能完成，**本工具源码中未见读取该导引文件的实现**。
- 工具成功后，调用方用返回的 `next_update_id` 精确更新导引文件的两行索引；这次索引更新属于同一次记录任务，不再调用 `system.update_log` 产生第二条日志。
- 工具仅返回紧凑确认，历史正文留在 SQLite，避免把完整历史加载进模型上下文；审计改用单条或范围读取工具。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`UpdateLogTool()`，默认创建 `UpdateLogRepository`。

## L5 Agent 基类与记忆总协调器（agent / manager / 更新日志读取工具）

### `agents/agent.py` — 通用 Agent：provider 解析、历史管理、工具注册与 native 工具循环

- **拓扑层**：L5（源码导入图中 Agent 依赖 L1/L2 的 provider、message 与 LLM 层，并调用 L4 `core` 工具运行时）｜ **依赖**：`constants` 的默认值与历史压缩参数；`core` 的 `ExecutionContext`、`ToolRegistry`、`ToolCatalogTool`、`SkillRegistry`、`SkillCatalogTool`、`ToolExecutionManager`、`ToolLoop`、`ToolResult`、`ToolCall`、`ToolSpecRepository`、发现函数和解析函数；`core.activity_log`；`.llm`；`.message_utils`；`.providers` ｜ **被依赖**：`agents.react`、`agents.__init__`；上层 `web.support` 和测试通过包门面或直接路径使用。
- **规模**：1167 行 / 48 个 def（含嵌套函数）
- **职责**：实现 provider profile 到具体 LLM 客户端的解析与缓存，按 profile/model 隔离并压缩历史，维护工具/技能注册表和 hot-reload 提示词分区，生成 OpenAI strict function schema。`run_with_tools` 负责 native function-calling 的完整“模型请求 → 解析 tool_calls → 批量执行 → Observation 回灌”循环，最终所有工具执行都委派给 `core.runtime.ToolExecutionManager`。
- **关键模块级对象**：
  - `Agent.UNBOUNDED_ROUND_SAFETY_LIMIT = ToolLoop.DEFAULT_SAFETY_LIMIT`：未显式给 `max_rounds` 时的安全上限（实际常量来自 `constants.TOOL_LOOP_SAFETY_LIMIT`）。
  - `Agent.TOOL_MODE_PROTOCOLS`：`native_strict`/`native_loose` → `native`，`text_react` → `react`，`none` → `None`。
  - `agent = Agent`：保留旧公共名称，等价于类对象。
  - `_strict_function_schema`：将对象根 schema 转为 OpenAI strict function 形状。
  - `compress_saved_history`、`trim_saved_history`、`_observation_stub`：历史压缩和裁剪工具函数。
  - 模块还导入 `DEFAULT_MAX_RETRIES`、`DEFAULT_SKILLS_ROOT`、温度/超时、历史/observation 阈值和 `PROMPT_CACHE_KEY_VERSION`，不在本文件重新定义。

#### 类 `Agent(ABC)` — 通用 Agent 壳与 native 工具协议实现

- **构造**：`__init__(self, name: str, *, llm: LLM | None = None, provider_config: str | None = None, provider_registry: ProviderRegistry | None = None, repository: ToolSpecRepository | None = None, auto_discover_tools: bool = True, tool_package: str | ModuleType = "tool", discovery_strict: bool = False, auto_discover_skills: bool = True, skills_root: str = DEFAULT_SKILLS_ROOT) -> None`。
  - `name`：必须是非空字符串，成为 agent 名称。
  - `llm`：可选注入的 LLM/兼容客户端；存在时优先于 profile 解析。
  - `provider_config`：可选 TOML 路径，仅在未注入 `provider_registry` 时传给 `ProviderRegistry`。
  - `provider_registry`：可选现成注册表；传入时必须类型正确。
  - `repository`：可选 `ToolSpecRepository`，用于保存 schema 和提供 catalog-only 工具清单。
  - `auto_discover_tools`：是否在构造末尾扫描工具包。
  - `tool_package`：工具包模块名或 `ModuleType`，默认 `"tool"`。
  - `discovery_strict`：发现技能/工具时是否使用严格模式。
  - `auto_discover_skills`：是否自动扫描技能目录。
  - `skills_root`：技能根目录非空字符串。
- **关键属性**：
  - `name`、`llm`、`provider_registry`、`active_profile`：agent 身份和 provider 选择状态。
  - `_profile_clients: dict[tuple[str, str], LLM]`：按 `(profile, model)` 缓存客户端。
  - `_profile_histories: dict[str, list[dict[str, Any]]]`：按 profile/history key 隔离历史。
  - `repository`、`tool_package`、`tools`、`catalog_tool`：工具 schema 存储、包配置、运行时注册表和 catalog 工具。
  - `tool_discovery_report`、`skills_root`、`skills`、`skill_catalog_tool`、`skill_discovery_report`：技能注册和发现报告。
  - `execution_manager = ToolExecutionManager(self.tools)`：native/ReAct 共用的批次执行器。
  - `global_model`、`role`、`prompt`、`history`、`max_retries`：兼容旧调用方的状态与默认提示词存储。
  - `_frozen_manifest`、`_hot_tools`、`cache_epoch`：prompt 前缀冻结清单、hot-zone 工具指纹和缓存命名空间版本。
- **方法**：
  - `default_tool_protocol(self) -> str | None` — 读取 active profile 的 `tool_mode` 并通过 `TOOL_MODE_PROTOCOLS` 映射；没有 profile 时返回 `"native"`，未知值也回退 `"native"`。只读，不校验 profile 是否仍存在。
  - `_save_history(self, history_key: str, conversation: list[dict[str, Any]]) -> None` — 先用 `compress_saved_history` 压缩超大 tool/Observation 载荷，再用 `trim_saved_history` 限制消息数，按 `history_key` 保存；同时把顶层字典复制到 `self.history`。这是 profile 历史隔离和持久化的核心写入口。
  - `_dispatch_model_call(completion_llm: Any, messages: list[dict[str, Any]], options: dict[str, Any], *, round_number: int, echo_mode: EchoMode | None = None) -> Any` — **staticmethod**；在同步线程/调用方线程中执行一次模型调用。优先使用可调用的 `complete_streaming`（若 options 中 stream 是 False），移除 `stream` 后传入回调、echo_mode 和剩余参数；否则调用 `complete(messages, **options)`；再否则兼容 `think`，只挑 `temperature/timeout/prompt_cache_key/prompt_cache_retention`，并在签名允许时加 `stream_response_bool=False`。记录 first-chunk 延迟与模型完成耗时；没有可用方法抛 `TypeError`。
  - `_dispatch_model_call._log_first_chunk() -> None` — `_dispatch_model_call` 内嵌函数；把 `time.perf_counter()` 追加到局部列表，用于在流式首 chunk 到达后记录延迟。无参数、无返回；不改 agent 状态。
  - `async run_auto(self, messages: str | list[dict[str, Any]], context: ExecutionContext | None = None, **kwargs: Any) -> str` — 根据 `default_tool_protocol()` 分派。`none` 抛 `ValueError`；`react` 动态查找 `run_with_react`，没有时抛 `TypeError`；字符串在 native 路径包装为 user message；native 路径调用 `run_with_tools`。它是“按 profile 自动选择 native/text_react”的入口。
  - `__init__(self, name: str, *, llm: LLM | None = None, provider_config: str | None = None, provider_registry: ProviderRegistry | None = None, repository: ToolSpecRepository | None = None, auto_discover_tools: bool = True, tool_package: str | ModuleType = "tool", discovery_strict: bool = False, auto_discover_skills: bool = True, skills_root: str = DEFAULT_SKILLS_ROOT) -> None` — 校验参数、建立 provider/技能/工具/历史/缓存状态；详见上面的构造说明。
  - `clear_history(self) -> None` — 清空当前 `history` 和所有 `_profile_histories`；不改变工具、provider 或缓存前缀。
  - `run(self, query: str) -> str` — **abstractmethod**；抽象入口，默认只 `raise NotImplementedError`，具体协议由子类实现。
  - `register_tool(self, tool: BaseTool, *, replace: bool = False) -> None` — 将工具加入 `ToolRegistry`，并在有 repository 时用 `replace=True` 保存 spec；registry 的重复处理受 `replace` 控制，repository 保存则始终替换同 spec。
  - `register_hot_tool(self, tool: BaseTool, *, replace: bool = False) -> None` — 立即注册可执行工具；若 frozen manifest 尚未建立则不额外处理；已冻结时用 fingerprint 判断是否真正变化，变化则写入 `_hot_tools`，与旧指纹相同则从 hot 区移除。用于避免修改缓存前缀。
  - `unregister_tool(self, name: str) -> None` — 校验非空字符串并从 runtime registry 移除；未冻结时清 hot 项；若移除的是 frozen 项则更新 manifest 并递增 `cache_epoch`；移除 hot 项不递增 epoch。源码未在此处同步 repository。
  - `_prompt_fingerprint(spec: Any) -> str` — **staticmethod**；返回 `f"{spec.schema_hash}#{spec.description}"`，把 schema 指纹和描述文本合并，以便描述变更也能被 hot-zone 识别。
  - `_sync_frozen_manifest(self) -> None` — 首次请求前捕获 frozen 工具清单；清单是 runtime 快照与 repository active names 的并集；runtime 工具有 `BaseTool` 时使用 `_prompt_fingerprint`，仅 repository 存在且 schema hash 合法时使用 `hash#description`。之后不重复重建。
  - `discover_tools(self, *, package: str | ModuleType | None = None, replace: bool = False, strict: bool = False, reload_modules: bool = False) -> ToolDiscoveryReport` — 选择显式 package 或 `self.tool_package`，调用 `core.discover_tools` 同步注册表/repository；保存并返回 `ToolDiscoveryReport`。它不自动刷新 frozen manifest。
  - `discover_skills(self, *, root: str | None = None, replace: bool = False, strict: bool = False) -> SkillDiscoveryReport` — 用显式 root 或 `self.skills_root` 调用 `core.discover_skills`；保存/返回报告。显式 root 参数不会回写 `self.skills_root`。
  - `is_tool_registered(self, name: str, *, version: str | None = None, schema_hash: str | None = None) -> bool` — 转发 `ToolRegistry.is_registered`，可按版本和 schema hash 判断注册状态。
  - `tool_registration_status(self, name: str) -> dict[str, Any]` — 转发 registry 状态查询。
  - `async execute_tool_calls(self, calls: list[ToolCall], context: ExecutionContext | None = None)` — 无显式返回注解；调用 `self.execution_manager.execute_batch(calls, context)` 并返回 `BatchToolResult`。执行上下文中的 `confirmed_side_effects` 会在 runtime 中参与副作用的确认校验。
  - `tool_definitions(self, names: list[str] | None = None) -> list[dict[str, Any]]` — 校验 names 是非空字符串列表/元组；未指定时按 registry specs 的顺序取全部；检查未知工具；调用 `_definitions_for_registrations`，只返回 OpenAI definitions，不返回 alias map。
  - `_definitions_for_registrations(self, registrations: Mapping[str, tuple[BaseTool, int]]) -> tuple[list[dict[str, Any]], dict[str, str]]` — 生成 provider definitions 和 alias-to-canonical map。排序把 catalog 工具放首位、其余按名称稳定排序，以保证请求前缀稳定；将点号替换成 `__`，拒绝超过 64 字符或映射冲突的名称；每个 function 使用 `model_description`、strict schema、`strict=True`。
  - `_openai_tool_name(name: str) -> str` — **staticmethod**；`name.replace(".", "__")`，保留 namespaced canonical ID 的同时生成 provider-safe function name。
  - `async run_with_tools(self, messages: list[dict[str, Any]], context: ExecutionContext | None = None, *, max_rounds: int | None = None, model: str | None = None, temperature: float = DEFAULT_TEMPERATURE, timeout: float = DEFAULT_TIMEOUT, tool_names: list[str] | None = None, profile_name: str | None = None, provider_name: str | None = None, use_history: bool = True, defer_tool_loading: bool = False, prompt_cache_key: str | None = None, prompt_cache_retention: str | None = None, enable_prompt_cache: bool = True, stream_echo: bool = False) -> str` — 实现 native function-calling 工具循环。参数中 `messages` 是本次输入消息；`context` 提供执行上下文/确认状态；`max_rounds` 控制循环上限；`model/profile_name/provider_name` 选择目标；`tool_names` 限定暴露工具；`use_history` 决定是否拼接 profile 历史；`defer_tool_loading` 初始只暴露 catalog；`prompt_cache_*` 和 `enable_prompt_cache` 控制缓存字段；`stream_echo` 选择 content 流回显。主要步骤：
    1. 校验 max_rounds、messages、context、布尔参数和 cache 参数；以 `stream_echo` 决定 `echo_mode="content"`。
    2. 通过 `_completion_target` 解析客户端、模型和 history key；有历史时复制 profile 历史，否则使用 configured prompt，再追加本次消息。
    3. 取初始 registry 快照；在计算默认 cache key 前调用 `_sync_frozen_manifest`。显式 `tool_names` 时校验并去重，`defer_tool_loading=True` 时初始只暴露 catalog，否则暴露全部工具。
    4. 每轮生成当前 registrations 和 strict tool definitions；由 `asyncio.to_thread(self._dispatch_model_call, ...)` 发起流式优先的模型调用；请求前通过 `_with_registered_tool_names` 注入“所有已注册工具名”和技能目录系统消息。
    5. 解析 choices/message；把 assistant message 追加到会话。没有 native calls 时保存历史并返回 `content or ""`。
    6. 有 native calls 时，将 provider 别名映回 canonical 名；缺 id 时生成 `native-call-N`；用 `parse_openai_tool_calls` 逐项解析。解析失败根据是否“已注册但未暴露”标记 `TOOL_NOT_EXPOSED`，否则 `INVALID_TOOL_CALL`，并形成失败 `ToolResult`；合法调用送入一次 `execute_tool_calls` 批量执行。
    7. 按原调用顺序追加 role=`tool` 消息和 `_result_json(result)`；注意 tool 消息的 `tool_call_id` 直接取原始 native call 的 `id`，若 provider 原本省略 id 则这里仍可能是 `None`，而解析/错误路径内部使用了 fallback id。defer 模式下成功 catalog 结果会更新 `loaded_order`。
    8. 达到 ToolLoop 上限仍未结束则抛 `RuntimeError("maximum tool-call rounds exceeded")`。整个循环会真实执行工具、写入历史和活动日志。
  - `_load_catalog_result(self, result: ToolResult, loaded_order: list[str], context: ExecutionContext) -> None` — 只处理成功且 tool_name 为 catalog 的结果；从 `result.data["specs"]` 及兼容字段 `spec` 中提取候选，解析成功后将 runtime registry 中存在的工具名追加到 `loaded_order`。`context` 参数保留但不参与逻辑。
  - `_configured_prompt_messages(self) -> list[dict[str, Any]]` — 按固定顺序把 `self.prompt` 中 system/user/assistant 转成消息；若存在 `"tool"` prompt，则追加 role=`system`、`content=f"Tool-use instructions: ..."`。没有 prompt 时返回空列表。
  - `_with_registered_skill_names(self) -> dict[str, str] | None` — 没有技能时返回 None；否则构造稳定的技能目录 system message，只列名称、版本、描述和 triggers，并明确要求先调用 `system.skill_catalog` 的 `view`，不嵌入技能正文。
  - `_with_registered_tool_names(self, conversation: list[dict[str, Any]]) -> list[dict[str, Any]]` — 把 runtime 与 repository active names 的排序清单作为一条 system message，再附加可选技能目录，最后拼接输入 conversation；返回新列表，不修改原 conversation。
  - `_validate_prompt_cache_key(value: str | None) -> str | None` — **staticmethod**；None 原样返回；否则要求非空字符串、原始长度不超过 64，返回 strip 后值；失败抛 `ValueError`。
  - `_validate_prompt_cache_retention(value: str | None) -> str | None` — **staticmethod**；只允许 `in_memory`、`24h` 或 None；失败抛 `ValueError`。
  - `_default_prompt_cache_key(self, provider_key: str, model: str | None, *, mode: str) -> str` — 生成稳定 prompt prefix 的 cache routing key。材料包含版本、provider、model、mode、configured prompt、工具名、技能 `(name, version, content_hash)` 和 `cache_epoch`；若已有 frozen manifest，工具名改用 manifest 集合，避免 hot reload 改变 key；ReAct mode 还纳入三个协议常量。材料按 JSON sort_keys 序列化后取 SHA-256 前 48 位，并以 `PROMPT_CACHE_KEY_VERSION-` 为前缀。不含用户文本、tool result 或 hot schema 正文。
  - `profiles(self) -> Mapping[str, ProviderProfile]` — **property**；返回 `provider_registry.profiles` 的只读映射。
  - `set_active_profile(self, profile_name: str) -> None` — 通过 registry 校验并解析 profile，再同步 agent 与 registry 的 active profile；不清理已有客户端或历史。
  - `reload_provider_profiles(self) -> None` — 重新加载 TOML，更新 active profile，并清空 `_profile_clients` 以便 URL/key 变更在下一次请求生效；保留 profile history。
  - `profile_info(self, profile_name: str | None = None) -> dict[str, Any]` — 指定名称或使用 active profile，返回脱敏后的 `public_info`。
  - `list_profiles(self) -> list[dict[str, Any]]` — 按 registry 映射顺序返回所有 profile 的脱敏信息。
  - `add_provider(self, provider_name: str, api_key: str, base_url: str, default_model: str) -> None` — 旧接口兼容；调用 `register_ephemeral`，不探测 `/models`，不写 TOML。
  - `detect_models(self, base_url: str, api_key: str) -> list[str]` — 旧兼容接口；延迟导入 OpenAI SDK，调用 `models.list()`，要求响应 `data` 是 list/tuple，过滤空 id 并按出现顺序去重；网络/SDK 异常向外传播。
  - `_completion_target(self, profile_name: str | None, model: str | None, *, provider_name: str | None = None) -> tuple[Any, str | None, str]` — provider/模型解析的实际实现。校验 profile/provider 名称和 model；若同时传入二者必须相同；未显式给 model 时解析 `global_model`，当它是 `provider:model` 且 provider 已配置时分别作为 provider/model，否则 whole string 作为 model。未选 provider 且已有注入 `self.llm` 时返回 `(self.llm, selected_model, "__injected__")`，不再做 profile 校验；否则选择 active profile、套默认模型、确认模型在 profile.models 中，按 `(profile, model)` 缓存或创建 `LLM`，返回 `(client, selected_model, provider)`。项目没有 `_resolve_llm` 方法，此函数承担其职责。
  - `set_system_prompt(self, prompt: str) -> None` — 调用 `_set_prompt("system", prompt)`。
  - `set_user_prompt(self, prompt: str) -> None` — 调用 `_set_prompt("user", prompt)`。
  - `set_assistant_prompt(self, prompt: str) -> None` — 调用 `_set_prompt("assistant", prompt)`。
  - `set_tool_prompt(self, prompt: str) -> None` — 调用 `_set_prompt("tool", prompt)`。
  - `_set_prompt(self, role: str, prompt: str) -> None` — 要求 prompt 是非空字符串并写入 `self.prompt[role]`；它不校验 role 是否属于预定义四类，公开 setter 才限定 role。
  - `set_global_model(self, model: str | None) -> None` — 旧兼容 setter；None 或非空字符串才有效，写入 `global_model`。推荐调用方改用 `set_active_profile` 和 `model=`。

#### 函数 `_strict_function_schema(schema: dict[str, Any]) -> dict[str, Any]` — 转换 OpenAI strict function schema

- **参数 / 返回**：输入 schema 必须是根 `type="object"` 且 `properties` 为 dict；返回深复制后的 strict schema。输入对象不被修改。
- **关键实现 / 调用关系**：递归删除所有层级的 `default`；所有对象节点强制 `additionalProperties=False`，并已有 properties 时把其全部键放入 `required`。若原对象显式声明了非 False 的 `additionalProperties`，抛 `ValueError`。`Agent._definitions_for_registrations` 和 ReAct 的 `_strict_react_schema` 使用它。
- **嵌套函数**：`normalize(value: Any) -> None` — 递归遍历 list 和 dict；对象节点做上述规范化，非对象节点仅继续下降；无返回值。
- **副作用 / 异常**：只改深复制值；根结构不合规或存在任意 object keys 时抛 `ValueError`。

#### 函数 `compress_saved_history(conversation: list[dict[str, Any]]) -> list[dict[str, Any]]` — 将历史中的超大工具载荷替换为 stub

- **参数 / 返回**：`conversation` 是消息列表；返回逐项顶层复制后的新列表。
- **关键实现 / 调用关系**：role=`user` 且 content 以 `"Observation: "` 开头、长度超过 `OBSERVATION_COMPRESS_THRESHOLD` 且尚未是 stub 时，只把 Observation 后 payload 转成 `_observation_stub`；role=`tool` 且 content 超过阈值时直接把 content 转成 stub；其余消息浅复制。由 `Agent._save_history` 在落历史前调用。
- **副作用 / 异常**：不修改输入列表；普通消息的嵌套对象仍与原对象共享引用。

#### 函数 `trim_saved_history(conversation: list[dict[str, Any]], *, max_messages: int = HISTORY_MAX_MESSAGES) -> list[dict[str, Any]]` — 裁剪历史但保留前导 system 块

- **参数 / 返回**：`max_messages` 是 rest 部分最大消息数，默认 60；返回裁剪后的消息列表。
- **关键实现 / 调用关系**：若 `max_messages < 1` 或总长度不超限，直接返回原对象；否则从开头连续收集 role=`system` 作为 leading，其余作为 rest。若 rest 未超限仍返回原对象；否则保留 rest 最后 `max_messages` 条，并删除开头孤立的 `"Observation: "` user 消息，返回 leading+kept。
- **副作用 / 异常**：正常裁剪时复制字典；提前返回时可能返回同一 list 对象。leading system 不计入 `max_messages`，因此结果总条数可能大于该值。

#### 函数 `_observation_stub(payload: str) -> str` — 生成被压缩 Observation/tool payload 的说明文本

- **参数 / 返回**：`payload` 是原始工具结果文本；返回包含 `OBSERVATION_STUB_PREFIX`、原字符数、前 `OBSERVATION_PREVIEW_CHARS` 字符预览和“重新查询”提示的单行字符串。
- **关键实现 / 调用关系**：只被 `compress_saved_history` 调用；预览不尝试按 Unicode 边界或换行整理。
- **副作用 / 异常**：无。

---

### `memory/manager.py` — 四层记忆总协调器

- **拓扑层 / 依赖 / 被依赖**：最高层协调器；依赖 `base`、`embedding`、`storage`、`types`。被包门面、RAG 管线/图检索和外部 Web/Agent 工具依赖。
- **规模**：173 行 / 13 个 def。
- **职责**：持有四层 `BaseMemory`、共享文档库、向量库、图存储和嵌入服务，统一暴露 `add/search/list/get/delete/clear/close`，并提供跨层联合检索、过期清理和状态统计。启动时仅为 `InMemoryVectorStore` 从文档库重建索引；远端/持久化向量库已有数据时不重复 upsert。上下文管理器在退出时释放资源。
- **关键模块级对象/常量**：类属性无显式常量；类内别名 `remember = add`、`store = add`、`remove = delete`、`retrieve = get`、`query = search`；管理器持有 `working`、`episodic`、`semantic`、`perceptual` 和以 `MemoryType` 为键的 `memories`。

#### 类 `MemoryManager` — 四层记忆总协调器

- **构造**：`def __init__(self, config: MemoryConfig | None = None, *, embedding: BaseEmbedding | None = None, embedding_service: BaseEmbedding | None = None, document_store: BaseDocumentStore | None = None, vector_store: BaseVectorStore | None = None, graph_store: Neo4jGraphStore | None = None) -> None`。
  - `config` 为空时创建 `MemoryConfig()`；同时传 `embedding` 和 `embedding_service` 抛 `ValueError`，否则采用 embedding、embedding_service 或 `make_default_embedding(config)`。
  - `document_store` 为空时创建 `SQLiteDocumentStore(config.sqlite_path)`。
  - `vector_store` 优先使用注入值；否则 `config.qdrant_url` 非空时创建 `QdrantVectorStore(url=..., collection_name=..., dimension=embedding_dimension)`；否则创建 `InMemoryVectorStore`。
  - `graph_store` 优先使用注入值；否则创建 `Neo4jGraphStore(config.neo4j_uri, config.neo4j_username, config.neo4j_password)`，URI 为空时该对象回退内存图。
  - 把 document、vector、embedding、config 组成 `common`，构造 `WorkingMemory`、`EpisodicMemory`、`SemanticMemory`、`PerceptualMemory`，并建立 `memories` 映射。
  - **启动重建条件**：仅当 `isinstance(self.vector_store, InMemoryVectorStore)`，遍历 `self.document_store.list()`（默认排除过期）并 `upsert` 到向量库。源码没有重建前调用 `clear()`，因此注入一个原本已有内容的内存向量库时会保留未对应文档的陈旧向量；这是源码中可见的边界行为。
- **关键属性**：`config`、`embedding`、`document_store`、`vector_store`、`graph_store`、`working`、`episodic`、`semantic`、`perceptual`、`memories`。
- **方法**：
  - `for_type(self, memory_type: MemoryType | str) -> BaseMemory` — 把字符串/枚举转成 `MemoryType` 并查 `self.memories`；未知类型或非法枚举捕获 `ValueError`/`KeyError` 后抛统一的 `ValueError("unknown memory type: ...")`；返回层对象。
  - `add(self, content: str, *, memory_type: MemoryType | str = MemoryType.WORKING, **kwargs: Any) -> MemoryItem` — 路由到 `for_type(memory_type).add(content, **kwargs)`；默认写 working；返回层写入结果，层内校验/存储异常原样传播。
  - `get(self, item_id: str, *, memory_type: MemoryType | str | None = None) -> MemoryItem | None` — 指定 `memory_type` 时委托该层 `get`；未指定时直接查文档库，若 item 存在且 `is_expired`，调用 `self.delete(item_id)` 并返回 None，否则返回 item。未指定类型时按 ID 全局查找，不做类型过滤（除过期处理）；过期删除通过 item 的 `memory_type` 路由。
  - `delete(self, item_id: str, *, memory_type: MemoryType | str | None = None) -> bool` — 指定类型时委托该层 `delete`；未指定时先查文档，找不到返回 `False`，否则按 `item.memory_type` 路由到具体层删除。语义层删除会继续清理图边；返回层删除结果。
  - `search(self, query: str, *, memory_type: MemoryType | str | None = None, limit: int | None = None, threshold: float | None = None, metadata: Mapping[str, Any] | None = None) -> list[MemorySearchResult]` — 指定类型时直接委托该层；未指定时先解析并校验 limit（默认 `config.search_limit`，正整数），然后对四层逐一调用 `search(query, limit=limit, threshold=threshold, metadata=metadata)`，把各层结果合并，按 `(-score, item.created_at)` 排序，即分数降序、同分按创建时间升序，最后截取 `limit`。没有跨层分数加权或归一化；各层分别独立嵌入并检索。异常来自各层嵌入/向量操作。
  - `cleanup_expired(self) -> int` — 列出文档库 `include_expired=True` 的所有 item，对 `is_expired` 的项调用 `self.delete(item.id)` 并累计成功数；返回清除数量。会按层清理对应的向量和图关系（若层删除实现如此），异常向上抛。
  - `list(self, *, memory_type: MemoryType | str | None = None, include_expired: bool = False) -> list[MemoryItem]` — 指定类型时委托层 `list`；否则直接查共享文档库，默认排除过期；返回统一文档视图，不合并向量/图数据。
  - `clear(self, *, memory_type: MemoryType | str | None = None) -> int` — 指定类型时委托层 `clear`；否则对四层 `clear` 求和并返回总数。逐层清空文档和向量，semantic 层当前 `clear` 继承 `BaseMemory.clear`，源码没有在此额外清理全部图边。
  - `stats(self) -> dict[str, int]` — 返回四个 `MemoryType.value` 到 `len(document_store.list(memory_type=...))` 的字典；默认排除过期项，因此统计的是非过期文档记录数，不是向量点数或图边数。
  - `close(self) -> None` — 无条件先 `document_store.close()`；再通过 `getattr` 尝试调用 vector_store 的 `close`（`InMemoryVectorStore` 和 `QdrantVectorStore` 源码中都没有 `close`，因此通常不执行）；最后同样尝试调用 graph_store 的 `close`。返回 `None`；存储 close 异常向上抛；没有幂等标记。
  - `__enter__(self) -> Self` — 返回 `self`，用于 `with MemoryManager(...) as manager:`；无副作用。
  - `__exit__(self, exc_type: type[BaseException] | None, exc_value: BaseException | None, traceback: TracebackType | None) -> None` — 调用 `self.close()`；不吞异常，返回 `None`；参数仅满足上下文管理器协议，源码未根据异常类型做差异化处理。

---

##### 覆盖清单

以下 `def` 数量由 AST 对目标文件逐个统计；别名不是新的函数定义，不重复计数。

| 文件 | 行数 | def 数量 | 覆盖状态 |
|---|---:|---:|---|
| `memory/__init__.py` | 85 | 0 | 已覆盖聚合门面、再导出清单与 32 项 `__all__` |
| `memory/base.py` | 501 | 23 | 已覆盖全部函数、dataclass 字段/校验、属性、classmethod 与 `BaseMemory` 方法 |
| `memory/embedding.py` | 317 | 17 | 已覆盖抽象接口、两种嵌入实现、批处理、请求与响应解析 |
| `memory/ids.py` | 81 | 6 | 已覆盖私有清理、规范化、实体/关系/谓词/旧版 ID |
| `memory/storage/__init__.py` | 22 | 0 | 已覆盖存储聚合导出，并说明无 `BaseGraphStore` |
| `memory/storage/document.py` | 176 | 18 | 已覆盖抽象文档接口、SQLite 连接/建表/CRUD/解码/关闭 |
| `memory/storage/vector.py` | 73 | 10 | 已覆盖抽象向量接口、余弦相似度和内存实现 |
| `memory/storage/qdrant.py` | 108 | 7 | 已覆盖 Qdrant 懒建 collection、upsert/delete/search/clear 与点 ID |
| `memory/storage/graph.py` | 185 | 6 | 已覆盖 Neo4j 及本地回退的增删查、按 memory_id 删除和 close |
| `memory/types/__init__.py` | 8 | 0 | 已覆盖四层实现聚合导出 |
| `memory/types/working.py` | 58 | 6 | 已覆盖 TTL、键值接口、读取回退和容量淘汰 |
| `memory/types/episodic.py` | 29 | 2 | 已覆盖事件写入和时间窗口排序 |
| `memory/types/semantic.py` | 153 | 6 | 已覆盖事实主键、旧 ID 回退、图边、删除、查询和 supersede 元数据边界 |
| `memory/types/perceptual.py` | 27 | 2 | 已覆盖模态校验、文本/content 派生和 payload 读取 |
| `memory/manager.py` | 173 | 13 | 已覆盖分层路由、联合检索、索引重建、过期清理、统计、关闭与上下文管理 |
| **合计** | **1,784** | **116** | **全部目标文件已覆盖** |

##### 不确定点与源码边界摘要

1. `memory` 目标文件没有 Pydantic 模型/`field_validator`；本文按 dataclass `__post_init__` 记录校验行为。
2. `SemanticMemory` 本身没有 `supersede()`/`retract()` 方法，退役策略由 RAG 知识物化层通过 metadata 协议完成。
3. `SemanticMemory.add_fact` 更新已有事实时不再次调用 `graph_store.add_relation`，源码中未见对已有图边的属性更新路径。
4. `WorkingMemory.get_by_key` 直接按 key 查文档时发现过期项会返回并通过列表扫描继续，不会像 `get` 一样先删除该过期记录。
5. `InMemoryVectorStore.upsert` 对 `embedding=None` 直接返回，未删除旧向量；`QdrantVectorStore.upsert` 同样静默跳过无向量 item。
6. `MemoryManager.__init__` 重建内存索引前没有 `vector_store.clear()`，注入已有内容的内存索引时可能留下陈旧条目。
7. `MemoryManager.clear()` 未显式清空 semantic 的全部图关系；`BaseMemory.clear()` 只清当前层文档/向量。
8. `MemoryConfig.__post_init__` 未显式校验 `qdrant_url`、Neo4j 三个连接字符串和 `sqlite_path` 的类型/取值。
9. `HashEmbedding` 与 `APIEmbedding` 的向量空间不兼容；切换嵌入后端应重新索引，源码仅以注释说明该边界。
10. `_extract_embedding_vectors` 对显式 `index/text_index` 的 `int(...)` 转换未捕获异常，异常类型可能直接暴露给调用方。
11. `BaseMemory.add` 对 `ttl_seconds` 未显式检查 `NaN/inf`，有限性边界依赖后续 `timedelta`/`datetime` 的异常行为。

### `tool/read_update_log.py` — 按 ID 读取一条更新日志

- **拓扑层**：L5 ｜ **依赖**：`pydantic`、`core.BaseTool`/`ToolSpec`、`core.update_log.UpdateLogRepository`、`tool.update_log.UpdateLogFileChange` ｜ **被依赖**：`core.discovery` 扫描并注册 `system.read_update_log`；`read_update_logs.py` 复用其输出模型。
- **规模**：119 行 / 3 个 def。
- **工具名 / 副作用**：`system.read_update_log`、`side_effect="read"`。
- **职责**：按正整数 ID 查询一条不可变日志并解码；同时返回当前最高 ID，便于调用方从 1 顺序审计到最新；不批量返回历史记录。
- **关键常量**：`TOOL_ENABLED = True`；没有定义数据库路径常量。

#### 类 `ReadUpdateLogInput(BaseModel)` — 输入契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `update_id: int`：无默认；`ge=1`；要读取的单条日志 ID。
- 类内没有额外 `def`。

#### 类 `ReadUpdateLogOutput(BaseModel)` — 输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `update_id: int`：无默认；`ge=1`；存储的更新 ID。
- `timestamp: str`：无默认；`min_length=1`、`max_length=64`；UTC 写入时间。
- `system_name: str`：无默认；`min_length=1`、`max_length=200`；记录的系统名。
- `executor: str`：无默认；`min_length=1`、`max_length=200`；执行修改的 AI/模型或人员。
- `update_type: str`：无默认；`min_length=1`、`max_length=64`；变更类别。
- `title: str`：无默认；`min_length=1`、`max_length=300`；更新标题。
- `task_background: str`：无默认；`min_length=1`、`max_length=4000`；背景和目标。
- `update_details: str`：无默认；`min_length=1`、`max_length=12000`；实现细节和决策。
- `added_features: str`：无默认；`min_length=1`、`max_length=6000`；新增能力或 `none`。
- `files: list[UpdateLogFileChange]`：无默认；`min_length=1`、`max_length=100`；受影响文件。
- `behavior_impact: str`：无默认；`min_length=1`、`max_length=6000`；兼容性和用户影响。
- `validation: str`：无默认；`min_length=1`、`max_length=6000`；实际检查及结果。
- `risks: str`：无默认；`min_length=1`、`max_length=4000`；风险和回滚说明。
- `follow_up: str`：无默认；`min_length=1`、`max_length=4000`；后续工作。
- `latest_update_id: int`：无默认；`ge=0`；当前最高存储 ID；审计完整历史时从 1 逐条读到该值。
- 类内没有额外 `def`。

#### 类 `ReadUpdateLogTool(BaseTool)` — 工具实现

- `spec`：`name="system.read_update_log"`、`description` 说明按 ID 取一条完整记录、只读、不批量、结果附当前最高 ID、`version="1.0"`、输入输出模型、`side_effect="read"`、`permissions=()`、`timeout_seconds=5.0`、`idempotent=True`、`parallel_safe=True`、`max_concurrency=8`、`tags=("update-log", "audit", "read", "project")`；`recommended_before_tools` 未显式传入，使用默认空元组。
- **声明理由**：纯读取、可重复、可并发；5 秒覆盖单条 SQLite 查询；最高 ID 是帮助审计顺序的元数据，不改变只读语义。

##### 方法 `__init__(self, repository: UpdateLogRepository | None = None) -> None`

```python
def __init__(self, repository: UpdateLogRepository | None = None) -> None
```

- **作用 / 参数 / 实现**：与更新写工具相同，注入或创建仓储；不查询数据。

##### 方法 `execute(self, arguments: ReadUpdateLogInput) -> ReadUpdateLogOutput`

```python
def execute(self, arguments: ReadUpdateLogInput) -> ReadUpdateLogOutput
```

- **作用**：查询指定 ID，补充最高 ID，并校验为稳定输出。
- **参数**：`arguments` 必须是 `ReadUpdateLogInput`，否则抛 `TypeError`。
- **返回**：`ReadUpdateLogOutput(**record)`。
- **副作用**：只读；例外是 `record["latest_update_id"] = self.repository.latest_id()` 会修改仓储本次返回的临时字典，不影响数据库。
- **异常**：记录不存在抛 `LookupError`；仓储校验或 SQL 错误由仓储传播；输出字段不符合模型时由 Pydantic 抛校验错误。
- **调用关系**：`repository.get()` → `repository.latest_id()` → 输出模型；`ReadUpdateLogOutput` 又被 `read_update_logs.py` 复用。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`ReadUpdateLogTool()`。

## L6 协议与知识层（ReAct / 图检索 / 知识抽取 / 星云图映射）

### `agents/react.py` — 文本 ReAct 协议 Agent：提示词、解析容错、Observation 回灌与批量工具执行

- **拓扑层**：L6（依赖 L5 `Agent` 和 L2 `LLM.EchoMode`，并调用同一 `core` 工具运行时）｜ **依赖**：`constants` 的重试上限/默认值；`core` 的 `ExecutionContext`、`ToolCall`、`ToolError`、`ToolLoop`、`ToolRegistry`、`ToolResult`、`ToolSpecRepository`、`discover_tools`、`loads_model_json`、`parse_openai_tool_calls`、`BaseTool`；`core.activity_log`；`.agent` 的 `Agent` 与消息/错误转换辅助；`.llm.EchoMode` ｜ **被依赖**：`agents.__init__`；`web.support` 和测试直接导入 `ReActAgent`/`parse_react_response`。
- **规模**：1545 行 / 33 个 def
- **职责**：为只返回文本的 provider 实现 Thought/Action/Action Input/Observation/Final Answer 协议。它复用 `Agent` 的 provider、注册表、历史和工具执行能力，把模型文本解析成工具调用，经 `execute_tool_calls` 执行后再把 `ToolResult` 序列化为 Observation 回灌；同时兼容 provider 偶尔返回 native tool calls 的情况。
- **关键模块级对象**：
  - `_REACT_MARKER_LOOKAHEAD`：Thought/Action/Action Input/Observation/Final Answer 及中文别名、工具标记的 lookahead 片段。
  - `_FINAL_RE`：跨行提取 Final Answer 主体，支持粗体、全角和半角冒号。
  - `_ACTION_RE`：提取单行 Action/行动/工具。
  - `_ACTION_INPUT_RE`：提取 Action Input/行动输入/参数/输入的多行 JSON 载荷。
  - `_XML_ANSWER_RE`：匹配完整 `<answer>...</answer>`。
  - `_EXPLICIT_FINAL_RE`：判断响应是否显式写出 Final Answer 标记。
  - `ParsedReActResponse`：不可变解析结果 dataclass。
  - `ReActAgent.REACT_INSTRUCTIONS`：基础协议说明和示例。
  - `ReActAgent.CATALOG_FIRST_REACT_INSTRUCTIONS`：要求先经 catalog resolve schema 的惰性模式说明。
  - `ReActAgent.LAZY_REACT_INSTRUCTIONS`：上述常量的旧公共别名。
  - `ReActAgent.SKILL_VIEW_REACT_INSTRUCTIONS`：技能内容须通过 `system.skill_catalog` 的 view 获取。
  - `ReActAgent.UNBOUNDED_ROUND_SAFETY_LIMIT`：与父类相同的工具循环安全上限。
  - 文件末尾 `ReactAgent`、`ReAct`、`React` 均是 `ReActAgent` 别名，`react` 同样指向类。

#### 类 `ParsedReActResponse` — 一次 ReAct 文本响应的安全解析结果

- **构造**：dataclass 字段依次为 `raw: str`、`thought: str | None = None`、`final_answer: str | None = None`、`action: str | None = None`、`arguments: dict[str, Any] | None = None`、`error: str | None = None`。`arguments=None` 表示缺失或解析失败，解析器不会执行工具。
- **关键属性**：`raw` 是原文；`thought` 是提取出的思考；`final_answer` 是最终答案；`action` 是工具名；`arguments` 是 JSON object；`error` 是给模型的纠正信息。
- **方法**：
  - `is_final(self) -> bool` — **property**；返回 `final_answer is not None`。空字符串答案若被解析成非 None，也视为 final。
  - `has_action(self) -> bool` — **property**；返回 `action is not None`；即使 Action Input 解析有 error，也可能同时为 True。

#### 函数 `parse_react_response(text: str) -> ParsedReActResponse` — 容错解析文本 ReAct 协议

- **参数 / 返回**：`text` 必须是字符串，否则抛 `TypeError`；返回 `ParsedReActResponse`。解析只做形状转换，不执行工具。
- **关键实现 / 调用关系**：
  1. 先用 `_FINAL_RE` 搜索 Final Answer；找到后返回 final，分组为空时取匹配结束位置后的尾部文本。该分支优先于 Action。
  2. 全文恰好是 `<answer>...</answer>` 时返回其中非空内容；空标签返回 error；`<answer>`、`<final_answer>`、`[answer]` 等单独占位符返回 error。
  3. 没有 Action 标记时，把 strip 后普通文本视为 final answer，这是无标记 provider 的兼容回退。
  4. 有 Action 时先清理反引号；空 Action 返回 error。随后必须在 Action 之后找到 Action Input，否则返回“必须提供 JSON object”的 error。
  5. Action Input 先经 `_strip_code_fence`，空载荷返回 error，再经 `_decode_action_input` 解码；只有对象成功时返回 `arguments`。
- **副作用 / 异常**：无副作用；输入非字符串抛 `TypeError`。支持英文/中文 marker、粗体、全角冒号、代码围栏、对象外散文和单引号修正是通过正则与 `loads_model_json` 共同实现的。

#### 函数 `_extract_thought(text: str) -> str | None` — 提取 Thought/思考/想法

- **参数 / 返回**：扫描行首 marker，值延伸到下一个 REACT marker 或文本末尾；strip 后为空返回 None。
- **关键实现 / 调用关系**：`parse_react_response` 在所有分支调用；使用 lookahead，目标内容由 `re.DOTALL` 跨行。
- **副作用 / 异常**：无。

#### 函数 `_decode_action_input(payload: str) -> tuple[dict[str, Any] | None, str | None]` — 解码 Action Input

- **参数 / 返回**：成功返回 `(dict, None)`；失败返回 `(None, 纠正消息)`。
- **关键实现 / 调用关系**：调用 `core.parser.loads_model_json(payload, object_only=True)`，会依次尝试原载荷、首个平衡 `{...}` 块和单引号字面量修复。捕获 `TypeError`/`ValueError` 后返回面向模型的中文/英文错误提示，要求双引号键且 JSON 后不能追加解释。它由 `parse_react_response` 调用。
- **副作用 / 异常**：无；所有解码失败都转为返回值中的 error，不向调用方抛出。

#### 函数 `_strip_code_fence(value: str) -> str` — 去除 Action Input 外围 Markdown 代码围栏

- **参数 / 返回**：按行 strip；首行以 ``` 开头时移除，尾行恰好为 ``` 时再移除；返回重新 join 并 strip 的文本。
- **关键实现 / 调用关系**：只处理最外层围栏，不解析语言名，也不校验围栏配对。`parse_react_response` 在解码前调用。
- **副作用 / 异常**：无。

#### 函数 `_coerce_string_scalars(arguments: dict[str, Any], schema: Mapping[str, Any] | None) -> dict[str, Any]` — 按 schema 把字符串参数轻量转成声明类型

- **参数 / 返回**：返回原地修改后的 `arguments`，便于文本协议 provider 发来的 `"3"` 等字符串通过严格 Pydantic 校验。
- **关键实现 / 调用关系**：只遍历顶层参数；只处理字符串值；从 schema `properties[key]` 经 `_declared_schema_types` 获取声明类型。声明含 `integer` 时先试 `int`，含 `number` 时试 `float`，含 `boolean` 且文本为 `true/false`（大小写不敏感）时转换；转换失败或未声明类型时保持原字符串，让正式校验给出错误。不递归数组/嵌套对象。
- **副作用 / 异常**：直接修改传入字典；转换异常在局部捕获，不向调用方抛出。`_execute_action` 在构造 `ToolCall` 前调用。

#### 函数 `_declared_schema_types(subschema: Mapping[str, Any]) -> frozenset[str]` — 提取 schema 声明类型

- **参数 / 返回**：支持 `type` 为字符串、`type` 为字符串列表/元组，以及 `anyOf` 中各项的字符串 `type`；返回去重后的 frozenset，无法识别返回空集合。
- **关键实现 / 调用关系**：只服务 `_coerce_string_scalars`，不递归 `oneOf`、`allOf` 或嵌套属性。
- **副作用 / 异常**：无。

#### 函数 `_run_sync(coro: Any) -> Any` — 从同步上下文运行协程

- **参数 / 返回**：`coro` 是协程对象；用只含一个 worker 的 `ThreadPoolExecutor` 提交 `asyncio.run(coro)` 并等待 `.result()`，返回协程结果。
- **关键实现 / 调用关系**：`ReActAgent.run` 使用它，因此即使调用线程已有 event loop 也不需要直接嵌套 `asyncio.run`。异常原样传播；线程池在调用结束后关闭。
- **副作用 / 异常**：创建并关闭 worker 线程；协程异常、取消或 executor 异常会传播。

#### 类 `ReActAgent(Agent)` — 文本 ReAct 协议的 Agent 变体

- **构造**：`__init__(self, name: str, *, llm: Any | None = None, provider_config: str | None = None, provider_registry: Any | None = None, repository: ToolSpecRepository | None = None, auto_discover_tools: bool = True, tool_package: str | Any = "tool", discovery_strict: bool = False, lazy_tools: bool = False) -> None`。
  - `name`：传给父类，必须非空。
  - `llm`：可选注入客户端。
  - `provider_config`、`provider_registry`：provider 配置/注册表。
  - `repository`：工具 catalog 存储；lazy 模式下若未提供且要求自动发现，会新建 `ToolSpecRepository`。
  - `auto_discover_tools`：是否在构造尾扫描工具。
  - `tool_package`：工具包名或模块对象。
  - `discovery_strict`：发现严格模式。
  - `lazy_tools`：是否只持久化工具契约、延迟实例化。
- **关键属性**：继承父类全部属性；新增/覆盖 `lazy_tools`；`catalog_tool.repository_only` 在 lazy 模式下设为 True，使 catalog 后续检索仍基于完整 repository；父类构造时 `auto_discover_tools=False`，所以 ReAct 自己控制发现时机。该构造函数没有暴露 `auto_discover_skills` 或 `skills_root` 参数，技能发现沿用父类默认值。
- **方法**：
  - `discover_tools(self, *, package: str | Any | None = None, replace: bool = False, strict: bool = False, reload_modules: bool = False):` — 无返回注解。非 lazy 直接调用 `super().discover_tools`。lazy 时要求 repository，使用临时 `ToolRegistry` 调 `discover_tool_modules(..., metadata_only=True)` 持久化元数据，不保留实例；把报告写入 `self.tool_discovery_report` 并返回，临时 registry 随后丢弃。
  - `register_tool(self, tool: BaseTool, *, replace: bool = False) -> None` — 非 lazy 转发父类。lazy 时要求 `BaseTool` 和 repository，按 `type(tool).__module__:type(tool).__qualname__` 生成 implementation ref，保存 spec 并记录注册日志；不把实例放进 runtime registry。
  - `is_tool_registered(self, name: str, *, version: str | None = None, schema_hash: str | None = None) -> bool` — lazy 模式下 catalog 工具仍查父类；其他工具从 repository 查版本/哈希；repository 缺失返回 False。它不验证实现类是否可导入。
  - `tool_registration_status(self, name: str) -> dict[str, Any]` — non-lazy 或 catalog 工具用父类；lazy 且 repository 有记录时返回 name、registered、version、schema_hash、runtime generation（未加载为 None）和 implementation ref；无记录则回退父类。
  - `tool_confirmation_key(self, name: str) -> str` — 已加载工具返回 `ToolRegistry.confirmation_key`；未加载工具从 repository 取 version/hash，返回 `f"{name}@{version}#{schema_hash}:1"`。无 repository/无记录抛 `KeyError`。`get_tool_confirmation_key = tool_confirmation_key` 是兼容别名。此 key 是 `ExecutionContext.confirmed_side_effects` 与 core runtime 对接的确认键。
  - `run(self, query: str, **kwargs: Any) -> str` — 校验 query 非空字符串，通过 `_run_sync` 运行 `run_with_react`；是同步便利入口。
  - `async arun(self, query: str, **kwargs: Any) -> str` — 异步别名，直接 await `run_with_react`。
  - `async run_react(self, messages: str | Sequence[Mapping[str, Any]], context: ExecutionContext | None = None, *, max_rounds: int | None = None, model: str | None = None, temperature: float = DEFAULT_TEMPERATURE, timeout: float = DEFAULT_TIMEOUT, tool_names: list[str] | None = None, profile_name: str | None = None, provider_name: str | None = None, use_history: bool = True, defer_tool_loading: bool = True, prompt_cache_key: str | None = None, prompt_cache_retention: str | None = None, enable_prompt_cache: bool = True, stream_echo: bool = False) -> str` — 对 `run_with_react` 的显式参数转发包装；自身只负责 await，参数语义与后者相同。
  - `async run_with_react(self, messages: str | Sequence[Mapping[str, Any]], context: ExecutionContext | None = None, *, max_rounds: int | None = None, model: str | None = None, temperature: float = DEFAULT_TEMPERATURE, timeout: float = DEFAULT_TIMEOUT, tool_names: list[str] | None = None, profile_name: str | None = None, provider_name: str | None = None, use_history: bool = True, defer_tool_loading: bool = True, prompt_cache_key: str | None = None, prompt_cache_retention: str | None = None, enable_prompt_cache: bool = True, stream_echo: bool = False) -> str` — 文本协议完整循环，签名与 `run_react` 完全一致。参数说明：`messages` 可为非空 query 字符串或 mapping 序列；`context` 为执行上下文；`max_rounds` 为循环上限；`model/profile_name/provider_name` 选择目标；`temperature/timeout` 控制模型请求；`tool_names` 限定工具；`use_history` 控制历史；`defer_tool_loading` 控制初始 catalog-only；`prompt_cache_*`/`enable_prompt_cache` 控制缓存；`stream_echo` 启用 `react_final` 回显。完整流程：
    1. 把字符串包装成单条 user message；序列必须是 mapping 且排除 bytes/bytearray；校验 max_rounds、context、布尔参数和 prompt cache。
    2. 通过 `_completion_target` 解析客户端、模型、history key；由 `_initial_conversation` 拼接历史/默认 prompt 与本次请求；取 registry 快照并同步 frozen manifest。
    3. 选择工具：显式 `tool_names` 会去重，并允许 runtime 中存在或 repository 中存在的工具；lazy 时把 catalog 放在首位，非 lazy 则以请求顺序使用；否则 deferred/lazy 初始只加载 catalog，普通模式加载全部可见工具。
    4. 每轮先 `log_react_round_started`；lazy 模式下按 `loaded_order` 加载尚未实例化的工具；再把本回合 hot tools 追加到可执行顺序，按固定顺序积累 `loaded_tool_schemas`。
    5. 通过 `_with_tool_instructions` 组装协议、工具清单/schema、可选 catalog 和技能目录；若有 hot tools，把它们以 task-tail system block 追加到请求最后一条消息之后。
    6. 用 `asyncio.to_thread(self._dispatch_model_call, ...)` 发起流式优先请求，`echo_mode="react_final"` 时由 LLM 层只回显最终答案部分。
    7. 若响应直接是字符串（兼容只实现 `think` 的客户端），按文本 content 处理；否则 `_response_message` 提取 message。若检测到 native tool calls，记录重复调用签名、追加 assistant、调用 `_execute_native_calls` 批量执行、追加 Observation 并 continue；deferred/lazy 时还解析 catalog observation。
    8. 文本响应统一生成字符串 content 并 `parse_react_response`，原始 content 追加为 assistant message。若非 final 且仅缺 Action Input 等 malformed error，则最多重试 `REACT_MALFORMED_ANSWER_RETRY_LIMIT` 次，把错误作为 `Observation:` user message 回灌；超限抛 `RuntimeError` 并带最后 200 字符响应。
    9. parsed.is_final 时，如果没有显式 Final marker/XML answer，且用户请求命中工具意图关键词，则计入 unmarked 重试，前 `REACT_UNMARKED_ANSWER_RETRY_LIMIT` 次回灌协议纠正，超限接受答案。随后记录 final、必要时补打印答案、`_save_history` 并返回答案。
    10. 有 Action 时先用 `round_loop.record_call` 做同调用重复检测（超过 3 次抛 `RuntimeError`），再由 `_execute_action` 执行；结果序列化为 `Observation: ...` 追加到 user message，deferred/lazy 时更新 catalog 和 hot 列表；然后进入下一轮。
    11. ToolLoop 耗尽时抛 `RuntimeError("maximum ReAct rounds exceeded")`。
  - `_should_require_tool_action(request_messages: Sequence[Mapping[str, Any]], registrations: Mapping[str, tuple[Any, int]]) -> bool` — **staticmethod**；没有可用 registrations 返回 False。否则把所有 role=`user` 的 content 转字符串、casefold 后拼接，命中“现在/当前时间/今天/实时/查询/搜索/查找/检索/运势/天气/search/look up/current time/latest/today”任一标记返回 True。它是保守启发式，不是完整意图分类。
  - `_initial_conversation(self, request_messages: list[dict[str, Any]], *, use_history: bool, history_key: str) -> list[dict[str, Any]]` — `use_history=True` 且该 key 有历史时复制历史前缀，否则使用 `_configured_prompt_messages()`；最后拼接请求消息并返回新列表。
  - `_with_tool_instructions(self, conversation: list[dict[str, Any]], registrations: Mapping[str, tuple[Any, int]], loaded_tool_schemas: Mapping[str, Mapping[str, Any]] | None = None, *, catalog_first: bool = False) -> list[dict[str, Any]]` — 组装 ReAct system prompt。lazy 模式只保留 catalog registration；frozen manifest 决定稳定的“All registered tool names”，hot tools 从 cached prefix 排除。依次加入 `REACT_INSTRUCTIONS`、可选 catalog-first 指令、技能目录和技能 view 指令、当前可调用范围声明、`Available tools` 及 schema；已经 resolve 但不在本回合 registrations 中的 schema 另列为 `Loaded tool input schemas`。每段 schema 先经 `_strict_react_schema`，JSON 用 sort_keys 和固定 separators 保持稳定；返回 `[instruction, *conversation]`，不修改输入会话。
  - `_hot_zone_lines(self, loaded_tool_schemas: Mapping[str, Mapping[str, Any]]) -> list[str]` — 无 hot tools 时返回空列表。从 `self._hot_tools` 取当前 runtime 可渲染名称，最多给排序后最后 4 个工具完整描述和 schema，其余渲染 roster 行；实现暂时缺失的名称仍列出但标记 unavailable。该块每轮重建，不写入历史。
  - `_skill_directory_lines(self) -> list[str]` — 无技能返回空列表；否则按技能快照顺序生成名称、版本、描述和 triggers 行，明确内容需经 `system.skill_catalog` 查看。它与父类技能目录保持相同的信息边界。
  - `_ensure_tool_loaded(self, name: str) -> None` — 已加载则直接返回；要求 repository，读取存储记录并校验 `implementation_ref` 必须是 `module:qualname`；导入 module、逐级 getattr 得到 target；若模块级 `create_tool` 可调用则优先调用它，否则调用 target；要求结果是 `BaseTool`，并核对 spec name/version/schema_hash 与 catalog 完全一致，最后注册到 runtime。任何缺失、导入或契约不匹配会抛 `RuntimeError`/其他导入异常。
  - `_load_catalog_result(self, result: ToolResult, loaded_order: list[str], context: ExecutionContext, loaded_tool_schemas: dict[str, dict[str, Any]] | None = None) -> None` — 只处理成功 catalog 结果；提取 `specs` 及兼容 `spec`，在 repository 存在时按 name/version 再校验记录；把 `input_schema` 存入 loaded schema map，并把工具名追加到 `loaded_order`。`context` 参数未参与本函数逻辑。
  - `_load_catalog_observation(self, observation: Mapping[str, Any], loaded_order: list[str], context: ExecutionContext, loaded_tool_schemas: dict[str, dict[str, Any]] | None = None) -> None` — 只处理 content 以 `"Observation: "` 开头的 native observation；截去前缀后 `json.loads`，再用 `ToolResult.model_validate(payload, strict=True)` 还原；失败静默返回，成功后转发 `_load_catalog_result`。用于 ReAct 循环兼容 native calls 时的 catalog 更新。
  - `_strict_react_schema(schema: dict[str, Any]) -> dict[str, Any]` — **staticmethod**；延迟导入父模块 `_strict_function_schema` 并直接返回其结果，保证 native 和 ReAct schema 使用同一转换逻辑。
  - `_unavailable_tool_error(self, action_name: str, call_id: str, *, requested_names: set[str] | None = None) -> ToolResult` — 当工具已注册但不在本回合 registrations 时生成失败结果。若 `requested_names` 存在且 action 不在其中，返回 `TOOL_NOT_ENABLED`，要求不要重试/经 catalog 解析；否则返回 `TOOL_SCHEMA_REQUIRED`，要求先调用 catalog 的 `resolve`。工具名先经 `_safe_tool_name`。
  - `_response_message(response: Any) -> Any` — **staticmethod**；从 response 取 choices 和第一个 message；缺 choices/message 抛 `RuntimeError`，否则返回 message。
  - `async _execute_action(self, parsed: ParsedReActResponse, current_snapshot: Mapping[str, tuple[Any, int]], registrations: Mapping[str, tuple[Any, int]], context: ExecutionContext, *, call_number: int, loaded_tool_schemas: dict[str, dict[str, Any]] | None = None, requested_names: set[str] | None = None) -> ToolResult` — 执行单条文本 Action。先用 `_canonical_action_name` 处理 provider alias；call id 为 `react-call-{call_number}`。若工具不在快照且 lazy 模式可解析 `__` 回点号名，则从 repository 加载实现并刷新快照/registrations/schema。仍未知则 `UNKNOWN_TOOL`；已注册但不在 registrations 则返回 `_unavailable_tool_error`；解析 error 则返回 `INVALID_TOOL_CALL`。有效时按 `_coerce_string_scalars` 修正参数，构造带 schema version/hash/registry generation 的 `ToolCall`，记录 started/completed 时间，await `execute_tool_calls([call], context)` 并返回首个结果。工具执行异常仍由 runtime 转为 ToolResult；调度本身的异常会传播，并由 finally 记录完成日志。
  - `async _execute_native_calls(self, native_calls: Sequence[Any], current_snapshot: Mapping[str, tuple[Any, int]], registrations: Mapping[str, tuple[Any, int]], context: ExecutionContext, *, round_number: int, loaded_tool_schemas: dict[str, dict[str, Any]] | None = None, requested_names: set[str] | None = None) -> list[dict[str, Any]]` — ReAct 中的 native tool-call 兼容批量执行。复制快照/registrations，第一遍对 provider alias 先做 canonical 映射，lazy 且 repository 有记录时即时加载缺失实现；构造 name_map/aliases，逐调用用 `parse_openai_tool_calls` 解析，缺 id 生成 `react-native-call-N`，解析失败按 TOOL_NOT_ENABLED/TOOL_SCHEMA_REQUIRED 或 INVALID_TOOL_CALL形成结果。所有合法调用一次 `execute_tool_calls(calls, context)` 批量执行，并由 core runtime 按依赖、`parallel_safe` 和并发上限调度。最终按原调用顺序返回 role=`user` 的 Observation 消息列表（不是 native role=`tool` 消息），内容为 `_result_json(result)`。
  - `_canonical_action_name(self, action: str, snapshot: Mapping[str, tuple[Any, int]]) -> str` — 若 action 是快照原名直接返回；否则在快照中查找 `_openai_tool_name(name)==action` 的唯一映射，找到则返回 canonical name；否则原样返回。用于接受点号/双下划线 provider 命名差异。

##### 模块别名与公共拼写

- `ReactAgent = ReActAgent`、`ReAct = ReActAgent`、`React = ReActAgent`、`react = ReActAgent` 是源码末尾定义的兼容别名；它们不是额外函数，不增加 def 计数。

---

### `memory/rag/graph_rag.py` — 向量证据与图多跳路径的混合检索

- **拓扑层 / 依赖 / 被依赖**：L6；依赖 `constants`、`memory.base` 的 `MemorySearchResult/MemoryType`、`memory.manager.MemoryManager`；被 `memory.rag.__init__` 和 `RAGPipeline` 使用。
- **规模**：244 行 / 9 个 def
- **职责**：先用向量检索拿语义证据，再从证据和查询中识别种子实体，沿语义记忆关系做有界广度优先扩展，最后形成可直接返回 JSON 的路径对象和拼装上下文文本。历史/撤回边保留审计记录，但不会承载检索跳数。
- **关键模块级对象/常量/单例**：默认常量 `RAG_RETRIEVE_LIMIT=5`、`RAG_GRAPH_HOPS=1`、`RAG_GRAPH_MAX_HOPS=3`、`RAG_GRAPH_PATH_LIMIT=20`、`RAG_CONTEXT_MAX_CHARS=12000`；`__all__` 导出 `GraphPath`、`GraphRAGPipeline`、`GraphRAGResult`。无模块级单例。

#### 类 `GraphPath` — 一条从种子实体出发的只读图路径

- **构造**：frozen dataclass 字段为 `source: str`、`target: str`、`relations: tuple[str, ...]`、`entities: tuple[str, ...]`、`confidence: float = 0.0`、`evidence: tuple[dict[str, Any], ...] = ()`。
- **关键属性**：`source` 是起始种子；`target` 是当前末端；`relations` 是按顺序关系标签；`entities` 是完整实体链；`confidence` 是路径上各边置信度的最小值；`evidence` 是沿路径收集的边属性字典。
- **方法**：

##### `to_dict(self) -> dict[str, Any]`

作用：把不可变路径转成 FastAPI/JSON 可直接序列化的 `dict`，元组字段转为列表，`evidence` 中每项复制为 `dict`。参数仅为自身；返回六字段字典；无持久化副作用，不声明异常。

#### 类 `GraphRAGResult` — 混合检索结果聚合对象

- **构造**：普通 dataclass，字段为 `query: str`、`evidence: list[MemorySearchResult] = []`、`paths: list[GraphPath] = []`、`entities: list[str] = []`；后三个使用 `default_factory` 避免共享可变默认值。
- **关键属性**：原始查询、最多 `limit` 条向量证据、图路径、种子实体。
- **方法**：

##### `to_dict(self) -> dict[str, Any]`

作用：输出 `{"query", "evidence", "paths", "entities"}`；证据调用各 `MemorySearchResult.to_dict()`，路径调用 `GraphPath.to_dict()`，实体列表复制。返回字典；无副作用与显式异常。

##### `build_context(self, *, max_chars: int = RAG_CONTEXT_MAX_CHARS) -> str`

作用：把证据、路径和路径证据拼成以双换行分隔的上下文字符串，并在最后直接做字符截断。证据头包含来源 `filename/source/记忆库`、三位小数相似度，以及非当前记录的 `|历史记录，已被更新` 标记；路径头包含路径置信度和实体箭头，关系使用 `" -".join(...)` 的标签形式，关系证据再按 `evidence/source` 输出。参数仅为最大字符数；返回字符串；无异常处理，异常会来自底层对象访问。

#### 类 `GraphRAGPipeline` — 向量检索后进行图多跳扩展

- **构造**：`__init__(self, manager: MemoryManager) -> None` — 保存共享的 `self.manager`；没有校验或复制，返回 `None`。
- **关键属性**：`manager` 同时提供 `search`、`semantic.list`、`semantic.related`、`get`，是检索与图遍历的数据源。
- **方法**：

##### `retrieve(self, query: str, *, limit: int = RAG_RETRIEVE_LIMIT, hops: int = RAG_GRAPH_HOPS, threshold: float | None = None, path_limit: int = RAG_GRAPH_PATH_LIMIT) -> GraphRAGResult`

作用：校验查询和三个整数参数，调用 `manager.search(query, memory_type=SEMANTIC, limit=max(limit * 3, limit), threshold=threshold)`，先从最多三倍候选证据找种子，再扩展图路径，最终只保留前 `limit` 条证据。参数分别是查询、证据上限、跳数、相似度阈值、路径上限。返回 `GraphRAGResult`。

异常：空/非字符串查询、非法 `limit`、超出 `0..RAG_GRAPH_MAX_HOPS` 的 `hops`、非法 `path_limit` 均抛 `ValueError`；搜索或图访问异常由下层原样抛出。副作用是只读检索，但下层可能按实现加载/初始化存储。调用关系：`RAGPipeline.graph_retrieve`、`RAGPipeline.graph_context`、Web `/api/graph-rag` 和 `/api/chat` 最终调用它。

##### `build_context(self, query: str, *, limit: int = 5, hops: int = 1, max_chars: int = 12000) -> str`

作用：薄封装，调用 `retrieve(...).build_context(max_chars=...)`。参数为查询、证据上限、跳数和最大字符数；返回上下文文本；校验/检索异常沿用 `retrieve`，无额外副作用。调用关系：`RAGPipeline.graph_context`。

##### `_find_seed_entities(self, query: str, evidence: list[MemorySearchResult]) -> list[str]`

作用：从证据项的 `kind=entity` 元数据、事实的 `subject/object`，以及全部已存实体规范名/别名中寻找在查询中直接出现的名字。参数为查询和证据列表；返回保持插入顺序并去重的名字列表。查询和名称使用 `casefold()`；无异常处理，不写数据。调用关系：由 `retrieve` 在向量搜索后调用。

##### `_expand(self, seeds: list[str], *, hops: int, path_limit: int) -> list[GraphPath]`

作用：从种子做广度优先扩展，最多产出 `path_limit` 条路径。队列元素包含当前实体、实体链、关系链、证据链、深度、路径置信度；对每个当前节点调用 `manager.semantic.related(current)`，把一边的远端作为邻居。`hops == 0` 或无种子时立即返回空列表。

过滤规则：边属性 `active is False` 或 `_edge_is_active` 判定不活跃则跳过；已访问的 `(neighbor, relation-chain)` 跳过；邻居已在实体链中跳过；新路径置信度取当前值与边置信度的较小值，边置信度为 0 时沿用当前值。每条新路径会进入结果与队列，最后按“置信度降序、关系数升序、目标名升序”排序并截取。参数为种子、跳数和路径上限；返回 `list[GraphPath]`。异常来自语义存储或数值转换；只读副作用。调用关系：`retrieve` 调用。

##### `_edge_is_active(self, properties: dict[str, Any]) -> bool`

作用：用边属性中的 `memory_id` 回到语义记忆记录核验活跃状态，防止内存/Neo4j 副本仍持旧退休标记。参数是边属性字典；没有 `memory_id` 时返回 `True`，记录不存在返回 `False`，存在则看记录元数据 `active` 是否为 `False`。返回布尔值；不修改数据。调用关系：`_expand` 在遍历每条边时调用。

### `memory/rag/knowledge.py` — LLM 知识抽取、实体消歧与图补丁落库

- **拓扑层 / 依赖 / 被依赖**：L6；依赖 Pydantic、`constants`、`memory.base`、`memory.ids`、`memory.manager`；被 `memory.rag.__init__`、`pipeline`，并通过 `memory.rag` 被 `web.support` 使用。
- **规模**：803 行 / 31 个 def
- **职责**：定义受严格校验的实体、关系和抽取结果模型，提供可注入的规则/LLM 抽取器，维护实体规范名、别名、前缀及模糊匹配索引，并把抽取建议落成实体记录和 `assert/supersede/retract` 图关系补丁。另把既有相关子图渲染成提示词上下文，强调单次 ingest 复用同一 `EntityResolver`。
- **关键模块级对象/常量/单例**：`ENTITY_PREFIX_BOUNDARIES` 规定前缀边界；`__all__` 导出 14 个名称。默认常量包括相似度阈值 0.88、实体默认置信度 0.8、默认类型“概念”、前缀最小长度 3、图上下文最多 60 条关系、上下文最多 12000 字符。`LLMKnowledgeExtractor.SYSTEM_PROMPT` 是固定中文抽取协议，要求只输出 JSON、复用已知规范名、给出原文 evidence，并区分三种 action 与两种 cardinality。模块无进程级单例。

#### 函数 `_normalize_for_match(value: str) -> str`

作用：为匹配生成保留词分隔符的规范化键。它调用 `_clean_text(value, max_length=ENTITY_NAME_MAX_LENGTH)`、`casefold()`、折叠空白，并去除除空格外的首尾边界标点。参数为字符串；返回规范化字符串。异常由 `_clean_text` 决定；源码中未见额外捕获。调用关系：`EntityResolver` 的索引、匹配、别名写入及 `build_graph_context` 全词组匹配都使用它。

#### 函数 `_starts_at_boundary(shorter: str, longer: str) -> bool`

作用：判断 `shorter` 是否以完整前缀形式起始于 `longer`。完全相同返回 `True`；否则要求 `longer` 在 `shorter` 后的首个字符属于 `ENTITY_PREFIX_BOUNDARIES`。参数为短、长匹配键；返回布尔值；无副作用、无显式异常。调用关系：`is_prefix_match`。

#### 函数 `is_prefix_match(left_key: str, right_key: str) -> bool`

作用：先按长度确定短、长键，短键长度小于 `ENTITY_PREFIX_MIN_LENGTH` 时不匹配，否则委托 `_starts_at_boundary`。用于让 `web` 匹配 `web 中转站`、`deepseek` 匹配 `deepseek-v4.1-flash`，但不让 `web` 匹配 `webfoo`。参数为两个匹配键；返回布尔值；无显式异常与副作用。调用关系：`EntityResolver._prefix_candidate` 与 `build_graph_context`。

#### 类 `EntityCandidate(BaseModel)` — 单个抽取实体的严格输入模型

- **构造**：Pydantic 字段为 `name: str`（1 到 200）、`entity_type: str`（默认“概念”，最长 80）、`description: str`（默认空，最长 1000）、`confidence: float`（默认 0.8，范围 0 到 1）、`aliases: list[str]`（默认空，最多 20 项）；`extra="forbid"`、`strict=True`。
- **关键属性**：属性与字段同名；不维护跨模型状态。
- **方法**：

##### `@field_validator("name", "entity_type", "description", mode="before") @classmethod normalize_strings(cls, value: Any) -> str`

作用：在 Pydantic 基础校验前把三个字符串字段交给 `_clean_text(value, max_length=1000)`，统一清理和截断。参数 `cls` 为模型类、`value` 为原始输入；返回清理后的字符串。无副作用；`_clean_text` 的异常或类型问题会阻止模型构造。调用关系：Pydantic 模型验证链。

#### 类 `RelationCandidate(BaseModel)` — 单个关系补丁的严格输入模型

- **构造**：字段为 `subject: str`（1 到 200）、`predicate: str`（1 到 100）、`object: str`（1 到 200）、`action: Literal["assert", "supersede", "retract"] = "assert"`、`cardinality: Literal["single", "multi"] = "multi"`、`confidence: float`（默认 0.75，0 到 1）、`evidence: str`（默认空，最长 1200）；`extra="forbid"`、`strict=True`。
- **关键属性**：`action` 决定添加、退役后添加或撤回；`cardinality` 决定同槽位是否只保留当前值；`evidence` 应来自原文。
- **方法**：

##### `@field_validator("subject", "predicate", "object", "evidence", mode="before") @classmethod normalize_strings(cls, value: Any) -> str`

作用：四字段进入常规校验前统一执行 `_clean_text(value, max_length=1200)`。参数为模型类和原始值；返回清理后字符串；无业务副作用。调用关系：Pydantic 验证链。

#### 类 `ExtractionResult(BaseModel)` — 一个知识块的完整抽取结果

- **构造**：字段为 `domain: str`（默认“未分类”，1 到 100）、`topics: list[str]`（最多 20）、`entities: list[EntityCandidate]`（最多 50）、`relations: list[RelationCandidate]`（最多 80）、`keywords: list[str]`（最多 30）；`extra="forbid"`、`strict=True`。列表均使用 `default_factory=list`。
- **关键属性**：领域、主题、实体、关系、关键词；实体和关系嵌套各自的严格模型。
- **方法**：

##### `@field_validator("domain", mode="before") @classmethod normalize_domain(cls, value: Any) -> str`

作用：先 `_clean_text(value, max_length=100)`，空值回退 `DEFAULT_DOMAIN`。参数为模型类和原值；返回非空领域字符串；无副作用。调用关系：Pydantic 验证链。

##### `@field_validator("topics", "keywords", mode="before") @classmethod normalize_lists(cls, value: Any) -> list[str]`

作用：`None` 转为空列表；非列表抛 `TypeError("topics and keywords must be lists")`；列表逐项清理，过滤清理后为空的值，并保留重复项。参数为模型类与原值；返回字符串列表。调用关系：Pydantic 验证链，之后仍受字段最大长度约束。

#### 类 `KnowledgeExtractor(Protocol)` — 抽取器的结构化类型协议

- **构造**：Protocol 不提供运行时可实例化的业务构造器；只定义接口形状。
- **关键属性**：无。
- **方法**：

##### `extract(self, text: str, *, metadata: Mapping[str, Any] | None = None) -> ExtractionResult`

作用：协议中的同步抽取方法，源码以 `...` 表示无实现。参数为文本和可选元数据；约定返回 `ExtractionResult`；无副作用与异常契约。调用关系：`RAGPipeline` 保存该类型；具体实现为 `NullKnowledgeExtractor` 或 `LLMKnowledgeExtractor`。

#### 类 `NullKnowledgeExtractor` — 无模型配置时的安全空抽取器

- **构造**：源码未定义 `__init__`，使用无参默认构造。
- **关键属性**：无。
- **方法**：

##### `extract(self, text: str, *, metadata: Mapping[str, Any] | None = None) -> ExtractionResult`

作用：忽略文本和元数据，直接返回默认 `ExtractionResult()`，即“未分类、无实体、无关系”。不抛业务异常、不写状态。调用关系：`RAGPipeline` 默认抽取器；`support.build_knowledge_extractor` 的离线回退。

#### 类 `LLMKnowledgeExtractor` — 通过 OpenAI 兼容聊天客户端抽取结构化知识

- **构造**：`__init__(self, complete: Callable[..., Any], *, model: str | None = None, timeout: float = 60.0) -> None` — 校验 `complete` 可调用，否则 `TypeError`；保存调用函数、模型名和超时。无网络请求。
- **关键属性**：`complete` 是注入的 LLM 完成函数；`model` 传给该函数；`timeout` 默认 60 秒；类属性 `SYSTEM_PROMPT` 定义 JSON 协议。
- **方法**：

##### `extract(self, text: str, *, metadata: Mapping[str, Any] | None = None, graph_context: str = "") -> ExtractionResult`

作用：空/非字符串文本直接返回默认结果；从元数据提取 `filename/source` 作为来源，正文最多发送前 12000 字符；非空 `graph_context` 作为“已知图”加入 payload。构造 system/user 消息，调用 `complete(messages, model=..., temperature=0.0, timeout=..., stream=False)`，再经 `_content` 和 `_parse_json`，最后用 `ExtractionResult.model_validate` 严格验证。参数为正文、元数据和已有图上下文；返回抽取结果。网络、调用协议、JSON 或 Pydantic 异常向上抛。调用关系：`support.build_knowledge_extractor` 用 `agents.llm.LLM.complete` 装配；`RAGPipeline.ingest` 根据签名决定是否传 `graph_context`。

##### `@staticmethod _content(response: Any) -> str`

作用：兼容字符串响应、映射响应和对象响应，逐层取得 `choices[0].message.content`。参数为任意响应；返回非空文本。无 choices 抛 `ValueError("knowledge extraction response contained no choices")`；无有效文本抛 `ValueError("knowledge extraction response contained no text")`。无副作用。调用关系：`extract`。

##### `@staticmethod _parse_json(raw: str) -> dict[str, Any]`

作用：去掉可能的 Markdown ```json 围栏；先直接 `json.loads`，失败后截取第一个 `{` 到最后一个 `}` 再解析；要求最终为字典。参数为原始文本；返回字典。无法定位对象时抛 `ValueError("knowledge extraction response was not valid JSON")`；JSON 仍非法时 `JSONDecodeError` 向上抛；非对象抛 `TypeError("knowledge extraction response must be a JSON object")`。调用关系：`extract`。

#### 函数 `_entity_similarity(left: str, right: str, aliases: list[str] | None = None) -> float`

作用：计算名称相似度。对 `left` 与 `right` 及所有别名分别用 `SequenceMatcher` 算规范名字符串比值，同时计算词元 Jaccard 重叠，最终取两者较大值。参数为待匹配名、候选规范名、可选别名列表；返回浮点分数；无副作用，除 `normalize_entity_name` 等下层异常外无显式异常。调用关系：`EntityResolver.resolve` 的模糊匹配阶段。

#### 类 `EntityResolver` — 规范实体名、别名与相似名称的统一解析器

- **构造**：`__init__(self, manager: MemoryManager, *, similarity_threshold: float = ENTITY_SIMILARITY_THRESHOLD) -> None` — 保存管理器和阈值，初始化 `_entities` 与 `_index` 后立即 `_load()`。无显式参数校验。
- **关键属性**：`manager` 是持久化语义记忆；`similarity_threshold` 是模糊匹配门槛；`_entities` 以规范匹配键映射实体记录；`_index` 同时索引规范键与所有别名键。
- **方法**：

##### `_load(self) -> None`

作用：清空两个索引后遍历 `manager.semantic.list()`，把 `metadata.kind == "entity"` 的记录交给 `_remember`。返回 `None`；副作用是重建内存索引；存储读取异常向上抛。调用关系：构造器以及需要全量刷新时可显式调用。

##### `_remember(self, item: MemoryItem) -> None`

作用：从 `canonical_name/title/content` 取规范名，规范化后写入 `_entities` 和 `_index`，再为每个非空别名写 `_index`。参数为实体记忆项；返回 `None`。空规范键直接忽略；别名索引使用 `_index[...] = item`，规范键使用 `setdefault`。调用关系：`_load`、`resolve` 写回实体后。

##### `_exact(self, key: str) -> MemoryItem | None`

作用：依次查规范名映射和完整索引，返回实体或 `None`。参数为已经规范化的键；无副作用与显式异常。调用关系：`match`、`resolve`。

##### `_alias(self, key: str) -> MemoryItem | None`

作用：只从 `_index` 查键，再核对该键是否不是规范键，以区分“纯别名命中”。参数为规范化键；返回实体或 `None`；无副作用。调用关系：`match`、`resolve`。

##### `_prefix_candidate(self, key: str) -> MemoryItem | None`

作用：遍历索引快照，以 `is_prefix_match` 找最长前缀候选；共享长度相同时保留先前候选。参数为规范化键；返回实体或 `None`。方法只读，别名写入由 `resolve` 负责。调用关系：`match`、`resolve`。

##### `_store_alias(self, item: MemoryItem, name: str) -> MemoryItem`

作用：把 `name` 加入实体元数据别名集合，按规范名重写实体记录，并把原先指向旧快照 id 的所有索引项重接到新记录。参数为候选实体和实际书写名；返回持久化后的新 `MemoryItem`。副作用是语义存储写入与索引更新；空别名由调用方避免，存储异常向上抛。调用关系：`resolve` 在前缀命中且键尚无索引时调用。

##### `match(self, name: str) -> MemoryItem | None`

作用：规范化名称后按精确规范名/别名、纯别名、最长前缀的顺序匹配。参数为名称；返回实体或 `None`；空键返回 `None`；无写入。调用关系：`build_graph_context` 对正文词元进行匹配。

##### `resolve(self, name: str, *, domain: str, entity_type: str = ENTITY_DEFAULT_TYPE, description: str = "", confidence: float = ENTITY_DEFAULT_CONFIDENCE, aliases: list[str] | None = None, source_id: str | None = None) -> str`

作用：把抽取名称解析为稳定实体并写回语义记忆。匹配顺序为精确规范名/别名、前缀、模糊相似度；前缀只需“书写名已存在于索引”才写入别名，避免把拼写相近名污染已有实体；模糊命中不再新增书写别名。参数包括名称、领域、实体类型、描述、置信度、附加别名和来源记忆 id。返回规范名字符串。

副作用与异常：空名抛 `ValueError("entity name must not be empty")`；新实体的 id 由 `entity_id_for(name)` 生成，已有实体保留 id；元数据合并既有类型/描述/领域、别名和 `source_ids`；`importance` 取本次置信度与既有重要度最大值；调用 `manager.semantic.add` 后刷新 `_remember`。存储异常向上抛。调用关系：`materialize_extraction` 的实体阶段和 `resolve_endpoint`。

#### 函数 `materialize_extraction(manager: MemoryManager, extraction: ExtractionResult, *, source_item: MemoryItem, source_metadata: Mapping[str, Any] | None = None, relation_threshold: float = 0.6, resolver: EntityResolver | None = None) -> dict[str, Any]`

作用：把一次已校验抽取落成实体与关系补丁，是 LLM 建议到持久化事实之间的边界。默认没有 resolver 时新建；调用者传入时可跨块复用。返回包含 `domain/topics/entities/relations/superseded/retracted/skipped_relations/relation_items` 的报告字典。

实体阶段：逐个 `candidate` 调 `resolver.resolve(...)`，保存“原始名规范化键 → 规范名”的 `canonical_by_key`，`entities` 计数逐项递增。

事实阶段：低于 `relation_threshold` 的关系只计入 `skipped_relations`；端点先经 `resolve_endpoint` 规范名；关系 id 由 `relation_id_for(subject, predicate, object)` 决定；已有记录合并 source id 和最多 20 条 evidence。`retract` 只在已有边时写回 `active=False`、`superseded_at` 并计 retracted；无已有边则跳过。`supersede` 或 `single` 会退休同槽位其他活跃事实，写 `active=False`、`superseded_at`、`superseded_by` 并累计；随后写入或强化当前事实，记录 `action/cardinality/active/supersedes` 等元数据并计 relations。

异常与副作用：所有实体和事实都通过 `manager.semantic.add`/`add_fact` 持久化；单条失败无内部捕获，直接向上抛。调用关系：`RAGPipeline.ingest` 在抽取器返回后调用。以下五个函数均嵌套在本函数中：

##### 嵌套函数 `slot_key(subject: str, predicate: str) -> str`

作用：用 `normalize_entity_name(subject)` 和 `normalize_entity_name(predicate)` 拼出 `主|谓` 槽键。参数为主语和谓词；返回字符串；无副作用。调用关系：`index_all_facts`、`remember`、`facts_for`。

##### 嵌套函数 `index_all_facts() -> None`

作用：首次调用时用 `nonlocal facts_indexed` 置位，并全量遍历 `manager.semantic.facts()`，把有主语、谓词的事实按槽键归入 `known_items`。无参数；返回 `None`；存储异常向上抛。调用关系：`remember` 与 `facts_for` 的惰性全量索引入口。

##### 嵌套函数 `remember(item: MemoryItem) -> None`

作用：在一笔事实写入后刷新本块缓存：读取主语/谓词，必要时先全量索引，找到同 id 缓存则替换，否则追加。参数为刚写入的事实；返回 `None`；无主语或谓词直接返回。调用关系：`materialize_extraction` 在 retract、supersede 旧值和写入新值后调用。

##### 嵌套函数 `facts_for(subject: str, predicate: str) -> list[MemoryItem]`

作用：确保索引后就返回指定槽位的所有活跃事实，`active is False` 被排除。参数为主语和谓词；返回列表；无写入。调用关系：supersede/single 退休逻辑。

##### 嵌套函数 `resolve_endpoint(name: str) -> str`

作用：先查本块 `canonical_by_key`，未命中时用共享 `resolver.resolve(name, domain=extraction.domain, source_id=source_item.id)` 解析，但该回退解析不会把规范名写回 `canonical_by_key`。参数为关系端点名；返回规范名；解析异常向上抛。调用关系：每条非跳过关系的 subject/object。

#### 函数 `build_graph_context(manager: MemoryManager, text: str, *, resolver: EntityResolver | None = None, max_relations: int = GRAPH_CONTEXT_MAX_RELATIONS, max_chars: int = RAG_CONTEXT_MAX_CHARS) -> str`

作用：为抽取器渲染已有知识图的相关片段。参数为管理器、待抽取文本、可选共享解析器、关系上限、字符上限；返回文本，空文本或无匹配实体返回空字符串。

行为：先校验两个上限是正 `int` 且非 `bool`，否则 `ValueError`；文本 casefold 后按 `[\w\u4e00-\u9fff]+` 分词，逐词 `resolver.match` 收集种子，再用整段文本与索引键做 `is_prefix_match`，覆盖带分隔符实体名。随后以规范实体名扩展一跳，只纳入活跃事实。输出先列“已知实体（必须复用）”，再列按置信度降序的当前关系和最多 `max(1, max_relations // 3)` 条历史关系，最后交给 `_fit_lines`。副作用主要是读取/首次构造解析器索引；调用关系：`RAGPipeline.ingest` 在每个块抽取前调用，并传入同一个 `resolver`。

#### 函数 `_fit_lines(lines: list[str], max_chars: int) -> str`

作用：只按完整行拼接，保证不把关系截成半句；`cost` 包含换行符，累计超限立即停止。参数为行列表和字符上限；返回用换行连接的文本；无副作用和显式异常。调用关系：`build_graph_context` 的最终裁剪。

### `tool/read_update_logs.py` — 一次调用读取连续更新日志范围

- **拓扑层**：L6 ｜ **依赖**：`pydantic`（含 `model_validator`）、`core.BaseTool`/`ToolSpec`、`core.update_log.UpdateLogRepository`、`tool.read_update_log.ReadUpdateLogOutput` ｜ **被依赖**：`core.discovery` 扫描并注册 `system.read_update_logs`。
- **规模**：133 行 / 4 个 def。
- **工具名 / 副作用**：`system.read_update_logs`、`side_effect="read"`。
- **职责**：读取从 `start_id` 到 `end_id`（含端点）的连续日志；`end_id=None` 时使用当前最高 ID；仓储支持时用一次范围查询，否则逐 ID 兼容回退；验证范围内没有缺口，并让每次审计只需一次模型工具调用。
- **关键常量**：`TOOL_ENABLED = True`；没有定义范围大小或数据库路径常量。

#### 类 `ReadUpdateLogsInput(BaseModel)` — 输入契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `start_id: int = 1`：`ge=1`；起始 ID，包含。
- `end_id: int | None = None`：`ge=1`；结束 ID，包含；`None` 表示当前最新 ID。
- 方法 `validate_range` 在模型构造后检查显式 `end_id` 不小于 `start_id`。

##### 方法 `validate_range(self) -> ReadUpdateLogsInput`

```python
@model_validator(mode="after")
def validate_range(self) -> ReadUpdateLogsInput
```

- **参数**：无显式参数（`self` 为正在构造的模型）。
- **返回**：`self`；若 `self.end_id is not None and self.end_id < self.start_id` 则抛 `ValueError("end_id must be greater than or equal to start_id")`。
- **关键实现 / 调用关系**：由 Pydantic 模型验证链调用；此时 `end_id=None` 不会在此处与数据库最新 ID 比较，数据库边界在 `execute()` 中检查。

#### 类 `ReadUpdateLogsOutput(BaseModel)` — 输出契约

- `model_config = ConfigDict(extra="forbid", strict=True)`。
- `records: list[ReadUpdateLogOutput]`：无默认；`min_length=1`；按升序 ID 读取的记录。
- `start_id: int`：无默认；`ge=1`；本次实际请求起点。
- `end_id: int`：无默认；`ge=1`；本次实际请求终点（`None` 已在 execute 中解析为最新 ID）。
- `latest_update_id: int`：无默认；`ge=0`；当前最高存储 ID。
- 类内没有额外 `def`。

#### 类 `ReadUpdateLogsTool(BaseTool)` — 工具实现

- `spec`：`name="system.read_update_logs"`、`description` 说明在工具内部逐 ID/范围读取、升序返回、一次模型调用完成审计、`version="1.0"`、输入输出模型、`side_effect="read"`、`permissions=()`、`timeout_seconds=10.0`、`idempotent=True`、`parallel_safe=True`、`max_concurrency=4`、`tags=("update-log", "audit", "read", "batch", "project")`；`recommended_before_tools` 未显式传入，使用默认空元组。
- **声明理由**：只读、可重复、可并发；批次越大越可能等待 SQLite，因此并发上限设为 4；10 秒覆盖一次范围查询和逐条兼容回退。

##### 方法 `__init__(self, repository: UpdateLogRepository | None = None) -> None`

```python
def __init__(self, repository: UpdateLogRepository | None = None) -> None
```

- **作用 / 参数 / 实现**：注入或创建 `UpdateLogRepository`；不会在构造时读取范围。

##### 方法 `execute(self, arguments: ReadUpdateLogsInput) -> ReadUpdateLogsOutput`

```python
def execute(self, arguments: ReadUpdateLogsInput) -> ReadUpdateLogsOutput
```

- **作用**：解析实际范围、读取连续记录、验证无缺口并返回统一输出。
- **参数**：`arguments` 必须是 `ReadUpdateLogsInput`，否则抛 `TypeError`。
- **返回**：`ReadUpdateLogsOutput`；每条 `ReadUpdateLogOutput` 被写入当前 `latest_update_id` 后构造。
- **副作用**：只读数据库；会修改本次仓储返回的记录字典，不修改持久化数据。
- **异常**：`latest_update_id < start_id` 时抛 `LookupError`，消息包含起点和最新 ID；`end_id > latest_update_id` 时抛 `LookupError`；范围数量与期望不一致时找出第一个缺失 ID 并抛 `LookupError`；仓储/SQL 错误自然传播；Pydantic 输出校验错误会传播。
- **关键实现**：先 `latest_id()`；`end_id` 为 `None` 时用最新 ID；若仓储有 `get_range`，调用一次 `get_range(start_id, end_id)`；否则用列表推导逐 ID 调用 `get()` 并过滤 `None`；`expected_count = end_id - start_id + 1`，数量不符时用 `present` 集合寻找第一个缺失 ID；最后把每条记录补上 `latest_update_id` 并构造 `ReadUpdateLogOutput`。
- **调用关系**：复用单条输出模型和仓储；与 `update_log` 写入协议配套，审计方可以一次读取连续范围，而写入仍由 `system.update_log` 负责。

#### 函数 `create_tool() -> BaseTool`

```python
def create_tool() -> BaseTool
```

- **参数**：无。
- **返回**：`ReadUpdateLogsTool()`。

##### 源码观察与不确定点

- `ReadDirTool` 定义了 `MAX_ENTRIES = 5_000`，但 `execute()` 只使用输入字段 `arguments.max_entries`，未直接引用该常量。
- `ReadFileGlobTool` 定义了 `MAX_MATCHES = 10_000`，但 `execute()` 只使用输入字段 `arguments.max_matches`。
- `WriteTextInput` 中 `content` 使用字面量 `1_000_000`，`MAX_CONTENT_CHARS` 常量未在类字段或执行代码中直接引用。
- `EditTextInput` 的两个上限常量使用等值字面量，`MAX_OLD_CHARS`/`MAX_NEW_CHARS` 未在字段定义中直接引用。
- `cap_text()` 的截断后缀不占用 `max_chars`，因此返回文本长度可能超过参数名义上限；源码没有单独返回值说明这一点。
- `SearchTool._read_response_body()` 对只支持零参数 `read()` 且没有有效 `Content-Length` 的测试/适配对象不提供硬上限；源码注释明确承认这一点。
- `SearchTool.execute()` 只对 `HTTPError` 的 501 做回退；其他 HTTP 错误、`URLError` 等网络异常没有在本模块中进一步归一化。
- `SearchTool._normalize_response()` 在 `data` 是字典但缺少 `results` 时使用空列表，而不是抛缺字段错误；只有 `data is None` 且顶层也没有 `items`/`results` 时才报缺数据。
- FS 工具的 `base_dir` 是构造函数注入的沙箱根；源码没有额外检查自定义 `base_dir` 是否位于 `workspace_root()` 内。
- `atomic_write_text()` 使用同目录临时文件和 `os.replace()`，但源码没有显式 `fsync` 父目录，也没有显式保留原目标文件的权限位/ACL。
- `WriteTextTool.execute()` 的 `existed = path.exists()` 与后续替换不是原子检查，`overwrite=False` 存在跨进程竞态窗口。
- `EditTextTool.execute()` 没有在 `new_string == old_string` 时短路，仍会写回并返回 `changed=True`。
- `UpdateLogTool` 本身没有读取 `update_log_readme_first.md`；写前读索引是项目工作流协议，不是该工具的实现步骤。
- `ReadUpdateLogTool`/`ReadUpdateLogsTool` 的 `latest_update_id` 是查询时快照；若其他写者在查询期间追加，返回的最新 ID 可能落后于新的数据库状态，源码没有显式加锁或事务快照协调。

##### 覆盖清单

| 文件 | 行数 | def 数量 |
|---|---:|---:|
| `tool/__init__.py` | 33 | 2 |
| `tool/base.py` | 5 | 0 |
| `tool/_shared.py` | 349 | 14 |
| `tool/tool_template.py` | 252 | 3 |
| `tool/current_time.py` | 76 | 2 |
| `tool/fs_read_text.py` | 150 | 3 |
| `tool/fs_read_dir.py` | 183 | 3 |
| `tool/fs_read_file_glob.py` | 177 | 4 |
| `tool/fs_write_text.py` | 156 | 3 |
| `tool/fs_edit_text.py` | 179 | 3 |
| `tool/search.py` | 441 | 17 |
| `tool/update_log.py` | 159 | 3 |
| `tool/read_update_log.py` | 119 | 3 |
| `tool/read_update_logs.py` | 133 | 4 |
| **合计** | **2412** | **64** |

### `web/graph_builder.py` — 把四层记忆投影成星云 nodes/edges

- **拓扑层 / 依赖 / 被依赖**：L6；依赖 `constants`、`memory.MemoryItem/MemoryManager`、`web.domain_classifier`；被 `web.app` 的 `/api/graph` 使用。
- **规模**：279 行 / 7 个 def
- **职责**：全量读取记忆项，按元数据优先级分类成恒星、行星、卫星和边，并将结果输出为前端可直接绘制的 `stats/nodes/edges` JSON。领域是 `kind=domain` 恒星；显式实体和其他端点成为 `kind=entity` 行星；事实、知识块、备注及事件成为 `kind=fact/chunk/note/event` 卫星。事实仅活跃时产生实体间边，历史/撤回事实保留为历史卫星但不参与图遍历。Phase 7（U7）起实体节点 `meta` 会带上图库里的别名（`_entity_aliases` 一次取回，图库读不到则退化为「无别名」，不因此整图失败）。
- **关键模块级对象/常量/单例**：颜色由 `NEBULA_PALETTE` 和 zlib CRC32 决定；内容预览、日期、事件标题长度分别由 `NEBULA_CONTENT_PREVIEW_CHARS`、`NEBULA_DATE_CHARS`、`NEBULA_EVENT_TITLE_CHARS` 控制；时间线使用 `TIMELINE_DOMAIN/TIMELINE_ENTITY/TIMELINE_ID`；`__all__` 导出 `NEBULA_PALETTE`、`build_graph`、`domain_color`。无单例。

#### 函数 `domain_color(name: str) -> str`

作用：对领域名的 UTF-8 字节做 `zlib.crc32`，按调色板长度取模，返回稳定颜色；空名使用 `DEFAULT_DOMAIN`。参数为领域名；返回色值字符串；无异常处理与副作用。调用关系：`_node` 以及文档实体重挂恒星系时调用。

#### 函数 `_node(node_id: str, kind: str, title: str, *, content: str = "", domain: str = "", date: str = "", importance: float = 0.5, parent: str | None = None, source: str | None = None) -> dict[str, Any]`

作用：构造统一节点字典，包含 `id/kind/title/content/domain/date/importance/color/parent/source/meta`；领域空值回退“未分类”，重要度转 `float` 并保留三位小数，`meta` 初始为空字典。参数为节点基础字段；返回字典；无异常捕获，`importance` 不可转浮点时向上抛。调用关系：所有恒星、行星、卫星节点创建。

#### 函数 `_entity_aliases(manager: MemoryManager) -> dict[str, list[str]]`

作用：一次取回「实体名 → 别名」映射（U7 实体侧栏）。从 `manager.graph_store` 上探测可调用的 `entity_aliases`（图库实现，见 `memory/storage/graph.py`），调用失败或不可用时返回空表。参数为管理器；返回字典；副作用是可能对图库发一次查询；`Exception` 全部吞掉并退化为空表，避免图库抖动导致整图失败。调用关系：`build_graph` 在循环前调用一次。

#### 函数 `build_graph(manager: MemoryManager) -> dict[str, Any]`

作用：把 `manager.list()` 的全部记忆投影为 `{"stats", "nodes", "edges"}`。核心映射规则如下：

- 领域节点：由 `domain_node(name)` 懒创建，`kind="domain"`、`level` 约定由前端解释为恒星、重要度 0.9；节点 id 为 `dom:<领域名>`。
- 实体节点：显式 `kind=entity` 记录优先第一遍创建；节点 id 使用记忆项原 id，标题取 `metadata.title` 或名称，领域来自元数据；事实端点不存在时会补建 `ent:<名称>` 行星；实体节点 `meta.aliases` 填图库别名（有则填）。
- 事实节点：元数据同时有 `subject/predicate/object` 即认定为事实；节点 id 为记忆 id，`parent` 是主语实体，标题是三元组，活跃事实重要度保留，历史事实重要度上限 0.3；活跃事实额外产生 `source -> target` 边，边 id 为 `edge:<item.id>`，标签为谓词，并携带置信度、evidence、来源文档、chunk、cardinality。历史事实不产生边。
- 备注节点：`kind=note`，挂到 `metadata.entity` 对应实体；找不到时挂到内置事件时间线实体，不产生边。
- 知识块节点：同时有 `document_id` 和 `chunk_index` 即认定 RAG 块；每块先调用 `classify_domain`，再创建/复用 `doc:<document_id>` 文档实体，块节点 id 为记忆 id、`parent` 为文档实体，内容只保留预览长度，并在 `meta.document_id` 留原文档 id。
- 文档实体领域重挂：聚合该文档所有块的领域，用 `majority_domain` 决定最终恒星和颜色；单块领域不因此覆盖整体多数领域。
- 事件节点：不满足事实、显式实体、备注、知识块条件的其余记忆都成为 `kind=event`，挂在内置 `ent:__timeline__` 时间线下，领域为“时间线”。
- `stats`：统计 `domains/entities/facts/chunks/notes/events/edges/historical_facts/total`；历史事实统计的是事实节点中 `meta.active` 为假者。

参数为 `MemoryManager`；返回上述字典。遍历是 O(N)（知识块分类另按关键词表线性扫描）；无内部异常捕获，存储或单条字段类型问题向上抛。源码中的嵌套函数：

##### 嵌套函数 `domain_node(name: str) -> str`

作用：规范化领域名，若尚未创建则生成 `dom:<name>` 恒星节点并记入 `domains`，返回节点 id。参数为领域名；返回 id；副作用是修改闭包中的 `nodes/domains`。调用关系：实体、文档实体和时间线创建。

##### 嵌套函数 `entity_node(name: str, *, domain: str = "", item: MemoryItem | None = None, title: str | None = None) -> str`

作用：空名回退“未分类”；若名称已有 id 直接返回。给 `item` 时使用记忆 id、标题、正文、日期、重要度及来源；无 item 时使用 `ent:<名称>`、空正文/日期、重要度 0.6。节点会挂到对应 `domain_node`，并登记 `entity_ids`；按名称在 `aliases_by_entity` 查别名，非空则写入 `node["meta"]["aliases"]`（U7，只读图库，不写）。参数为名称、领域、可选记忆项和标题；返回节点 id；副作用是修改闭包状态。调用关系：显式实体、事实两端和端点补建。

#### 函数 `_date(item: MemoryItem) -> str`

作用：优先调用 `item.created_at.date().isoformat()`；若对象没有 `date()`，回退 `str(created_at)` 并截取 `NEBULA_DATE_CHARS` 长度。参数为记忆项；返回日期字符串；无写入副作用，非 `AttributeError` 异常向上抛。调用关系：所有需要节点日期的分支。

## L7 RAG 总管道

### `memory/rag/pipeline.py` — RAG 摄取、检索、上下文与图检索总管道

- **拓扑层 / 依赖 / 被依赖**：L7；依赖 `document`、`graph_rag`、`knowledge`、`embedding`（`gateway_reachable`）、`storage.document_repo`、`memory.base/manager`；被 `memory.rag.__init__`、`web.support`、`web.app` 使用。
- **规模**：372 行 / 20 个 def
- **职责**：组装文档处理、语义记忆、知识抽取、图检索与**documents/chunks 真值源**，向调用方提供 `ingest/ingest_source/retrieve/hybrid_retrieve/build_context/graph_retrieve/graph_context/answer/delete_document/close` 统一入口。每次 `ingest` 只创建一个 `EntityResolver`，从而让同一批块共享实体索引；抽取失败不会丢失已经写入的源文本（原文先落真值源）。检索侧提供「向量 × FTS5 关键词」RRF 融合（`hybrid_retrieve`），嵌入网关不可达时自动降级为纯关键词并把降级原因写进 `last_retrieval_note`（D8/U4）。
- **关键模块级对象/常量/单例**：默认块长度 1000、重叠 100、检索 5、图跳 1、上下文 12000；`__all__` 导出 `RAGPipeline`、`RetrievedChunk`。实例级 `last_ingest_report` 是最近一次摄取报告、`last_retrieval_note` 是最近一次混合检索的降级说明，均非进程单例。混合检索开关 `HELLOAGENTS_MEMORY_HYBRID`（默认开）。

#### 类 `RetrievedChunk` — 面向调用方的检索块值对象

- **构造**：frozen dataclass，字段为 `content: str`、`score: float`、`memory_id: str`、`metadata: Mapping[str, Any]`、`detail: Mapping[str, Any] = field(default_factory=dict)`。
- **关键属性**：正文、相似度分数、记忆 id、原始元数据、混合检索的分数明细（`rrf_score/vector_score/keyword_score`，U4 溯源面板用；纯向量/关键词路径时为空）。
- **方法**：

##### `@classmethod from_result(cls, result: MemorySearchResult) -> RetrievedChunk`

作用：从搜索结果的 `item.content/score/item.id/item.metadata` 构造值对象（`detail` 留默认空）。参数为搜索原始结果；返回 `RetrievedChunk`；无副作用和显式异常。调用关系：`RAGPipeline.retrieve`。

#### 函数 `_document_tags(metadata: Mapping[str, Any]) -> list[str]`

作用：从调用方元数据读 `tags`（列表、单个字符串或缺失），统一成字符串列表。调用关系：`ingest` 写 `DocumentRecord.tags`。

#### 函数 `_document_permission(metadata: Mapping[str, Any]) -> str`

作用：读 `metadata["permission"]`，不在 `PERMISSIONS = ("private","shared","public")` 内一律回落 `"private"`——**失败关闭**：未知权限按「留在本机」处理（D9）。调用关系：`ingest` 写 `DocumentRecord.permission`。

#### 函数 `_hybrid_enabled() -> bool`

作用：读 `HELLOAGENTS_MEMORY_HYBRID`，`0/false/off/no`（去空白、小写）视为关闭，其余（含未设置）默认开启。调用关系：`hybrid_retrieve`。

#### 函数 `_rrf_fuse(rank_lists: list[list[str]], *, k: int = 60) -> list[tuple[str, float]]`

作用：Reciprocal Rank Fusion——只按名次计分（`sum(1/(k+rank))`），避免余弦相似度与 BM25 两套量纲混算；按融合分降序、id 升序返回。调用关系：`hybrid_retrieve`。

#### 函数 `_chunk_metadata(chunk: ChunkRecord) -> dict[str, Any]`

作用：把 `ChunkRecord` 投影为检索块元数据 `{document_id, chunk_index, char_start, char_end}`。调用关系：`hybrid_retrieve` 构造 `RetrievedChunk`。

#### 函数 `_accepts_graph_context(extractor: KnowledgeExtractor) -> bool`

作用：检查 `extractor.extract` 的签名是否含 `graph_context` 或 `**kwargs`，用于兼容旧两参数抽取器。参数为抽取器；返回布尔值；`inspect.signature` 的 `TypeError/ValueError` 会转为 `False`；无副作用。调用关系：`RAGPipeline.ingest` 在循环前调用一次。

#### 类 `RAGPipeline` — 记忆库上的 RAG 总管道

- **构造**：`__init__(self, manager: MemoryManager | None = None, *, processor: DocumentProcessor | None = None, extractor: KnowledgeExtractor | None = None, auto_extract: bool = True) -> None` — 未提供时分别创建 `MemoryManager()`、`DocumentProcessor()`、`NullKnowledgeExtractor()`；保存 `auto_extract`，创建 `GraphRAGPipeline(self.manager)`，初始化空 `last_ingest_report`、`last_retrieval_note` 和惰性 `_repository`。
- **关键属性**：`manager`、`processor`、`extractor`、`auto_extract`、`graph`、`last_ingest_report`、`last_retrieval_note`、`_repository`（缓存的真值源仓储）。

##### `document_repo(self) -> DocumentRepository | None`

作用：返回 `documents/chunks` 真值源仓储；按 `manager.document_store.path` 惰性创建（同一文件复用、不重复开库）。**两种情形返回 `None` 而不是让摄取失败**：注入的文档库不是 SQLite 后端（没有 `path`），或 `:memory:`（再开一个内存库就是与要镜像的库毫不相干的私有连接）。调用关系：`ingest`、`retrieve`、`hybrid_retrieve`、`delete_document` 以及 `web.app` 的文档/对账路由。

##### `ingest(self, documents: Document | Iterable[Document], *, chunk_size: int = RAG_CHUNK_SIZE, overlap: int = RAG_CHUNK_OVERLAP) -> list[MemoryItem]`

作用：接受单个 `Document` 或可迭代文档；把每个文档切块、**先写 documents/chunks 真值源**、再写入 `SEMANTIC` 记忆、按配置抽取知识并落图补丁。返回所有写入的知识块 `MemoryItem` 列表。

报告字段：`chunks/domains/entities/relations/superseded/retracted/skipped_relations/errors`；领域最后去重。单次调用只创建一个 `EntityResolver`，并通过 `_accepts_graph_context` 选择是否向抽取器传 `graph_context`。真值源写入顺序：先 `upsert_document`（`raw_text=normalized_text(document)`、tags 经 `_document_tags`、permission 经 `_document_permission`、状态 `parsed`），再逐块 `upsert_chunk`（含 `char_start/char_end`）——**先写真值源再写向量**，避免向量成功而真值行失败留下孤立向量。`try` 从 `build_graph_context` 之前开始，因此上下文构建、抽取和 materialize 的异常只追加 `类型: 消息` 到 `errors`，并累计 `document_error`（有错误时文档状态置 `failed` + error，否则置 `extracted`（auto_extract）或 `vectorized`）；`processor.chunks_with_spans` 和 `manager.add` 位于该 `try` 之前，相关异常会直接向上抛。副作用是批量写真值源、语义记忆和图。调用关系：`ingest_source`、Web `/api/knowledge`、`support.extract_graph_patches` 使用。

##### `ingest_source(self, source: str | Path, *, base_dir: str | Path | None = None, **kwargs: Any) -> list[MemoryItem]`

作用：把 `source` 先转 `Path`，若给 `base_dir` 则用 `resolve_within` 做收容；从 `kwargs` 弹出 `metadata` 交给 `DocumentProcessor.parse(Path, metadata=...)`，剩余关键字参数原样转发 `ingest`。参数为路径、可选基目录和摄取参数。返回块列表。

异常：`Path` 构造、路径越界、文件读取/解析、剩余 `ingest` 参数校验异常向上抛；`metadata` 不会继续传给 `ingest`。调用关系：Web `/api/ingest`，其 metadata 包含上传文件名和块参数。

##### `retrieve(self, query: str, *, limit: int = RAG_RETRIEVE_LIMIT, threshold: float | None = None, metadata: Mapping[str, Any] | None = None) -> list[RetrievedChunk]`

作用：封装 `manager.search(...)`（固定 `MemoryType.SEMANTIC`）；每条结果**以 chunks 真值源回查正文**——同一 id 的内容可能已被修正，`repository.get_chunk(result.item.id)` 命中就用真值源正文，查不到才用 `from_result` 的 memories 正文。参数为查询、上限、阈值、元数据过滤；返回列表；搜索异常向上抛，无写入。调用关系：`build_context`、`answer`、`hybrid_retrieve` 的关闭分支。

##### `_vector_hits(self, query: str, *, limit: int, threshold: float | None, metadata: Mapping[str, Any] | None) -> list[tuple[str, float]]`

作用：混合检索的向量路。嵌入实例有 `base_url` 且 `gateway_reachable` 为假时，直接返回空表并写 `last_retrieval_note`（网关不可达降级）；`manager.search` 抛 `ConnectionError/OSError/RuntimeError` 时同样降级并记录原因。成功返回 `(chunk_id, score)` 列表。**绝不切换到别的向量空间**（D8）。调用关系：`hybrid_retrieve`。

##### `hybrid_retrieve(self, query: str, *, limit: int = RAG_RETRIEVE_LIMIT, threshold: float | None = None, metadata: Mapping[str, Any] | None = None) -> list[RetrievedChunk]`

作用：混合检索总入口——`_hybrid_enabled()` 为假或真值源不可用时退回 `retrieve`（纯向量）；否则两路召回（向量路 `limit*2` + FTS5 关键词路 `repository.search_keywords`），`_rrf_fuse` 融合后取前 `limit`；对每条命中以真值源构造 `RetrievedChunk`，`detail` 带 `rrf_score/vector_score/keyword_score`（融合前各存一份，RRF 只留名次、原始分数供 UI 溯源）。**真值源没有的分块不返回**（孤立向量不外泄）。向量不可用时退化为纯关键词（U4/D8）。调用关系：`web.app` `/api/chat` 的 `retrieval` 报告。

##### `build_context(self, query: str, *, limit: int = RAG_RETRIEVE_LIMIT, separator: str = "\n\n") -> str`

作用：校验 `separator` 为字符串，然后检索并由分隔符连接正文。参数为查询、上限、分隔符；返回上下文文本；非字符串分隔符抛 `TypeError("separator must be a string")`。调用关系：`answer`。

##### `graph_retrieve(self, query: str, *, limit: int = RAG_RETRIEVE_LIMIT, hops: int = RAG_GRAPH_HOPS) -> GraphRAGResult`

作用：把参数转给 `self.graph.retrieve`。参数为查询、证据上限、跳数；返回 `GraphRAGResult`；校验和检索异常由下层抛出；无额外副作用。调用关系：Web `/api/graph-rag`、`/api/chat`。

##### `graph_context(self, query: str, *, limit: int = RAG_RETRIEVE_LIMIT, hops: int = RAG_GRAPH_HOPS, max_chars: int = RAG_CONTEXT_MAX_CHARS) -> str`

作用：调用 `self.graph.build_context` 返回混合图上下文文本。参数为查询、证据上限、跳数、最大字符数；异常和副作用沿用图检索层。调用关系：需要给回答器注入图上下文的调用方。

##### `answer(self, query: str, generator: Callable[[str], str], *, limit: int = RAG_RETRIEVE_LIMIT) -> str`

作用：验证 `generator` 可调用；检索普通上下文；构造 `"Context:\n...\n\nQuestion: ...\nAnswer:"` 提示词；返回 `str(generator(prompt))`。参数为查询、生成函数、检索上限；`generator` 不可调用时抛 `TypeError("generator must be callable")`；生成器异常向上抛。无 RAG 写入副作用。调用关系：面向需要自行提供生成函数的调用方。

##### `delete_document(self, document_id: str) -> int`

作用：列出 `SEMANTIC` 且 `include_expired=True` 的记忆，删除所有 `metadata.document_id == document_id` 的项，返回成功删除数；随后**同步删除真值源**（`repository.delete_document`），否则 documents/chunks 会留下永远查不到来源的孤儿行。参数为文档 id；存储异常向上抛；副作用是不可逆删除。调用关系：源码中未在 Web 路由直接暴露，但可作为管道 API 使用。

##### `close(self) -> None`

作用：先关闭 `_repository`（若有）并置空，再调用 `self.manager.close()` 关闭共享管理器。无参数；返回 `None`；关闭异常向上抛。调用关系：拥有该管理器生命周期的调用方；Web 应用实际通过 `support.close_manager()` 管理全局单例。

## L8 记忆工具公共后端装配

### `tool/_memory.py` — 记忆工具公共 schema 与默认后端装配

- **拓扑层 / 工具名 / side_effect**：L8 公共后端装配模块；非可发现工具模块；无工具名、无 `side_effect`、无 `TOOL_ENABLED`、无 `create_tool()`。
- **规模**：93 行 / 4 个 `def`。
- **职责**：提供记忆工具共用的 metadata schema、metadata 归一化函数、默认 `MemoryManager` 构造器和默认 `RAGPipeline` 构造器。导入本模块只绑定日志器、类型和函数，不打开数据库、不发起网络请求；后端在首次访问工具属性或显式调用构造器时才建立。`build_default_manager` 与 `web.support.get_manager` 同一口径：先 `MemoryConfig.from_env()`（`HELLOAGENTS_MEMORY_*` 控制 Qdrant/Neo4j），再把 `sqlite_path` 覆盖为 `default_sqlite_path()`（`MEMORY_DB_PATH` 仍优先）。
- **关键常量/模块级单例**：模块级 `LOGGER = logging.getLogger(__name__)`；`MemoryScope` 四层字面量类型。本文件没有缓存式单例、锁或全局 manager/pipeline 实例。`build_default_manager()` 和 `build_default_pipeline()` 每次调用都会新建对象；Web 层的进程级共享由 `web.support.get_manager()` / `get_pipeline()` 另行负责。
- **模块接口**：`__all__` 导出 `LOGGER`、`MemoryMetadata`、`MemoryScope`、`build_default_manager`、`build_default_pipeline`、`metadata_dict`、`normalize_metadata_payload`。

#### 类 `MemoryMetadata(BaseModel)` — 单个 metadata 键值对

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 约束/含义 |
|---|---|---|---|
| `key` | `str` | 必填 | `Field(min_length=1)`；metadata 键，不允许空字符串。 |
| `value` | `str` | 必填 | metadata 值；严格模式下要求字符串。友好的 `{key: value}` mapping 会先由归一化函数把值转为字符串。 |

本类源码中没有自定义 `def`。已验证的额外字段会被拒绝，字段类型按严格模式校验。

#### 函数 `def normalize_metadata_payload(value: Any) -> Any:` — 归一化友好 metadata 形式

- 当 `value` 是 `dict` 且 `value.get("metadata")` 也是 `dict` 时，先做浅拷贝，再把 `metadata` 替换为 `[{"key": key, "value": str(item)}, ...]`。
- 当条件不满足时原样返回，不做递归归一化。
- 该函数只转换值；mapping 的键不会被转成字符串，后续仍由 Pydantic 严格校验。
- 不修改原始输入字典，替换 `metadata` 前先执行 `value = dict(value)`。

#### 函数 `def metadata_dict(entries: list[MemoryMetadata] | None) -> dict[str, str]:` — 展平 metadata

- `None` 或空列表返回 `{}`。
- 使用字典推导式把每个 `MemoryMetadata` 的 `key`/`value` 转成映射。
- 没有显式重复键检查；重复键按 Python 字典规则由后出现项覆盖前项。

#### 函数 `def build_default_manager() -> MemoryManager:` — 构造默认记忆管理器

- 先调用 `MemoryConfig.from_env()`，让 `HELLOAGENTS_MEMORY_*` 环境变量生效（Qdrant/Neo4j 连接开关也在这里被读取）。
- 再把 `config.sqlite_path` 覆盖为 `default_sqlite_path()`；其结果为环境变量 `MEMORY_DB_PATH` 的值，若未设置则为项目相对文件名 `memory.sqlite3`——即 SQLite 路径仍以 `MEMORY_DB_PATH` 优先，与 `web.support.get_manager` 保持一致。
- 未显式传入 embedding、document store、vector store 或 graph store，因此由 `MemoryManager` 按自身配置选择默认后端（未配置 Qdrant/Neo4j 时走内存回退）。
- 每次调用都新建 `MemoryManager`，本函数自身不缓存连接。

#### 函数 `def build_default_pipeline() -> RAGPipeline:` — 构造默认 RAG 管道和抽取器

- 先创建 `NullKnowledgeExtractor()` 作为降级默认值。
- 在 `try` 中导入 `agents.llm.LLM` 和 `agents.providers.ProviderRegistry`，取得当前 provider profile，并解析 API key。
- 仅当 key 存在且不以 `"replace-with"` 开头时，创建 `LLM(api_key=key, base_url=profile.base_url, model=profile.default_model)`，再用 `LLMKnowledgeExtractor(client.complete, model=profile.default_model)` 替换空抽取器。
- 捕获任何异常后记录 warning，异常栈写入日志，继续使用 `NullKnowledgeExtractor`，使文档仍可入库并做向量检索。
- 最终返回 `RAGPipeline(build_default_manager(), extractor=extractor)`。
- 源码边界：模块 docstring 说“missing key / placeholder key ... records why in the log”，但当前代码只在异常分支写日志；key 缺失或 `replace-with` 前缀分支只是静默保留空抽取器，没有单独日志记录。

---

## L9 五个记忆与知识工具

### `tool/memory_add.py` — 单条记忆写入工具 `memory.add`

- **拓扑层 / 工具名 / side_effect**：L9 最上层工具；`memory.add`；`side_effect="write"`。
- **规模**：110 行 / 5 个 `def`。
- **职责**：只负责“记住一条新内容”，与删除/清空等管理写入拆分，使调用方可以只授权窄范围增量写入。默认延迟创建 `MemoryManager`。
- **关键常量/模块级单例**：`TOOL_ENABLED = True`；没有模块级 manager 单例、锁或缓存；默认库路径由 `_memory.build_default_manager()` 决定。

#### 类 `MemoryAddInput(BaseModel)` — 输入契约

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 约束/含义 |
|---|---|---|---|
| `content` | `str` | 必填 | `min_length=1`；待记忆文本。 |
| `memory_type` | `MemoryScope` | `"working"` | 写入层。持久用户事实/偏好在说明中建议放 episodic 或 semantic；working 是默认且更短期。 |
| `item_id` | `str \| None` | `None` | 可选自定义 id；用于确定性 id 或覆盖同层已有项。 |
| `metadata` | `list[MemoryMetadata] \| None` | `None` | 要写入的键值 metadata。 |
| `importance` | `float` | `0.5` | `ge=0, le=1`；记忆重要度。 |
| `ttl_seconds` | `float \| None` | `None` | `gt=0`；显式生存时间。 |

类方法：

- `@model_validator(mode="before")`
  `@classmethod`
  `def normalize_metadata(cls, value: Any) -> Any:`
  校验前调用 `normalize_metadata_payload`，允许模型输出 `{ "metadata": { "k": "v" } }` 的友好形式；值会被转成字符串，非 mapping 形式继续走严格的 `MemoryMetadata` 列表校验。

#### 类 `MemoryAddOutput(BaseModel)` — 输出契约

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 含义 |
|---|---|---|---|
| `action` | `str` | `"add"` | 固定写入动作。 |
| `count` | `int` | `0` | 成功写入时返回 `1`。 |
| `items` | `list[dict[str, Any]]` | `[]` | 写入后的完整 `MemoryItem.to_dict()`。 |

#### 类 `MemoryAddTool(BaseTool)` — 工具实现

##### `spec`

| 字段 | 值 | 说明/理由 |
|---|---|---|
| `name` | `"memory.add"` | 窄范围增量写入工具名。 |
| `description` | `"Store one new item in the agent memory system."` | 明确只存储一个条目。 |
| `version` | `"1.0.0"` | 当前实现版本。 |
| `input_model` | `MemoryAddInput` | 输入契约。 |
| `output_model` | `MemoryAddOutput` | 输出契约。 |
| `side_effect` | `"write"` | 运行时要求确认。 |
| `permissions` | `("memory.write",)` | 写权限分类元数据。 |
| `timeout` | `timeout_seconds=10.0` | 单条写入的超时。 |
| `idempotent` | `False` | 默认生成新 UUID；重复调用通常产生不同条目。 |
| `parallel_safe` | `False` | 对同一存储的写操作声明为不可安全并行。 |
| `max_concurrency` | 未显式传入，有效值 `None` | 使用 `ToolSpec` 默认。 |
| `tags` | `("memory", "storage", "write")` | 写入分类标签。 |
| `recommended_before_tools` | 未显式传入，有效值 `()` | 没有建议前置工具。 |

##### 方法

- `def __init__(self, manager: MemoryManager | None = None) -> None:`
  保存注入的 manager，不主动建连；未注入时保持懒加载。

- `@property`
  `def manager(self) -> MemoryManager:`
  首次访问时构造并缓存默认 manager。

- `def execute(self, arguments: MemoryAddInput) -> MemoryAddOutput:`
  调用 `manager.add(...)`，参数映射为：
  - `content=arguments.content`
  - `memory_type=MemoryType(arguments.memory_type)`
  - `metadata=metadata_dict(arguments.metadata)`
  - `importance=arguments.importance`
  - `ttl_seconds=arguments.ttl_seconds`
  - `item_id=arguments.item_id`
  成功后返回 `MemoryAddOutput(action="add", count=1, items=[item.to_dict()])`。写入异常不在本工具内捕获或改写，会向运行时传播。

##### metadata、记忆层、`item_id`、`importance`、`ttl_seconds` 的完整语义

- **metadata 归一化**：模型输入若是顶层 dict 且 `metadata` 是 dict，`normalize_metadata_payload` 将其转成 `MemoryMetadata` 列表；`metadata_dict` 再展平为 `dict[str, str]`，传给 manager。普通列表输入不经过 mapping 转值，必须严格满足 `key/value` 都是字符串。重复键由后者覆盖前者。
- **记忆层选择**：`memory_type` 直接决定 `MemoryType`，没有自动分类或自动改层逻辑。默认 `working`。字段说明建议持久用户事实放 `episodic` 或 `semantic`；这不是执行期的强制规则。
- **`item_id`**：`None` 时由 `BaseMemory.add` 生成 UUID 字符串；非空时作为指定 id。若该 id 已存在于同层，底层执行 upsert/覆盖；若已存在于其他层，则抛出 `ValueError`，提示该 id 属于另一层。工具自身不检查 id 是否存在。
- **`importance`**：写入 `MemoryItem.importance`，普通记忆中主要是元数据；working memory 超过容量时会优先淘汰 importance 更低者，并列时更早更新者优先。
- **`ttl_seconds`**：显式大于 0 时由底层把 `expires_at` 设为当前 UTC 时间加该秒数。未提供时，working memory 使用 `MemoryConfig.default_ttl_seconds`（源码常量默认 3600 秒）；其他层默认不设置 TTL。字段说明中的“working expires with session”是设计表述；实际实现是默认 TTL 加 working capacity 淘汰，并没有把 TTL 绑定到某个 session 对象。

#### 函数 `def create_tool() -> BaseTool:` — 工具工厂

零参数创建并返回 `MemoryAddTool()`，不注入 manager。

---

### `tool/memory_query.py` — 只读查询工具 `memory.query`

- **拓扑层 / 工具名 / side_effect**：L9 最上层工具；`memory.query`；`side_effect="read"`。
- **规模**：127 行 / 5 个 `def`。
- **职责**：提供 `search`、`get`、`list` 三种只读读取动作，是 `memory.add` / `memory.manage` 的读取侧配套。默认延迟创建 `MemoryManager`，不会在导入或工具发现时打开 SQLite。
- **关键常量/模块级单例**：`TOOL_ENABLED = True`；没有模块级 manager 单例、锁或缓存；`MemoryQueryTool.manager` 是实例级懒加载属性。默认库由 `_memory.build_default_manager()` 决定为 `MEMORY_DB_PATH` 或 `memory.sqlite3`。

#### 类 `MemoryQueryInput(BaseModel)` — 输入契约

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 约束/含义 |
|---|---|---|---|
| `action` | `Literal["search", "get", "list"]` | 必填 | 查询动作。 |
| `memory_type` | `MemoryScope \| None` | `None` | `search` 为 `None` 时跨 working/episodic/semantic/perceptual 四层搜索；`get`/`list` 为 `None` 时工具内映射为 `working`。 |
| `item_id` | `str \| None` | `None` | `get` 所需的记忆项 id。 |
| `query` | `str \| None` | `None` | `search` 所需的查询文本。 |
| `metadata` | `list[MemoryMetadata] \| None` | `None` | 精确 metadata 过滤项。 |
| `limit` | `int` | `10` | `ge=1, le=100`；`search` 直接限制后端结果数，`list` 在取回后做切片，`get` 不使用。 |

类方法：

- `@model_validator(mode="before")`
  `@classmethod`
  `def normalize_metadata(cls, value: Any) -> Any:`
  在模型校验前调用 `normalize_metadata_payload(value)`，把常见的 `{"metadata": {"k": "v"}}` 转成 `MemoryMetadata` 可接收的列表形式。它本身不校验 `action` 与 `query`/`item_id` 的组合，这些组合由 `execute` 在运行时检查。

#### 类 `MemoryQueryOutput(BaseModel)` — 输出契约

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 含义 |
|---|---|---|---|
| `action` | `str` | 必填 | 回显输入动作。 |
| `count` | `int` | `0` | 本次返回的 `items` 数量。 |
| `items` | `list[dict[str, Any]]` | `[]` | `search` 返回 `MemorySearchResult.to_dict()`，因此每项比普通项多一个 `score`；`get` 最多一项；`list` 为层内条目。 |

#### 类 `MemoryQueryTool(BaseTool)` — 工具实现

##### `spec`

| 字段 | 值 | 说明/理由 |
|---|---|---|
| `name` | `"memory.query"` | 命名空间化工具名。 |
| `description` | `"Search, inspect, or list what is stored in the agent memory system. Read-only: never modifies memory."` | 声明三种读取用途。 |
| `version` | `"1.0.0"` | 当前实现版本。 |
| `input_model` | `MemoryQueryInput` | 输入契约。 |
| `output_model` | `MemoryQueryOutput` | 输出契约。 |
| `side_effect` | `"read"` | 绕过运行时写确认，允许 Agent 自主检索。 |
| `permissions` | `("memory.read",)` | 读取权限分类元数据。 |
| `timeout` | `timeout_seconds=10.0` | 短查询超时。 |
| `idempotent` | `True` | 正常情况下同一查询可重复执行。 |
| `parallel_safe` | `True` | 声明可并行读取。 |
| `max_concurrency` | 未显式传入，有效值 `None` | 使用 `ToolSpec` 默认；源码未声明专门上限。 |
| `tags` | `("memory", "search", "read")` | 用于分类和检索。 |
| `recommended_before_tools` | 未显式传入，有效值 `()` | 源码没有建议前置工具。 |

##### 方法

- `def __init__(self, manager: MemoryManager | None = None) -> None:`
  只保存 `self._manager = manager`；没有打开数据库，支持测试或 Web 层注入共享 manager。

- `@property`
  `def manager(self) -> MemoryManager:`
  首次访问时若 `self._manager is None`，调用 `build_default_manager()` 并缓存到实例；之后返回同一 manager。

- `def execute(self, arguments: MemoryQueryInput) -> MemoryQueryOutput:`
  按 `action` 执行：
  - `search`：要求 `arguments.query is not None`，否则抛 `ValueError("query is required for search")`。调用 `manager.search(query, memory_type=arguments.memory_type, limit=..., metadata=...)`。因为这里直接传原始 `scope`，未指定 `memory_type` 时传 `None`，`MemoryManager.search` 会逐层搜索并合并、排序、截断，确实跨四层。结果由 `MemorySearchResult.to_dict()` 转出，含 `score`。
  - `get`：要求 `item_id`，否则抛 `ValueError("item_id is required for get")`。工具先把 `None` scope 映射为 `MemoryType.WORKING`，再调用 `manager.get(item_id, memory_type=working)`。因此不指定 `memory_type` 时不能按 id 直接取 episodic/semantic/perceptual 项。找到返回单元素列表，否则 `[]`。
  - `list`：同样把 `None` scope 映射为 working，调用 `manager.list(memory_type=working)`，然后只对返回值做 `[:limit]` 截断。`include_expired` 未暴露，采用默认 `False`。
  - 最终返回 `MemoryQueryOutput(action=action, count=len(items), items=items)`。
  - 声明为 read 不要求确认；但底层 `get`/`search` 的过期清理路径可能删除已过期条目，这是“读取”实现中的一个存储层边界。

#### 函数 `def create_tool() -> BaseTool:` — 工具工厂

零参数创建并返回 `MemoryQueryTool()`，不注入 manager，所以 manager 保持懒加载，使用 `_memory.py` 的默认路径。这是发现/注册协议的工厂。

---

### `tool/memory_tool.py` — 管理动作工具 `memory.manage`

- **拓扑层 / 工具名 / side_effect**：L9 最上层工具；`memory.manage`；`side_effect="write"`。
- **规模**：99 行 / 4 个 `def`。
- **职责**：执行删除单条记忆或清空一个记忆层的破坏性管理动作。与 `memory.add` 分离，Web 聊天不会为它预置确认，因此模型不能沿聊天确认链路自行删除或清空记忆。
- **关键常量/模块级单例**：`TOOL_ENABLED = True`；没有模块级 manager 单例、锁或缓存；manager 为实例内懒加载。

#### 类 `MemoryManageInput(BaseModel)` — 输入契约

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 约束/含义 |
|---|---|---|---|
| `action` | `Literal["delete", "clear"]` | 必填 | 管理动作集合只有删除和清空。 |
| `memory_type` | `MemoryScope \| None` | `None` | 目标层；工具内把 `None` 映射为 `working`。 |
| `item_id` | `str \| None` | `None` | `delete` 所需 id。 |

本文件没有自定义输入 validator；不存在 metadata 字段或 metadata 归一化方法。

#### 类 `MemoryManageOutput(BaseModel)` — 输出契约

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 含义 |
|---|---|---|---|
| `action` | `str` | 必填 | 回显 `"delete"` 或 `"clear"`。 |
| `count` | `int` | `0` | 删除成功返回 1，删除未命中返回 0；清空返回实际删除数量。 |
| `items` | `list[dict[str, Any]]` | `[]` | 本工具始终返回空列表。 |

#### 类 `MemoryManageTool(BaseTool)` — 工具实现

##### `spec`

| 字段 | 值 | 说明/理由 |
|---|---|---|
| `name` | `"memory.manage"` | 管理动作工具名。 |
| `description` | `"Delete one memory item or clear an entire memory layer. Destructive; requires explicit user confirmation."` | 明确破坏性和确认要求。 |
| `version` | `"2.0.0"` | 当前版本；背景说明该工具曾与读写动作拆分。 |
| `input_model` | `MemoryManageInput` | 输入契约。 |
| `output_model` | `MemoryManageOutput` | 输出契约。 |
| `side_effect` | `"write"` | 运行时要求确认。 |
| `permissions` | `("memory.write",)` | 管理写入权限分类。 |
| `timeout` | `timeout_seconds=10.0` | 删除/清空操作超时。 |
| `idempotent` | `False` | 重复执行的状态和返回计数不能保证一致。 |
| `parallel_safe` | `False` | 明确声明为不可安全并行。 |
| `max_concurrency` | 未显式传入，有效值 `None` | 使用默认。 |
| `tags` | `("memory", "storage", "admin")` | 管理类标签。 |
| `recommended_before_tools` | 未显式传入，有效值 `()` | 没有建议前置工具。 |

##### 方法

- `def __init__(self, manager: MemoryManager | None = None) -> None:`
  保存 manager，不建连；允许注入测试或共享后端。

- `@property`
  `def manager(self) -> MemoryManager:`
  首次访问时懒加载默认 manager。

- `def execute(self, arguments: MemoryManageInput) -> MemoryManageOutput:`
  先把 `arguments.memory_type or "working"` 转成 `MemoryType`，所以 `memory_type=None` 永远只作用于 working，不会清空全库。随后：
  - `delete`：要求 `item_id`，否则抛 `ValueError("item_id is required for delete")`；调用 `manager.delete(item_id, memory_type=memory_type)`，把布尔结果转成 `int`。
  - `clear`：调用 `manager.clear(memory_type=memory_type)`；`BaseMemory.clear` 会遍历该层，包括过期项，逐项删除向量和文档记录。
  - 返回 `MemoryManageOutput(action=action, count=count, items=[])`。

##### 破坏性与确认语义

- 动作集合严格为 `{delete, clear}`，不支持 update、move、rename 或其他管理动作。
- `delete` 只删除指定层内命中的单个 id；id 不属于该层时返回 0。
- `clear` 会清空调用者显式指定的一个层；省略 `memory_type` 时只清 working。
- 工具 schema 没有“清空全部四层”的单一开关；要清空所有层必须由外部调用方分别对四层执行，或使用其他未在本工具契约中的管理路径。
- 删除/清空没有撤销、回收站或软删除语义；对于持久层是直接破坏性操作。
- `side_effect="write"`，调用方必须提供 `registry.confirmation_key("memory.manage")`。Web 聊天只自动确认 `memory.add`，所以本工具在该路径不会被自动放行。

#### 函数 `def create_tool() -> BaseTool:` — 工具工厂

零参数创建并返回 `MemoryManageTool()`。

---

### `tool/rag_search.py` — 只读 RAG 检索工具 `memory.rag_search`

- **拓扑层 / 工具名 / side_effect**：L9 最上层工具；`memory.rag_search`；`side_effect="read"`。
- **规模**：125 行 / 4 个 `def`。
- **职责**：对已入库知识执行向量检索、图检索、上下文装配或图上下文装配。它不负责入库，与写入工具 `memory.rag` 分离。
- **关键常量/模块级单例**：`TOOL_ENABLED = True`；没有模块级 pipeline 单例、锁或缓存；`RAGSearchTool.pipeline` 是实例级懒加载属性。默认 pipeline 由 `_memory.build_default_pipeline()` 构造。

#### 类 `RAGSearchInput(BaseModel)` — 输入契约

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 约束/含义 |
|---|---|---|---|
| `action` | `Literal["retrieve", "context", "graph_retrieve", "graph_context"]` | 必填 | 四种检索输出模式。 |
| `query` | `str` | 必填 | `min_length=1`；查询文本，所有 action 都使用。 |
| `limit` | `int` | `5` | `ge=1, le=50`；向量/图证据条数上限。 |
| `hops` | `int` | `1` | `ge=0, le=3`；仅 `graph_retrieve` / `graph_context` 使用。 |

#### 类 `RAGSearchOutput(BaseModel)` — 输出契约

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 含义 |
|---|---|---|---|
| `action` | `str` | 必填 | 回显动作。 |
| `context` | `str` | `""` | `context` / `graph_context` 的拼装文本，或 `graph_retrieve` 的图上下文。 |
| `items` | `list[dict[str, Any]]` | `[]` | `retrieve` 的向量片段；`graph_retrieve` 的图证据。 |
| `count` | `int` | `0` | 对 `retrieve` 是片段数；对 `graph_retrieve` 是证据数；对 context 类动作非空为 1、空为 0。 |
| `entities` | `list[str]` | `[]` | 图检索命中的种子实体名。 |
| `paths` | `list[dict[str, Any]]` | `[]` | 图路径对象序列化结果。 |

#### 类 `RAGSearchTool(BaseTool)` — 工具实现

##### `spec`

| 字段 | 值 | 说明/理由 |
|---|---|---|
| `name` | `"memory.rag_search"` | RAG 只读检索工具名。 |
| `description` | `"Retrieve vector matches, graph facts, or a ready-made context block from stored knowledge. Read-only: never ingests."` | 明确只读、不入库。 |
| `version` | `"1.0.0"` | 当前实现版本。 |
| `input_model` | `RAGSearchInput` | 输入契约。 |
| `output_model` | `RAGSearchOutput` | 输出契约。 |
| `side_effect` | `"read"` | 不要求写确认，使 Agent 可自主检索。 |
| `permissions` | `("memory.read",)` | 读取权限分类。 |
| `timeout` | `timeout_seconds=30.0` | 向量/图检索可能较慢，给出更长超时。 |
| `idempotent` | `True` | 相同查询在底层数据不变时可重复。 |
| `parallel_safe` | `True` | 声明可并行检索。 |
| `max_concurrency` | 未显式传入，有效值 `None` | 使用默认。 |
| `tags` | `("memory", "rag", "retrieval", "read")` | RAG 读取标签。 |
| `recommended_before_tools` | 未显式传入，有效值 `()` | 没有建议前置工具。 |

##### 方法

- `def __init__(self, pipeline: RAGPipeline | None = None) -> None:`
  仅保存注入的 `RAGPipeline`，不打开数据库或构造后端。

- `@property`
  `def pipeline(self) -> RAGPipeline:`
  首次访问时调用 `build_default_pipeline()` 并缓存到实例。

- `def execute(self, arguments: RAGSearchInput) -> RAGSearchOutput:`
  读取 `query`，按 action 分支：
  - `retrieve`：调用 `pipeline.retrieve(query, limit=limit)`；返回 `count=len(values)`，`items` 的每项只包含 `content`、`score`、`memory_id`、`metadata`。不使用 `hops`。底层是 semantic 层向量检索。
  - `graph_retrieve`：调用 `pipeline.graph_retrieve(query, limit=limit, hops=hops)`；返回 `count=len(result.evidence)`，`items=[item.to_dict() for item in result.evidence]`，`entities=result.entities`，`paths=[path.to_dict() for path in result.paths]`，`context=result.build_context()`。`items` 是带 `score` 的完整记忆项字典；`context` 由图证据和关系路径拼装，默认最大 12000 字符。
  - `graph_context`：调用 `pipeline.graph_context(query, limit=limit, hops=hops)`；只返回 `context`，`count=1 if context else 0`；`items/entities/paths` 保持空。
  - 其余合法 action 为 `context`：调用 `pipeline.build_context(query, limit=limit)`；把语义检索片段用 `"\n\n"` 拼接成字符串，只返回 `context`，`count=1 if context else 0`；`items/entities/paths` 保持空。
  - `action` 是 Pydantic `Literal`，未知动作在参数校验阶段被拒绝。

##### 四种 action 的输出差异

| action | `items` | `context` | `entities` | `paths` | `count` |
|---|---|---|---|---|---|
| `retrieve` | 向量片段：`content/score/memory_id/metadata` | `""` | `[]` | `[]` | 片段数 |
| `context` | `[]` | 片段内容按段落拼接 | `[]` | `[]` | 非空 1，否则 0 |
| `graph_retrieve` | 图证据 `MemorySearchResult.to_dict()` | 图证据+关系路径拼装 | 种子实体列表 | 图路径字典列表 | 证据数 |
| `graph_context` | `[]` | 图上下文拼装字符串 | `[]` | `[]` | 非空 1，否则 0 |

补充边界：

- `retrieve`/`context` 只检索 semantic 记忆层；图动作也先取 semantic 证据，再扩展图关系。
- `limit` 范围为 1 到 50，默认 5；图路径自身还有底层默认 `path_limit=20`，工具未暴露该参数。
- `hops` 范围为 0 到 3，仅图动作用到。
- 工具没有把 pipeline 的 `metadata` 过滤参数暴露给模型。
- 声明为 read；底层语义搜索的 `get` 路径仍可能清理过期项，与只读声明存在存储层边界。

#### 函数 `def create_tool() -> BaseTool:` — 工具工厂

零参数创建并返回 `RAGSearchTool()`。

---

### `tool/rag_tool.py` — RAG 入库工具 `memory.rag`

- **拓扑层 / 工具名 / side_effect**：L9 最上层工具；`memory.rag`；`side_effect="write"`。
- **规模**：113 行 / 4 个 `def`。
- **职责**：把字面文本或工作区内的文件送入 RAG pipeline，切块写入 semantic memory，并在 pipeline 配置了可用抽取器时抽取知识实体和关系。读取由 `memory.rag_search` 单独承担。
- **关键常量/模块级单例**：`TOOL_ENABLED = True`；没有模块级 pipeline 单例、锁或缓存；pipeline 为实例内懒加载。`source` 通过 `tool._shared.workspace_root()` 和 `resolve_path()` 解析。

#### 类 `RAGToolInput(BaseModel)` — 输入契约

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 约束/含义 |
|---|---|---|---|
| `action` | `Literal["ingest"]` | `"ingest"` | 当前只有入库动作。 |
| `text` | `str \| None` | `None` | 字面文本；与 `source` 二选一。 |
| `source` | `str \| None` | `None` | 工作区相对/受控路径；与 `text` 二选一，路径经沙箱解析。 |
| `chunk_size` | `int` | `1000` | `ge=1, le=100000`；切块长度。 |
| `overlap` | `int` | `100` | `ge=0`；块重叠长度。工具 schema 没有显式要求 `overlap < chunk_size`，但底层 `DocumentProcessor.chunks` 会检查并要求该条件。 |

类中没有自定义 validator；`text`/`source` 的互斥与必填关系在 `execute` 中运行期校验。

#### 类 `RAGToolOutput(BaseModel)` — 输出契约

`model_config = ConfigDict(extra="forbid", strict=True)`。

| 字段 | 类型 | 默认 | 含义 |
|---|---|---|---|
| `action` | `str` | 必填 | 固定 `"ingest"`。 |
| `count` | `int` | `0` | 本次创建的记忆块数量。 |
| `items` | `list[dict[str, Any]]` | `[]` | 每个入库 `MemoryItem.to_dict()`。 |
| `report` | `dict[str, Any]` | `{}` | pipeline 的 `last_ingest_report`。 |

#### 类 `RAGTool(BaseTool)` — 工具实现

##### `spec`

| 字段 | 值 | 说明/理由 |
|---|---|---|
| `name` | `"memory.rag"` | RAG 写入工具名。 |
| `description` | `"Ingest text or a workspace file into agent memory, extracting knowledge for later retrieval."` | 明确文本/文件入库和知识抽取。 |
| `version` | `"2.0.0"` | 当前版本；背景说明该工具已从组合读写工具拆分。 |
| `input_model` | `RAGToolInput` | 输入契约。 |
| `output_model` | `RAGToolOutput` | 输出契约。 |
| `side_effect` | `"write"` | 入库会改变持久知识库，要求确认。 |
| `permissions` | `("memory.write",)` | 写权限分类。 |
| `timeout` | `timeout_seconds=30.0` | 文件解析、切块和知识抽取可能较慢。 |
| `idempotent` | `False` | 重复入库可能生成新块、覆盖同 id 块或增加抽取谱系，不能声明幂等。 |
| `parallel_safe` | `False` | 声明不可安全并行。 |
| `max_concurrency` | 未显式传入，有效值 `None` | 使用默认。 |
| `tags` | `("memory", "rag", "ingest", "write")` | RAG 写入标签。 |
| `recommended_before_tools` | 未显式传入，有效值 `()` | 没有建议前置工具。 |

##### 方法

- `def __init__(self, pipeline: RAGPipeline | None = None) -> None:`
  保存 pipeline，不主动构造数据库或模型客户端。

- `@property`
  `def pipeline(self) -> RAGPipeline:`
  首次访问时调用 `build_default_pipeline()` 并缓存到实例。

- `def execute(self, arguments: RAGToolInput) -> RAGToolOutput:`
  执行流程：
  1. 若 `text is None and source is None`，抛 `ValueError("text or source is required for ingest")`。
  2. 若两者都非空，抛 `ValueError("provide either text or source, not both")`。
  3. 文本分支：`Document(arguments.text)`，再调用 `pipeline.ingest(document, chunk_size=..., overlap=...)`。
  4. 文件分支：断言 `source` 已非空，调用 `resolve_path(workspace_root(), arguments.source)`，再调用 `pipeline.ingest_source(resolved, chunk_size=..., overlap=...)`。
  5. 返回 `RAGToolOutput(action="ingest", count=len(values), items=[item.to_dict() for item in values], report=self.pipeline.last_ingest_report)`。

##### `source` 的工作区沙箱解析

- `workspace_root()` 优先读取环境变量 `WORKSPACE_ROOT`；为空时使用进程当前工作目录的 resolved path。
- `resolve_path(base_dir, path)` 对 `base_dir` 和候选路径做 `expanduser().resolve()`；相对路径锚定在工作区根，绝对路径也必须落在工作区根内。
- 包含性检查使用规范化字符串的等值或 `base + os.sep` 前缀比较，与 `fs.*` 工具使用的工作区 containment 规则一致。
- 默认不允许越界；若部署方设置 `WORKSPACE_ALLOW_OUTSIDE=1/true/yes/on`，`resolve_path` 会允许工作区外路径。这是部署方环境开关，不是模型可通过输入字段修改的选项。
- 沙箱错误会作为执行异常向运行时传播；运行时通常把模型可见结果包装成 `EXECUTION_ERROR`，测试还验证越界文件内容不会进入知识库。
- 这里先解析并完成 containment，再把 `Path` 交给 `pipeline.ingest_source`；没有向 `ingest_source` 重复传 `base_dir`。底层 `DocumentProcessor.parse(Path)` 读取字节并按后缀解析。

##### 入库报告结构

`pipeline.ingest()` 每次调用都会初始化并最终设置 `last_ingest_report`，主要字段如下：

| 字段 | 类型 | 含义 |
|---|---|---|
| `chunks` | `int` | 本次处理的块数，与工具 `count`/`items` 长度一致。 |
| `domains` | `list[str]` | 抽取得到的领域列表；最终按出现顺序去重。 |
| `entities` | `int` | materialize 阶段创建的实体累计数。 |
| `relations` | `int` | materialize 阶段创建的关系累计数。 |
| `superseded` | `int` | 被新事实取代而标记为历史的旧事实数量。 |
| `retracted` | `int` | 被撤回/失效标记的记录数量。 |
| `skipped_relations` | `int` | 因校验或冲突而跳过的关系数量。 |
| `errors` | `list[str]` | 单块知识抽取失败的错误文本，格式为 `"{ExceptionType}: {message}"`；抽取失败不丢弃已入库源文本。 |

补充语义：

- `items` 中每一项是 semantic memory 的块，具体 metadata 由 `DocumentProcessor.chunks` 添加 `document_id`、`chunk_index`，pipeline 还会补齐 `source`。
- 文本会先经空白归一化再切块；纯空白文本可能返回零个块。
- `overlap >= chunk_size` 会由底层 `DocumentProcessor.chunks` 抛出 `ValueError`，虽然 Pydantic 输入模型自身没有这个跨字段约束。
- `report` 来自当前 pipeline 实例的最后一次 ingest；如果 pipeline 被其他并发调用共享，本字段不是按单次请求隔离的快照。该类声明 `parallel_safe=False` 以表达这一风险。

#### 函数 `def create_tool() -> BaseTool:` — 工具工厂

零参数创建并返回 `RAGTool()`。

---

##### 覆盖清单

| 文件 | 行数 | `def` 数量 | 覆盖内容 |
|---|---:|---:|---|
| `tool/_memory.py` | 93 | 4 | `normalize_metadata_payload`、`metadata_dict`、`build_default_manager`、`build_default_pipeline`。 |
| `tool/memory_query.py` | 127 | 5 | `normalize_metadata`、`MemoryQueryTool.__init__`、`manager`、`execute`、`create_tool`。 |
| `tool/memory_add.py` | 110 | 5 | `normalize_metadata`、`MemoryAddTool.__init__`、`manager`、`execute`、`create_tool`。 |
| `tool/memory_tool.py` | 99 | 4 | `MemoryManageTool.__init__`、`manager`、`execute`、`create_tool`。 |
| `tool/rag_search.py` | 125 | 4 | `RAGSearchTool.__init__`、`pipeline`、`execute`、`create_tool`。 |
| `tool/rag_tool.py` | 113 | 4 | `RAGTool.__init__`、`pipeline`、`execute`、`create_tool`。 |
| **合计** | **663** | **26** | 六个文件的全部 AST 函数/方法签名均已覆盖。 |

##### 源码边界与需注意的不确定点

- `tool/_memory.py` 的 docstring 声称缺失或占位 key 也会记录降级原因；当前实现只在异常分支记录 warning，缺失 key 与 `replace-with` 前缀分支没有单独日志。
- `ToolSpec.max_concurrency` 和 `recommended_before_tools` 在六个工具中多数未显式设置，本文按 `core/models.py` 的默认值解释为 `None` 和 `()`。
- `MemoryQueryTool` / `BaseMemory.get` 的“读取”路径会清理过期项，所以 `side_effect="read"` 是运行时确认语义，不等价于存储层绝对无写操作。
- `memory.rag` 的 `source` 默认受工作区 containment 限制，但 `WORKSPACE_ALLOW_OUTSIDE` 可由部署环境显式放开，届时边界取决于部署配置。
- `memory.add` 中 working 的“会话过期”在工具字段说明中是设计意图；当前底层实际使用默认 TTL 和容量淘汰，没有 session 级绑定实现。
- `RAGSearchTool.execute` 对 `context` / `graph_context` 的 `count` 是布尔式 1/0，不是片段数量；真实片段数仅在 `retrieve` / `graph_retrieve` 的 `count` 中体现。

## L10 Web 单例装配层

### `web/support.py` — Web 进程级单例、嵌入降级、聊天与后台图抽取支撑

- **拓扑层 / 依赖 / 被依赖**：L10；依赖标准库异步/线程/环境变量、`dotenv`、`constants`、`memory`、`memory.rag`，并在函数内导入 `agents` 与 `tool`；被 `web.seed`、`web.app` 依赖。
- **规模**：443 行 / 17 个 def
- **职责**：固定项目级数据库路径并加载 `.env`；按优先级选择嵌入实现（本机转发网关 `EMBEDDING_BASE_URL` → DashScope key → 离线 Hash）；以双检锁维护 MemoryManager、RAGPipeline、ReActAgent 单例；定义聊天工具可见性、确认键、问答留痕和后台知识抽取；提供聊天配置可用性检测。
- **关键模块级对象/常量/单例**：`LOGGER`、项目/静态/种子路径、`DB_PATH`；全局 `_manager`、`_pipeline`、`_agent` 及对应锁；`GRAPH_REVISION` 和 `_graph_revision_lock`；`CHAT_CONFIRMED_TOOLS=("memory.add",)`；`SEARCH_TOOL_NAME="web.search"`；`SYSTEM_PROMPT` 约束知识管家先检索再回答、区分历史/当前值。模块导入时执行 `load_dotenv(PROJECT_ROOT / ".env")` 并设置默认 `MEMORY_DB_PATH`。

#### 函数 `build_embedding()`

作用：按优先级选择嵌入实例：`EMBEDDING_BASE_URL` 非空时返回 `EmbedServerEmbedding(base_url=..., dimension=MEMORY_EMBEDDING_DIMENSION)`（本机转发网关，自定义 `/embed` 协议）；否则 `DASHSCOPE_API_KEY` 非空时返回 `APIEmbedding(api_key=..., model=DEFAULT_EMBEDDING_MODEL, dimension=MEMORY_EMBEDDING_DIMENSION)`；都没有时返回 `HashEmbedding(dimension=MEMORY_EMBEDDING_DIMENSION)`。无参数；返回嵌入实例；无显式异常捕获。三个向量空间互不兼容，切换后需要重索引已有条目。调用关系：`get_manager` 构造共享管理器。

#### 函数 `build_knowledge_extractor()`

作用：在函数内导入 `agents.llm.LLM` 与 `ProviderRegistry`；读取默认 provider 配置。若文件名不是 `provider.toml` 或文件不存在，返回 `NullKnowledgeExtractor`；解析 profile 后若 key 为空或以 `replace-with` 开头，也返回空抽取器；否则构造 LLM 并返回 `LLMKnowledgeExtractor(client.complete, model=profile.default_model)`。无参数；返回抽取器。整个配置/构造过程被 `except Exception` 包裹，任何异常均退回空抽取器。调用关系：`get_pipeline`、`app.create_app` 的 lifespan、`extract_graph_patches`。

#### 函数 `bump_graph_revision() -> None`

作用：在 `_graph_revision_lock` 内将全局 `GRAPH_REVISION` 加一。无参数；返回 `None`；副作用是让 `/api/graph` 检测外部 revision 变化。调用关系：`extract_graph_patches` 在确实写入实体或关系后调用；`app.create_app` 的 `invalidate_graph` 也调用它——本地 API 写入与后台抽取共用同一个进程级计数，`/api/graph?since=` 因此只有一个真相来源。

#### 函数 `graph_revision() -> int`

作用：加锁读取全局 revision。无参数；返回整数；无写入。调用关系：`app.create_app` 初始化缓存和 `/api/graph` 比对。

#### 函数 `get_manager() -> MemoryManager`

作用：双检锁创建进程级 `MemoryManager`。配置先 `MemoryConfig.from_env()`（读 `HELLOAGENTS_MEMORY_*` 全套，Qdrant/Neo4j 开关在这里生效），再覆盖 `sqlite_path = str(DB_PATH)`（`MEMORY_DB_PATH` 优先级不变），`embedding=build_embedding()`。未配置 Qdrant/Neo4j 时与旧行为完全一致（内存向量 + 内存图）。无参数；返回单例；构造异常向上抛。副作用是首次调用创建存储/嵌入资源。调用关系：`get_pipeline`、`get_agent`、`extract_graph_patches`、`app` lifespan 与 `/api/graph` 等 manager 访问路径。

#### 函数 `close_manager() -> None`

作用：若全局 manager 存在则 `close()` 并置回 `None`。无参数；返回 `None`；关闭异常向上抛。调用关系：`app` lifespan 在应用拥有 manager 时退出调用；测试/脚本也可调用。注意它只清理 manager 单例，不会同步清理已经缓存的 `_pipeline` 或 `_agent`。

#### 函数 `get_pipeline() -> RAGPipeline`

作用：使用 `_manager_lock` 双检锁创建进程级 `RAGPipeline(get_manager(), extractor=build_knowledge_extractor())`。无参数；返回单例；构造异常向上抛。副作用是首次创建管道。调用关系：`get_agent` 注册 RAG 工具。

#### 函数 `get_agent()`

作用：使用 `_agent_lock` 双检锁懒创建 `ReActAgent("knowledge-butler")`；设置 `SYSTEM_PROMPT`；用 `replace=True` 注册 `MemoryQueryTool`、`MemoryAddTool`、`RAGSearchTool`、`RAGTool`，四个工具均绑定 Web 单例 manager/pipeline。无参数；返回 agent；导入/构造/注册异常向上抛。调用关系：`app./api/chat` 在 `chat_ready` 通过后调用。

#### 函数 `chat_confirmed_side_effects(agent) -> frozenset[str]`

作用：为聊天回合生成可由用户本轮显式授权的副作用确认键。遍历 `CHAT_CONFIRMED_TOOLS`，依次尝试 `agent.tool_confirmation_key(name)` 和 `agent.tools.confirmation_key(name)`；只有可调用查询成功才加入 key。失败策略是关闭放行：未知工具、缺失查询或 `KeyError/ValueError/TypeError` 都只记录 warning/跳过。参数为任意 agent；返回不可变 `frozenset`；无持久化副作用。调用关系：`/api/chat` 构造 `ExecutionContext(confirmed_side_effects=...)`。

#### 函数 `search_available() -> bool`

作用：按顺序取首个非空 `SEARCH_BASE_URL`/`ANYSEARCH_BASE_URL` 和首个非空 `SEARCH_API`/`SEARCH_API_KEY`/`ANYSEARCH_API_KEY`，两者同时存在才返回 `True`。无参数；返回布尔值；无异常和副作用。调用关系：`chat_tool_names`、`/api/health`。

#### 函数 `chat_tool_names(agent, *, online: bool) -> list[str] | None`

作用：实现聊天工具白名单。`online=True` 且 `search_available()` 为真时返回 `None`，表示运行时不限制工具，`web.search` 因而可见；否则返回 `agent.tools.snapshot()` 中除 `web.search` 外的全部工具名。参数为 agent 与联网开关；返回工具名列表或 `None`；快照异常向上抛。调用关系：`/api/chat` 用返回值判断 `effective_mode`。

#### 函数 `record_qa(manager: MemoryManager, question: str, answer: str, *, mode: str) -> MemoryItem`

作用：用 `utc_now()` 把 `问：...\n答：...` 写入 episodic 记忆；元数据包含 `kind/type=qa`、截断标题、完整 question/answer、mode 和 `asked_at`，同时传 `timestamp=now`。参数为 manager、问题、回答、实际模式；返回写入项；存储异常向上抛。副作用是新增一条可检索事件。调用关系：`/api/chat` 在模型回答成功后调用。

#### 函数 `knowledge_extract_enabled() -> bool`

作用：读取 `WEB_QA_EXTRACT`，默认 `"1"`；值为 `0/false/no/off`（大小写无关、去空白）时返回 `False`，其余返回 `True`。无参数；返回布尔值；无副作用。说明：函数本身未调用 `chat_ready()`，所以“无真实模型时自动关闭”由实际调用链 `schedule_qa_extraction` 的第二道 `chat_ready()` 门禁完成。调用关系：`schedule_qa_extraction`。

#### 函数 `extract_graph_patches(question: str, answer: str, *, manager: MemoryManager | None = None) -> dict[str, Any] | None`

作用：把问答文本拼成“用户陈述 + 助手回答”的文档，用一个新的 `RAGPipeline` 做图和实体抽取。参数为问题、回答、可选 manager；返回摄取报告或 `None`。

前置检查：问题或回答不是字符串、去空白后为空，返回 `None`；未传 manager 时调用 `get_manager()`。它刻意不调用底层 `get_manager()`（在后台线程场景中由调用方传入 manager，避免应用关闭时的锁/生命周期问题）；源码函数本身仍保留默认获取单例的行为。抽取用 `Document` 元数据 `source/filename=问答抽取`，块长 `QA_EXTRACT_CHUNK_SIZE`、无重叠；异常被捕获并记录日志后返回 `None`。报告中有 `entities` 或 `relations` 时调用 `bump_graph_revision()`。副作用是新增语义记忆/图补丁及可能的 revision 变化。调用关系：同步测试、`extract_graph_patches_async`、`schedule_qa_extraction`。

#### 函数 `async extract_graph_patches_async(question: str, answer: str, *, manager: MemoryManager | None = None) -> None`

作用：通过 `await asyncio.to_thread(extract_graph_patches, ...)` 把阻塞 LLM/SQLite 工作移出事件循环。参数与同步版本一致；返回 `None`；内部异常遵循同步函数的捕获边界，`to_thread` 自身异常可能向上抛。调用关系：`schedule_qa_extraction` 创建的后台任务。

#### 函数 `schedule_qa_extraction(question: str, answer: str, *, manager: MemoryManager | None = None) -> None`

作用：按环境配置调度问答图抽取。先检查 `knowledge_extract_enabled()`，再调用 `chat_ready()`；任一门禁不通过直接返回。`WEB_QA_EXTRACT_SYNC` 为 `1/true/yes/on` 时同步调用；无当前事件循环时也同步调用；否则用 `asyncio.create_task(extract_graph_patches_async(...))` 投递 fire-and-forget 任务。参数为问题、回答、可选 manager；返回 `None`；副作用是可能同步写库或创建后台任务，不等待结果。调用关系：`/api/chat` 在 `record_qa` 后调用。

#### 函数 `chat_ready() -> tuple[bool, str]`

作用：检测真实聊天模型是否可用并给用户可读原因。先检查 `ProviderRegistry.default_config_path()` 是否为真实的 `provider.toml` 且文件存在，否则返回 `(False, 配置说明)`。随后构造 registry、取 active profile；key 为空时尝试 `profile.api_key_env`，若为空或以 `replace-with` 开头返回 `(False, 原因)`；解析异常返回 `(False, "provider.toml 解析失败：...")`；成功返回 `(True, "")`。无参数；返回 `(bool, message)`；无写副作用。调用关系：`/api/chat` 的 503 门禁、`/api/health`、`schedule_qa_extraction`。

## L11 种子数据播种

### `web/seed.py` — 幂等导入 Aetheria 星图种子数据

- **拓扑层 / 依赖 / 被依赖**：L11；依赖 `json/pathlib`、`memory` 与 `web.support.SEED_FILE`；被 `web.app` 的 lifespan 与 `/api/seed` 使用，也可作为模块命令执行。
- **规模**：111 行 / 1 个 def
- **职责**：读取 `web/seed_data.json`，把实体、关系和三方备注分别写入语义记忆；用统一 `metadata.seed == "aetheria-seed-v1"` 标记保证重复导入时跳过。模块命令会获取共享 manager、播种、关闭。
- **关键模块级对象/常量/单例**：`SEED_MARK = "aetheria-seed-v1"`；`SEED_FILE` 由 support 指向 `PROJECT_ROOT/web/seed_data.json`；`__main__` 分支使用 support 单例。无模块级 manager。

#### 函数 `seed(manager: MemoryManager, path: Path | None = None) -> dict[str, Any]`

作用：未传 `path` 时使用 `SEED_FILE`；文件不存在返回 `{"seeded": False, "reason": "种子文件不存在：..."}`；读取全部语义项，只要任一项的 `metadata.seed` 等于 `SEED_MARK`，返回 `{"seeded": False, "reason": "已播种过（幂等跳过）", "existing": N}`。参数为管理器和可选路径；返回结果字典。

导入行为：读取 UTF-8 JSON，取 `entities/relations/notes` 或空列表；实体要求非空 `name`，写入 `kind=entity`、`title`、`domain` 或“未分类”、`seed` 元数据，重要度来自输入默认 0.85；关系要求 subject/predicate/object 都非空，调用 `semantic.add_fact` 写入领域、note、date、seed 和默认置信度 1.0；备注要求非空 content，写 `kind=note`、所属 entity/domain/title/date/seed，重要度 0.4。成功返回 `seeded/source/entities/relations/notes` 计数。

异常与副作用：文件读取、JSON 解析、字段转换、记忆写入异常都没有内部捕获，会向上抛；文件检查不写入，实际导入会写语义记忆。调用关系：`app` lifespan 自动播种（除非 `WEB_AUTOSEED=0`），`POST /api/seed` 强制重新检查，`__main__` 直接执行。

## L12 FastAPI 应用与全部路由

### `web/app.py` — FastAPI 应用工厂、Pydantic 请求模型与全部 HTTP 路由

- **拓扑层 / 依赖 / 被依赖**：L12（最上层）；依赖 FastAPI/Pydantic、`constants`、`core.ExecutionContext`、`memory`、`memory.rag`、`memory.embedding`（`EmbedServerEmbedding`/`gateway_reachable`）、`memory.storage.document_repo`、`graph_builder`、`seed`、`support`；被 `web.__init__` 导入，也可直接 `python -m web.app` 运行。
- **规模**：851 行 / 31 个 def
- **职责**：定义 `/api/health`、`/api/graph`（支持 `?since=` 增量）、`/api/chat`、`/api/ingest`、`/api/graph-rag`、`/api/facts`、`/api/knowledge`、`/api/seed`、`/api/export`、`/api/import`，以及 Phase 6/7 新增的 `/api/documents`（列表/详情/重嵌入）、`/api/stats`、`/api/reconcile`（三库对账与自愈）和静态首页挂载。应用工厂可注入 `MemoryManager` 供测试使用，否则在 lifespan 中获取/关闭共享单例；图缓存失效统一走 `support.bump_graph_revision()`（本地写入与后台抽取共用一个进程级计数，`/api/graph?since=` 只有一个真相来源）。
- **关键模块级对象/常量/单例**：五个 Pydantic 模型 `ChatBody/FactBody/GraphRAGBody/KnowledgeBody/ReconcileBody`；模块级函数 `_is_fact_item`、`embedding_tunnel_hint`、`embedding_unavailable_detail`；模块导入末尾 `app = create_app()`；`__main__` 使用 Uvicorn 监听 `LOCALHOST`、端口 `NEBULA_PORT` 或 `DEFAULT_WEB_PORT`（8765）。`STATIC_DIR` 存在时根路径挂载 `StaticFiles(..., html=True)`。

##### Pydantic 请求模型

#### 类 `ChatBody(BaseModel)` — `/api/chat` 请求体

字段：`message: str`，最短 1、最长 `WEB_CHAT_MAX_CHARS=8000`；`mode: Literal["offline", "online"]`，默认 `"offline"`。约束/类型错误由 FastAPI 返回 422；模型没有额外自定义校验器或方法。

#### 类 `FactBody(BaseModel)` — `/api/facts` 请求体

字段：`subject`（1 到 200）、`predicate`（1 到 100）、`object`（1 到 200）、`domain`（可空，最长 100）、`note`（可空，最长 4000）、`confidence`（默认 1.0，范围 0 到 1）。字段校验错误由 FastAPI 返回 422。

#### 类 `GraphRAGBody(BaseModel)` — `/api/graph-rag` 请求体

字段：`query`（1 到 `WEB_GRAPH_RAG_QUERY_MAX=8000`）、`limit`（默认 `RAG_RETRIEVE_LIMIT=5`，范围 1 到 50）、`hops`（默认 1，范围 0 到 `RAG_GRAPH_MAX_HOPS=3`）。字段校验错误由 FastAPI 返回 422。

#### 类 `KnowledgeBody(BaseModel)` — `/api/knowledge` 请求体

字段：`text`（最短 1、最长 `WEB_KNOWLEDGE_MAX_CHARS=20000`）。字段校验错误由 FastAPI 返回 422；路由还要二次检查去空白后的非空。

#### 类 `ReconcileBody(BaseModel)` — `/api/reconcile` 修复请求体

字段：`repair: list[str] = Field(default_factory=list)` — 要执行的修复类型（`missing_vector`/`missing_edge`）；空列表表示只报告不修。字段校验错误由 FastAPI 返回 422。

#### 函数 `_is_fact_item(item: Any) -> bool`

作用：判定一个 semantic 行是否代表一条 `(subject, predicate, object)` 事实——元数据三项全非空。参数为任意对象；返回布尔值；无副作用。调用关系：`fact_items`、导入去重。

#### 函数 `embedding_tunnel_hint() -> str`

作用：读 `EMBEDDING_TUNNEL_HINT` 并去空白；隧道命令由部署方通过环境变量下发，避免把服务器地址写进仓库。无参数；返回字符串；无副作用。调用关系：`embedding_unavailable_detail`、`/api/health`。

#### 函数 `embedding_unavailable_detail(manager: MemoryManager) -> str`

作用：网关已配置（嵌入实例有 `base_url`）但 `gateway_reachable` 探测不可达时，返回可执行的降级说明（含 `embedding_tunnel_hint()` 的隧道命令）；可用或无网关时返回空串。**绝不因为不可达就切换到别的向量空间**（D8）。参数为管理器；返回字符串；副作用是发起一次 TCP 探测。调用关系：`/api/ingest` 的 503 门禁、`revectorize_document`、`repair_drift`。

#### 函数 `async _save_upload(file: UploadFile, *, prefix: str, suffix: str | None = None) -> Path`

作用：把上传流分块（每次最多 1 MiB）写入系统临时文件，并在写入过程中执行 `MAX_UPLOAD_BYTES=64 MiB` 上限。参数为 `UploadFile`、临时文件前缀、可选后缀；返回临时文件 `Path`，调用方负责删除。后缀未提供时取原文件名扩展或 `.txt`。

异常与副作用：超限时关闭并删除临时文件，抛 `HTTPException(413, detail="文件超过上传上限 64MB")`；总大小为 0 时删除临时文件并抛 `HTTPException(400, detail="上传内容为空")`。成功时副作用是磁盘上产生一个未删除的临时文件。调用关系：`/api/ingest`、`/api/import`。

#### 函数 `create_app(manager: MemoryManager | None = None) -> FastAPI`

作用：构造 FastAPI 应用、lifespan、缓存和全部路由。参数为可选注入 manager；返回应用；源码末尾的模块级 `app` 使用无注入默认值。无异常捕获，注册阶段的配置问题向上抛。

以下几个函数均嵌套在 `create_app` 中：

##### 嵌套 async contextmanager `lifespan(app: FastAPI)`

作用：启动时记录 `owns_manager = manager is None`；把注入 manager 或 `get_manager()` 放入 `app.state.manager`；为应用构造独立 `RAGPipeline` 并存入 `app.state.pipeline`；在当前事件循环创建 `asyncio.Lock()` 供聊天串行化。`WEB_AUTOSEED != "0"` 时调用 `seed(app.state.manager)`。yield 后仅在应用拥有 manager 时调用 `close_manager()`。参数为 FastAPI 应用；返回异步上下文管理器；启动/播种异常会阻止应用进入服务；注入 manager 不会被关闭。调用关系：FastAPI lifespan 生命周期。

##### 嵌套函数 `the_manager() -> MemoryManager`

作用：返回 `app.state.manager`。无参数；返回当前管理器；lifespan 未初始化时访问会抛 `AttributeError`。调用关系：各路由与缓存图构建。

##### 嵌套函数 `invalidate_graph() -> None`

作用：调用 `bump_graph_revision()`（进程级全局计数加一，与后台抽取共用）并把闭包 `graph_cache["payload"]` 置 `None`。无参数；返回 `None`；副作用是让下次 `/api/graph` 重建。调用关系：聊天、摄取、事实、知识、播种和成功导入后调用。

#### 路由 `GET /api/graph` — `graph(since: int = -1) -> dict[str, Any]`

请求体：无；查询参数 `since` 默认 -1（**不能用 0 当哨兵**：进程刚启动时 revision 就是 0，客户端带 0 来问会被误判成「没带参数」而永远拿全量）。响应：`build_graph(the_manager())` 的统计、节点和边字典加 `revision`/`unchanged` 字段。缓存读取顺序：若 payload 为空或缓存的外部 revision 与 `graph_revision()` 不同，则重建并同步外部 revision；否则直接返回缓存对象。当 `since >= 0` 且 `since == revision` 时返回 `{"revision", "unchanged": True, "nodes": [], "edges": [], "stats"}`——期间没有图可见写入，前端沿用本地图（U6 增量刷新，省一次全量 build 与整树重排）。状态码：正常 200；存储/构建异常未捕获，由 FastAPI 处理。调用关系：`build_graph`、`graph_revision` 和 `invalidate_graph` 共同维护缓存。

#### 路由 `POST /api/graph-rag` — `graph_rag(body: GraphRAGBody) -> dict[str, Any]`

请求体：`GraphRAGBody`。调用：`app.state.pipeline.graph_retrieve(body.query, limit=body.limit, hops=body.hops)`。响应：`result.to_dict()` 与 `{"context": result.build_context()}` 合并，即 `query/evidence/paths/entities/context`。状态码：字段非法由 FastAPI 返回 422；检索校验或存储异常未捕获。无写副作用。

#### 路由 `POST /api/chat` — `async chat(body: ChatBody) -> dict[str, Any]`

请求体：`ChatBody.message` 与 `mode`。完整调用链与响应如下：

1. 调用 `chat_ready()`；不 ready 时抛 `HTTPException(503, detail=reason)`。
2. `get_agent()` 获取共享 ReActAgent。
3. `online = body.mode == "online"`；调用 `chat_tool_names(agent, online=online)`。联网且搜索配置可用时白名单为 `None`，否则获得所有已注册工具中排除 `web.search` 的列表；源码据此把 `effective_mode` 定为 `"online"` 或 `"offline"`，因此在线配置缺失时响应会降级为 offline。
4. 构造 `ExecutionContext(confirmed_side_effects=chat_confirmed_side_effects(agent))`。当前支持层只为 `memory.add` 提供确认 key，其他删除/清空/摄取工具仍需人工确认。
5. 在 `async with app.state.chat_lock` 内用 `asyncio.to_thread(agent.run, body.message, tool_names=tool_names, context=context)` 执行同步 agent；锁会使并发聊天排队，避免共享 agent 历史互相覆盖。
6. agent 调用被 `try/except Exception` 包裹，任何异常归一为 `HTTPException(502, detail="聊天模型调用失败：类型: 消息")`；agent 成功后才继续。
7. `record_qa(the_manager(), body.message, answer, mode=effective_mode)` 写 episodic 问答留痕；该调用不在上述 502 捕获块内，异常未显式归一。
8. `schedule_qa_extraction(body.message, answer, manager=the_manager())` 尝试后台抽取；`invalidate_graph()` 使当前请求后的星图缓存失效，避免问答事件或记录变化继续使用旧图。
9. `app.state.pipeline.graph_retrieve(body.message, limit=RAG_RETRIEVE_LIMIT, hops=RAG_GRAPH_HOPS)` 再取图多跳检索证据和路径；该调用也不在 502 捕获块内。
10. `app.state.pipeline.hybrid_retrieve(body.message, limit=RAG_RETRIEVE_LIMIT)` 走混合检索（向量 × FTS5 RRF），把每条命中的 `rrf_score/vector_score/keyword_score` 与 `last_retrieval_note` 组装成 `retrieval` 报告（U4 依据面板；网关不可用时这里自动退化为纯关键词，降级原因对用户可见而不是空来源列表）。
11. 返回 `{"answer", "mode", "sources", "paths", "retrieval"}`。每个 source 为 `memory_id/score/source/chunk_id`，其中来源优先级是 `filename` 再 `source`，chunk 优先级是 `chunk_id` 再 `document_id`；paths 是 `GraphPath.to_dict()` 列表；`retrieval` 为 `{note, hits:[{chunk_id, document_id, chunk_index, snippet, score, rrf_score, vector_score, keyword_score}]}`。

状态码：请求体非法 422；模型未配置 503；agent 调用失败 502；后续 record/retrieval 异常源码中未见显式处理，可能成为 FastAPI 默认 500。副作用：可能写 episodic、创建后台图抽取任务、触发全局 graph revision，并把生成的星图缓存置空。

#### 路由 `POST /api/ingest` — `async ingest(file: UploadFile) -> dict[str, Any]`

请求体：multipart 上传，字段名 `file`，类型 `UploadFile`。先取 `filename = file.filename or "untitled"`；**降级门禁（D8）**：`embedding_unavailable_detail(the_manager())` 非空时直接抛 `HTTPException(503, detail=...)`，明确报错并给隧道命令，而不是写出一条注定失败的文档记录。随后调用 `_save_upload(file, prefix="nebula-ingest-")`；共享上传逻辑可能返回 413 或 400。临时文件路径传给 `pipeline.ingest_source(...)`，元数据为 `source/filename`，块长 `WEB_INGEST_CHUNK_SIZE=800`、重叠 `RAG_CHUNK_OVERLAP=100`。解析/摄取异常被捕获为 `HTTPException(422, detail="文档解析失败：类型: 消息")`；`finally` 始终删除临时文件。随后 `episodic.record` 写上传事件，`invalidate_graph()` 失效缓存。

成功响应：`{"filename": ..., "chunks": len(items), "extraction": pipeline.last_ingest_report}`，状态 200。错误：非 multipart/缺少 file 由 FastAPI 422；嵌入网关不可达 503；大小超限 413；空上传 400；解析/摄取 422；最终 episodic 写入或图缓存失效异常未显式捕获。调用关系：`_save_upload`、`embedding_unavailable_detail`、`RAGPipeline.ingest_source`、`MemoryManager.episodic.record`。

#### 路由 `POST /api/facts` — `add_fact(body: FactBody) -> dict[str, Any]`

请求体：`FactBody`。调用 `manager.semantic.add_fact(subject, predicate, object, metadata={"domain": body.domain or DEFAULT_DOMAIN, "note": body.note or ""}, confidence=body.confidence)`；成功后 `invalidate_graph()`。响应 `{"ok": True, "item_id": item.id}`。状态码：字段非法 422；存储异常未捕获，通常 500。副作用是新增事实并失效图缓存。

#### 路由 `POST /api/knowledge` — `add_knowledge(body: KnowledgeBody) -> dict[str, Any]`

请求体：`KnowledgeBody.text`。路由先 `strip()`；空内容抛 `HTTPException(422, detail="一句话内容不能为空")`。构造 `Document`，元数据 `source/filename=一句话入库`、`note=text[:400]`，调用 `pipeline.ingest(..., chunk_size=RAG_CHUNK_SIZE, overlap=RAG_CHUNK_OVERLAP)`；摄取内部的知识抽取错误进入报告，但切块/存储等异常未在这里捕获。随后写 episodic 记录并 `invalidate_graph()`。

成功响应：`{"ok": True, "chunks": len(items), "items": [item.to_dict()...], "extraction": pipeline.last_ingest_report}`。状态码：字段非法 422；去空白后为空 422；其他管道/store 异常未显式处理。副作用是语义块、实体/关系补丁、episodic 记录及图缓存失效。

#### 路由 `POST /api/seed` — `reseed() -> dict[str, Any]`

请求体：无。调用 `seed(the_manager())`；无论播种是否实际发生，调用 `invalidate_graph()`；返回 `seed()` 的原始字典，例如跳过原因或成功计数。状态码正常 200；文件缺失由 `seed` 的返回字典表达，JSON/写入异常未捕获。副作用是可能写入种子数据并使缓存失效。

#### 路由 `GET /api/export` — `export(request: Request) -> JSONResponse`

请求体：无；参数 `Request` 当前只保留为 FastAPI 注入，源码未读取它。调用 `manager.list(include_expired=False)` 并逐项 `to_dict()`。响应 JSON：`format="knowledge-nebula-export/v1"`、UTC `exported_at`、`counts`（`total`、`semantic`、`episodic`）、`items`。HTTP header `Content-Disposition` 使用 UTC 时间戳文件名 `knowledge_export_YYYYmmdd-HHMMSS.json`。状态码正常 200；列表/序列化异常未捕获。无持久化副作用。

#### 路由 `POST /api/import` — `async import_file(file: UploadFile) -> dict[str, Any]`

请求体：multipart 字段 `file`，后缀固定 `.json`。先调用 `_save_upload`（413/400 分支），读取字节后立即删除临时文件。JSON 解码异常抛 `HTTPException(400, detail="不是合法的 JSON：...")`；若顶层是字典取 `data["items"]`，否则把顶层当 entries；entries 不是列表时抛 `HTTPException(400, detail="JSON 中找不到 items 数组")`。

导入去重与逐条规则：先收集当前语义事实 `(subject, predicate, object)` 集合；每项必须是字典，否则 skipped 并记录；必须有 id 和 content，否则 skipped；已存在相同记忆 id 的项 skipped；若元数据同时含 subject/predicate/object，则按三元组幂等，重复 skipped，否则 `semantic.add_fact`；其他项调用 `manager.add` 并保留 id/type/metadata/importance。单条异常只 skipped 并记录 `类型: 消息`；错误列表最多 `WEB_IMPORT_ERRORS_MAX=20` 条。若 imported 非 0 才 `invalidate_graph()`。

成功响应：`{"imported": N, "skipped": N, "errors": [...]}`，这些内部导入错误返回 200 而非 400；协议/JSON 问题返回 400，上传大小问题 413，FastAPI 缺字段问题 422。副作用是导入记忆和可能失效缓存。

##### 嵌套函数 `note_error(message: str) -> None`

作用：仅在 `len(errors) < WEB_IMPORT_ERRORS_MAX` 时追加错误，保持响应体有界。参数为错误消息；返回 `None`；副作用是修改闭包中的 `errors`。调用关系：`/api/import` 的所有跳过/异常分支。

#### 文档中心与三库对账嵌套辅助（均在 `create_app` 内）

##### 嵌套函数 `the_repository() -> DocumentRepository`

作用：复用管道缓存的那个真值源仓储（`app.state.pipeline.document_repo()`）——同一 sqlite 文件、自带锁、每作用域独立连接。仓储为 `None`（内存库）时抛 `HTTPException(400, detail="当前记忆库是内存模式（:memory:），没有 documents/chunks 真值源")`。无参数；返回仓储。调用关系：文档/对账路由。

##### 嵌套函数 `fact_items() -> list[Any]`

作用：返回 `the_manager().semantic.facts()` 中满足 `_is_fact_item` 的事实列表。无参数；返回列表。调用关系：`/api/stats`、`reconcile_report`、`repair_drift`、`/api/import`（三元组幂等去重）。

##### 嵌套函数 `projected_vector_ids() -> set[str] | None`

作用：Qdrant 里的 app 级 id 集合（`vector_store.list_ids()`）；存储不支持（无 `list_ids`）或不可达（异常）时返回 `None`（跳过向量对账、不误报漂移）。无参数；返回集合或 `None`。调用关系：`reconcile_report`。

##### 嵌套函数 `projected_edge_ids() -> set[str] | None`

作用：图投影里的 `memory_id` 集合（`graph_store.relation_memory_ids`，内存回退与 Neo4j 都实现）；不可用/异常时返回 `None`。无参数；返回集合或 `None`。调用关系：`reconcile_report`。

##### 嵌套函数 `reconcile_report() -> dict[str, Any]`

作用：三库计数与漂移（只看不改）：真值源（chunks 总数 / `indexed` 数）↔ 向量投影（Qdrant 点数）↔ 图投影（Neo4j 边数）+ SQLite 事实数。漂移项：`missing_vector`（SQLite 标记 indexed 但向量层没有）、`orphan_vector`（向量层有但真值源/记忆都没有）、`missing_edge`（事实有但图没有边）。无参数；返回 `{counts: {chunks, chunks_indexed_sqlite, qdrant_points, facts, neo4j_edges}, drift: [...]}`。调用关系：`GET /api/reconcile`、`repair_drift`。

##### 嵌套函数 `repair_drift(kinds: list[str]) -> dict[str, Any]`

作用：幂等自愈——只补缺失的投影，**绝不删除或改写真值源**。`kinds` 去重后只允许 `missing_vector`/`missing_edge`，否则 422。补向量前先过 `embedding_unavailable_detail` 门禁（网关不可达 503）；逐块 `manager.vector_store.upsert_chunk` 并回写 `chunk vector_status=indexed`；补边按事实 id 逐条 `semantic.add_fact`（重写图边）。返回 `{"repaired": {"missing_vector": N, "missing_edge": N}}`。调用关系：`POST /api/reconcile`。

#### 路由 `GET /api/documents` — `list_documents(tag: str = "", status: str = "", page: int = 1, page_size: int = 20) -> dict[str, Any]`

请求体：无；查询参数 tag/status/page/page_size。调用 `the_repository().list_documents(...)`（`ValueError` → 422）；响应 `{total, page, page_size, items:[{document_id, title, source, tags, status, chunk_count, created_at}]}`，`chunk_count` 来自 `repository.chunk_counts()`。调用关系：`the_repository`、`DocumentRepository.list_documents/chunk_counts`。

#### 路由 `GET /api/documents/{document_id}` — `get_document(document_id: str) -> dict[str, Any]`

请求体：无；路径参数。文档不存在抛 404；响应含 `raw_text`、permission、status、error、created/updated_at 与 `chunks`（`chunk_id/chunk_index/char_start/char_end/text/vector_status`）。调用关系：`DocumentRepository.get_document/list_chunks`。

#### 路由 `POST /api/documents/{document_id}/revectorize` — `revectorize_document(document_id: str) -> dict[str, Any]`

请求体：无。文档不存在 404；无分块 422；`embedding_unavailable_detail` 非空 503（**绝不切换到别的向量空间**，D8）。对全部 chunk 批量重嵌入、`vector_store.upsert_chunk` 逐块回写、状态置 `extracted`（原为 extracted）或 `vectorized`；执行异常归一为 502。响应 `{document_id, chunks_reindexed, status}`。调用关系：`DocumentRepository`、`QdrantVectorStore.upsert_chunk`。

#### 路由 `GET /api/stats` — `stats() -> dict[str, Any]`

请求体：无。返回真值源计数（`documents/chunks/chunks_indexed`，内存库时为 0）+ `facts` + `memories_total`（`manager.document_store.list(include_expired=True)` 长度）。调用关系：`DocumentRepository.stats`、`fact_items`。

#### 路由 `GET /api/reconcile` — `reconcile() -> dict[str, Any]`

请求体：无。返回 `reconcile_report()`。无写副作用。

#### 路由 `POST /api/reconcile` — `reconcile_repair(body: ReconcileBody) -> dict[str, Any]`

请求体：`ReconcileBody.repair`。返回 `repair_drift(body.repair)`；空列表 = 只报告不修。异常：不支持的修复类型 422；补向量时网关不可达 503。

#### 路由 `GET /api/health` — `health() -> dict[str, Any]`

请求体：无。调用 `chat_ready()` 取布尔值（原因字符串被丢弃）；读取 `manager.embedding`，**用是否有 `base_url` 属性而非类名判断远端实现**（`EmbedServerEmbedding` 与 `APIEmbedding` 都有、`HashEmbedding` 没有）：`embedding_mode` 为 `"api"` 或 `"local-hash"`；再定 `embedding_state`（`gateway`/`api`/`hash`/`unreachable`，网关不可达时把状态标成 `unreachable`）与 `embedding_reachable`（`gateway_reachable(base_url)`，只上报、不换向量空间）。响应：`{"ok": True, "chat_ready": bool, "embedding_mode": str, "embedding_reachable": bool, "embedding": ..., "search_available": bool, "store_modes": {document: "memory"|"sqlite", vector: 实现类名, graph: "neo4j"|"inmemory"}, "degraded": {embedding, embedding_endpoint, chat_ready, keyword_fallback, embedding_hint}}`。状态码正常 200；属性/序列化异常未显式捕获。无写副作用。

##### 静态首页与模块入口

- `create_app` 最后在 `STATIC_DIR.is_dir()` 时执行 `app.mount("/", StaticFiles(directory=str(STATIC_DIR), html=True), name="static")`。挂载发生在所有 API 路由注册之后，因此 `/api/*` 不会被静态路由吞掉；目录不存在时不挂载，源码中未见额外首页 fallback。
- 模块级 `app = create_app()` 在导入 `web.app`/`web` 时创建应用对象，但不启动服务器。
- `if __name__ == "__main__"` 分支导入 Uvicorn，以 `uvicorn.run(app, host=LOCALHOST, port=int(os.getenv("NEBULA_PORT", str(DEFAULT_WEB_PORT))))` 启动应用；host 用字面 `127.0.0.1`（避免 `localhost` 在部分 Windows 环境解析到 `::1` 而服务只监听 IPv4），端口环境变量不能转换为整数时抛 `ValueError`。

##### 覆盖清单

计数口径：AST `FunctionDef` + `AsyncFunctionDef`，包含方法、property/classmethod/staticmethod、Pydantic 校验器、Protocol 方法及嵌套函数。

| 文件 | 行数 | def 数量 | 覆盖对象 |
|---|---:|---:|---|
| `memory/rag/__init__.py` | 27 | 0 | 包导入/`__all__` |
| `memory/rag/document.py` | 162 | 7 | `ChunkSpan`、`Document`、`DocumentProcessor`、`resolve_within` |
| `memory/rag/graph_rag.py` | 244 | 9 | `GraphPath`、`GraphRAGResult`、`GraphRAGPipeline` |
| `memory/rag/knowledge.py` | 803 | 31 | 7 个模块函数、5 个模型/协议/抽取器类、`EntityResolver`、5 个嵌套函数 |
| `memory/rag/pipeline.py` | 372 | 20 | `RetrievedChunk`、`_document_tags`、`_document_permission`、`_hybrid_enabled`、`_rrf_fuse`、`_chunk_metadata`、`_accepts_graph_context`、`RAGPipeline` |
| `web/__init__.py` | 9 | 0 | 包导入/`__all__` |
| `web/domain_classifier.py` | 111 | 2 | `classify_domain`、`majority_domain` |
| `web/graph_builder.py` | 279 | 7 | `domain_color`、`_node`、`_entity_aliases`、`build_graph`、2 个嵌套函数、`_date` |
| `web/support.py` | 443 | 17 | 嵌入/抽取器、单例、revision、聊天工具、留痕、后台抽取、就绪检测 |
| `web/seed.py` | 111 | 1 | `seed` |
| `web/app.py` | 851 | 31 | 5 个 Pydantic 模型、`_is_fact_item`、隧道提示/降级详情、upload helper、`create_app`、lifespan、manager helper、cache invalidator、全部路由（含文档/统计/对账）、导入错误 helper |
| **合计** | **3,412** | **125** | 11 个目标文件 |

##### 不确定点与源码不一致

1. `memory/rag/__init__.py` 的 `__all__` 列出 `relation_id_for`，但该名称未在顶部从 `knowledge` 导入；包级 `relation_id_for` 的可用性因此依赖导入顺序或其他代码是否另行赋值，源码中未见显式处理。
2. `web.support.knowledge_extract_enabled()` 只读取环境变量，不检查真实聊天配置；模型未配置时的实际门禁由 `schedule_qa_extraction()` 另外调用 `chat_ready()` 实现。
3. `web.app` 的 `/api/chat` 只把 `agent.run` 相关异常归一为 502；`record_qa`、`schedule_qa_extraction` 和最终 `graph_retrieve` 在 502 捕获块之外，源码中未见这些步骤的显式 HTTP 错误映射。
4. `web.app` 的 `/api/import` 对单条导入异常采用“跳过并记录”策略，并返回 200；只有整个上传/JSON/顶层 items 协议错误使用 400。
5. 源码未定义静态目录不存在时的额外首页或 404 页面；根路径只注册 `StaticFiles` 当目录存在时。
6. `RAGPipeline.document_repo()` 对非 SQLite 文档库（无 `path`）和 `:memory:` 返回 `None`：真值源摄取/混合检索/FTS5 关键词降级在这些场景下静默不可用（`retrieve` 退回纯向量、`hybrid_retrieve` 退回 `retrieve`）。
7. `/api/reconcile` 的向量/图投影无法枚举（存储不支持 `list_ids`/`relation_memory_ids` 或后端不可达）时，`qdrant_points`/`neo4j_edges` 返回 `-1`，对应漂移检测被跳过而不报错——这是「不能枚举就不误报漂移」的刻意取舍。
8. **降级门禁只做在摄取侧**：`/api/ingest` 与 `/api/documents/{id}/revectorize` 会先查 `embedding_unavailable_detail` 并返回 503，但 `/api/chat` 及其驱动的 `memory.*` 工具**没有**这道前置检查。网关隧道断开时，chat 请求不会得到 503，而是由工具层逐次抛 `RuntimeError`（`embedding API request failed ... EMBED_GATEWAY_HINT`）并被 `core.runtime` 记为「Tool execution failed」，最终表现为 agent 在工具失败的情况下继续作答（`/api/chat` 源码中未见针对该情形的显式映射）。

---

### `web/static/index.html` — 单文件前端（手写 HTML/CSS/JS，非 Python，不计入 AST 清单）

- **拓扑层 / 依赖 / 被依赖**：L12 的静态资源；由 `web.app` 在 `STATIC_DIR` 存在时以 `StaticFiles(..., html=True)` 挂载到 `/`。**零外部资源**：样式与脚本全部内联，不引用任何 CDN/框架/字体，因此离线可用、无需构建步骤。
- **规模**：2,709 行（对比文档基线约 1,773 行，Phase 7 增加约 936 行）。本文件不是 Python 模块，故不进入第 5 章的 AST 覆盖统计，只按结构描述。
- **职责**：知识星云的可视化前端。用 Canvas 2D 做静态布局 + 视口剔除的星系图渲染（领域=恒星、实体=行星、事实/知识块=卫星），并通过 `/api/*` 与后端交互。正文分 12 个编号区（0 小工具 → 11 启动）：

| 区 | 作用 |
|---|---|
| 0. 小工具 | `$`/`escapeHtml`/`shortTitle`/`toast`/`apiFetch`（带 `AbortController` 超时）与 `apiGet`/`apiPostJson`/`apiPostForm` 三个封装 |
| 1. 数据层 | `loadGraph` 拉 `/api/graph` → 星系树；支持 `?since=<revision>` 增量（`data.unchanged` 为真时沿用本地图，U6） |
| 2. 渲染引擎 | `buildStarSystem`/`place`/`computeOverview`/`drawCelestialBody`/`drawEdges`/`render`：布局一次算好、闪烁、缩放平移、悬停邻域高亮 |
| 3. 引力桥 | 三元组边的视口剔除与悬停高亮 |
| 4. 主渲染循环 | 底色 → 微尘 → 边 → 星点 → 标签的绘制顺序 |
| 6. 下钻/面包屑/抽屉 | `lodLevel`（U6 LOD 分档：低档不画卫星、最低档省标签）、`drillIntoNode`、`zoomToLevel`、`getBreadcrumbPath`、`renderDrawer`；抽屉展示实体**别名**（读 `node.meta.aliases`，U7） |
| 7. 点击命中 | 与渲染共用同一份深度排序数组（修掉早期的命中错位） |
| 8. 搜索 | 节点检索与定位 |
| 9 / 9.1 添加知识 | 三元组 `POST /api/facts`、一句话 `POST /api/knowledge` |
| 10 导入/导出/聊天 | 上传导入、导出、对话面板；`appendRetrieval` 渲染 `retrieval` 报告（snippet + 向量/关键词/RRF 三个分数，U4 回答依据） |
| 10.3 文档中心（U2） | 调 `/api/documents` 列表、`/api/documents/{id}` 详情、`.../revectorize` 重嵌入 |
| 10.4 存储健康（U3） | 调 `/api/health` + `/api/reconcile` 展示存储模式与一致性漂移，并可触发修复 |
| 11. 启动 | 首屏加载与状态初始化 |

- **可访问性与交互（Phase 7 U8）**：`aria-label`/`aria-live`/`role` 约 16 处读屏标注（对话消息区 `aria-live="polite"`）；详情抽屉与文档列表有 `keydown` 键盘可达路径；抽屉支持 `pointerdown`/`pointermove` 手势；`@media (max-width: 720px)` 提供小屏布局。源码中**没有** `prefers-reduced-motion` 处理，动画在减少动态偏好的环境下不会自动关闭——这是当前未覆盖的可访问性项。

## 4. 包门面（`__init__.py`）逐文件解析

门面不实现业务逻辑，只负责「聚合 + 稳定公共 API」。它们是全项目的**接口契约层**：外部只需要记 5 个入口（`core`、`agents`、`memory`、`memory.rag`、`web`），内部实现可以自由搬动。理解门面能避免把 `memory/__init__.py` 的聚合关系误读成实现依赖（第 1.2 节的环就是被门面放大后看起来更严重）。

### `constants.py`（严格说不是门面，但是全局常量总表）
见第 3 章 L1 的 `constants.py` 一节；此处只强调它在门面体系中的角色：所有跨包共享的默认值（超时、分页、切块、上限、文件名）都集中在这里，因此**读任何模块前先读 `constants.py`**，否则会误以为某个数字是魔法值。

### `core/__init__.py` — 工具框架唯一公共入口

- **拓扑层**：L5（聚合层，位于所有 core 实现模块之后）｜**依赖**：core 的全部 13 个实现模块｜**被依赖**：`agents.*`、`tool/*`、`web.*`
- **职责**：把工具模型的 7 个类型、注册表、解析器、运行时、目录/发现/技能、更新日志仓储一次性导出，使插件只需 `from core import BaseTool, ToolSpec, ToolError, ...`。同时它是「项目内禁止深层导入」这一约定的执行点：写新工具时不应 `from core.runtime import ToolExecutionManager`。
- **`__all__`（32 项）**：`DEFAULT_UPDATE_LOG_FILENAME`、`BaseTool`、`BatchToolResult`、`CatalogInput`、`CatalogOutput`、`ExecutionContext`、`SkillCatalogInput`、`SkillCatalogOutput`、`SkillCatalogTool`、`SkillDiscoveryError`、`SkillDiscoveryRecord`、`SkillDiscoveryReport`、`SkillRegistry`、`SkillSpec`、`ToolCall`、`ToolCatalogTool`、`ToolDiscoveryError`、`ToolDiscoveryRecord`、`ToolDiscoveryReport`、`ToolError`、`ToolExecutionManager`、`ToolLoop`、`ToolRegistry`、`ToolResult`、`ToolSpec`、`ToolSpecRepository`、`UpdateLogRepository`、`discover_skills`、`discover_tools`、`parse_openai_tool_calls`、`parse_tool_calls`、`read_skill_content`

| 再导出自 | 名称 | 作用 |
|---|---|---|
| `.catalog` | `CatalogInput`、`CatalogOutput`、`ToolCatalogTool` | 工具目录/发现工具（模型可按意图 resolve 出 schema） |
| `.discovery` | `ToolDiscoveryError`、`ToolDiscoveryRecord`、`ToolDiscoveryReport`、`discover_tools` | 从 `tool/` 目录扫描并注册插件 |
| `.models` | `BatchToolResult`、`ExecutionContext`、`ToolCall`、`ToolError`、`ToolResult`、`ToolSpec` | 框架的数据契约（冻结 dataclass + Pydantic 模型） |
| `.parser` | `parse_openai_tool_calls`、`parse_tool_calls` | 原生 tool_calls 与文本工具调用的解析 |
| `.registry` | `BaseTool`、`ToolRegistry` | 工具基类与注册表（含世代号与确认键） |
| `.repository` | `ToolSpecRepository` | 工具 spec 的持久化/查询仓储 |
| `.runtime` | `ToolExecutionManager` | 校验、写确认、并发调度、错误归一的执行器 |
| `.skill_catalog` | `SkillCatalogInput`、`SkillCatalogOutput`、`SkillCatalogTool` | 技能按需加载工具 |
| `.skill_discovery` | `SkillDiscoveryError`、`SkillDiscoveryRecord`、`SkillDiscoveryReport`、`discover_skills`、`read_skill_content` | `skills/<name>.md` 的发现与读取 |
| `.skill_models` | `SkillSpec` | 技能元数据模型 |
| `.skill_registry` | `SkillRegistry` | 技能注册表 |
| `.tool_loop` | `ToolLoop` | 多轮工具调用循环（轮次上限、批处理） |
| `.update_log` | `DEFAULT_UPDATE_LOG_FILENAME`、`UpdateLogRepository` | 项目协议要求的更新日志仓储（SQLite + 连续 ID） |

### `agents/__init__.py` — Agent 门面

- **拓扑层**：L8｜**依赖**：`agents.agent`、`agents.llm`、`agents.providers`、`agents.react`｜**被依赖**：`web.support`
- **职责**：对外暴露 Agent 家族与 LLM/Provider 类型；`__all__` 里同时导出了 `agent`/`react`/`ReAct`/`React` 这些**工厂别名**，容易踩坑：`from agents import agent` 得到的是工厂函数而不是模块（测试里必须用 `importlib.import_module("agents.agent")` 才能拿到模块对象）。

| 再导出自 | 名称 | 作用 |
|---|---|---|
| `.agent` | `Agent`、`agent` | 通用 Agent 类与其工厂函数 |
| `.llm` | `LLM` | OpenAI 兼容客户端（含流式与重试） |
| `.providers` | `ProviderProfile`、`ProviderRegistry` | `config/provider.toml` 的解析与密钥解析 |
| `.react` | `ReAct`、`ReActAgent`、`React`、`ReactAgent`、`parse_react_response`、`react` | 文本 ReAct Agent 类/别名、响应解析器、工厂函数 |

### `memory/__init__.py` — 四层记忆门面

- **拓扑层**：门面（聚合 `memory` 全部实现）｜**依赖**：`memory.base`、`memory.embedding`、`memory.manager`、`memory.rag`、`memory.storage`、`memory.types`｜**被依赖**：`tool.*`、`web.*`、`agents` 的调用方
- **职责**：把「实体/配置/嵌入/四层类型/存储/管理器/RAG 入口」一次性导出，是使用记忆系统的推荐入口（`from memory import MemoryManager, MemoryConfig, HashEmbedding`）。它同时也是第 1.2 节环路的**根因放大器**：门面先 `base` 后 `storage` 的导入顺序决定了存储实现能否拿到 `MemoryItem`。
- **`__all__`（32 项）**：`DEFAULT_EMBEDDING_BASE_URL`、`DEFAULT_EMBEDDING_MODEL`、`APIEmbedding`、`BaseDocumentStore`、`BaseEmbedding`、`BaseMemory`、`BaseVectorStore`、`Document`、`DocumentProcessor`、`EmbedServerEmbedding`、`EpisodicMemory`、`HashEmbedding`、`InMemoryVectorStore`、`MemoryConfig`、`MemoryItem`、`MemoryManager`、`MemorySearchResult`、`MemoryType`、`Neo4jGraphStore`、`PerceptualMemory`、`QdrantVectorStore`、`RAGPipeline`、`RetrievedChunk`、`SQLiteDocumentStore`、`SemanticMemory`、`WorkingMemory`、`cosine_similarity`、`default_sqlite_path`、`ensure_datetime`、`load_dotenv_once`、`make_default_embedding`、`utc_now`

| 再导出自 | 名称 | 作用 |
|---|---|---|
| `.base` | `BaseMemory`、`MemoryConfig`、`MemoryItem`、`MemorySearchResult`、`MemoryType`、`default_sqlite_path`、`ensure_datetime`、`make_default_embedding`、`utc_now` | 记忆实体、配置、时间与嵌入装配 |
| `.embedding` | `DEFAULT_EMBEDDING_BASE_URL`、`DEFAULT_EMBEDDING_MODEL`、`APIEmbedding`、`BaseEmbedding`、`EmbedServerEmbedding`、`HashEmbedding`、`load_dotenv_once` | 网关/API/离线三种嵌入实现与 .env 加载（旧别名 `EmbeddingService` 已删除） |
| `.manager` | `MemoryManager` | 四层记忆总协调器 |
| `.rag` | `Document`、`DocumentProcessor`、`RAGPipeline`、`RetrievedChunk` | RAG 的三个核心类型与管道 |
| `.storage` | `BaseDocumentStore`、`BaseVectorStore`、`InMemoryVectorStore`、`Neo4jGraphStore`、`QdrantVectorStore`、`SQLiteDocumentStore`、`cosine_similarity` | 文档/向量/图存储后端与相似度函数 |
| `.types` | `EpisodicMemory`、`PerceptualMemory`、`SemanticMemory`、`WorkingMemory` | 四层记忆实现 |

### `memory/rag/__init__.py` — RAG 子包门面

- **拓扑层**：门面（聚合 4 个 rag 实现模块）｜**依赖**：`.document`、`.graph_rag`、`.knowledge`、`.pipeline`｜**被依赖**：`tool.rag_*`、`web.support`
- **职责**：集中导出文档解析、知识抽取、实体解析、图检索、总管道。
- **`__all__`（19 项）**：`Document`、`DocumentProcessor`、`EntityCandidate`、`EntityResolver`、`ExtractionResult`、`GraphPath`、`GraphRAGPipeline`、`GraphRAGResult`、`LLMKnowledgeExtractor`、`NullKnowledgeExtractor`、`RAGPipeline`、`RelationCandidate`、`RetrievedChunk`、`build_graph_context`、`entity_id_for`、`materialize_extraction`、`normalize_entity_name`、`predicate_key_for`、`relation_id_for`

> ⚠ **源码不一致（仅记录，未修改）**：`__all__` 声明了 `relation_id_for`，但本文件只从 `.knowledge` 导入了 `entity_id_for` / `normalize_entity_name` / `predicate_key_for` 等，`relation_id_for` **没有**被导入或定义。实测结果：
> - `from memory.rag import relation_id_for` → `ImportError: cannot import name 'relation_id_for' from 'memory.rag'`
> - `from memory.rag import *` → `AttributeError: module 'memory.rag' has no attribute 'relation_id_for'`
>
> 影响面有限：真正的定义在 `memory/ids.py:40`，仓库内所有调用点（`memory/rag/knowledge.py:36`、`memory/types/semantic.py:9`）都直接从 `memory.ids` 或相对路径导入，所以运行时未暴露。但任何按 `__all__` 做星号导入或依赖包级属性的外部代码都会失败。修法有两种：在门面补 `relation_id_for` 导入，或从 `__all__` 删除该名字。

| 再导出自 | 名称 | 作用 |
|---|---|---|
| `.document` | `Document`、`DocumentProcessor` | 文档解析与切块 |
| `.graph_rag` | `GraphPath`、`GraphRAGPipeline`、`GraphRAGResult` | 向量+图混合检索 |
| `.knowledge` | `EntityCandidate`、`EntityResolver`、`ExtractionResult`、`LLMKnowledgeExtractor`、`NullKnowledgeExtractor`、`RelationCandidate`、`build_graph_context`、`entity_id_for`、`materialize_extraction`、`normalize_entity_name`、`predicate_key_for` | 知识抽取、实体归一、图补丁落库 |
| `.pipeline` | `RAGPipeline`、`RetrievedChunk` | 入库/检索总管道与检索结果 |

### `memory/storage/__init__.py` — 存储后端门面

- **拓扑层**：门面｜**依赖**：`.document`、`.document_repo`、`.graph`、`.qdrant`、`.vector`｜**被依赖**：`memory.base`（这就是环的一侧）、`memory.rag.pipeline`
- **职责**：统一暴露「文档库 / 向量库 / 图库 / documents·chunks 真值源」的抽象与实现，`memory/base.py` 只按这些协议装配默认后端。
- **`__all__`（10 项）**：`BaseDocumentStore`、`BaseVectorStore`、`ChunkRecord`、`DocumentRecord`、`DocumentRepository`、`InMemoryVectorStore`、`Neo4jGraphStore`、`QdrantVectorStore`、`SQLiteDocumentStore`、`cosine_similarity`

| 再导出自 | 名称 | 作用 |
|---|---|---|
| `.document` | `BaseDocumentStore`、`SQLiteDocumentStore` | 文档（原文/分块）持久化 |
| `.document_repo` | `ChunkRecord`、`DocumentRecord`、`DocumentRepository` | documents/chunks 真值源（原文 + 分块边界 + FTS5） |
| `.graph` | `Neo4jGraphStore` | 图数据库（含内存回退） |
| `.qdrant` | `QdrantVectorStore` | Qdrant 向量库（持久化向量检索） |
| `.vector` | `BaseVectorStore`、`InMemoryVectorStore`、`cosine_similarity` | 向量库协议、内存实现、余弦相似度 |

### `memory/types/__init__.py` — 四层记忆门面

- **拓扑层**：门面｜**依赖**：`.episodic`、`.perceptual`、`.semantic`、`.working`｜**被依赖**：`memory.manager`
- **职责**：暴露四层记忆类；四者都实现 `memory.base.BaseMemory` 协议，由 `MemoryManager` 统一路由。
- **`__all__`（4 项）**：`EpisodicMemory`、`PerceptualMemory`、`SemanticMemory`、`WorkingMemory`

### `tool/__init__.py` — 工具包门面（懒加载特例）

- **拓扑层**：L4｜**依赖**：`.base`｜**被依赖**：按需
- **职责**：默认只导出 `BaseTool`/`ToolRegistry`；搜索工具的公共类型**不直接导入**，而是通过 PEP 562 的模块级 `__getattr__` 在首次访问时 `import_module(".search", __name__)` 并缓存到 `globals()`。这样做的原因是注释里写明的：**一个坏掉的插件不应让整个 `tool` 包在发现器隔离它之前就不可导入**。
- **函数**：
  - `def __getattr__(name: str) -> Any` — 惰性导出钩子：只放行 `_SEARCH_EXPORTS`（`SearchInput`/`SearchItem`/`SearchOutput`/`SearchTool`）四个名字，命中时 `import_module(".search", __name__)` 取属性、写入 `globals()` 后返回（后续访问直接走模块字典）；未命中抛 `AttributeError(f"module {__name__!r} has no attribute {name!r}")`。
  - `def __dir__() -> list[str]` — 让 `dir(tool)` 也显示惰性名字：返回 `sorted(set(globals()) | _SEARCH_EXPORTS)`。
- **`__all__`（6 项）**：`BaseTool`、`SearchInput`、`SearchItem`、`SearchOutput`、`SearchTool`、`ToolRegistry`
- 关键点：`tool.SearchTool` 这类访问是**惰性**的；`_SEARCH_EXPORTS` + `__dir__()` 让静态检查与 `dir()` 也能看到它们。因此「`__all__` 里有名字但文件里没有对应赋值」在这里是**有意为之**，不是缺陷（静态检查工具会误报）。

### `web/__init__.py` — Web 门面

- **拓扑层**：门面｜**依赖**：`.app`｜**被依赖**：无（对外使用 `python -m web.app` / `from web import create_app`）
- **职责**：只导出 `create_app`，保证 `python -m web.app` 与包内导入两种入口都成立；`__all__` 仅 `create_app`。

### 门面小节总结

- 外部使用**只需要门面**；包内实现之间使用相对导入（`from .base import ...`），避免门面反向参与实现依赖。
- 唯一需要警惕的门面是 `memory/__init__.py`：它聚合了包括存储与类型在内的全部子模块，是第 1.2 节循环依赖的成因之一。
- 已知不一致只有一处：`memory.rag.__all__` 的 `relation_id_for`（见上）。

---

## 5. 本文档的生成与校验方式

| 步骤 | 做法 | 结果 |
|---|---|---|
| 1. 清点 | 递归枚举 `*.py`，排除 `.venv/`、`__pycache__/`、`tests/`、`scripts/` | 实现文件 69 个（61 个实现模块 + 8 个包门面） |
| 2. 结构抽取 | `ast` 解析每个文件，抽取模块 docstring、`__all__`、模块级常量、类/方法/函数/嵌套函数（含装饰器、完整签名、默认值、返回注解、行号） | 函数/方法/嵌套函数 **626** 个、类 **133** 个（合计 759 个定义） |
| 3. 依赖图 | 逐个 import 解析为项目内部模块；`from pkg import Name` 用**符号定义位置**回填到真正定义它的子模块，避免把门面误当依赖 | 61 模块、1 个真实强连通分量 |
| 4. 拓扑排序 | Tarjan 求 SCC → 压缩成分量 → Kahn 分层 | **12 层**；唯一环 = `memory.base ↔ memory.storage.{document,qdrant,vector}`；`memory.storage.document_repo` 单向依赖 `memory.base`，在 L3 但不在环内 |
| 5. 语义分析 | 逐文件阅读源码，补全 AST 拿不到的「为什么」（协议、边界、副作用、调用方） | 第 3、4 章 |
| 6. 覆盖率校验 | 按 `### \`路径\`` 切分本文档为模块小节，再用 AST 得到每个模块的类/函数名集合，逐个检查是否出现在对应小节 | 见下 |
| 7. 运行时校验 | `python scripts/check.py`（ruff + 全量 pytest）、`python -m web.app` 真实 HTTP 冒烟、`/api/chat` 离线端到端冒烟 | 复核时实测：ruff `All checks passed`、pytest **422 passed / 1 skipped**（详见 5.2 节；本版对应 `update_log.sqlite3` 的 `update-log-113`） |

覆盖率自检的做法（可复现）：

```powershell
# 1) 生成结构化清单（模块 → 类/函数名集合）
.venv\Scripts\python.exe <抽取脚本> coverage 项目代码拓扑与接口解析.md
# 2) 期望输出
#    COVERAGE OK: every implementation module has a section and every class/def name appears in it
```

### 5.1 阅读本文档时容易误判的三处

1. **`memory.rag.__all__` 的 `relation_id_for`**：真实悬空名字（`ImportError`/`AttributeError`），见第 4 章。
2. **`tool/__init__.py` 的 `SearchTool` 等 4 个名字**：`__all__` 里有、文件里没有赋值，但这是 **PEP 562 模块级 `__getattr__` 懒加载**，运行时可正常访问，不是缺陷。
3. **`memory` 子包的 import 环**：不是设计上的层次依赖，而是「门面聚合 + 实体/存储互相引用」造成的；读代码时按第 1.2 节给的解释顺序读即可。

### 5.2 测试与嵌入网关的关系（离线可跑的前提）

Phase 0-7 之后，嵌入后端的选择顺序是 `EMBEDDING_BASE_URL`（本机转发网关）> `DASHSCOPE_API_KEY` > `HashEmbedding`。仓库里的 `.env` 通常把 `EMBEDDING_BASE_URL` 指向 `http://127.0.0.1:10800`，**隧道未开时该地址会连接被拒**，凡是构造默认 `MemoryConfig` 并真正调用嵌入的代码路径都会抛 `RuntimeError`（`memory/embedding.py` 的网关请求分支，`EMBED_GATEWAY_HINT` 会提示开隧道），而不是静默降级——这是 D8「绝不切换到别的向量空间」的刻意取舍。

因此测试必须显式隔离该变量才能离线跑：`tests/test_web_chat_react.py:_chat_env` 用 `monkeypatch.setenv("EMBEDDING_BASE_URL", "")` 把网关置空（空串 == 未配置，回落到 `HashEmbedding`；Python 的 `os.environ` 会保留空串，`load_dotenv(override=False)` 不会再用 `.env` 覆盖它）。注意 PowerShell 里 `$env:EMBEDDING_BASE_URL=""` 在 Windows 上等于**删除**变量，反而会让 `.env` 被重新加载——必须在 Python 侧设置空串才有效。

---

## 附录 A：环境变量总表（按读取位置）

| 变量 | 读取位置 | 默认 / 回退 | 作用 |
|---|---|---|---|
| `MEMORY_DB_PATH` | `memory/base.py:default_sqlite_path`、`web/support.py:DB_PATH` | `memory.sqlite3`（项目根） | 记忆库 SQLite 路径；Web、Agent 工具、RAG 共用同一份库 |
| `EMBEDDING_BASE_URL` | `memory/base.py:make_default_embedding`、`web/support.py:build_embedding` | 空 → 用 `DASHSCOPE_API_KEY` | 本机 `qwen-embed` 转发网关地址（`EmbedServerEmbedding`，`POST {base}/embed`），优先级最高；与 API/离线三套向量空间互不兼容 |
| `DASHSCOPE_API_KEY` | `memory/base.py:make_default_embedding`/`MemoryConfig.from_env`、`web/support.py` | 空 → 离线 `HashEmbedding` | 真实嵌入服务密钥（qwen3-embedding-0.6b），仅在无 `EMBEDDING_BASE_URL` 时生效 |
| `HELLOAGENTS_MEMORY_<字段名大写>` | `memory/base.py:MemoryConfig.from_env` | 无 | 批量覆盖 `MemoryConfig` 的任意字段，例如 `HELLOAGENTS_MEMORY_SEARCH_LIMIT=20`；数值字段自动转 `int`/`float`，`none` 视为 `None`；**空值等同未设置**（`VAR=` 不再让 `__post_init__` 因非空字段抛错）。连接真 Qdrant/Neo4j 也在这里：`HELLOAGENTS_MEMORY_QDRANT_URL=http://127.0.0.1:6333`、`HELLOAGENTS_MEMORY_NEO4J_URI=bolt://127.0.0.1:7687`（+`_USERNAME`/`_PASSWORD`），不设则走内存回退 |
| `HELLOAGENTS_MEMORY_HYBRID` | `memory/rag/pipeline.py:_hybrid_enabled` | 开 | 混合检索开关：值为 `0/false/off/no` 时关闭「向量×FTS5 RRF」，退回纯向量 |
| `EMBEDDING_TUNNEL_HINT` | `web/app.py:embedding_tunnel_hint`、`/api/health` | 空 | 网关不可达时由后端下发的隧道命令（部署信息，不写进仓库）；前端只负责显示 |
| `WORKSPACE_ROOT` | `tool/_shared.py:workspace_root` | 项目根 | 文件类工具的沙箱根目录 |
| `WORKSPACE_ALLOW_OUTSIDE` | `tool/_shared.py` | 关闭 | 允许越过沙箱根（危险开关，默认拒绝越界路径） |
| `SEARCH_BASE_URL` / `ANYSEARCH_BASE_URL` | `tool/search.py`（`_first_env`） | 无 → 搜索工具不可用 | 联网搜索服务地址（前者优先） |
| `SEARCH_API` / `SEARCH_API_KEY` / `ANYSEARCH_API_KEY` | `tool/search.py`（`_first_env`） | 无 → 搜索工具不可用 | 联网搜索密钥（按顺序取第一个非空） |
| `UPDATE_LOG_DB_PATH` | `core/update_log.py` | `update_log.sqlite3` | 更新日志 SQLite 路径 |
| `WEB_AUTOSEED` | `web/app.py` lifespan | `1`（开启） | 设为 `0` 关闭启动时种子数据播种 |
| `WEB_QA_EXTRACT` | `web/support.py` | `1`（开启） | 设为 `0`/`false` 关闭问答后的后台图抽取 |
| `WEB_QA_EXTRACT_SYNC` | `web/support.py` | 空（异步） | 设为 `1`/`true`/`yes`/`on` 时同步执行抽取（调试用） |
| `NEBULA_PORT` | `web/app.py:__main__` | `8765`（constants.`DEFAULT_WEB_PORT`） | Web 应用监听端口（仅绑定 `127.0.0.1`） |
| `<profile>.api_key_env` 指向的变量 | `agents/providers.py:resolve_api_key`、`web/support.py:chat_ready` | 无 → `chat_ready=False` | 聊天模型密钥；`config/provider.toml` 里默认配置为 `DEEPSEEK_API_KEY` |

说明：`.env` 与 `.env.*` 被 `.gitignore` 排除，只有 `.env.example` 入库；`load_dotenv(override=False)` 意味着**真实环境变量优先于 `.env` 文件**。

---

## 附录 B：跨文件的关键约定（读代码前必须知道的 8 条）

1. **工具插件协议**：`tool/*.py` 必须导出 `TOOL_ENABLED: bool` 与零参 `create_tool()`；模块名以 `_` 开头（如 `tool/_shared.py`）会被 `core.discovery` 跳过，因此不会被当成工具。`tool/tool_template.py` 是最小可复制模板。
2. **`ToolSpec` 是静态契约**：`name`/`description`/`version`/`input_model`/`output_model`/`side_effect`/`permissions`/`timeout_seconds`/`idempotent`/`parallel_safe`/`max_concurrency`/`tags`/`recommended_before_tools` 在类上声明；`permissions` 目前只是元数据，不做门禁（既定范围）。
3. **写确认门**：`side_effect != "read"` 的工具，调用方必须在 `ExecutionContext.confirmed_side_effects` 里给出 `registry.confirmation_key(name)`，否则 `core.runtime` 直接拒绝并返回结构化错误。Web 聊天只放行 `memory.add`，删除/清空/入库仍需人工确认。
4. **并发调度**：`parallel_safe=False` 的工具在 `core.runtime` 中被强制单飞（每次只跑一个），`max_concurrency` 限制同类并发；`parallel_safe=True` 才允许同层 `asyncio.gather`。文件写/编辑工具因此被标注为不并发安全。
5. **工具错误对外统一脱敏**：执行期异常在 `core.runtime` 中被归一为 `ToolError(code="EXECUTION_ERROR", message="tool execution failed")`；模型与日志看不到原始异常文本（细节只进日志）。写测试断言错误信息时要按这个契约来。
6. **四层记忆语义**：`working`（会话内、可 TTL、容量上限）、`episodic`（事件与问答留痕，带时间戳与 `kind=qa` 等元数据）、`semantic`（长期事实/实体/关系，含 `supersede` 退役与图边）、`perceptual`（多模态/感知条目）。`MemoryManager.search` 默认跨四层，`RAGPipeline.retrieve` 只搜语义层。
7. **嵌入空间不兼容**：`APIEmbedding`（真实语义向量）与 `HashEmbedding`（离线哈希词袋，中文不分词时召回弱）产出的向量**不能混用**；同一份库在切换嵌入实现后应重建索引。`make_default_embedding` 无 key 时自动选离线实现。
8. **项目更新日志协议**：任何实际改动都要调用 `system.update_log` 写入一条记录，用返回的 `next_update_id` 更新 `update_log_readme_first.md` 的**两处**索引，并提交 `update_log.sqlite3`；提交信息格式 `update-log-<id>: <标题>`。`core/update_log.py` 与 `tool/update_log.py` 是这套协议的实现。

---

## 附录 C：运行与调试入口速查

```powershell
# 质量门禁（ruff + 全量 pytest，首个失败即停）
.venv\Scripts\python.exe scripts/check.py

# 启动 Web 应用（知识星云）：默认 http://127.0.0.1:8765
.venv\Scripts\python.exe -m web.app

# 指定端口与独立记忆库（互不干扰地做实验）
$env:NEBULA_PORT="8791"; $env:MEMORY_DB_PATH="$env:TEMP\nebula.sqlite3"; .venv\Scripts\python.exe -m web.app

# 只跑某个关注点
.venv\Scripts\python.exe -m pytest tests/test_web_chat_react.py -q
.venv\Scripts\python.exe -m ruff check .
```

| 想改什么 | 该看哪个文件 |
|---|---|
| 工具协议、执行、确认、并发 | `core/models.py` → `core/registry.py` → `core/runtime.py` → `core/parser.py` |
| Agent 行为、提示词、模型解析 | `agents/agent.py`（基类与调度）→ `agents/react.py`（文本 ReAct 细节） |
| 新增一个工具 | `tool/tool_template.py` + `tool/_shared.py`（沙箱/公共逻辑） |
| 记忆读写、四层路由 | `memory/manager.py`、`memory/types/*`、`memory/base.py` |
| 文档入库与知识抽取 | `memory/rag/pipeline.py` → `memory/rag/knowledge.py` → `memory/rag/graph_rag.py` |
| Web API 与前端数据 | `web/app.py`、`web/support.py`、`web/graph_builder.py` |
| 星云图种子数据 | `web/seed.py`、`web/seed_data.json` |

---

## 附录 D：本解析覆盖范围声明

- **已覆盖**：`constants.py`（1 文件）、`core/`（15 文件）、`agents/`（6 文件）、`tool/`（20 文件）、`memory/`（21 文件，含 `rag/`、`storage/`（新增 `document_repo.py`）与 `types/` 子包）、`web/`（6 文件）—— 即项目全部实现代码，逐文件、逐类、逐函数（含私有函数与嵌套函数），共 69 个文件、626 个函数/方法、133 个类。
- **按结构描述（不逐函数）**：`web/static/index.html`（2,709 行手写单文件前端；零外部资源、无构建步骤，按 12 个编号区与 Phase 7 交互特性描述）。
- **未覆盖（按要求排除）**：`tests/`（36 个测试文件、629 个 def）、`scripts/`（`check.py` 开发门禁、`migrate_storage.py` 迁移回填、`reindex_embeddings.py` 重索引——按需阅读的运维脚本，不逐函数展开）、`skills/*.md`（技能提示词）、`docs/`、各 `README.md`、`update_log_readme_first.md`、`存储架构规划方案.md`。
- **第三方依赖**（`.venv/`）不在范围内；文档中出现的 `openai`/`qdrant-client`/`neo4j`/`fastapi`/`pydantic` 等只按项目内的使用方式描述。
- **时间点**：基于 `python-course-design` 分支提交 `913f4f7`（日志 113）时的代码，覆盖 Phase 0-7（存储全本地 + 双云端出网点、documents/chunks 真值源、混合检索、对账自愈、Phase 7 UI 收官）；此后若要长期使用，请在每次结构性改动后重跑第 5 章的覆盖率校验。

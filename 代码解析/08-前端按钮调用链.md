# 08 · 前端按钮调用链

> 分析对象：`web/static/index.html`（3173 行，HTML + 内联 IIFE JS）与其后端 `web/app.py`（1092 行）及下游 `web/*`、`memory/*`、`tool/*`、`agents/*`。
> 本文只做只读分析，所有行号均来自实际读取的文件内容，未编造。
> 前端代码整体包在 `(function () { "use strict"; ... })();`（`index.html:1395`–`3170`）内，所有处理函数都是该 IIFE 的局部函数，靠 `addEventListener` 绑定，没有内联 `onclick` 属性。

> ## ⚠️ 行号时效性警告（务必先读）
>
> 本次分析期间，工作区被**其它进程/协作者持续并发重构**。分析过程中我至少观察到三轮改动：
>
> | 时间 | 被改动的文件 | 变化 |
> | --- | --- | --- |
> | 22:28–22:34 | `agents/agent.py`、`agents/providers.py`、`memory/rag/pipeline.py`、`memory/rag/knowledge.py`、`memory/base.py`、`memory/storage/graph.py` | 方法增删，行号整体位移 |
> | 22:34–22:35 | 新增 `tool/hybrid_index.py`、`tool/hybrid_recall.py` | 混合检索从 `RAGPipeline` 抽出成独立工具 |
> | 22:40–22:45 | `tool/hybrid_index.py`、`tool/hybrid_recall.py`、`memory/rag/knowledge.py`(22:41:12)、`memory/rag/pipeline.py`(22:42:26)、`memory/rag/graph_rag.py`(22:42:36)、`web/app.py`(22:42:52)、新增 `tool/graph_node_update.py`(22:44:44)、`tool/multi_recall.py`(22:45:16) | 大幅重构 |
>
> **本文所有行号对应 2026-09-21 22:48 的快照**，并已在 22:47–22:48 对当时磁盘内容逐条重新核对。§10 给出了每个文件的确切 mtime 与「符号 → 行号」对照表，**文件一旦再被改动，请用符号名重新定位，不要直接信任本文行号**。
>
> ### 本轮重构造成的结构性变化（不只是行号位移）
>
> 1. **混合召回搬家了**：`RAGPipeline.hybrid_retrieve`、`RAGPipeline._vector_hits`、`RAGPipeline._rrf_fuse` 已从 `memory/rag/pipeline.py` **删除**，现在唯一实现在 `tool/hybrid_recall.py`（`hybrid_recall` 在 `hybrid_recall.py:143`，`rrf_fuse` 在 `hybrid_recall.py:55`）。`web/app.py:71` 导入、`web/app.py:366` 调用。`memory/rag/pipeline.py:14` 的文件注释也明确记录了这次搬家。
> 2. **`GraphRAGPipeline.retrieve_multi` 被删除**（原 `graph_rag.py:148`）。
> 3. **`memory/rag/pipeline.py` 从 534 行缩到 402 行**，`memory/rag/knowledge.py` 从 1017 行缩到 934 行，`memory/rag/graph_rag.py` 从 397 行缩到 360 行，`web/app.py` 从 1090 行增到 1092 行。
> 4. **新增了 4 个工具文件**，但 `web/support.py` 未同步改动，因此它们**尚未进入聊天工具白名单**：`CHAT_TOOL_ALLOWLIST`（`support.py:239–245`）仍只有 `memory.query` / `memory.add` / `memory.rag_search` / `memory.rag` / `web.search` 五项；`tool/hybrid_recall.py`（`knowledge.hybrid_recall`）、`tool/hybrid_index.py`、`tool/multi_recall.py`、`tool/graph_node_update.py` 目前只被 `web/app.py` 与 `memory/rag/pipeline.py` 直接调用，LLM 在聊天里看不到它们。

---

## 0. 总体架构与请求入口分层

```
┌─ 浏览器 index.html（单页，无框架，原生 fetch）
│   事件入口：<button id> / <input> / <select> / <textarea> / keydown / 动态生成的卡片
│   ↓
│   前端工具层（index.html:1419–1476）
│     apiFetch(path, options, timeoutMs)      1419  统一 AbortController 超时 + 错误归一
│     apiGet   = (path, t)                    1439  GET
│     apiPostJson = (path, body, t)           1440  POST + JSON body
│     apiPostForm = (path, form, t)           1445  POST + FormData（上传）
│     withConfirmRebuild(path, confirm)       1446  追加 ?confirm_rebuild=true
│     withEmbeddingGuard(send)                1469  409 embedding_lock_mismatch → 二次确认重发
│   ↓  HTTP
├─ FastAPI 路由层 web/app.py:create_app()     196
│   20 个 /api/* 端点（见 §2 总表）
│   ↓
├─ web 支撑层
│   web/support.py      单例 MemoryManager / RAGPipeline / ReActAgent、图谱 revision、问答留痕
│   web/graph_builder.py 四层记忆 → 星云图 nodes+edges
│   web/ingest_queue.py  一句话入库的 SQLite 持久化后台队列
│   web/seed.py          Aetheria 种子数据播种
│   web/cleanup.py       孤儿实体统计
│   web/domain_classifier.py 领域分类
│   ↓
├─ memory/rag 层
│   RAGPipeline（pipeline.py:89） ingest / ingest_media / ingest_source / retrieve / graph_retrieve
│   GraphRAGPipeline（graph_rag.py:86） seed 实体 → 多跳扩展 → 回忆即强化
│   EntityResolver + materialize_extraction（knowledge.py:377 / 528） LLM 抽取补丁落库
│   DocumentProcessor（document.py:48） 解析 + 切块
│   ↓
├─ tool 层（本轮重构新增的落点）
│   tool/hybrid_recall.py:143  hybrid_recall()  向量路 × FTS5 路 → RRF 融合
│   tool/hybrid_index.py:72    index_chunk()    一段文本同时写关键词真值源 + 向量投影
│   tool/multi_recall.py:150   hybrid_recall_multi()  多路子查询融合
│   ↓
├─ memory 门面
│   MemoryManager（manager.py:40） add/get/delete/search/list/clear + 四个记忆类型实例
│   BaseMemory.add（base.py:418） 先写真值源、再写向量（失败回滚）
│   SemanticMemory.add_fact（types/semantic.py:28） → _write_edge（150）
│   EpisodicMemory.record（types/episodic.py:15）
│   ↓
└─ 存储（四层落地）
    SQLite `memories`            memory/storage/document.py:81（SQLiteDocumentStore）
    SQLite `documents`+`chunks`  memory/storage/document_repo.py:144/161（DocumentRepository，真值源）
    SQLite `chunks_fts` (FTS5)   memory/storage/document_repo.py:219（BM25 关键词路）
    SQLite `ingest_jobs`         memory/storage/document_repo.py:192（入库队列状态）
    SQLite `embedding_lock`      memory/storage/document_repo.py:209（嵌入维度锁）
    Qdrant 向量                  memory/storage/qdrant.py:73（未配置时 InMemoryVectorStore vector.py:34）
    Neo4j 图                     memory/storage/graph.py:82（未配置时内存回退，graph_snapshot 不可用时 graph_builder 退回 SQLite 推断）
```

---

## 1. 入口统计口径

本文的计数规则（写死在这里，避免歧义）：

| 类别 | 计数 | 说明 |
| --- | --- | --- |
| 按钮（`<button id="...">`） | **27** | 26 个静态写在 HTML 里 + 1 个（`doc-revectorize`）由 `renderDocDetail` 动态注入 |
| 其它交互入口 | **44** | 表单/输入 16 + 文件选择 3 + 动态生成的交互节点 9 类 + 画布/事件委托/键盘/手势 16 |
| 页面加载自动请求 | **5** | 4 个无条件（boot 两个 + 两个轮询）+ 1 个条件触发（`embedding_mismatch` 时） |
| 后端 `/api/*` 端点 | **20** | `web/app.py` 中的 20 个路由装饰器 |
| 有前端入口的端点 | **18** | 缺 `POST /api/seed`、`GET /api/stats` |
| 前端调用了但后端不存在 | **0** | 见 §6 |

> `web/static/index.html` 全文在本次分析期间**未被改动**（mtime 21:48:44），因此本文所有 `idx:` 行号稳定可信。

---

## 2. 总表：入口 → 后端 → 存储

行号格式为 `文件:行`。`index.html` 简写为 `idx`。

| 入口 id | 界面文案 | index.html 行号 | 处理函数 | 后端端点 | 后端函数(文件:行) | 主要下游调用链 | 触及存储 | 前端渲染 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `search-input` | 搜索星图：标题/内容/领域… | 1174（绑定 2366/2367） | `doSearch` (2372) | **无（纯前端）** | — | 遍历 `graph.nodes` 本地 `includes` | 读：无 | `#search-results` (1175) |
| `btn-add-knowledge` | ＋ 添加知识 | 1177（绑定 2584） | 内联箭头函数 | **无（纯前端）** | — | `renderDrawer` / `#fact-subject.focus()` | 无 | `#detail-drawer` |
| `btn-ingest` | ⤒ 导入文档 | 1178（绑定 2595） | 内联 → `#file-input.click()` | 见 `file-input` | — | — | — | — |
| `btn-documents` | ▤ 文档中心 | 1179（绑定 2867） | `openDocumentsPanel` (2858) | `GET /api/documents` | `list_documents` (app.py:896) | `the_repository`(774) → `RAGPipeline.document_repo`(pipeline.py:106) → `DocumentRepository.list_documents`(document_repo.py:311) + `chunk_counts`(445) | 读：SQLite `documents`/`chunks` | `renderDocList`(2744) → `#doc-list`(1241) |
| `btn-export` | ⤓ 导出星图 | 1180（绑定 2617） | 内联 → `window.open("/api/export")` | `GET /api/export` | `export` (app.py:639) | `MemoryManager.list`(manager.py:144) → `SQLiteDocumentStore.list`(document.py:140) | 读：SQLite `memories` | 浏览器直接下载（无 JS 渲染） |
| `btn-chat` | ✦ 知识管家 | 1181（绑定 3133） | 内联 toggle | **无（纯前端）** | — | `#chat-panel.classList.toggle("open")` | 无 | `#chat-panel` |
| `drawer-reopen-trigger` | 展开天体详情 | 1195（绑定 2232） | 内联 → `renderDrawer` | **无（纯前端）** | — | — | 无 | `#detail-drawer` |
| `btn-reset` | 回到全景宏观视野 | 1204（绑定 2305） | 内联 → `zoomToLevel(-1)` (2081) | **无（纯前端）** | — | 相机 + 面包屑 | 无 | canvas |
| `edge-toggle-btn` | 引力桥 ON/OFF | 1205（绑定 2306） | 内联箭头函数 | **无（纯前端）** | — | 翻转 `edgesVisible`(1807) | 无 | canvas |
| `filter-domain` | 全部领域（select） | 1206（绑定 2346） | `applyDomainFilter` (2337) | **无（纯前端）** | — | `buildStarSystem`(1616)/`renderTourPanel`(1742)/`computeOverview`(1717)/`updateStats`(1734) | 读：内存 `graph.nodes` | canvas + `#tour-list` |
| `graph-as-of` | datetime-local 时间点 | 1209（被 2348 读取） | 被 `graph-as-of-apply` 读取 | 见下行 | — | — | — | — |
| `graph-as-of-apply` | 时间点 | 1210（绑定 2347） | 内联 async → `loadGraph({at})` (1569) | `GET /api/graph?at=ISO` | `graph` (app.py:271) | `build_graph`(graph_builder.py:93) → `_graph_snapshot`(77) → `Neo4jGraphStore.graph_snapshot`(graph.py:520)；`MemoryManager.list`(manager.py:144) | 读：Neo4j 图 + SQLite `memories` | `buildStarSystem` + `updateStats` |
| `graph-as-of-clear` | 当前 | 1211（绑定 2355） | 内联 async → `loadGraph({at:""})` | `GET /api/graph` | `graph` (app.py:271) | 同上 | 同上 | 同上 |
| `btn-storage` | 存储健康 | 1219（绑定 2980） | 内联 async | `GET /api/reconcile` | `reconcile` (app.py:1005) | `reconcile_report`(810) → `chunk_ids`(document_repo.py:454)/`semantic.facts`(semantic.py:260)/`vector_store.list_ids`(qdrant.py:297)/`graph_store.relation_memory_ids`(graph.py:685) | 读：SQLite `chunks`/`memories` + Qdrant + Neo4j | `renderStoragePanel`(2926) → `#storage-modes`(1261)/`#storage-counts`(1265)/`#storage-drift`(1269) |
| `docs-close` | ✕ | 1225（绑定 2870） | `openDocumentsPanel(false)` | 无 | — | — | 无 | `#documents-panel` |
| `doc-tag` | 按标签筛选… | 1228（绑定 2875） | `loadDocuments(1)` | `GET /api/documents?tag=` | `list_documents` (app.py:896) | 同 `btn-documents` | 读：SQLite `documents`/`chunks` | `renderDocList` |
| `doc-status` | 全部状态（select） | 1229（绑定 2876） | `loadDocuments(1)` | `GET /api/documents?status=` | `list_documents` (app.py:896) | 同上 | 同上 | 同上 |
| `doc-refresh` | 刷新 | 1237（绑定 2871） | `loadDocuments(1)` (2763) | `GET /api/documents` | `list_documents` (app.py:896) | 同上 | 同上 | 同上 |
| `doc-import` | 恢复导出 JSON | 1238（绑定 2895） | 内联 → `importInput.click()` | 见 `importInput` | — | — | — | — |
| `doc-prev` | 上一页 | 1243（绑定 2872） | `loadDocuments(page-1)` | `GET /api/documents?page=` | `list_documents` (app.py:896) | 同上 | 同上 | 同上 |
| `doc-next` | 下一页 | 1245（绑定 2873） | `loadDocuments(page+1)` | `GET /api/documents?page=` | `list_documents` (app.py:896) | 同上 | 同上 | 同上 |
| `doc-back` | ← 返回列表 | 1249（绑定 2874） | `showDocList` (2852) | 无 | — | — | 无 | 切 `#doc-list-wrap`/`#doc-detail-wrap` |
| `storage-close` | ✕ | 1257（绑定 2988） | 内联 | 无 | — | — | 无 | `#storage-panel` |
| `btn-reconcile` | 重新对账 | 1272（绑定 2992） | `refreshReconcile` (2971) | `GET /api/reconcile` | `reconcile` (app.py:1005) | 同 `btn-storage` | 读三库 | `renderStoragePanel` |
| `btn-repair` | 一键修复 | 1273（绑定 2993） | 内联 async | `POST /api/reconcile` | `reconcile_repair` (app.py:1009) → `repair_drift`(845) | `reconcile_report`(810) → `embedding.embed_batch`(embedding.py:186) → `vector_store.upsert_chunk`(qdrant.py:233) → `set_chunk_vector_status`(document_repo.py:410)；`semantic.add_fact`(semantic.py:28) | 写：Qdrant 向量 + SQLite `chunks`/`memories` + Neo4j 边 | toast + `refreshReconcile` |
| `drawer-close` | ✕ | 1278（绑定 2231） | `closeDrawer` (2222) | 无 | — | — | 无 | `#detail-drawer` |
| `fact-submit` | ✦ 点亮新的知识节点 | 1338（绑定 2429） | `submitFact` (2402) | `POST /api/facts` | `add_fact` (app.py:443) | `semantic.add_fact`(semantic.py:28) → `BaseMemory.add`(base.py:418) → `document_store.upsert`(document.py:101) + `vector_store.upsert`(qdrant.py:219)；`_write_edge`(semantic.py:150) → `graph_store.add_relation`(graph.py:190) | 写：SQLite `memories` + Qdrant + Neo4j | `loadGraph()` 全量 + `renderDrawer` |
| `knowledge-submit` | ✦ AI 一句话入库 | 1343（绑定 2463） | `submitKnowledgeSentence` (2435) | `POST /api/knowledge` | `add_knowledge` (app.py:460) | `guard_embedding`(233) → `IngestJobQueue.submit`(ingest_queue.py:99) → `create_ingest_job`(document_repo.py:525) → worker `_run_job`(163) → `RAGPipeline.ingest`(pipeline.py:122) | 写：SQLite `ingest_jobs`/`documents`/`chunks`/`memories` + Qdrant + Neo4j | toast + `refreshIngestJobs()`(2498) |
| `ingest-jobs-toggle` | — 入库记录（后台队列） — | 1344（绑定 2543/2544） | `toggle`（IIFE 内） | **无（纯前端）** | — | `localStorage` 记忆折叠态 | 无 | `#ingest-jobs` 折叠 |
| `image-submit` | 📷 选择图片并识图入库 | 1355（绑定 2553） | `submitImageKnowledge` (2549) → `#image-input.click()` | 见 `image-input` | — | — | — | — |
| `chat-close` | ✕ | 1368（绑定 3137） | 内联 | 无 | — | — | 无 | `#chat-panel` |
| `chat-mode-select` | 非联网/联网/图谱推理 | 1374（绑定 2662） | `onChatModeChange` (2652) | **无（纯前端，影响后续 /api/chat 的 mode）** | — | `applyChatMode`(2646) → `refreshSendButton`(2640) | 无 | `#chat-mode-hint`(1382) |
| `chat-textarea` | 输入问题… | 1379（绑定 3129） | `sendChat` (3062) | `POST /api/chat` 或 `POST /api/graph-rag` | 见 `chat-send-btn` | — | — | — |
| `chat-send-btn` | 发送 | 1380（绑定 3128） | `sendChat` (3062) | `POST /api/chat` / `POST /api/graph-rag` | `chat` (app.py:316) / `graph_rag` (app.py:303) | 见 §5.12 | 读/写：三库 + 外部 LLM | `appendBubble`(3053) + `appendRetrieval`(2696) + `renderEvidence`(2665) + `highlightPath`(1509) |
| `file-input` | （隐藏）导入文档 | 1385（绑定 2596） | 内联 async | `POST /api/ingest` | `ingest` (app.py:405) | `_save_upload`(160) → `guard_embedding`(233) → `pipeline.ingest_source`(pipeline.py:322) → `DocumentProcessor.parse`(document.py:51) → `RAGPipeline.ingest`(pipeline.py:122) | 写：`documents`/`chunks`/`memories` + Qdrant + Neo4j | toast + `loadGraph()` |
| `image-input` | （隐藏）拍照/选图 | 1356（绑定 2554） | 内联 async | `POST /api/knowledge/image` | `add_image_knowledge` (app.py:574) | `_save_upload`(160) → `pipeline.ingest_media`(pipeline.py:241) → `BaseMemory.add`(base.py:418, PERCEPTUAL+payload) → `extractor.extract`(knowledge.py:305) → `materialize_extraction`(knowledge.py:528) | 写：`memories`(perceptual) + Qdrant + Neo4j | toast + `loadGraph()` |
| `doc-revectorize` | 重索引（重嵌入全部块） | **2812**（动态注入；绑定 2819） | `revectorizeDocument` (2833) | `POST /api/documents/{id}/revectorize` | `revectorize_document` (app.py:957) | `guard_embedding`(963) → `embedding.embed_batch`(embedding.py:186) → `vector_store.upsert_chunk`(qdrant.py:233) → `set_chunk_vector_status`(410) + `set_status`(342) | 写：Qdrant + SQLite `chunks`/`documents` | `openDocument` + `loadDocuments` |
| `importInput`（动态创建） | 恢复导出 JSON | **2890**（创建；绑定 2896） | 内联 async | `POST /api/import` | `import_file` (app.py:666) | `_save_upload`(666) → `manager.get`/`semantic.add_fact`(semantic.py:28)/`manager.add`(manager.py:109) | 写：`memories` + Qdrant + Neo4j | toast + `loadGraph()` + `loadDocuments(1)` |
| `job-retry`（动态生成） | 重试 | **2477**（模板；委托 2517） | `retryIngestJob` (2486) | `POST /api/knowledge/jobs/{id}/retry` | `retry_knowledge_job` (app.py:555) | `guard_embedding`(561) → `queue.retry`(ingest_queue.py:111) → `reset_failed_ingest_job`(document_repo.py:613) | 写：SQLite `ingest_jobs` | `refreshIngestJobs` |
| `doc-item`（动态生成） | 文档卡片 | **2752**（委托 2877/2881） | `openDocument` (2822) | `GET /api/documents/{id}` | `get_document` (app.py:927) | `repository.get_document`(304) + `list_chunks`(403) | 读：SQLite `documents`/`chunks` | `renderDocDetail`(2802)/`renderDocText`(2781) → `#doc-detail`(1250) |
| `canvas#universe` | 星云画布 | 1201（绑定 2291/2292） | 内联 → `drillIntoNode` (2065) | **无（纯前端）** | — | `renderDrawer` + `updateBreadcrumb` | 无 | `#detail-drawer` |
| `.crumb-item`（动态生成） | 面包屑 | **2105–2114** | 内联 → `zoomToLevel` | 无 | — | — | 无 | canvas |
| `.tour-btn`（动态生成） | 天体系统飞越 | **1750–1756** | 内联 → `drillIntoNode` | 无 | — | — | 无 | `#detail-drawer` |
| `.parent-uplink-btn`（动态生成） | ← 飞向母星 | **2136–2139** | 内联 → `drillIntoNode` | 无 | — | — | 无 | `#detail-drawer` |
| `.subnode-card`（关联/伴生，动态生成） | 因果引力关联 / 伴生下一级 | **2171–2179** / **2193–2204** | 内联 → `drillIntoNode` | 无 | — | — | 无 | `#detail-drawer` |
| `.search-hit`（动态生成） | 搜索结果行 | **2385–2392** | 内联 → `drillIntoNode` | 无 | — | — | 无 | canvas + 抽屉 |
| `.evidence-item`（动态生成） | 检索依据 | **2674–2683** | 内联 → `renderDrawer` | 无 | — | — | 无 | `#detail-drawer` |
| `mark.doc-chunk`（动态生成） | 分块高亮 | **2792–2795**（委托 2814） | 内联 → `toast` | 无 | — | — | 无 | toast |

### 2.1 页面加载 / 定时自动请求

| # | 触发点 | index.html 行号 | 请求 | 后端函数 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 1 | `boot()` 首步 | 3147 → 3149 → 1581 | `GET /api/graph`（`since=0` 时即全量） | `graph` (app.py:271) | 先 `restoreGraphCache()`(1546) 读 `sessionStorage`，命中则带 `since` 走增量 |
| 2 | `boot()` 第二步 | 3150 → 3031 | `GET /api/health` | `health` (app.py:1030) | 结果写 `backendUp`/`chatReady`，并调 `renderStoragePanel` + `offerEmbeddingRebuild` |
| 3 | 定时器 20s | 3168（`GRAPH_POLL_MS`=1483） | `GET /api/graph?since=<revision>` | `graph` (app.py:271) | 后端 `unchanged=true` 时前端直接 `return false`（1583），不重排 |
| 4 | 定时器 3s | 2514 → 2498 → 2500 | `GET /api/knowledge/jobs?limit=8` | `list_knowledge_jobs` (app.py:537) | `document.hidden` 时跳过（2514）；若发现新 `done` 任务，追加一次 `loadGraph({since:true})`（2511） |
| 5 | 条件触发 | 3043 → 3005 → 3020 | `POST /api/embedding/rebuild?confirm_rebuild=true` | `rebuild_embedding` (app.py:1013) | 仅当 `/api/health` 返回 `embedding_mismatch=true` 且用户确认时 |

另有 `render()`（3169 → 1964）通过 `requestAnimationFrame` 自循环，纯本地绘制，不发请求。

---

## 3. 前端工具层与关键约定

### 3.1 `withConfirmRebuild` 与嵌入锁 409 协议

- `withConfirmRebuild(path, confirm)`（1446）：`confirm` 为真时把 `confirm_rebuild=true` 拼到 query 上。
- `withEmbeddingGuard(send)`（1469）：先用 `confirm=false` 调一次；若抛出的错误满足 `err.status === 409 && err.detail.code === "embedding_lock_mismatch"`（判断在 `confirmEmbeddingRebuild` 1455–1459），弹 `window.confirm`，用户同意后用 `confirm=true` 重发。
- 使用该守卫的前端入口共 **5** 个：`/api/knowledge`(2446)、`/api/knowledge/image`(2567)、`/api/ingest`(2605)、`/api/knowledge/jobs/{id}/retry`(2489)、`/api/documents/{id}/revectorize`(2838)。
- 后端对应：`guard_embedding`(app.py:233) → `apply_embedding_lock`(embedding_lock.py:135) → 不一致且未确认时抛 `EmbeddingLockMismatch`(embedding_lock.py:25)，`to_detail()`(41) 产出 `code/locked/current` 三段，由 `raise HTTPException(409, detail=...)`(app.py:241) 抛出。确认后走 `rebuild_vector_projection`(embedding_lock.py:189) 重建集合 + 全量重灌。

### 3.2 星云图增量协议

- `loadGraph`(1569) 的 query 选择逻辑：`at` 非空 → `/api/graph?at=`；否则 `track`（`options.since` 为真且无 `at`）→ `/api/graph?since=<graph.revision>`；否则 `/api/graph`。
- 后端 `graph`(app.py:271) 用进程内 `graph_cache`(app.py:256) 按 `revision + at` 缓存 `build_graph` 结果（命中判断 282–289）；`since >= 0 && since == revision && !at` 时只回 `{revision, unchanged:true, nodes:[], edges:[]}`（约 291–298）。
- 写操作统一走 `invalidate_graph()`(app.py:261) → `bump_graph_revision()`(support.py:105)，后台抽取线程也走同一计数器（`extract_graph_patches` 内 `bump_graph_revision()` support.py:367）。

---

## 4. 后端端点清单（`web/app.py`）

| # | 方法 | 路径 | 函数 | 行号 | 前端入口 |
| --- | --- | --- | --- | --- | --- |
| 1 | GET | `/api/graph` | `graph` | 271 | ✅ boot / 轮询 / 时间点 |
| 2 | POST | `/api/graph-rag` | `graph_rag` | 303 | ✅ `chat-send-btn`（graphrag 模式） |
| 3 | POST | `/api/chat` | `chat` | 316 | ✅ `chat-send-btn` / `chat-textarea` |
| 4 | POST | `/api/ingest` | `ingest` | 405 | ✅ `btn-ingest` + `file-input` |
| 5 | POST | `/api/facts` | `add_fact` | 443 | ✅ `fact-submit` |
| 6 | POST | `/api/knowledge` | `add_knowledge` | 460 | ✅ `knowledge-submit` |
| 7 | GET | `/api/knowledge/jobs` | `list_knowledge_jobs` | 537 | ✅ 3s 轮询 |
| 8 | POST | `/api/knowledge/jobs/{job_id}/retry` | `retry_knowledge_job` | 555 | ✅ `job-retry`（动态） |
| 9 | POST | `/api/knowledge/image` | `add_image_knowledge` | 574 | ✅ `image-submit` + `image-input` |
| 10 | POST | `/api/seed` | `reseed` | 633 | ❌ **无前端入口**（仅 lifespan 自动播种 app.py:220–222） |
| 11 | GET | `/api/export` | `export` | 639 | ✅ `btn-export` |
| 12 | POST | `/api/import` | `import_file` | 666 | ✅ `doc-import` + `importInput`（动态） |
| 13 | GET | `/api/documents` | `list_documents` | 896 | ✅ 文档中心全部列表操作 |
| 14 | GET | `/api/documents/{document_id}` | `get_document` | 927 | ✅ `doc-item` |
| 15 | POST | `/api/documents/{document_id}/revectorize` | `revectorize_document` | 957 | ✅ `doc-revectorize` |
| 16 | GET | `/api/stats` | `stats` | 994 | ❌ **无前端入口** |
| 17 | GET | `/api/reconcile` | `reconcile` | 1005 | ✅ `btn-storage` / `btn-reconcile` |
| 18 | POST | `/api/reconcile` | `reconcile_repair` | 1009 | ✅ `btn-repair` |
| 19 | POST | `/api/embedding/rebuild` | `rebuild_embedding` | 1013 | ✅ 自动（`checkHealth` → `offerEmbeddingRebuild`） |
| 20 | GET | `/api/health` | `health` | 1030 | ✅ boot |

> 文档字符串不一致：`web/app.py:5` 的模块注释写「GET /api/graph-rag」，实际装饰器是 `@app.post`（app.py:302）。前端（3080）用 POST，与实现一致。
>
> 其它 `web/app.py` 关键位置（当前快照）：`_reject_json_constant` 94、`_is_fact_item` 106（本轮新增）、`embedding_config_hint` 113、`_save_upload` 160、`create_app` 196、`lifespan` 200、`the_manager` 230、`guard_embedding` 233、`raise_embedding_http` 243、`invalidate_graph` 261、`the_repository` 774、`fact_items` 785、`projected_vector_ids` 788、`projected_edge_ids` 799、`reconcile_report` 810、`repair_drift` 845。

---

## 5. 分组调用链（缩进树）

约定：节点写成 `模块.函数 (文件:行)`；叶子节点末尾标注 `[读]`/`[写]`/`[外部]`。

### 5.1 星图浏览 / 时间点 / 筛选

```
用户点击 canvas / .tour-btn / .crumb-item / .subnode-card / .parent-uplink-btn
└─ index.html 内联 → drillIntoNode (idx:2065)                        [纯前端]
   ├─ updateBreadcrumb (idx:2102) → getBreadcrumbPath (idx:2095)
   └─ renderDrawer (idx:2118) → #drawer-* DOM 写入

用户点击 btn-reset (idx:1204)
└─ zoomToLevel(-1) (idx:2081)                                        [纯前端]

用户点击 edge-toggle-btn (idx:1205)
└─ 内联 (idx:2306)：edgesVisible = !edgesVisible                      [纯前端]

用户切换 filter-domain (idx:1206)
└─ applyDomainFilter (idx:2337)                                      [纯前端，领域列表来自 /api/graph]
   ├─ buildStarSystem (idx:1616)
   ├─ renderTourPanel (idx:1742)
   ├─ computeOverview (idx:1717)
   ├─ zoomToLevel(-1) (idx:2081)
   └─ updateStats (idx:1734) → filteredStats (idx:2323)

用户点击 graph-as-of-apply (idx:1210)
└─ loadGraph({at: value}) (idx:1569)
   └─ apiGet(`/api/graph?at=<ISO>`) (idx:1579, 1439)
      └─ web.app.graph (web/app.py:271)
         ├─ web.support.graph_revision (web/support.py:111)
         ├─ web.graph_builder.build_graph (web/graph_builder.py:93)
         │  ├─ memory.manager.MemoryManager.list (memory/manager.py:144)
         │  │  └─ memory.storage.document.SQLiteDocumentStore.list (memory/storage/document.py:140)
         │  │     └─ SQLite `memories`                                [读]
         │  ├─ web.graph_builder._graph_snapshot (web/graph_builder.py:77)
         │  │  └─ memory.storage.graph.Neo4jGraphStore.graph_snapshot (memory/storage/graph.py:520)
         │  │     └─ Neo4j（内存回退 / 不可用时返回 None → 退回 SQLite 推断）  [读]
         │  ├─ web.graph_builder._entity_aliases (web/graph_builder.py:62)
         │  │  └─ memory.storage.graph.Neo4jGraphStore.entity_aliases (memory/storage/graph.py:502) [读]
         │  ├─ web.domain_classifier.classify_domain (web/domain_classifier.py:80)
         │  ├─ web.domain_classifier.majority_domain (web/domain_classifier.py:104)
         │  └─ web.cleanup.find_orphan_entities (web/cleanup.py:19)    [读]
         └─ 进程内 graph_cache (web/app.py:256)                        [缓存]
   └─ 前端：buildStarSystem(1616) / renderDomainOptions(2315) / renderTourPanel(1742)
            / updateStats(1734) / saveGraphCache(1541)

用户点击 graph-as-of-clear (idx:1211)
└─ loadGraph({at: ""}) → 同上，`/api/graph`（无 at）
```

### 5.2 搜索（纯前端，不触后端）

```
search-input input(150ms debounce, idx:2366) / focus (idx:2367)
└─ doSearch (idx:2372)
   ├─ 遍历 graph.nodes 做 `hay.includes(q)`（idx:2376–2379）           [纯前端]
   └─ 渲染 #search-results (idx:1175)，每行 .search-hit (idx:2385)
      └─ 点击 → drillIntoNode (idx:2391 → 2065)

⚠️ 这是全库内存过滤，不调用 /api/*；因此它看不到 chunks 真值源里的
   分块原文，只能匹配 /api/graph 已下发的 title/content/domain 字段。
```

### 5.3 添加三元组

```
fact-submit click (idx:1338 → 2429)
└─ submitFact (idx:2402)
   └─ apiPostJson("/api/facts", {subject,predicate,object,domain,note}) (idx:2415, 1440)
      └─ web.app.add_fact (web/app.py:443)
         ├─ memory.manager.MemoryManager.semantic → SemanticMemory (memory/manager.py:88)
         ├─ memory.types.semantic.SemanticMemory.add_fact (memory/types/semantic.py:28)
         │  ├─ memory.ids.relation_id_for (memory/ids.py:42)          [确定性 id，去重]
         │  ├─ memory.base.BaseMemory.add (memory/base.py:418)
         │  │  ├─ memory.base.BaseMemory._embed_item (memory/base.py:400)
         │  │  │  └─ memory.embedding.BaseEmbedding.embed_item (memory/embedding.py:64)
         │  │  │     └─ APIEmbedding.embed (memory/embedding.py:181) / HashEmbedding.embed (memory/embedding.py:100)
         │  │  │        └─ 云端嵌入 HTTP 或本地哈希                     [外部/本地]
         │  │  ├─ memory.storage.document.SQLiteDocumentStore.upsert (memory/storage/document.py:101)
         │  │  │  └─ SQLite `memories`                                 [写]
         │  │  └─ memory.storage.qdrant.QdrantVectorStore.upsert (memory/storage/qdrant.py:219)
         │  │     └─ 或 memory.storage.vector.InMemoryVectorStore.upsert (memory/storage/vector.py:41)
         │  │        └─ Qdrant / 内存向量                              [写]
         │  └─ memory.types.semantic.SemanticMemory._write_edge (memory/types/semantic.py:150)
         │     ├─ memory.storage.graph.Neo4jGraphStore.add_relation (memory/storage/graph.py:190) [写]
         │     └─ memory.storage.graph.Neo4jGraphStore.add_observation (memory/storage/graph.py:278) [写]
         └─ web.app.invalidate_graph (web/app.py:451)
            └─ web.support.bump_graph_revision (web/support.py:105)   [缓存失效]
   └─ 前端：toast → loadGraph() 全量 → renderDrawer(当前焦点) (idx:2418–2422)
```

### 5.4 一句话入库（后台队列）

```
knowledge-submit click (idx:1343 → 2463)
└─ submitKnowledgeSentence (idx:2435)
   ├─ 立即清空 #knowledge-text（idx:2443）
   └─ withEmbeddingGuard (idx:1469)
      └─ apiPostJson(withConfirmRebuild("/api/knowledge", confirm), {text, wait:false}) (idx:2446)
         └─ web.app.add_knowledge (web/app.py:460)
            ├─ web.app.guard_embedding (web/app.py:233)
            │  └─ memory.embedding_lock.apply_embedding_lock (memory/embedding_lock.py:135)
            │     ├─ memory.embedding_lock.inspect_embedding_lock (memory/embedding_lock.py:85)
            │     │  └─ memory.storage.document_repo.DocumentRepository.get_embedding_lock (document_repo.py:478)
            │     │     └─ SQLite `embedding_lock`                    [读]
            │     └─ memory.embedding_lock.rebuild_vector_projection (memory/embedding_lock.py:189)（仅确认重建时）
            ├─ web.ingest_queue.IngestJobQueue.submit (web/ingest_queue.py:99)
            │  ├─ memory.storage.document_repo.DocumentRepository.create_ingest_job (document_repo.py:525)
            │  │  └─ SQLite `ingest_jobs`                             [写]
            │  └─ web.ingest_queue.IngestJobQueue._enqueue (web/ingest_queue.py:159)
            │     └─ ThreadPoolExecutor(max_workers=1)（web/ingest_queue.py:90） [后台线程]
            │        └─ web.ingest_queue.IngestJobQueue._run_job (web/ingest_queue.py:163)
            │           ├─ memory.storage.document_repo.DocumentRepository.set_ingest_job_status (document_repo.py:555) [写]
            │           ├─ memory.rag.pipeline.RAGPipeline.ingest (memory/rag/pipeline.py:122)
            │           │  ├─ memory.rag.pipeline.RAGPipeline.document_repo (pipeline.py:106)
            │           │  ├─ memory.embedding_lock.apply_embedding_lock (embedding_lock.py:135)
            │           │  ├─ memory.rag.document.DocumentProcessor.normalized_text (document.py:77)
            │           │  ├─ memory.storage.document_repo.DocumentRepository.upsert_document (document_repo.py:270)
            │           │  │  └─ SQLite `documents`                   [写]
            │           │  ├─ memory.rag.document.DocumentProcessor.chunks_with_spans (document.py:87)
            │           │  ├─ tool.hybrid_index.index_chunk (tool/hybrid_index.py:72)   ← 本轮重构新增
            │           │  │  ├─ memory.storage.document_repo.DocumentRepository.upsert_chunk (document_repo.py:359)
            │           │  │  │  ├─ SQLite `chunks`                   [写]
            │           │  │  │  └─ FTS5 `chunks_fts` 触发器 (document_repo.py:219) [写]
            │           │  │  └─ memory.manager.MemoryManager.add (manager.py:109) → BaseMemory.add (base.py:418)
            │           │  │     ├─ SQLite `memories`                 [写]
            │           │  │     └─ Qdrant / 内存向量                 [写]
            │           │  │  （调用点：pipeline.py:173 导入、pipeline.py:175 调用）
            │           │  ├─ memory.storage.document_repo.DocumentRepository.set_chunk_vector_status (document_repo.py:410) [写]
            │           │  ├─ memory.rag.knowledge.build_graph_context (knowledge.py:794) [读]
            │           │  ├─ memory.rag.knowledge.LLMKnowledgeExtractor.extract (knowledge.py:305)
            │           │  │  └─ agents.llm.LLM.complete → 外部 LLM    [外部]
            │           │  ├─ memory.rag.knowledge.materialize_extraction (knowledge.py:528)
            │           │  │  ├─ memory.rag.knowledge.EntityResolver.resolve (knowledge.py:474)
            │           │  │  └─ memory.types.semantic.SemanticMemory.add_fact (semantic.py:28)
            │           │  │     └─ _write_edge (semantic.py:150) → Neo4j add_relation/add_observation [写]
            │           │  └─ memory.storage.document_repo.DocumentRepository.set_status (document_repo.py:342) [写]
            │           ├─ memory.types.episodic.EpisodicMemory.record (types/episodic.py:15) [写 memories]
            │           └─ web.ingest_queue.IngestJobQueue._notify (web/ingest_queue.py:212)
            │              └─ web.app.invalidate_graph (web/app.py:213 的 lambda) [缓存失效]
            └─ 返回 {ok, job_id, status:"排队中", async:true}（约 web/app.py:473–479）

前端后续（异步收尾，见 §5.5）：
   toast「已加入后台入库队列」→ refreshIngestJobs() (idx:2456 → 2498)
   3s 轮询看到 done → loadGraph({since:true}) (idx:2511) 整图刷新
```

回退分支：队列不可用（`:memory:` 测试库）时 `add_knowledge` 走同步路径 `pipeline.ingest(...)`（约 web/app.py:505–534）。

### 5.5 入库队列与重试

```
setInterval 3000ms (idx:2514，document.hidden 时跳过)
└─ refreshIngestJobs (idx:2498)
   └─ apiGet("/api/knowledge/jobs?limit=8") (idx:2500)
      └─ web.app.list_knowledge_jobs (web/app.py:537)
         ├─ web.ingest_queue.IngestJobQueue.list (web/ingest_queue.py:127)
         │  └─ memory.storage.document_repo.DocumentRepository.list_ingest_jobs (document_repo.py:589)
         │     └─ SQLite `ingest_jobs`                                [读]
         └─ web.ingest_queue.job_to_dict (web/ingest_queue.py:37)
   └─ 前端渲染 renderJobRow (idx:2468) → #ingest-jobs (idx:1347)
   └─ 若签名变化且出现 done：loadGraph({since:true}) (idx:2511)

#ingest-jobs 内 .job-retry click（事件委托 idx:2517）
└─ retryIngestJob(jobId) (idx:2486)
   └─ withEmbeddingGuard → apiPostJson(withConfirmRebuild("/api/knowledge/jobs/{id}/retry"), {}) (idx:2489)
      └─ web.app.retry_knowledge_job (web/app.py:555)
         ├─ web.app.guard_embedding (web/app.py:233)
         ├─ web.ingest_queue.IngestJobQueue.retry (web/ingest_queue.py:111)
         │  ├─ memory.storage.document_repo.DocumentRepository.get_ingest_job (document_repo.py:548) [读]
         │  ├─ memory.storage.document_repo.DocumentRepository.reset_failed_ingest_job (document_repo.py:613) [写]
         │  └─ IngestJobQueue._enqueue (ingest_queue.py:159) → _run_job (163) 同上
         └─ 404（app.py:568）/ 400（app.py:570）/ 503（app.py:564）

ingest-jobs-toggle click/keydown (idx:2543/2544)
└─ toggle（IIFE idx:2528–2547）→ localStorage["ingestJobsCollapsed"]   [纯前端]
```

### 5.6 图片入库

```
image-submit click (idx:1355 → 2553)
└─ submitImageKnowledge (idx:2549) → $("image-input").click()

image-input change (idx:1356 → 2554)
└─ FormData{file, text=#image-note, captured_at=#image-event-at 或 now} (idx:2561–2565)
   └─ withEmbeddingGuard → apiPostForm(withConfirmRebuild("/api/knowledge/image"), form) (idx:2567)
      └─ web.app.add_image_knowledge (web/app.py:574)
         ├─ web.app._save_upload (web/app.py:160)（流式落临时文件 + MAX_UPLOAD_BYTES 上限）
         ├─ web.app.guard_embedding (web/app.py:233)
         ├─ memory.rag.pipeline.RAGPipeline.ingest_media (memory/rag/pipeline.py:241)
         │  ├─ memory.embedding_lock.apply_embedding_lock (embedding_lock.py:135)
         │  ├─ memory.manager.MemoryManager.add(memory_type=PERCEPTUAL, payload=image, modality="image") (manager.py:109)
         │  │  └─ memory.base.BaseMemory.add (memory/base.py:418)
         │  │     ├─ SQLite `memories`（含 payload / 文本）           [写]
         │  │     └─ memory.embedding.APIEmbedding.embed_item (memory/embedding.py:259)
         │  │        └─ embed_multimodal (memory/embedding.py:255) / embed_image (251) [外部]
         │  │        └─ Qdrant upsert                                [写]
         │  ├─ memory.rag.knowledge.build_graph_context (knowledge.py:794)
         │  ├─ memory.rag.knowledge.LLMKnowledgeExtractor.extract(image=...) (knowledge.py:305) [外部]
         │  └─ memory.rag.knowledge.materialize_extraction (knowledge.py:528)
         │     └─ memory.types.semantic.SemanticMemory.add_fact (semantic.py:28) → _write_edge (150) → Neo4j [写]
         └─ web.app.invalidate_graph (web/app.py:615)
   └─ 前端 toast（含 multimodal 警告 idx:2572）→ loadGraph() (idx:2575)
```

注意：图片路径**不写** `documents`/`chunks` 真值源（`ingest_media` 内没有 `repository.upsert_*` / `index_chunk` 调用），只写 `memories`（perceptual）+ 向量 + 图。

### 5.7 导入文档

```
btn-ingest click (idx:1178 → 2595) → $("file-input").click()

file-input change (idx:1385 → 2596)
└─ FormData{file} → withEmbeddingGuard → apiPostForm(withConfirmRebuild("/api/ingest"), form) (idx:2605)
   └─ web.app.ingest (web/app.py:405)
      ├─ web.app._save_upload (web/app.py:160)
      ├─ web.app.guard_embedding (web/app.py:233)（调用点 app.py:412）
      ├─ memory.rag.pipeline.RAGPipeline.ingest_source (memory/rag/pipeline.py:322)
      │  └─ memory.rag.document.DocumentProcessor.parse (memory/rag/document.py:51)
      │     └─ memory.rag.document.DocumentProcessor._parse_bytes (document.py:169)
      │  └─ memory.rag.pipeline.RAGPipeline.ingest (pipeline.py:122)   ← 同 §5.4 的 ingest 子树
      │     ├─ SQLite `documents` / `chunks` / `chunks_fts`           [写]
      │     ├─ SQLite `memories` + Qdrant                              [写]
      │     └─ 抽取 → Neo4j `RELATED`/`MemoryObservation`              [写]
      ├─ memory.types.episodic.EpisodicMemory.record("上传并导入了文档…") (types/episodic.py:15) [写]
      └─ web.app.invalidate_graph (web/app.py:432)
   └─ 前端 toast（filename/chunks/extraction）→ loadGraph() (idx:2611)
```

### 5.8 文档中心

```
btn-documents click (idx:1179 → 2867)
└─ openDocumentsPanel(open) (idx:2858)         ← 与 #storage-panel 互斥 (idx:2863)
   └─ 首次打开（docState.total 为 0）→ loadDocuments(1) (idx:2864)
      └─ loadDocuments(page) (idx:2763)
         └─ apiGet(`/api/documents?page&page_size&tag&status`) (idx:2774)
            └─ web.app.list_documents (web/app.py:896)
               ├─ web.app.the_repository (web/app.py:774)
               │  └─ memory.rag.pipeline.RAGPipeline.document_repo (memory/rag/pipeline.py:106)
               ├─ memory.storage.document_repo.DocumentRepository.list_documents (document_repo.py:311)
               │  └─ SQLite `documents`                                [读]
               └─ memory.storage.document_repo.DocumentRepository.chunk_counts (document_repo.py:445)
                  └─ SQLite `chunks`                                   [读]
         └─ 渲染 renderDocList (idx:2744) → #doc-list (idx:1241) + #doc-page-info/#doc-prev/#doc-next

doc-item click (idx:2877 委托) / keydown Enter|Space (idx:2881 委托)
└─ openDocument(id) (idx:2822)
   └─ apiGet(`/api/documents/{id}`) (idx:2827)
      └─ web.app.get_document (web/app.py:927)
         ├─ DocumentRepository.get_document (document_repo.py:304)     [读 documents]
         └─ DocumentRepository.list_chunks (document_repo.py:403)      [读 chunks]
   └─ 渲染 renderDocDetail (idx:2802)
      ├─ renderDocText (idx:2781)（按 char_start/char_end 生成 <mark class="doc-chunk">）
      └─ 注入 #doc-revectorize 按钮 (idx:2812) 并绑定 (idx:2819)

mark.doc-chunk click (idx:2814 委托)
└─ toast（块号 + 向量状态）                                             [纯前端]

doc-revectorize click (idx:2812 → 2819)
└─ revectorizeDocument(id) (idx:2833)
   └─ withEmbeddingGuard → apiPostJson(withConfirmRebuild("/api/documents/{id}/revectorize"), {}) (idx:2838)
      └─ web.app.revectorize_document (web/app.py:957)
         ├─ web.app.guard_embedding (web/app.py:963)
         ├─ DocumentRepository.get_document (document_repo.py:304)      [读]
         ├─ DocumentRepository.list_chunks (document_repo.py:403)       [读]
         ├─ memory.embedding.APIEmbedding.embed_batch (memory/embedding.py:186) [外部]
         ├─ memory.storage.qdrant.QdrantVectorStore.upsert_chunk (qdrant.py:233) [写]
         ├─ DocumentRepository.set_chunk_vector_status (document_repo.py:410)   [写]
         └─ DocumentRepository.set_status (document_repo.py:342)                [写]
   └─ 前端：toast → openDocument(id) → loadDocuments(docState.page) (idx:2848–2849)

doc-refresh(2871) / doc-prev(2872) / doc-next(2873) / doc-tag change(2875) / doc-status change(2876)
└─ 均归到 loadDocuments(page)
docs-close(2870) / doc-back(2874)
└─ openDocumentsPanel(false) / showDocList (idx:2852)                    [纯前端]
```

### 5.9 导出 / 导入

```
btn-export click (idx:1180 → 2617)
└─ window.open("/api/export", "_blank")                                  [浏览器导航，非 fetch]
   └─ web.app.export (web/app.py:639)
      ├─ memory.manager.MemoryManager.list(include_expired=False) (manager.py:144)
      │  └─ memory.storage.document.SQLiteDocumentStore.list (document.py:140)
      │     └─ SQLite `memories`                                        [读]
      └─ JSONResponse + Content-Disposition 附件头（约 web/app.py:658–663）[下载]

doc-import click (idx:1238 → 2895) → importInput.click()
importInput change（动态创建 idx:2890，绑定 idx:2896）
└─ FormData{file} → apiPostForm("/api/import", form) (idx:2904)   ← 注意：未走 withEmbeddingGuard
   └─ web.app.import_file (web/app.py:666)
      ├─ web.app._save_upload (web/app.py:160, suffix=".json")
      ├─ json.loads(parse_constant=_reject_json_constant) (web/app.py:677, 94)
      ├─ memory.manager.MemoryManager.list(SEMANTIC) (manager.py:144)    [读，建幂等三元组集合]
      ├─ memory.manager.MemoryManager.get (manager.py:112)               [读，按 id 去重]
      ├─ memory.types.semantic.SemanticMemory.add_fact (semantic.py:28)  [写 memories + Neo4j]
      │  └─ _write_edge (semantic.py:150) → Neo4j add_relation (graph.py:190)
      ├─ memory.manager.MemoryManager.add (manager.py:109) → BaseMemory.add (base.py:418) [写 memories + 向量]
      └─ web.app.invalidate_graph (web/app.py:768)（仅 imported > 0 时）
   └─ 前端 toast（imported/skipped）→ loadGraph() + loadDocuments(1) (idx:2906–2907)
```

`import_file` 的响应还带 `errors`（由 `note_error` 收集，web/app.py:700，上限 `WEB_IMPORT_ERRORS_MAX`，判断在 app.py:701–702），但前端目前**只展示 imported/skipped**（idx:2905），未渲染 errors —— 这是一个已存在的信息丢失点。

### 5.10 存储健康与对账

```
btn-storage click (idx:1219 → 2980)
├─ 切换 #storage-panel（与 #documents-panel 互斥 idx:2984）
├─ renderStoragePanel(lastHealth, null) (idx:2926)                      [纯前端，先画缓存]
└─ refreshReconcile (idx:2971)
   └─ apiGet("/api/reconcile") (idx:2973)
      └─ web.app.reconcile (web/app.py:1005)
         └─ web.app.reconcile_report (web/app.py:810)
            ├─ memory.rag.pipeline.RAGPipeline.document_repo (pipeline.py:106)
            ├─ DocumentRepository.chunk_ids() (document_repo.py:454)          [读 chunks]
            ├─ DocumentRepository.chunk_ids(vector_status="indexed") (document_repo.py:454) [读]
            ├─ memory.manager.MemoryManager.document_store.list (document.py:140) [读 memories]
            ├─ web.app.fact_items (web/app.py:785)
            │  └─ web.app._is_fact_item (web/app.py:106) → 过滤出三元组项
            │  └─ memory.types.semantic.SemanticMemory.facts (semantic.py:260) [读]
            ├─ web.app.projected_vector_ids (web/app.py:788)
            │  └─ memory.storage.qdrant.QdrantVectorStore.list_ids (qdrant.py:297) [读 Qdrant]
            └─ web.app.projected_edge_ids (web/app.py:799)
               └─ memory.storage.graph.Neo4jGraphStore.relation_memory_ids (graph.py:685) [读 Neo4j]
   └─ 渲染 renderStoragePanel(health, reconcile) (idx:2926)
      ├─ #storage-modes (idx:1261)：store_modes + degraded.embedding + embedding_lock + qdrant_dimension
      ├─ #storage-counts (idx:1265)：chunks / chunks_indexed_sqlite / qdrant_points / facts / neo4j_edges
      └─ #storage-drift (idx:1269)：missing_vector / orphan_vector / missing_edge（DRIFT_LABEL idx:2918）

btn-reconcile click (idx:1272 → 2992) → refreshReconcile（同上，不修复）
storage-close click (idx:1257 → 2988) → 关闭面板（纯前端）
```

### 5.11 一键修复（对账修复）

```
btn-repair click (idx:1273 → 2993)
└─ apiPostJson("/api/reconcile", {repair:["missing_vector","missing_edge"]}) (idx:2996)
   └─ web.app.reconcile_repair (web/app.py:1009)
      └─ web.app.repair_drift (web/app.py:845)
         ├─ web.app.reconcile_report (web/app.py:810)   ← 先重算漂移，取 ids
         ├─ memory.rag.pipeline.RAGPipeline.document_repo (pipeline.py:106)
         ├─ DocumentRepository.get_chunk (document_repo.py:398)            [读 chunks]
         ├─ memory.embedding.APIEmbedding.embed_batch (memory/embedding.py:186) [外部]
         ├─ memory.storage.qdrant.QdrantVectorStore.upsert_chunk (qdrant.py:233) [写 Qdrant]
         ├─ DocumentRepository.set_chunk_vector_status (document_repo.py:410)  [写]
         ├─ DocumentRepository.get_document (document_repo.py:304)         [读]
         ├─ DocumentRepository.set_status (document_repo.py:342)           [写]
         └─ memory.types.semantic.SemanticMemory.add_fact(item_id=...) (semantic.py:28)
            └─ _write_edge (semantic.py:150) → Neo4j add_relation/add_observation [写图边]
   └─ 前端 toast（补向量/补图边计数）→ refreshReconcile() (idx:3002)

⚠️ 只补缺失投影，不删不改真值源（web/app.py:846 附近的注释）。
   repair 类型白名单 {missing_vector, missing_edge}，其他值 → 422 (web/app.py:851)。
```

### 5.12 聊天 / 知识管家 / 联网模式

```
chat-send-btn click (idx:1380 → 3128) / chat-textarea keydown Enter (idx:1379 → 3129)
└─ sendChat (idx:3062)
   ├─ 分支 A：usedMode === "graphrag"（idx:3078）
   │  └─ apiPostJson("/api/graph-rag", {query, limit:8, hops:2}) (idx:3080)
   │     └─ web.app.graph_rag (web/app.py:303)
   │        └─ memory.rag.pipeline.RAGPipeline.graph_retrieve (pipeline.py:363)  ← 调用点 app.py:304
   │           └─ memory.rag.graph_rag.GraphRAGPipeline.retrieve (graph_rag.py:92)
   │              ├─ memory.manager.MemoryManager.search(SEMANTIC) (manager.py:129)
   │              │  └─ memory.base.BaseMemory.search (base.py:489)
   │              │     ├─ memory.embedding.BaseEmbedding.embed (embedding.py:54) [外部/本地]
   │              │     └─ memory.storage.qdrant.QdrantVectorStore.search (qdrant.py:317) [读向量]
   │              ├─ memory.rag.graph_rag.GraphRAGPipeline._find_seed_entities (graph_rag.py:148)
   │              │  └─ memory.types.semantic.SemanticMemory.list (base.py:520) [读 memories]
   │              ├─ memory.rag.graph_rag.GraphRAGPipeline._expand (graph_rag.py:180)
   │              │  ├─ memory.storage.graph.Neo4jGraphStore.path_query (graph.py:749) [读图]
   │              │  └─ 收尾（graph_rag.py:274–277）：
   │              │     adopted = paths[:path_limit]                (graph_rag.py:274)
   │              │     if reinforce: self._reinforce(adopted)      (graph_rag.py:275–276)
   │              ├─ memory.rag.graph_rag.GraphRAGPipeline._reinforce (graph_rag.py:298)   ← 有副作用！
   │              │  ├─ 开关 _reinforce_enabled (graph_rag.py:293) ← MEMORY_EDGE_REINFORCE (constants.py:212) = True
   │              │  │  （开关值在 _expand 开头读入：graph_rag.py:185）
   │              │  └─ memory.storage.graph.Neo4jGraphStore.add_relation(*key, bump=True) (graph_rag.py:302/309)
   │              │     └─ memory.storage.graph.Neo4jGraphStore.add_relation (graph.py:190，bump 形参 203)
   │              │        └─ memory.storage.graph.Neo4jGraphStore._bump_relation (graph.py:433)
   │              │           └─ 写边属性 weight / accessed_at（Neo4j，或 _apply_bump graph.py:470 内存孪生）[写]
   │              ├─ memory.rag.graph_rag.GraphRAGPipeline._weighted_edges (graph_rag.py:279)
   │              ├─ memory.rag.graph_rag.GraphRAGPipeline._edge_is_active (graph_rag.py:311)
   │              └─ memory.rag.graph_rag.GraphRAGPipeline._edge_in_window (graph_rag.py:322)
   │     └─ 前端：highlightPath (idx:1509) 图上高亮 + renderEvidence (idx:2665) → #evidence-list
   │              + drawer.classList.add("visible") (idx:3099)
   └─ 分支 B：offline / online（idx:3102）
      └─ apiPostJson("/api/chat", {message, mode}) (idx:3102)
         └─ web.app.chat (web/app.py:316)
            ├─ web.support.chat_ready (web/support.py:415)               [配置检查，未配置 → 503 (app.py:319)]
            ├─ web.support.get_agent (web/support.py:170)
            │  ├─ agent.set_system_prompt (web/support.py:187)
            │  ├─ agent.register_tool × 5 (web/support.py:190/193/194/197/198)
            │  │  └─ MemoryQueryTool / MemoryAddTool / RAGSearchTool / RAGTool / SearchTool
            │  └─ agents.react.ReActAgent (agents/react.py:357) 单例
            ├─ web.support.chat_tool_names(online=...) (web/support.py:264)   ← 调用点 app.py:323
            │  ├─ web.support.search_available (web/support.py:248)
            │  │  └─ web.support._services_search (web/support.py:258)
            │  │     └─ core.services_config.load_services_config().search
            │  └─ 白名单 CHAT_TOOL_ALLOWLIST (web/support.py:239–245)
            ├─ effective_mode = "online" if SEARCH_TOOL_NAME in tool_names else "offline" (app.py:324)
            ├─ web.support.chat_confirmed_side_effects (web/support.py:208)   ← 调用点 app.py:332
            │  └─ 只为 memory.add 放行写确认（CHAT_CONFIRMED_TOOLS, support.py:205）
            ├─ agents.react.ReActAgent.run (agents/react.py:560)          [外部 LLM 多轮 ReAct]
            │  ├─ tool.memory_query.MemoryQueryTool.execute (tool/memory_query.py:91)
            │  │  └─ memory.manager.MemoryManager.search / get / list (manager.py:129/112/144)
            │  ├─ tool.memory_add.MemoryAddTool.execute (tool/memory_add.py:88)
            │  │  └─ memory.manager.MemoryManager.add (manager.py:109) → base.add (base.py:418) [写]
            │  ├─ tool.rag_search.RAGSearchTool.execute (tool/rag_search.py:74)
            │  │  └─ pipeline.retrieve (pipeline.py:344) / graph_retrieve (363) / build_context (358)
            │  ├─ tool.rag_tool.RAGTool.execute (tool/rag_tool.py:82)
            │  └─ tool.search.SearchTool.execute (tool/search.py:256)（name="web.search", search.py:188）[外部联网]
            ├─ web.support.record_qa (web/support.py:280)                 ← 调用点 app.py:347
            │  └─ memory.types.episodic.EpisodicMemory.record (types/episodic.py:15)
            │     └─ base.add (base.py:418) → SQLite `memories` + 向量    [写]
            ├─ web.support.schedule_qa_extraction (web/support.py:383)     ← 调用点 app.py:350
            │  ├─ web.support.extract_graph_patches (web/support.py:318)  [后台线程]
            │  │  └─ memory.rag.pipeline.RAGPipeline.ingest (pipeline.py:122)
            │  │     └─ …（同 §5.4 ingest 子树，source="问答抽取"）        [写三库]
            │  │  └─ web.support.bump_graph_revision (web/support.py:367)
            │  └─ web.support.extract_graph_patches_async (web/support.py:371)（异步分支）
            ├─ web.app.invalidate_graph (web/app.py:353)                  [缓存失效]
            ├─ memory.rag.pipeline.RAGPipeline.graph_retrieve (pipeline.py:363)（回答后的证据/路径，调用点 app.py:355）
            └─ tool.hybrid_recall.hybrid_recall (tool/hybrid_recall.py:143)   ← 本轮重构后的新落点
               │  调用点：web/app.py:366（`hybrid = hybrid_recall(app.state.pipeline, body.message, limit=...)`）
               │  导入点：web/app.py:71
               ├─ tool.hybrid_recall.hybrid_enabled (tool/hybrid_recall.py:74)
               │  └─ 开关 MEMORY_HYBRID (constants.py:220) = True（关闭即退回纯向量）
               ├─ tool.hybrid_recall.vector_hits (tool/hybrid_recall.py:80)
               │  └─ memory.manager.MemoryManager.search (manager.py:129) [读向量]
               ├─ tool.hybrid_recall.rrf_fuse (tool/hybrid_recall.py:55)
               │  └─ Reciprocal Rank Fusion：只按名次计分，避免余弦与 bm25 量纲混算
               ├─ memory.storage.document_repo.DocumentRepository.search_keywords (document_repo.py:429)
               │  └─ 调用点 tool/hybrid_recall.py:167
               │  └─ SQLite FTS5 `chunks_fts` + `bm25()` (document_repo.py:437) [读]
               ├─ tool.hybrid_recall.fuse_hits (tool/hybrid_recall.py:111)
               │  └─ 按真值源回查正文（孤立向量不外泄）
               └─ memory.storage.document_repo.DocumentRepository.get_chunk (document_repo.py:398) [读]
            └─ 返回 retrieval_report{note, hits[{chunk_id,document_id,chunk_index,snippet,score,rrf_score,vector_score,keyword_score}]} (app.py:367–382)
      └─ 前端：appendBubble (idx:3053) 写气泡 + appendRetrieval (idx:2696) 写 <details> 依据面板

chat-mode-select change (idx:1374 → 2662)
└─ onChatModeChange (idx:2652)                                        [纯前端]
   └─ 发送中：记 chatPendingMode，本轮结束后 applyChatMode (idx:3119–3123)
   └─ 空闲：applyChatMode (idx:2646) → refreshSendButton (idx:2640)
      └─ usable = backendUp && (chatReady || chatMode === "graphrag")   ← graphrag 无需聊天模型

btn-chat click (idx:1181 → 3133) / chat-close (idx:1368 → 3137) / ESC (idx:3138)
└─ #chat-panel.classList.toggle("open")                                [纯前端]
```

联网模式的实际生效点在 `web.app.chat`：`tool_names = chat_tool_names(agent, online=online)`（app.py:323），随后 `effective_mode = "online" if SEARCH_TOOL_NAME in tool_names else "offline"`（app.py:324）；响应回传 `mode`（app.py:385），但前端目前**不使用**响应里的 `mode` 字段（idx:3102–3112 只读 `answer`/`sources`/`paths`/`retrieval`）。前端能感知联网是否配置，靠的是 `/api/health` 的 `search_available`（app.py:1050）改写下拉文案（idx:3038–3041）。

### 5.13 播种（无前端入口）

```
应用启动 lifespan（web/app.py:200）
└─ if WEB_AUTOSEED (web/app.py:220)
   └─ web.seed.seed (web/seed.py:23)      调用点 web/app.py:222
      ├─ memory.manager.MemoryManager.list(SEMANTIC)（幂等检查，web/seed.py:28–31） [读]
      ├─ json.loads(web/seed_data.json)（web/seed.py:33）                        [读文件]
      ├─ memory.manager.MemoryManager.add(kind="entity") (manager.py:109)         [写 memories + 向量]
      ├─ memory.types.semantic.SemanticMemory.add_fact (semantic.py:28)           [写 memories + Neo4j]
      └─ memory.manager.MemoryManager.add(kind="note") (manager.py:109)           [写 memories + 向量]

POST /api/seed（web/app.py:633 reseed）
└─ 同上 seed()（调用点 app.py:634），然后 invalidate_graph (web/app.py:635)
   ⚠️ 前端 index.html 中检索不到 "/api/seed"，无任何入口；
      仅剩 lifespan 自动播种 + `python -m web.seed` 命令行（web/seed.py:106–111）。
```

### 5.14 健康检查

```
boot 第二步（idx:3150）
└─ checkHealth (idx:3029)
   └─ apiGet("/api/health") (idx:3031)
      └─ web.app.health (web/app.py:1030)
         ├─ web.support.chat_ready (web/support.py:415)                       [读 provider.toml]（调用点 app.py:1031）
         ├─ memory.embedding_lock.inspect_embedding_lock (embedding_lock.py:85)
         │  ├─ DocumentRepository.get_embedding_lock (document_repo.py:478)   [读 embedding_lock]
         │  └─ QdrantVectorStore.collection_dimension (qdrant.py:124)         [读 Qdrant]
         ├─ web.support.search_available (web/support.py:248)                 [调用点 app.py:1050]
         ├─ web.app.embedding_config_hint (web/app.py:113)                    [生成降级文案]
         └─ store_modes（document/vector/graph 各自实现名，约 web/app.py:1053–1058）
   ├─ 前端写 #chat-status-dot / #chat-status-text (idx:3034–3037)
   ├─ 前端改写 online option 文案 (idx:3038–3041)
   ├─ renderStoragePanel(h, null) (idx:3042)
   ├─ offerEmbeddingRebuild(h) (idx:3043) → 条件 POST /api/embedding/rebuild (idx:3020)
   └─ refreshSendButton (idx:3050)
   失败分支（idx:3044–3049）：backendUp=false，状态文案「服务不可达」
```

### 5.15 启动自举

```
boot() (idx:3145)  ← 在 IIFE 末尾直接调用 (idx:3165)
├─ restoreGraphCache() (idx:1546)  读 sessionStorage["nebula-graph-v1"]（GRAPH_CACHE_KEY idx:1484）
│  └─ 命中则立即 hide #boot-overlay (idx:3148)，并让随后的 loadGraph 带 since（增量）
├─ await loadGraph({since: warmed}) (idx:3149)
│  └─ apiGet("/api/graph" 或 "?since=N") (idx:1581) → web.app.graph (web/app.py:271)
│     └─ web.graph_builder.build_graph (web/graph_builder.py:93)
├─ await checkHealth() (idx:3150)   ← 见 §5.14
├─ hide #boot-overlay (idx:3151)
└─ toast（空库提示 idx:3154 / 就绪统计 idx:3156）
   失败：改 #boot-text/#boot-sub 文案（idx:3159–3160，含启动命令提示），3s 后隐藏浮层

随后注册两个定时器：
├─ setInterval(loadGraph({since:true}), GRAPH_POLL_MS=20000) (idx:3168)
└─ setInterval(refreshIngestJobs, 3000) (idx:2514，见 §5.5)
render() (idx:3169) → requestAnimationFrame 自循环 (idx:1965)          [纯前端]
```

---

## 6. 差异核对

### 6.1 前端调用了但后端不存在

**无。** 前端出现的 18 个 `/api/*` 路径全部能在 `web/app.py` 找到对应路由（清单见 §4）。逐一核对结果：

| 前端路径 | 出现行 | 后端 |
| --- | --- | --- |
| `/api/graph` | 1580 | app.py:270 ✅ |
| `/api/facts` | 2415 | app.py:442 ✅ |
| `/api/knowledge` | 2446 | app.py:459 ✅ |
| `/api/knowledge/jobs` | 2500 | app.py:536 ✅ |
| `/api/knowledge/jobs/{id}/retry` | 2489 | app.py:554 ✅ |
| `/api/knowledge/image` | 2567 | app.py:573 ✅ |
| `/api/ingest` | 2605 | app.py:404 ✅ |
| `/api/export` | 2617 | app.py:638 ✅ |
| `/api/import` | 2904 | app.py:665 ✅ |
| `/api/documents` | 2774 | app.py:895 ✅ |
| `/api/documents/{id}` | 2827 | app.py:926 ✅ |
| `/api/documents/{id}/revectorize` | 2838 | app.py:956 ✅ |
| `/api/reconcile`（GET） | 2973 | app.py:1004 ✅ |
| `/api/reconcile`（POST） | 2996 | app.py:1008 ✅ |
| `/api/embedding/rebuild` | 3020 | app.py:1012 ✅ |
| `/api/health` | 3031 | app.py:1029 ✅ |
| `/api/graph-rag` | 3080 | app.py:302 ✅ |
| `/api/chat` | 3102 | app.py:315 ✅ |

（表中「后端」列为**装饰器行**，函数体首行见 §4。）

### 6.2 后端存在但前端无入口

| 端点 | 后端函数 | 说明 |
| --- | --- | --- |
| `POST /api/seed` | `reseed` (web/app.py:633) | 前端 `index.html` 全文无 `/api/seed`。触发路径只剩 lifespan 自动播种（`WEB_AUTOSEED`，app.py:220–222）与命令行 `python -m web.seed`（seed.py:106）。若要手动重播，目前只能 curl。 |
| `GET /api/stats` | `stats` (web/app.py:994) | 前端 `index.html` 全文无 `/api/stats`。界面上的统计数字（`#stat-domains`/`#stat-total`/`#stat-edges`/`#stat-chunks`，idx:1215–1218）全部来自 `/api/graph` 响应里的 `stats` 字段（`updateStats` idx:1734 消费 `graph.stats`，来源 `build_graph` 的 `stats` 字典 graph_builder.py:541–553），与 `/api/stats` 是两套计数口径。 |

### 6.3 其它观察（不改代码，仅记录）

1. **文档注释与实现不符**：`web/app.py:5` 写「GET /api/graph-rag」，实际是 `@app.post`（app.py:302）。前端用 POST，行为一致，仅注释过时。
2. **`/api/import` 未走嵌入守卫**：前端 idx:2904 直接 `apiPostForm("/api/import", form)`，未包 `withEmbeddingGuard`；后端 `import_file`（app.py:666）内部也没有 `guard_embedding` 调用（已用 grep 核对：`guard_embedding` 在 app.py 只出现在定义处 233 与调用点 412/467/561/601/963/1017/1019，`import_file` 的 666–772 区间内没有）。导入事实走 `semantic.add_fact` → `BaseMemory.add` → 向量 upsert，因此在嵌入锁不一致时可能以 Qdrant 维度错误的形式失败，而不是走统一的 409 确认流程。（是否会在实际部署中触发，取决于 Qdrant 维度状态，未实测。）
3. **`/api/import` 的 errors 未展示**：后端收集 `errors`（`note_error` app.py:700，上限 `WEB_IMPORT_ERRORS_MAX` 见 app.py:701–702），前端只 toast imported/skipped（idx:2905）。
4. **搜索框纯本地**：`doSearch`（idx:2372）只过滤已下发的 `graph.nodes`，不调 `/api/*`，因此搜不到 `chunks` 真值源中未被 `build_graph` 纳入节点预览的文本，也没有 FTS5/向量语义检索能力。真正的语义检索入口只有聊天面板（graphrag / offline 模式）。
5. **`/api/graph` 的 `at` 只影响图关系**：`graph-as-of-apply` 传的 `at` 只作用于 `_graph_snapshot`（Neo4j 时序快照），SQLite 侧的 `manager.list()` 仍是全量当前数据（graph_builder.py:94–95 无时间过滤）。
6. **`checkHealth` 里 `offerEmbeddingRebuild` 只在 boot 调一次**：靠 `window.__embeddingRebuildAsked`（idx:3006–3007）去重；`checkHealth` 本身没有周期定时器，后续只在 `offerEmbeddingRebuild` 成功重建后再调一次（idx:3023）。
7. **`#doc-revectorize` 是唯一动态注入的 `<button id>`**：写在 `renderDocDetail` 的模板字符串里（idx:2812），每次打开文档详情都会重新绑定（idx:2819），因此不存在重复监听问题（旧的 `#doc-detail` innerHTML 被整体替换）。
8. **`POST /api/graph-rag` 名为「检索」但默认写图**：`_expand` 收尾会调 `_reinforce`（graph_rag.py:274–277 → 298），对最终 adopted 路径的边做 `add_relation(*key, bump=True)`（graph_rag.py:309 → graph.py:190/203 → 433），写入 Neo4j 边的 `weight`/`accessed_at`。开关 `constants.MEMORY_EDGE_REINFORCE`（constants.py:212）为 True。因此它并非纯只读端点；`POST /api/chat` 回答后的 `graph_retrieve`（app.py:355）同样带这个副作用。
9. **本轮新增工具未接入聊天白名单**：`tool/hybrid_recall.py`（工具名 `knowledge.hybrid_recall`，hybrid_recall.py:215）、`tool/hybrid_index.py`、`tool/multi_recall.py`（`hybrid_recall_multi` multi_recall.py:150）、`tool/graph_node_update.py` 都是 22:40–22:45 新增的，但 `web/support.py` 未改动（mtime 21:48:44），`CHAT_TOOL_ALLOWLIST`（support.py:239–245）仍只有 5 个旧工具。**因此 LLM 在聊天里看不到这些新工具**，它们目前只被 `web/app.py`（hybrid_recall，app.py:71/366）和 `memory/rag/pipeline.py`（index_chunk，pipeline.py:173/175）直接调用。这是重构进行中的中间状态，未必是最终意图。

---

## 7. 只读入口 vs 有副作用入口

### 7.1 有副作用的入口（会改变存储或缓存 revision）

| 入口 | 端点 | 写入目标 |
| --- | --- | --- |
| `fact-submit` | `POST /api/facts` | SQLite `memories` + Qdrant + Neo4j 边/观察 |
| `knowledge-submit` | `POST /api/knowledge` | SQLite `ingest_jobs`→`documents`/`chunks`/`memories` + Qdrant + Neo4j |
| `image-input` | `POST /api/knowledge/image` | SQLite `memories`(perceptual) + Qdrant + Neo4j |
| `file-input`（经 `btn-ingest`） | `POST /api/ingest` | SQLite `documents`/`chunks`/`memories` + Qdrant + Neo4j |
| `job-retry` | `POST /api/knowledge/jobs/{id}/retry` | SQLite `ingest_jobs`（状态重置并重新入队） |
| `doc-revectorize` | `POST /api/documents/{id}/revectorize` | Qdrant + SQLite `chunks.vector_status` + `documents.status` |
| `importInput`（经 `doc-import`） | `POST /api/import` | SQLite `memories` + Qdrant + Neo4j |
| `btn-repair` | `POST /api/reconcile` | Qdrant + SQLite `chunks`/`documents` + Neo4j 边 |
| 自动：`checkHealth`→`offerEmbeddingRebuild` | `POST /api/embedding/rebuild?confirm_rebuild=true` | 重建 Qdrant 集合 + SQLite `embedding_lock` + 全量重灌 |
| `chat-send-btn`（offline/online） | `POST /api/chat` | SQLite `memories`(episodic 问答留痕) + 后台抽取写三库；LLM 可能再触发 `memory.add`；回答后的 `graph_retrieve`(app.py:355) 还会经 `_reinforce` 强化边权重 |
| `chat-send-btn`（graphrag） | `POST /api/graph-rag` | **POST 但有副作用**：`_expand` 收尾调 `_reinforce`(graph_rag.py:298) → `graph_store.add_relation(bump=True)`(graph_rag.py:309 → graph.py:190/203) → `_bump_relation`(graph.py:433) 写 Neo4j 边属性 `weight`/`accessed_at`（开关 `constants.MEMORY_EDGE_REINFORCE`=True，constants.py:212）。向量/证据部分是只读。 |

### 7.2 只读入口

| 入口 | 端点 | 读取目标 |
| --- | --- | --- |
| boot / 20s 轮询 / `graph-as-of-apply` / `graph-as-of-clear` | `GET /api/graph` | SQLite `memories` + Neo4j 图快照（进程内 `graph_cache` 命中则不重建） |
| `btn-documents` / `doc-refresh` / `doc-prev` / `doc-next` / `doc-tag` / `doc-status` | `GET /api/documents` | SQLite `documents` + `chunks` |
| `doc-item` | `GET /api/documents/{id}` | SQLite `documents` + `chunks` |
| `btn-storage` / `btn-reconcile` | `GET /api/reconcile` | SQLite `chunks`/`memories` + Qdrant `list_ids` + Neo4j `relation_memory_ids` |
| `btn-export` | `GET /api/export` | SQLite `memories` |
| 自动：`boot`→`checkHealth` | `GET /api/health` | 配置 + SQLite `embedding_lock` + Qdrant 维度 |
| 自动：3s 轮询 | `GET /api/knowledge/jobs` | SQLite `ingest_jobs` |

（注：`POST /api/graph-rag` 虽是检索语义、且不写 SQLite/Qdrant，但默认会强化 Neo4j 边权重，故归入 §7.1 而非本节。）

### 7.3 纯前端（完全不发请求）

`btn-add-knowledge`、`btn-chat`、`btn-reset`、`edge-toggle-btn`、`filter-domain`、`search-input`、`drawer-reopen-trigger`、`drawer-close`、`docs-close`、`doc-back`、`storage-close`、`chat-close`、`chat-mode-select`、`ingest-jobs-toggle`、`canvas` 点击、面包屑、`.tour-btn`、`.parent-uplink-btn`、`.subnode-card`、`.search-hit`、`.evidence-item`、`mark.doc-chunk`、抽屉 pointer 手势、窗口 resize/wheel/mouse* 相机事件、两处 ESC keydown。

---

## 8. 最长的三条调用链（一句话）

1. **`knowledge-submit` → `POST /api/knowledge`（异步入库队列）**：前端 `submitKnowledgeSentence` → `withEmbeddingGuard` → `add_knowledge` → `guard_embedding`/`apply_embedding_lock` → `IngestJobQueue.submit`（写 `ingest_jobs`）→ 后台单线程 `_run_job` → `RAGPipeline.ingest`（经 `tool/hybrid_index.index_chunk` 写 `documents`/`chunks`/`chunks_fts`/`memories`/Qdrant）→ LLM 抽取 → `materialize_extraction` → `SemanticMemory.add_fact` → `_write_edge` → Neo4j `add_relation`+`add_observation` → `set_ingest_job_status` → `_notify` → `invalidate_graph`，最后由前端 3s 轮询 `/api/knowledge/jobs` 发现 `done` 后再触发 `loadGraph({since:true})` 整图刷新——跨 10 个模块（含新增的 `tool/hybrid_index.py`）、两条独立时间线（HTTP 请求 + 后台 worker + 轮询）。

2. **`chat-send-btn`（offline/online）→ `POST /api/chat`（知识管家 ReAct）**：`sendChat` → `chat` → `chat_ready` → `get_agent`（注册 5 个工具）→ `chat_tool_names` → `ReActAgent.run` 多轮工具调用（`memory.query`→向量库、`memory.rag_search`→`GraphRAGPipeline.retrieve`→Neo4j `path_query`、`memory.add`→写 memories+向量、`web.search`→外部联网）→ `record_qa` 写 episodic → `schedule_qa_extraction` 后台再跑一遍完整 ingest 抽取 → `invalidate_graph` → `graph_retrieve` + `hybrid_recall`（`tool/hybrid_recall.py` 内向量路 × FTS5 路 × `rrf_fuse` 融合，再按真值源回查正文）→ 前端 `appendBubble`+`appendRetrieval` 渲染依据面板。

3. **`btn-ingest`/`file-input` → `POST /api/ingest`（文档导入）**：`_save_upload` 流式落盘（含 413/400 边界）→ `guard_embedding` → `ingest_source` → `DocumentProcessor.parse`（多格式解析）→ `RAGPipeline.ingest` 逐块经 `index_chunk` 写真值源 `documents`/`chunks` + FTS5 触发器 + `memories` + Qdrant → 逐块 `build_graph_context` + LLM 抽取 + `materialize_extraction`（实体消解 / supersede / retract）→ Neo4j 边与观察 → 文档状态机 `parsed→extracted/failed` → `episodic.record` 留痕 → `invalidate_graph` → 前端 `loadGraph()` 全量重建星系树。

（并列候选：`btn-repair` → `POST /api/reconcile` → `repair_drift`，链长接近但分支少，故未列入前三。）

---

## 9. 附：文件与行号索引（本文引用到的）

| 文件 | 行数 | 关键位置 |
| --- | --- | --- |
| `web/static/index.html` | 3173 | 工具层 1419–1476；数据层 1479–1612；渲染 1764–2055；下钻/抽屉 2057–2285；事件绑定 2231–3169 |
| `web/app.py` | 1092 | `create_app` 196；路由 270–1070；静态挂载约 1077–1078 |
| `web/support.py` | 442 | `bump_graph_revision` 105；`graph_revision` 111；`get_manager` 116；`get_pipeline` 156；`get_agent` 170；`CHAT_CONFIRMED_TOOLS` 205；`chat_confirmed_side_effects` 208；`SEARCH_TOOL_NAME` 235；`CHAT_TOOL_ALLOWLIST` 239；`search_available` 248；`chat_tool_names` 264；`record_qa` 280；`extract_graph_patches` 318；`schedule_qa_extraction` 383；`chat_ready` 415 |
| `web/graph_builder.py` | 567 | `build_graph` 93；`_graph_snapshot` 77；`_entity_aliases` 62；`stats` 字典 541–553 |
| `web/ingest_queue.py` | 226 | `job_to_dict` 37；`IngestJobQueue` 61；`submit` 99；`retry` 111；`_enqueue` 159；`_run_job` 163；`_notify` 212 |
| `web/seed.py` | 111 | `seed` 23；CLI 106–111 |
| `web/cleanup.py` | — | `find_orphan_entities` 19 |
| `web/domain_classifier.py` | — | `classify_domain` 80；`majority_domain` 104 |
| `memory/rag/pipeline.py` | 402 | `RetrievedChunk` 39；`RAGPipeline` 89；`document_repo` 106；`ingest` 122；`index_chunk` 导入 173 / 调用 175；`ingest_media` 241；`ingest_source` 322；`retrieve` 344；`build_context` 358；`graph_retrieve` 363；`graph_context` 373；`answer` 376；`delete_document` 383；`close` 395。**`hybrid_retrieve`/`_vector_hits`/`_rrf_fuse` 已删除** |
| `memory/rag/graph_rag.py` | 360 | `GraphPath` 23；`GraphRAGResult` 47（`build_context` 61）；`GraphRAGPipeline` 86；`retrieve` 92；`build_context` 141；`_find_seed_entities` 148；`_expand` 180（reinforce 开关 185，adopted 274，调用 275–276）；`_weighted_edges` 279；`_reinforce_enabled` 293；`_reinforce` 298（add_relation 302/309）；`_edge_is_active` 311；`_edge_in_window` 322。**`retrieve_multi` 已删除** |
| `memory/rag/knowledge.py` | 934 | `KnowledgeExtractor` 176；`LLMKnowledgeExtractor` 241；`extract` 305；`EntityResolver` 377；`_exact` 415；`_prefix_candidate` 418；`_store_alias` 437；`match` 466；`resolve` 474；`materialize_extraction` 528；`build_graph_context` 794 |
| `memory/rag/document.py` | 215 | `DocumentProcessor` 48；`parse` 51；`normalized_text` 77；`chunks_with_spans` 87；`_parse_bytes` 169 |
| `memory/manager.py` | 175 | `MemoryManager` 40；`semantic` 88；`add` 109；`get` 112；`search` 129；`list` 144 |
| `memory/base.py` | 548 | `BaseMemory` 380；`_embed_item` 400；`add` 418；`search` 489；`list` 520 |
| `memory/types/semantic.py` | 271 | `add_fact` 28；`_write_edge` 150；`facts` 260 |
| `memory/types/episodic.py` | 19 | `record` 15 |
| `memory/storage/document.py` | 171 | `SQLiteDocumentStore` 34；`_initialize` 77（`memories` 表 81）；`upsert` 101；`list` 140 |
| `memory/storage/document_repo.py` | 901 | `DocumentRepository` 95；建表 140–217；`_initialize_fts` 219；`upsert_document` 270；`get_document` 304；`list_documents` 311；`set_status` 342；`upsert_chunk` 359；`get_chunk` 398；`list_chunks` 403；`set_chunk_vector_status` 410；`search_keywords` 429（`bm25` 437）；`chunk_counts` 445；`chunk_ids` 454；`get_embedding_lock` 478；`create_ingest_job` 525；`get_ingest_job` 548；`set_ingest_job_status` 555；`list_ingest_jobs` 589；`reset_failed_ingest_job` 613 |
| `memory/storage/qdrant.py` | — | `QdrantVectorStore` 73；`collection_dimension` 124；`upsert` 219；`upsert_chunk` 233；`list_ids` 297；`search` 317 |
| `memory/storage/vector.py` | — | `InMemoryVectorStore` 34 |
| `memory/storage/graph.py` | 881 | `Neo4jGraphStore` 82；`add_relation` 190（`bump` 形参 203）；`add_observation` 278；`update_entity` 378（并发新增）；`_bump_relation` 433；`_apply_bump` 470；`entity_aliases` 502；`graph_snapshot` 520；`relation_memory_ids` 685；`path_query` 749 |
| `memory/embedding.py` | 441 | `BaseEmbedding` 48；`embed` 54；`embed_item` 64；`HashEmbedding` 77；`APIEmbedding` 116；`embed` 181；`embed_batch` 186；`embed_image` 251；`embed_multimodal` 255；`embed_item` 259 |
| `memory/embedding_lock.py` | 245 | `EmbeddingLockMismatch` 25；`inspect_embedding_lock` 85；`apply_embedding_lock` 135；`rebuild_vector_projection` 189 |
| `memory/ids.py` | — | `relation_id_for` 42 |
| `tool/hybrid_recall.py` | — | **本轮新增**。`HybridRecallResult` 47；`rrf_fuse` 55；`chunk_metadata` 65；`hybrid_enabled` 74；`vector_hits` 80；`fuse_hits` 111；`hybrid_recall` 143（`search_keywords` 调用 167）；`HybridRecallTool` 213（工具名 215，`execute` 243）；`create_tool` 266 |
| `tool/hybrid_index.py` | — | **本轮新增**。`repository_for` 58；`index_chunk` 72（调用点 205）；`HybridIndexTool` 157（`execute` 187）；`create_tool` 227 |
| `tool/multi_recall.py` | — | **本轮新增**（22:45:16）。`hybrid_recall_multi` 150；工具 352；导出 402 |
| `tool/graph_node_update.py` | — | **本轮新增**（22:44:44）。未接入聊天白名单 |
| `tool/memory_query.py` | 127 | `execute` 91 |
| `tool/memory_add.py` | 110 | `execute` 88 |
| `tool/rag_search.py` | 125 | `execute` 74 |
| `tool/rag_tool.py` | — | `RAGTool` 54；`execute` 82 |
| `tool/search.py` | — | `SearchTool` 184（name="web.search" 188）；`execute` 256 |
| `agents/react.py` | 1450 | `ReActAgent` 357；`run` 560 |
| `constants.py` | 336 | `MEMORY_EDGE_REINFORCE` 212；`MEMORY_HYBRID` 220 |

---

## 10. 快照与漂移对照（复现用）

本节记录本文行号对应的**确切文件版本**，以及本轮重构前后的行号位移，便于文件再次变动时快速换算。

### 10.1 快照 mtime（2026-09-21）

| 文件 | 行数 | mtime | 本文行号是否可信 |
| --- | --- | --- | --- |
| `web/static/index.html` | 3173 | 21:48:44 | ✅ 全程未变 |
| `web/support.py` | 442 | 21:48:44 | ✅ |
| `web/graph_builder.py` | 567 | 21:48:44 | ✅ |
| `web/ingest_queue.py` | 226 | 21:48:44 | ✅ |
| `web/seed.py` | 111 | 21:48:44 | ✅ |
| `memory/manager.py` | 175 | 21:48:44 | ✅ |
| `memory/types/semantic.py` | 271 | 21:48:44 | ✅ |
| `memory/types/episodic.py` | 19 | 21:48:44 | ✅ |
| `memory/storage/document.py` | 171 | 21:48:44 | ✅ |
| `memory/storage/document_repo.py` | 901 | 21:48:44 | ✅ |
| `memory/embedding.py` | 441 | 21:48:44 | ✅ |
| `memory/embedding_lock.py` | 245 | 21:48:44 | ✅ |
| `memory/rag/document.py` | 215 | 21:48:44 | ✅ |
| `constants.py` | 336 | 21:48:44 | ✅ |
| `memory/base.py` | 548 | 22:29:23 | ⚠️ 已按新版本核对 |
| `memory/storage/graph.py` | 881 | 22:33:32 | ⚠️ 已按新版本核对 |
| `memory/rag/knowledge.py` | 934 | 22:41:12 | ⚠️ 已按新版本核对（原 1017 行） |
| `memory/rag/pipeline.py` | 402 | 22:42:26 | ⚠️ 已按新版本核对（原 534 行） |
| `memory/rag/graph_rag.py` | 360 | 22:42:36 | ⚠️ 已按新版本核对（原 397 行） |
| `web/app.py` | 1092 | 22:42:52 | ⚠️ 已按新版本核对（原 1090 行） |
| `tool/hybrid_recall.py` | — | 22:40:29 | 🆕 本轮新增 |
| `tool/hybrid_index.py` | — | 22:40:19 | 🆕 本轮新增 |
| `tool/graph_node_update.py` | — | 22:44:44 | 🆕 本轮新增 |
| `tool/multi_recall.py` | — | 22:45:16 | 🆕 本轮新增 |

### 10.2 行号位移速查（旧 → 新）

`web/app.py`：

| 符号 | 旧 | 新 |
| --- | --- | --- |
| `_reject_json_constant` | 93 | 94 |
| `_is_fact_item` | — | 106（新增） |
| `embedding_config_hint` | 112 | 113 |
| `_save_upload` | 159 | 160 |
| `create_app` | 195 | 196 |
| `lifespan` | 198 | 200 |
| `guard_embedding` | 232 | 233 |
| `invalidate_graph` | 260 | 261 |
| `graph` | 270 | 271 |
| `graph_rag` | 302 | 303 |
| `chat` | 315 | 316 |
| `ingest` | 403 | 405 |
| `add_fact` | 441 | 443 |
| `add_knowledge` | 458 | 460 |
| `list_knowledge_jobs` | 535 | 537 |
| `retry_knowledge_job` | 553 | 555 |
| `add_image_knowledge` | 572 | 574 |
| `reseed` | 631 | 633 |
| `export` | 637 | 639 |
| `import_file` | 664 | 666 |
| `the_repository` | 772 | 774 |
| `fact_items` | 783 | 785 |
| `projected_vector_ids` | 786 | 788 |
| `projected_edge_ids` | 797 | 799 |
| `reconcile_report` | 808 | 810 |
| `repair_drift` | 843 | 845 |
| `list_documents` | 894 | 896 |
| `get_document` | 925 | 927 |
| `revectorize_document` | 955 | 957 |
| `stats` | 991 | 994 |
| `reconcile` | 1003 | 1005 |
| `reconcile_repair` | 1007 | 1009 |
| `rebuild_embedding` | 1011 | 1013 |
| `health` | 1028 | 1030 |
| `guard_embedding` 调用点 | 410/465/559/599/961/1015/1017 | **412/467/561/601/963/1017/1019** |

`memory/rag/pipeline.py`：

| 符号 | 旧 | 新 |
| --- | --- | --- |
| `RAGPipeline` | 116 | 89 |
| `document_repo` | 135 | 106 |
| `ingest` | 151 | 122 |
| `ingest_media` | 270 | 241 |
| `ingest_source` | 351 | 322 |
| `retrieve` | 373 | 344 |
| `build_context` | 490 | 358 |
| `graph_retrieve` | 495 | 363 |
| `graph_context` | 505 | 373 |
| `answer` | 508 | 376 |
| `delete_document` | 515 | 383 |
| `close` | 527 | 395 |
| `_rrf_fuse` / `_vector_hits` / `hybrid_retrieve` | 97 / 387 / 401 | **已删除 → `tool/hybrid_recall.py`** |

`memory/rag/graph_rag.py`：

| 符号 | 旧 | 新 |
| --- | --- | --- |
| `_find_seed_entities` | 185 | 148 |
| `_expand` | 217 | 180 |
| `_weighted_edges` | 316 | 279 |
| `_reinforce_enabled` | 330 | 293 |
| `_reinforce` | 335 | 298 |
| `_edge_is_active` | 348 | 311 |
| `_edge_in_window` | 359 | 322 |
| `retrieve_multi` | 148 | **已删除** |

`memory/rag/knowledge.py`：

| 符号 | 旧 | 新 |
| --- | --- | --- |
| `LLMKnowledgeExtractor` | 203 | 241 |
| `LLMKnowledgeExtractor.extract` | 267 | 305 |
| `EntityResolver` | 448 | 377 |
| `_exact` | 486 | 415 |
| `_prefix_candidate` | 489 | 418 |
| `_store_alias` | 508 | 437 |
| `match` | 536 | 466 |
| `resolve` | 544 | 474 |
| `materialize_extraction` | 613 | 528 |
| `build_graph_context` | 879 | 794 |

### 10.3 未确认项

- `agents/agent.py`、`agents/providers.py` 在 22:28:24 被改动过，但本文只引用 `agents/react.py`（1450 行，未变动）的 `ReActAgent`(357) 与 `run`(560)；这两个文件对调用链的影响**未逐一核对**。
- `tool/multi_recall.py`、`tool/graph_node_update.py` 只核对了对外符号名与行号，**未追踪其内部调用链**（它们尚未接入任何前端入口或聊天白名单，故不影响本文的主调用链结论）。
- `web/app.py` 中 `graph_cache` 命中判断的精确行范围、`add_knowledge` 同步回退分支的精确行范围，本文标注为「约」，因这两处处于本轮改动密集区。

---

*本文档按 2026-09-21 22:48 的仓库快照撰写并重建（原文件所在的 `代码解析/` 目录曾被并发进程删除，已重新创建）。*

# 「知识星云」个人知识库与智能记忆系统 —— 课程设计答辩讲解文档

> 本文档用于课程设计答辩陈述与项目交接：先按「标准课设答辩流程」讲清选题、需求、设计、实现与测试，再逐条拆解系统全量调用链路与运行过程。
> 项目仓库：all-agent（当前分支 python-course-design）｜界面主题：知识星云（Knowledge Nebula）

---

## 一、三分钟开场口述稿（答辩用）

各位老师好，我的课设题目是「知识星云」个人知识库与智能记忆系统。它解决的核心问题是：普通人积累的文档、笔记、一句话感想散落在各处，难以检索、难以复用。

我的方案是用 Python 做一个带图形界面的网站：后端用 FastAPI 搭建 HTTP 服务，前端用 Canvas 2D 把整个知识库画成一幅动态「星云图」——每个知识领域是一颗恒星，每个实体是绕恒星公转的行星，每条事实、备注、事件是卫星，实体之间的关系是有向连线。

系统接入了大语言模型，提供三个核心能力：

1. 知识管家对话：提问时先在记忆库里做「向量 + 关键词 + 知识图谱」三重混合检索，让模型基于检索到的内容回答，并给出引用来源，避免模型「一本正经地胡说八道」；
2. 自动化知识入库：上传文档自动切块入库；输入一句话，大模型自动抽取其中的实体和关系，长成新的星图结构；
3. 数据持久化与备份：所有数据保存在 SQLite 数据库中，并支持一键导出为 JSON 文件、再从 JSON 文件导入，完全满足课设对数据库和文件导入导出的硬性要求。

整个项目配置了 41 个测试文件和自动化质量门禁，全部通过。下面我从需求、设计、调用链路、运行过程四个方面详细汇报。

---

## 二、选题背景与意义

### 2.1 课设要求解读

《Python 语言程序设计（课程设计）》指导文件对题目的硬性要求：

- 必须有图形用户界面（也可以是网站）；
- 数据必须保存到数据库；
- 必须能把数据库内容保存到文件，或从文件导入数据到数据库；
- 建议使用 VS Code / PyCharm 等现代开发环境；
- 每人单独完成或两人组队。

### 2.2 选题动机

大语言模型时代，「给模型接上个人知识库」（RAG，检索增强生成）是最热门的工程方向之一，但它通常被拆散在论文和教程里。本课设选择把 RAG 检索、知识图谱、Agent 工具调用、四层记忆模型、前后端分离网站融合到一个完整系统中，工作量饱满、技术覆盖面广，同时所有功能都能在答辩现场真实演示，非常适合作为课设题目。

### 2.3 项目意义

- 对课设：完整覆盖「界面 + 数据库 + 导入导出」三项硬性要求，并延伸到 LLM 应用工程；
- 对个人：系统本身就是一个真正可用的个人知识管理工具；
- 对工程能力：实践了分层架构、单例管理、后台任务队列、投影一致性、安全防护（SSRF / 上传限额 / 严格 JSON）等软件工程方法。

---

## 三、需求分析

### 3.1 课设硬性要求 → 实现对照

| 课设硬性要求 | 本系统实现 | 代码位置 |
| --- | --- | --- |
| 图形用户界面 | Canvas 2D 星云图单页应用 + 侧边操作面板 | web/static/index.html |
| 数据保存到数据库 | SQLite（真值源）+ Qdrant（向量投影）+ Neo4j（图投影） | memory/storage/ |
| 库 → 文件（导出） | GET /api/export 导出全库为 JSON 并触发浏览器下载 | web/app.py |
| 文件 → 库（导入） | POST /api/import 上传 JSON，逐条校验、幂等去重后入库 | web/app.py |

### 3.2 功能需求

1. 智能问答：与「知识管家」对话，先检索后回答，附带来源、图谱路径与检索分数明细；
2. 知识入库（四种入口）：文档上传（txt/md 等）、一句话入库（LLM 自动抽取实体与关系）、手工录入三元组事实、图片/拍摄多模态入库；
3. 知识可视化：星云图实时渲染，领域/实体/原句/事实/备注/事件分属不同星体层级，关系为有向边，支持按历史时间点回放；
4. 混合检索：向量语义检索 + FTS5 关键词检索，用 RRF（Reciprocal Rank Fusion）融合，再叠加图谱多跳路径；
5. 后台任务队列：一句话入库异步化，任务状态持久化，进程重启后自动续跑；
6. 数据自愈：对账接口检测「真值源与投影漂移」（有文档没向量、有事实没图边）并一键修复；
7. 双模式问答：offline 只用本地记忆；online 在配置了联网搜索时可调用 web.search。

---

## 四、总体设计

### 4.1 技术栈

| 类别 | 选型 | 用途 |
| --- | --- | --- |
| 语言 | Python 3.12 | 全部后端代码 |
| Web 框架 | FastAPI + uvicorn | HTTP API、生命周期管理、静态托管 |
| 数据校验 | Pydantic v2 | 工具 Schema、请求体、LLM 输出结构化 |
| 关系数据库 | SQLite（FTS5 全文检索） | 文档/分块/任务/日志的真值源 |
| 向量数据库 | Qdrant（可降级为进程内内存向量库） | 语义向量投影 |
| 图数据库 | Neo4j（可降级为内存图） | 实体关系拓扑 |
| LLM 接入 | OpenAI 兼容 SDK | 对话、实体/关系抽取、视觉理解 |
| 前端 | 原生 HTML + Canvas 2D + fetch 轮询 | 星云图渲染与交互 |
| 测试与质量 | pytest（41 个测试文件）+ ruff + pre-commit | 回归与质量门禁 |

设计要点：不配置任何外部服务也能完整运行——向量检索退化为确定性离线哈希向量，图检索退化为内存图，聊天在未配置模型时返回 503 并附配置指引。配置 config/services.toml / config/provider.toml 后平滑升级到 Qdrant / Neo4j / 云端 LLM。

### 4.2 分层架构

```text
┌──────────────────────────────────────────────────────────────┐
│ 表现层  web/static/index.html（Canvas 2D 星云图 + 操作面板）      │
│         fetch 轮询 /api/graph?since=revision，增量刷新星图        │
├──────────────────────────────────────────────────────────────┤
│ API 层  web/app.py（FastAPI，约 20 个 REST 端点）                │
│         chat / ingest / knowledge / facts / graph / graph-rag  │
│         jobs / documents / reconcile / export / import / health │
├──────────────────────────────────────────────────────────────┤
│ 服务层  memory/rag/pipeline.py   RAGPipeline（摄取+混合检索+图谱） │
│         web/ingest_queue.py      一句话入库后台队列（SQLite 持久化）│
│         agents/                  Agent / ReActAgent + LLM 客户端  │
├──────────────────────────────────────────────────────────────┤
│ 记忆层  memory/manager.py        MemoryManager 统一调度四层记忆    │
│         working / episodic / semantic / perceptual             │
├──────────────────────────────────────────────────────────────┤
│ 存储层  SQLiteDocumentStore（真值源）                            │
│         QdrantVectorStore / InMemoryVectorStore（向量投影）       │
│         Neo4jGraphStore / 内存图（图投影）                        │
├──────────────────────────────────────────────────────────────┤
│ 横切    constants.py（唯一常量源）· core/（工具运行时）            │
│         memory/embedding_lock.py（向量空间锁）                    │
└──────────────────────────────────────────────────────────────┘
```

### 4.3 目录与模块职责

| 目录/文件 | 职责 |
| --- | --- |
| web/app.py | FastAPI 应用工厂 create_app()，全部 REST 路由与 lifespan 装配 |
| web/support.py | 进程级单例：MemoryManager / RAGPipeline / ReActAgent，问答留痕与图抽取调度 |
| web/graph_builder.py | 把四层记忆压扁成星云图 nodes + edges |
| web/ingest_queue.py | 一句话入库后台队列（SQLite 持久化、单 worker、断点续跑） |
| web/domain_classifier.py | 领域关键词分类（决定恒星归属） |
| web/seed.py + web/seed_data.json | Aetheria 初始星图种子数据，幂等播种 |
| web/cleanup.py | 孤立实体清理（重投影前的卫生检查） |
| agents/agent.py | Agent 基座：provider 管理、历史压缩、提示缓存、工具执行 |
| agents/react.py | ReActAgent：Thought/Action/Observation 文本协议 |
| agents/providers.py | provider.toml 多 profile 注册表（api_key / base_url / tool_mode） |
| agents/llm.py | OpenAI 兼容客户端封装（流式/非流式） |
| core/ | 工具协议内核：发现（discovery）、注册表（registry）、执行管理（runtime）、轮次控制（tool_loop）、目录工具（catalog）、更新日志（update_log） |
| memory/manager.py | 四层记忆统一入口，协调三套存储 |
| memory/base.py | MemoryItem / MemoryType / MemoryConfig、嵌入选型 make_default_embedding |
| memory/embedding.py | APIEmbedding（云端）+ HashEmbedding（离线兜底） |
| memory/embedding_lock.py | 向量空间锁：防止云端/离线向量混用 |
| memory/rag/pipeline.py | RAGPipeline：摄取（切块→真值→向量→抽取）与检索（RRF 融合+图路径） |
| memory/rag/knowledge.py | LLM 抽取结果落地：实体消解、关系物化、时序边 |
| memory/rag/graph_rag.py | 图+向量混合检索 GraphRAGResult |
| memory/storage/document_repo.py | documents / chunks / ingest_jobs 三张表的仓储层（参数化 SQL） |
| memory/storage/graph.py | Neo4j 存储 + 内存回退 + Aura 代理隧道 |
| tool/*.py | 单文件工具插件（11 个已注册工具） |
| scripts/ | check.py 质量门禁、存储迁移、云端迁移、向量重建 |

### 4.4 数据架构：单一真值源 + 双投影

这是整个系统最重要的设计决策：

```text
        写入口（ingest / facts / knowledge / chat 抽取）
                     │
                     ▼
        ┌────────────────────────┐
        │  SQLite: documents +   │   ← 真值源（原文、分块边界、三元组）
        │  chunks + memories     │     唯一权威；其它一切可重建
        └───────────┬────────────┘
                    │ 投影（派生）
        ┌───────────┴───────────┐
        ▼                       ▼
  Qdrant 向量点             Neo4j 实体/边
  （可重建）                （可重建）
```

- 写入顺序永远「先真值、后投影」：向量写成功而真值失败会留下无法解释的孤立向量，因此 pipeline.ingest() 中先 repository.upsert_chunk()（真值）再 manager.add()（向量化）；
- 投影漂移可检测、可修复：GET /api/reconcile 比对三套存储找出 missing_vector / missing_edge，POST /api/reconcile 幂等自愈；
- 向量空间有锁：embedding_lock 记录当前投影使用的嵌入模型/维度，切换模型必须显式 confirm_rebuild 确认重建，否则拒绝执行——离线哈希向量与云端向量绝不静默混用。

---

## 五、核心调用链路（详细拆解）

以下每条链路均可对照源码走读。入口命令统一为：.venv\Scripts\python.exe -m web.app，监听 http://127.0.0.1:8765（端口唯一来源 constants.DEFAULT_WEB_PORT）。

### 5.1 系统启动链路

```text
python -m web.app
  → uvicorn.run(app, host=127.0.0.1, port=8765)
  → create_app()                       # web/app.py 应用工厂
      → FastAPI(lifespan=lifespan)     # 注册约 20 个路由 + 静态目录挂载
      → lifespan 启动（asynccontextmanager）：
          1) get_manager()             # web/support.py 双检锁单例
             → MemoryConfig.from_config()        # 读 config/services.toml（Qdrant/Neo4j 开关）
             → make_default_embedding(config)    # 选型：云端配置齐→APIEmbedding；否则 HashEmbedding
             → MemoryManager(config, embedding)
                 ├─ SQLiteDocumentStore(memory.sqlite3)   # 真值源
                 ├─ QdrantVectorStore 或 InMemoryVectorStore（未配 Qdrant）
                 ├─ Neo4jGraphStore（未配则内存图）
                 └─ working / episodic / semantic / perceptual 四层记忆
                 └─ 内存向量库场景：启动时把 document_store 全量回填索引
          2) RAGPipeline(manager, extractor=build_knowledge_extractor())
             → extractor：provider.toml 有效 → LLMKnowledgeExtractor；否则 NullKnowledgeExtractor（离线安全兜底）
          3) IngestJobQueue.start()
             → restart_stale_ingest_jobs()   # 上次异常退出遗留的 running 任务复位为 pending
             → 重新入队全部 pending 任务（断点续跑）
          4) seed(manager)                   # WEB_AUTOSEED=True 时自动播种 Aetheria 星图（幂等）
      → lifespan 关闭时：ingest_queue.shutdown() → close_manager()
```

### 5.2 对话问答链路 POST /api/chat（核心链路）

```text
前端 fetch POST /api/chat {message, mode}
  → chat(body)
    → chat_ready()                          # provider.toml 存在且 api_key 非占位才放行，否则 503 + 配置指引
    → get_agent()                           # 懒加载 ReActAgent 单例
        · auto_discover_tools=False，手工挂 5 个工具：
          memory.query / memory.add / memory.rag_search / memory.rag / web.search
        · 工具统一注入 Web 单例 manager/pipeline（「同一份记忆库」是硬要求）
        · set_system_prompt：要求「先检索记忆、再回答、注明依据」
    → chat_tool_names(agent, online=mode=="online")   # online 且 AnySearch 已配置才放行 web.search
    → ExecutionContext(confirmed_side_effects=chat_confirmed_side_effects(agent))
        · 只为 memory.add 预置写确认：用户说「记住」才能落库；删除/清空仍需人工确认
    → async with app.state.chat_lock        # 管家是共享单例且历史无锁，串行化避免并发污染上下文
        → asyncio.to_thread(agent.run, ...) # 同步阻塞调用丢进线程，避免卡住事件循环
            → Agent.run → run_auto
                → 按 profile.tool_mode 选协议：
                  · native_strict/native_loose → run_with_tools（OpenAI function calling）
                  · text_react                → ReActAgent 文本协议
                → ToolLoop 轮次控制：max_rounds/安全上限 64 轮；同一调用重复 >3 次强制停机
                → ToolExecutionManager.execute_batch：
                    Pydantic 严格校验 → 注册代次校验 → schema_version/hash 校验
                    → side-effect 确认检查 → depends_on 依赖分层 / parallel_safe=False 隔离
                    → 并发上限执行 → 每个调用返回一个结构化结果
                → 工具执行（如 memory.query → MemoryManager.search → 向量+FTS5 混合检索）
                → Observation 回传模型，循环直至给出最终答案
    → record_qa(manager, question, answer, mode)     # 问答留痕 episodic 记忆（时间线新增一颗事件星）
    → schedule_qa_extraction(question, answer)       # 后台 LLM 抽取图补丁（不拖慢本轮响应）
        → extract_graph_patches：ingest「用户陈述+助手回答」→ 抽取实体/关系 → bump_graph_revision()
    → invalidate_graph()                              # 图缓存作废
    → graph_retrieve(question) + hybrid_retrieve(question)   # 为前端生成「依据」面板
    → 返回 {answer, mode, sources, paths, retrieval{note, hits 分数明细}}
```

### 5.3 文档上传入库链路 POST /api/ingest

```text
POST /api/ingest (multipart file, confirm_rebuild?)
  → _save_upload()                        # 1MB 流式落盘，超 64MB 返回 413，空文件 400
  → guard_embedding(confirm_rebuild)      # 嵌入锁：维度/模型不符且未确认重建 → 拒绝
  → pipeline.ingest_source(tmp_path, metadata={source, filename}, chunk_size=1000, overlap)
    → RAGPipeline.ingest()
      → apply_embedding_lock(manager, repository)
      → DocumentProcessor.parse(path)     # txt/md/html → 归一化文本
      → chunks_with_spans(chunk_size, overlap)   # 字符窗口切块，记录 char_start/char_end
      → repository.upsert_document(status="parsed")        # 真值先行
      → 逐块：
          repository.upsert_chunk(...)                    # 原文与分块边界落 SQLite
          manager.add(SEMANTIC)                           # 向量化写入向量库
          repository.set_chunk_vector_status("indexed")
          build_graph_context(manager, chunk, resolver)   # 相关子图喂给抽取器（复用规范实体名）
          LLMKnowledgeExtractor.extract(chunk, metadata, graph_context)
              → LLM 输出 → Pydantic 严格校验（只让通过校验的数据进库）
          materialize_extraction(manager, extraction, resolver)
              → EntityResolver 实体消解（归一化/别名/前缀边界/相似度）→ 全局去重
              → add_fact 写语义三元组 → Neo4j 有向边（谓词=relation）
              → 时序观察保留 event_at，多元关系不拆散
      → set_status("extracted")（抽取异常记 "failed" 但原文不丢，错误写入 report.errors）
  → manager.episodic.record("上传并导入了文档《…》")   # 留痕
  → invalidate_graph()                     # 星云图下次轮询即刷新
  → 返回 {filename, chunks, extraction{entities, relations, domains, superseded, retracted…}}
```

### 5.4 一句话入库链路 POST /api/knowledge（后台队列）

```text
POST /api/knowledge {text, event_at, wait}
  → guard_embedding()
  → IngestJobQueue.submit(text)
      → ingest_jobs 表写一条 status="pending"（attempts=0）
  → wait=true（默认，兼容同步行为）：
      queue.wait(job_id) 阻塞等待 worker 跑完 → 成功返回 items/extraction；失败 500
  → wait=false：
      立即返回 {job_id, status:"排队中", async:true}，前端清空输入框
后台单 worker（ThreadPoolExecutor(max_workers=1)，串行是因为后一次入库可能依赖上一次写入的图/向量）：
  pending → running → pipeline.ingest(Document(text), chunk_size=QA_EXTRACT_CHUNK_SIZE, overlap=0)
      → 失败：status="failed"、error 落库（attempts+1，超过 3 次不再自动重试）
      → 成功：status="done"、result(JSON) 落库
      → on_progress 回调 → invalidate_graph() → 前端 since 轮询自动看到新星
重启恢复：start() 时 restart_stale_ingest_jobs() 把遗留 running 复位为 pending 重新入队
前端：GET /api/knowledge/jobs 展示历史（排队中/正在入库/入库成功/入库失败）；
      失败任务可 POST /api/knowledge/jobs/{job_id}/retry 重新入队
```

### 5.5 图片入库链路 POST /api/knowledge/image

```text
multipart(image + text + captured_at)
  → MIME 校验（必须 image/*）→ _save_upload（同样受 64MB 限额）
  → 读字节 → 删除临时文件
  → pipeline.ingest_media(image, text, mime_type, metadata)
      → manager.add(PERCEPTUAL, payload=image, modality="image", timestamp=captured_at)
      → VL 抽取器 extract(image=..., mime_type=...) → 实体 + 时间 + 多元关系物化
  → 返回 extraction 报告；若当前嵌入非 VL 模型，返回 warning（图片已留存、已识图，向量只用文字说明）
```

### 5.6 星云图渲染链路 GET /api/graph?since=N

```text
前端定时轮询 GET /api/graph?since={revision}
  → revision == graph_revision() → 直接返回 {unchanged:true, revision}（省带宽与渲染）
  → 否则 build_graph(manager, at):
      → manager.list() 取四层记忆 → 逐条分类压扁：
          domain（领域）  → 恒星  level 1
          entity（实体）  → 行星  level 2（全局同名同一个，绕所属领域公转）
          chunk（原句）  → 行星  level 2（一个文档凝聚一颗，有向「提及」边连到相关实体）
          fact（三元组） → 卫星（挂主语实体下）
          note（备注）   → 卫星（不产生边）
          event（事件）  → 卫星（问答留痕、导入事件等，绕领域公转）
      → 关系不再作为节点：二元关系 = 有向边 source→target，relation 字段即谓词
      → 时序观察 event_at / cardinality=temporal 附在边上，同一实体多条时序边并列保留
      → graph_snapshot 从 Neo4j 读真实拓扑；图库不可用 → 回退 SQLite 卫星兼容模式
      → domain_color：crc32(domain) 取色，跨进程稳定（不用内置 hash）
      → at 参数支持历史时间点回放
  → 返回 {nodes, edges, stats, revision}
前端 Canvas 2D 按 level 布局：恒星居中、行星公转、卫星环绕，增量渲染
```

### 5.7 混合检索链路 GraphRAG（/api/graph-rag 与聊天溯源共用）

```text
查询 q →
  1) embed(q) → 向量库 topK（Qdrant 或内存向量）
  2) FTS5 关键词检索 topK（SQLite 全文索引）
  3) _rrf_fuse([vec_ids, fts_ids], k=60)   # 只按名次计分，避免余弦与 BM25 两套量纲混算
  4) 命中实体出发，图库多跳遍历（hops，默认 1）得到关系路径 paths
  → GraphRAGResult{evidence, paths}
      → build_context() 拼装进模型提示词
      → detail 分数明细（rrf/vector/keyword）供前端「依据」面板展示
  → 云端网关不可用时自动降级纯关键词，并在 note 中说明（用户可见，而不是空列表）
```

### 5.8 导入导出链路（课设硬性要求）

```text
导出 GET /api/export
  → manager.list(include_expired=False) → 每条 to_dict()
  → payload{format: "knowledge-nebula-export/v1", exported_at, counts{total,semantic,episodic}, items}
  → JSONResponse + Content-Disposition 触发浏览器下载 knowledge_export_<时间戳>.json

导入 POST /api/import (multipart .json)
  → json.loads(parse_constant=_reject_json_constant)   # 严格 JSON，拒绝 NaN/Infinity
  → 逐条校验：必须是对象；id/content 非空；metadata 为对象；memory_type 合法
  → 三元组按 (subject,predicate,object) 幂等：已存在则跳过，避免长出重边
  → manager.add(...) / semantic.add_fact(...) 落库；错误信息最多回示 WEB_IMPORT_ERRORS_MAX 条
```

### 5.9 Agent 工具调用回路（模型视角）

```text
模型请求（每次）：
  system 消息列出全部已注册工具名（catalog-first 懒加载：首轮只带 system.tool_catalog 完整 schema）
  + 稳定前缀的 prompt_cache_key（用户文本/工具结果/懒加载 schema 都在稳定前缀之后，利于网关 KV 缓存）
  + 流式传输（非流式长生成会撞读超时并被 SDK 静默重试）
工具协议（单文件插件，core/discovery.py 自动发现）：
  模块提供 TOOL_ENABLED=True + Input/Output 模型（extra="forbid", strict=True）
  + ToolSpec(name 带命名空间) + create_tool()
执行保护：
  schema 严格校验 / 注册代次校验 / schema_version+hash 校验
  side-effect 写入确认（generation 绑定的 confirmation_key）
  depends_on 依赖分层执行；parallel_safe=False 隔离成单调用层
  并发上限 + 每工具超时 + 输出按 output_model 校验
  未知/过期调用作为结构化工具错误返回给模型自纠，而不是执行未校验输入
```

---

## 六、数据库设计

### 6.1 memory.sqlite3（业务真值源）

| 表 | 关键字段 | 说明 |
| --- | --- | --- |
| documents | document_id(PK), title, raw_text, source, tags, permission, status, created_at, updated_at | 文档真值；status: parsed→vectorized→extracted/failed |
| chunks | chunk_id(PK), document_id, chunk_index, char_start, char_end, text, vector_status | 分块真值 + 投影状态；FTS5 虚拟表同步建关键词索引 |
| ingest_jobs | job_id(PK), kind, text, status, attempts, error, result, created_at, updated_at | 一句话入库任务（pending/running/done/failed） |
| memories | id, memory_type, content, metadata, importance, timestamp, expires_at | 四层记忆统一落库 |

### 6.2 update_log.sqlite3（项目自身更新日志）

- 表 update_logs：update_id（AUTOINCREMENT）、timestamp、system_name、executor、update_type、title、task_background、update_details、added_features、files、behavior_impact、validation、risks、follow_up；
- 只追加不删改，由 system.update_log 工具写入——这本身就是对本项目「工具化 Agent」能力的一次实战使用。

### 6.3 Qdrant / Neo4j（投影）

- Qdrant：collection 维度由 embedding_lock 守护，换模型必须先确认重建；
- Neo4j：实体节点 + 有向关系边（谓词、权重上限与增长、时序属性），本地不可达时内存图兜底，星云图接口降级但不失败。

---

## 七、运行过程（演示脚本）

```powershell
# 1. 环境准备（Python 3.12+）
python -m pip install -e ".[dev]"          # 安装依赖（开发含 pytest/ruff）
# 2. 启动（项目根目录执行）
.venv\Scripts\python.exe -m web.app
# 3. 浏览器打开 http://127.0.0.1:8765
```

建议演示顺序（约 8 分钟）：

1. 打开首页：星云图自动渲染（首次启动自动播种 Aetheria 种子数据）；指出恒星=领域、行星=实体、卫星=事实/事件；
2. 对话问答：问一个种子数据里有的问题，展示「回答 + 来源 + 图谱路径 + 检索分数明细」四块溯源面板；切换到 online 模式演示联网搜索（如已配置）；
3. 一句话入库：输入「我下周要参加算法竞赛集训」，秒回「排队中」；打开任务面板看到 排队中→正在入库→入库成功 的状态流转，星云图实时长出新星星；
4. 文档上传：上传一个 txt/md 文档，返回切块数与实体/关系统计，星云图出现「原句行星」与新边；
5. 星云图交互：点击节点查看详情；用时间参数回放历史星图；
6. 混合检索：调用 /api/graph-rag（或界面检索）展示向量+关键词+图路径的结果；
7. 导出/导入：点导出下载 JSON → 清库（或换库）→ 导入恢复；
8. 自愈对账：/api/reconcile 展示漂移检测，一键修复；
9. 健康检查：/api/health 展示嵌入模式、各存储实现（sqlite/qdrant 或内存/neo4j 或内存）、降级提示。

---

## 八、关键技术与创新点

1. 四层记忆模型：working（短期）/ episodic（事件流：每次问答、每次导库都留痕）/ semantic（三元组事实）/ perceptual（图片观察），由 MemoryManager 统一调度；
2. 真值源 + 投影架构：SQLite 是唯一权威，向量与图是可重建投影，配合 reconcile 对账实现最终一致；
3. RRF 混合检索 + 图路径溯源：向量语义与关键词互补，回答附「证据链」而不是黑箱；
4. 一句话入库后台队列：提交即返回，SQLite 持久化任务状态，重启自动续跑，attempts 封顶防死循环；
5. 图缓存 revision 协议：写操作递增 GRAPH_REVISION，前端 since 轮询增量刷新，零锁失效；
6. 嵌入选型与向量空间锁：云端/离线自动切换，跨向量空间操作必须显式确认重建；
7. 工具化 Agent：单文件工具插件协议 + 自动发现 + 严格 Schema 校验 + 双协议（原生 function calling / ReAct 文本）+ 提示缓存优化（稳定前缀 + cache_key + 流式传输 + 历史压缩）；
8. 安全防护：SSRF 代理隧道、上传流式限额、严格 JSON（拒 NaN/Infinity）、写入副作用确认、参数化 SQL、删除提案原子确认。

---

## 九、测试与质量保障

- 41 个测试文件，覆盖：Agent/ReAct、工具执行管理、发现机制、记忆四层、RAG 摄取与检索、图谱语义边、时序入库、后台队列、嵌入 API/HTTP/锁、域分类器、Web API、UI 契约、迁移脚本、对账与删除提案等；
- python scripts/check.py 一键门禁：ruff 静态检查 + 全量 pytest，任一失败即停；
- .pre-commit-config.yaml 本地钩子，提交前自动跑同一门禁。

---

## 十、总结与展望

总结：本项目以「知识星云」为载体，把 FastAPI 网站、Canvas 可视化、SQLite/Qdrant/Neo4j 三存储、RAG 混合检索、LLM 知识抽取、Agent 工具循环、后台任务队列融合为一个可运行、可演示、可测试的完整系统，完整满足课设「界面 + 数据库 + 导入导出」的硬性要求，并在工程深度上做了充分延伸。

展望：多用户与权限体系、图谱增量订阅推送、embedding 模型热切换、移动端适配、更丰富的实体消解算法。

---

## 附录 A：答辩常见问题预案

1. Q：为什么用 SQLite 还要 Qdrant/Neo4j？
   A：SQLite 存原文与三元组（真值源，可审计可回放）；Qdrant 做近似向量检索，Neo4j 做多跳关系推理。三者是「真值源 + 可重建投影」关系，未配置外部服务时自动降级为内存实现，保证离线可演示。
2. Q：大模型会不会编造？
   A：检索增强（RAG）保证回答基于库内证据；LLM 抽取的结构化输出必须过 Pydantic 校验才落库；回答附来源、路径与分数明细，可人工核验。
3. Q：数据库和文件怎么互转？
   A：GET /api/export 导全库 JSON（下载文件），POST /api/import 导入，严格 JSON 校验 + 三元组幂等去重。
4. Q：后台队列崩了怎么办？
   A：任务状态持久化在 ingest_jobs 表，重启时把残留 running 复位为 pending 重新入队，attempts 超过 3 次停止自动重试并标记 failed，可人工 retry。
5. Q：并发安全怎么保证？
   A：单例加双检锁；聊天用 asyncio.Lock 串行化；图缓存用 revision 无锁失效；工具执行有注册代次校验、并发上限、依赖分层；嵌入锁防止向量空间混用。

## 附录 B：关键文件索引

- 入口：web/app.py（路由与 lifespan）、constants.py（全部常量）
- 后端核心：memory/manager.py、memory/rag/pipeline.py、memory/rag/knowledge.py、memory/rag/graph_rag.py
- Agent：agents/agent.py、agents/react.py、agents/providers.py
- 存储：memory/storage/document_repo.py、memory/storage/graph.py、memory/storage/vector.py、memory/storage/qdrant.py
- 前端：web/static/index.html
- 测试：tests/（41 个文件）、scripts/check.py

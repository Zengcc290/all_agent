# 知识星云：精简（删除死代码 + 删除与目标功能无关的活代码）与增强（四大功能）方案书

> 版本：v1.0 方案稿（**只出方案，未改代码**）
> 代码基线：`913f4f7`（update-log-113）
> 调研方式：5 个子代理分区审计（core/agents/tool、memory/、web、脚本配置文档、新功能锚点）+ 主代理逐条交叉验证
> 目标：**删掉与目标功能无关的代码，并落地四个能力**——边权重的记忆增强、LLM 驱动的删除（用户确认）、问句分解多路查询、导入时的逐句多元关系
> 本机现状：`.env` 已接上真 Qdrant（6333）与 Neo4j（7687）；`memory.sqlite3` 的 `documents/chunks` 仍为空（迁移未执行）
> 门禁现状：`ruff --no-cache` 有 2 条 RUF100 待清（见 §2.4 D1b）；全量 pytest 在真库接上后会超时（见 §2.4 D1c）

---

## 〇、一句话结论

项目**没有大面积死代码，但有一大片"活着却与目标功能无关"的代码**。这两者必须分开判：

- **死代码**（零引用、不可达）：框架主干 388 个符号里生产零引用只 30 个，真正可安全删除的集中在 **少数零引用常量 + 若干 test-only 方法 + 一处不可达分支**，量级约 **-262 行**（A/B 级）。
- **活代码但偏离目标**（D 级）：这些代码真实注册、真实可达、有测试、跑得通，却**与用户点名的功能毫无交叉**。最大的一笔是**聊天面板注册的 17 个工具里只有 7 个与目标功能相关**——另外 10 个是文件操作、技能包、开发日志三类能力。
  **最终决定**：删 **D1（skill 子系统）+ D2（`fs.*` 五工具）≈ -1,800 行**，工具面板 17 → 11；**保留 D3（update_log 开发日志簇）与 D4（`tool_template.py`）**。

四个新功能里，**两个是"从零加"**（边权重的回忆增强、LLM 提议删除的确认闸门），**两个是"补最后一公里"**（问句分解、逐句多元关系+时间状态）——现有代码已经把 80% 的基础设施铺好了（Neo4j 边属性透传管道、`_rrf_fuse` 天然 N 路、`RelationCandidate.action/cardinality`、确认键+世代号机制）。

**并且有一个必须先处理的前提**：仓库里的 `memory.sqlite3` 实测 `documents=0 / chunks=0`，而 `memories=37`（其中 31 条带 `aetheria-seed-v1` 种子标记）——**Phase 5 数据迁移从未真正执行过**。这直接影响"逐句多元关系"功能，见 §4.0。

---

## 一、保留红线（明确不动）

以下为用户点名的目标功能所依赖的代码，本方案**一律不删、不改接口**（另含用户明确选择保留的 D3/D4 两簇）：

| 编号 | 保留对象 | 关键落点 |
|---|---|---|
| R1 | **星云图 + 前端单文件** | `web/static/index.html`（2,709 行）、`/api/graph`、`web/graph_builder.py`、`web/seed.py` + `seed_data.json`（星图初始 10 实体/11 关系） |
| R2 | **SQLite 存文档与分块真值** | `memory/storage/document_repo.py`（`documents`/`chunks`/`chunks_fts`）、`DocumentRepository`、真值源状态机 |
| R3 | **Qdrant 存每块向量、专职向量检索** | `memory/storage/qdrant.py`（`QdrantVectorStore`、`upsert_chunk`、`_light_payload`、collection=`helloagents_memory`） |
| R4 | **Neo4j 建图/检索图关系** | `memory/storage/graph.py`（`Neo4jGraphStore`、`add_relation`、`get_relations`）、`memory/types/semantic.py`（`_write_edge`） |
| R5 | **LLM 加工用户输入** | `agents/react.py`（ReAct 循环）、`memory/rag/knowledge.py`（`LLMKnowledgeExtractor`、`EntityResolver`）、`web/support.py`（agent 装配与 4 个记忆工具注册） |
| R6 | **四层记忆 + SQLite `memories` 表** | `memory/base.py`（`BaseMemory`）、`memory/manager.py`、`memory/types/*`、`memory/storage/document.py` 的 `SQLiteDocumentStore`（**这是四层记忆的唯一落库点，删不得**） |
| R7 | **在线联网搜索** | `tool/search.py` 的 `web.search`（chat `online` 模式靠 `agents/agent.py:274` 自动发现注册） |
| ~~R8~~ | ~~技能包~~ **← 已改为删除** | ~~`skills/*.md` + `core/skill_catalog.py`/`skill_discovery.py`~~。修正：v1.0 曾把它列为红线，理由是"有真实加载链"；但**有调用链 ≠ 与目标功能有关**，用户已决定删除 → 见 **§2.4 簇 D1** |
| R9 | **更新日志协议** | `update_log.sqlite3`、`update_log_readme_first.md`（**D3 已决定保留**，与产品功能无关但保留该工作流） |
| R10 | **接口契约** | 前端实际调用的 11 个端点全部保留；`/api/health`、`/api/reconcile`、`/api/documents*`、`/api/stats` |
| R11 | **开发日志工具簇**（新增红线） | `core/update_log.py`、`core/activity_log.py`、`tool/update_log.py`、`tool/read_update_log.py`、`tool/read_update_logs.py`（**D3，用户已决定保留**；执行 D1/D2 时切勿误删） |
| R12 | **工具撰写范本**（新增红线） | `tool/tool_template.py`（**D4，用户已决定保留**；同时是 F2 新增 propose 工具的照抄范本） |

> 一句话判定依据：`SQLiteDocumentStore` 管 `memories` 表（记忆条目），`DocumentRepository` 管 `documents/chunks` 表（语料真值）——**同一 SQLite 文件里两张互不相交的表，不可互相替代**（证据：`memory/base.py:409-508` 全走 `document_store`；`memory/rag/pipeline.py:187/270/360` 全走 `document_repo`；`DocumentRepository` schema 里没有任何 `memory_type/content/embedding` 字段）。

---

## 二、删除清单（分级，逐条带证据）

### 2.0 立即可删（A 级：零引用、零联动、不影响任何测试）

| # | 对象 | 位置 | 行数 | 证据 | 删除动作 |
|---|---|---|---|---|---|---|
| A1 | `DEFAULT_EMBEDDING_TUNNEL_REMOTE_PORT` | `constants.py:132` | 1 | 全项目 0 引用（隧道命令由 `.env` 的 `EMBEDDING_TUNNEL_HINT` 下发，不在代码里读） | 删常量 |
| A2 | `DEFAULT_NEO4J_USERNAME` | `constants.py:144` | 1 | 0 引用（用户名由 `HELLOAGENTS_MEMORY_NEO4J_USERNAME` 覆盖） | 删常量 |
| A3 | `DEFAULT_NEO4J_PASSWORD` | `constants.py:145` | 1 | 0 引用 | 删常量 |
| A4 | `DOC_DOMAIN` | `constants.py:308` | 1 | 0 引用（`web/domain_classifier.py` 未使用） | 删常量 |
| A5 | `RAG_CHUNK_MAX` | `constants.py:216` | 1 | 0 引用；实际代码是 `tool/rag_tool.py:41` 的字面量 `le=100000`（注释与现实脱钩） | 删常量，或反向：改为引用常量（二选一，推荐删） |
| A6 | `EntityResolver._alias()` 及 `or` 链两处 | `memory/rag/knowledge.py:318-325, 382, 401` | 12 | `_exact(key)` = `_entities.get(key) or _index.get(key)`；`_remember()`（:308-313）对 canonical key 双写 `_entities`/`_index`，alias key 只进 `_index`。故 `_exact` 返 None ⟹ `_index.get(key)` 也是 None ⟹ `_alias` 必返 None，`or self._alias(key)` 不可达 | 删方法 + 去掉两处 `or` 链（行为完全一致） |
| A7 | `MemoryItem.from_dict` | `memory/base.py:200-204` | 5 | 0 引用（持久化走 `SQLiteDocumentStore._decode`） | 删方法（`_json_restore` 别删，`document.py` 在用） |
| A8 | `MemoryItem.event_time` | `memory/base.py:177-179` | 3 | 唯一消费者 `EpisodicMemory.timeline` 本身是死代码（见 A9） | 与 A9 一起删 |
| A9 | `EpisodicMemory.timeline` | `memory/types/episodic.py:18-26` | 9 | `\.timeline\(` 全项目 0 调用 | 删方法 |
| A10 | `MemoryConfig.to_dict` | `memory/base.py:348-352` | 5 | `.to_dict()` 调用点全覆盖 `MemoryItem`/`MemorySearchResult`/`GraphRAGResult`/`Document`/embedding，无 `config.to_dict` | 删方法 |
| A11 | `MemoryConfig.extra` | `memory/base.py:280, 312-313, 339-340` | 6 | 除自校验外 0 使用 | 删字段 + `from_env` 的 extra 分支 |
| A12 | `MemoryManager.cleanup_expired` | `memory/manager.py:132-137` | 7 | 0 调用（过期清理实际靠 `get` 时懒删除） | 删方法 |
| A13 | `MemoryManager.stats` | `memory/manager.py:149-150` | 3 | 0 调用（`/api/stats` 走 `repository.stats` + `document_store.list`） | 删方法 |
| A14 | `BaseDocumentStore.save` | `memory/storage/document.py:30-31` | 2 | `document_store.save` 0 调用 | 删方法 |
| A15 | `BaseDocumentStore.clear` + `SQLiteDocumentStore.clear` 重写 | `memory/storage/document.py:33-36, 149-155` | 13 | `store.clear` 0 调用（`tool/memory_tool.py:86` 走 `BaseMemory.clear` 逐条删） | 删两处 |
| A16 | `BaseVectorStore.clear` + `InMemoryVectorStore.clear` + `QdrantVectorStore.clear` | `memory/storage/vector.py:22-23, 68-70`; `qdrant.py:181-184` | 9 | `vector_store.clear` 0 调用 | 删三个 clear（**不动 Qdrant 类本体**） |
| A17 | `Neo4jGraphStore.delete_relation` | `memory/storage/graph.py:225-246` | 22 | 仅定义处；删除走 `delete_memory_relation`（`semantic.py:180-182`） | 删方法 |
| A18 | `Neo4jGraphStore.upsert_relation` 别名 | `memory/storage/graph.py:177-178` | 2 | 仅定义处 | 删别名 |
| A19 | `MemoryItem.to_dict(include_payload=...)` 参数 | `memory/base.py:181, 196-197` | 3 | `include_payload=False` 0 调用（全走默认 True） | 删参数，保留方法体 |
| A20 | `BaseMemory.add` 的 `ttl` / `expires_at` 直传参数 | `memory/base.py:387-388, 397-402` | 8 | `ttl=` / `expires_at=` 直传 0 调用（只有 `ttl_seconds` 被 `tool/memory_add.py:94` 使用） | 删两个死参数，保留 `ttl_seconds` |

**A 级小计：约 -112 行，零测试改动。**

### 2.1 B 级：需同步改测试（test-only）

| # | 对象 | 位置 | 行数 | 证据 | 联动 |
|---|---|---|---|---|---|---|
| B1 | `WorkingMemory.set/get_by_key/get_value` | `memory/types/working.py:25-46` | 22 | 仅 `tests/test_memory.py:28-32` 调用 | 改该测试为 `get`/`add` 断言 |
| B2 | `DocumentProcessor.chunks`（旧切分入口） | `memory/rag/document.py:87-88` | 2 | 仅 `tests/test_memory_rag.py:42,58,60`（pipeline 用 `chunks_with_spans`） | 改测试用 `chunks_with_spans` |
| B3 | `PerceptualMemory.store/get_data` + `remember` 别名 | `memory/types/perceptual.py:14-24` | 13 | `perceptual.` 全项目仅 `manager.py:69` 装配；三方法 0 调用 | **保留 `MemoryType.PERCEPTUAL` 枚举**（星图事件分档遍历需要），只删方法；`perceptual` 层保留为空壳 |
| B4 | `agents/agent.py` 的原型兼容 shim：`clear_history`、`set_*_prompt`、`set_global_model`、`profile_info`、`list_profiles`、`reload_provider_profiles`、`add_provider`、`detect_models` | `agents/agent.py:297, 922-961, 1038-1052` | ~60 | 生产 0 引用、测试 ≤1 处；文件注释自认是「read-only compatibility views，新代码应使用 `provider_registry`/`active_profile`」 | 删 shim + 改 2 个测试 |
| B5 | `agents/react.py` 同步便利入口 `arun` / `run_react` | `agents/react.py:586-591` | ~10 | 生产 0 引用（web/`app.py` 走 `agent.run` 异步路径） | 删或标注，视是否留给外部调用 |
| B6 | `agents/llm.py` 的 `get_query` | `agents/llm.py:249` | ~8 | 生产 0 引用、测试 0 | 删方法 |
| B7 | `core/registry.py` 的 `summaries` | `core/registry.py:171` | ~5 | 生产 0 引用 | 确认无动态取用后删 |
| B8 | `tool/search.py` 的四个 normalize/reject 帮助函数 | `tool/search.py:99-148` | ~30 | 生产 0 引用（`tool/__init__` 只做 PEP 562 懒加载钩子） | 删（**保留 `SearchTool` 本体**） |

**B 级小计：约 -150 行，需改 3 个测试文件。**

### 2.2 C 级：星图/存储文件内的方法 —— 建议**不删，改为增强**

以下两项被 grep 判定"仅测试引用"，但它们**正好是用户点名功能的载体**，删除会自断功能：

| # | 对象 | 位置 | 判定 | 处置建议 |
|---|---|---|---|---|
| C1 | `Neo4jGraphStore.path_query` + `_neighbours` + `_local_paths` | `memory/storage/graph.py:248-323`（约 76 行） | 生产 0 引用，仅 `tests/test_semantic_graph.py:104-128` | **不删**。用户要的"快速检索图关系"就是它——把它从"纯结构 BFS、无排序"升级为带权重的路径查询（见 §3.1）。当前唯一生产缺口是**没有被 RAG 检索层调用**，属于"接入"问题不是"删除"问题 |
| C2 | `Neo4jGraphStore.entity()` | `memory/storage/graph.py:122-144`（23 行） | 仅测试引用（星图走 `entity_aliases`） | **不删**：边权重方案需要按实体读回 `importance`，正好用它 |

### 2.3 明确"不删"（已核实为有用，避免误删）

- ~~`skills/` 6 个 `.md` + `core/skill_*.py`~~ —— **本条已推翻，移入 §2.4 D 级**。修正说明：v1.0 以"有完整运行时调用链"为由判它保留，但**有调用链 ≠ 与目标功能有关**。实测该子系统（630 行代码 + 302 行测试）与记忆/图/向量/抽取**零交叉**，注册的 4 个技能全是开发流程类，却每轮请求固定注入 1117 字符。
- `tool/search.py` 联网搜索 —— chat `online` 模式靠自动发现注册；删掉会让 online 静默失去搜索。
- `memory/embedding.py` 的 `APIEmbedding` —— `web/support.py:56-78` 与 `memory/base.py:86` 的「网关 → DASHSCOPE key → Hash」三级里仍有真实分支；`HashEmbedding` 是离线测试底座（`tests/conftest.py` 全局 fixture）。
- `tool/tool_template.py` —— `TOOL_ENABLED = False`（L179），不会被注册，作为"新工具撰写规范"存在；若要彻底精简可删（252 行），但会失去写工具的唯一范本，**建议保留**。
- `scripts/migrate_storage.py`（231 行）—— **迁移尚未执行**（见 §4.0），现在删就丢了执行载体。执行完可评估删除。
- `scripts/reindex_embeddings.py`（58 行）、`scripts/check.py`（43 行，被 `.pre-commit-config.yaml:9` 真实调用）—— 保留。
- 五处 Pydantic 校验器（`validate_action_arguments`、`validate_consistency`、`validate_unique_call_ids`）与 `for_tool`/`for_skill`（`@classmethod`/`@contextmanager` 工厂）—— grep 看不到动态派发，**不是死代码**，勿删。
- `.env.example` 里 8 个从未被读取的键（如 `SEARCH_TOOL_ENABLED`）—— 只改注释，代码不动。

### 2.4 D 级：**"活着、但与目标功能无关"的整簇**（新增，按用户"与我说的功能无关的都列入删除"要求）

判定标准（区别于 A/B 级）：这些**不是死代码**——它们被真实注册、真实可达、有测试、能跑通。但它们服务的是"**给 LLM 一套文件操作能力 + 技能包 SOP + 开发日志协议**"，与用户点名的功能（星云图、SQLite/Qdrant/Neo4j 三库、LLM 抽取记忆、边权增强、删除确认、逐句多元关系）**没有任何功能交叉**。

**决定性证据：聊天面板实际注册的 17 个工具里，只有 7 个与目标功能相关**

| 与目标功能**有关**（保留） | 与目标功能**无关**（D 级候选） |
|---|---|
| `memory.add`（写记忆） | `fs.read_text`、`fs.read_dir`、`fs.read_file_glob`、`fs.write_text`、`fs.edit_text`（5 个文件工具） |
| `memory.query`（查记忆） | `system.skill_catalog`（技能包） |
| `memory.manage`（记忆管理） | `system.update_log`、`system.read_update_log`、`system.read_update_logs`（3 个日志工具） |
| `memory.rag` / `memory.rag_search`（RAG 检索） | |
| `system.current_time`（事实时间戳） | |
| `system.tool_catalog`（工具面板） | |

**逐簇清单（含精确行数、耦合点、处置）**

| # | 簇 | 文件 | 行数 | 与目标功能的关系 | 删除耦合点 | 处置 |
|---|---|---|---|---|---|---|---|
| **D1** | **skill 技能包子系统** | `core/skill_catalog.py`(104)、`core/skill_discovery.py`(335)、`core/skill_models.py`(100)、`core/skill_registry.py`(91) | **630** | 零交叉。注册的 4 个技能（`code-change-delivery`/`paper-review`/`tool-authoring`/`weekly-report`）全是开发流程类 | `agents/agent.py` 49 处引用、`agents/react.py` 17 处、`core/__init__.py` 10 个导出、`constants.py` 4 常量；`tests/test_skills.py`(302)、`test_react.py`:315/487/509 硬断言、`test_hot_reload.py`:88 指纹基准 | **✅ 用户已决定：彻底删**，明细见 §2.4.1 |
| **D2** | **fs.* 文件工具簇** | `tool/fs_read_text.py`(150)、`tool/fs_read_dir.py`(183)、`tool/fs_read_file_glob.py`(177)、`tool/fs_write_text.py`(156)、`tool/fs_edit_text.py`(179) | **845** | 零交叉——**已实测**：`memory/`、`web/app.py`、`web/support.py`、`agents/react.py` 对 `fs.*` 的引用数为 **0** | `tests/test_fs_*.py` 5 个文件(796 行)、`test_p0_integration.py` 工具名单与 3 处 import、`tool/_shared.py` 的 fs 专属辅助 | **✅ 用户已决定：删**，明细见 §2.4.2 |
| **D3** | **update_log 开发日志簇** | `core/update_log.py`(246)、`core/activity_log.py`(123)、`tool/update_log.py`(159)、`tool/read_update_log.py`(119)、`tool/read_update_logs.py`(133) | **780** | 零交叉——**已实测**：`memory/` 与 `memory/rag` 对 `update_log`/`activity_log` 引用数为 **0**。它是"给 LLM 记开发日志"的协议 | `agents/agent.py:43-45` 的 import、`core/__init__.py` 导出、`update_log.sqlite3` 数据文件、`tests/test_update_log.py`(143) | **✅ 用户已决定：不删**（保留开发日志能力；它虽与产品功能无关，但你选择留下这个工作流） |
| **D4** | **工具撰写范本** | `tool/tool_template.py` | **252** | 只作为"新工具怎么写"的范本，`TOOL_ENABLED=False` 不注册 | 无（grep `tool_template` 全项目 0 引用） | **✅ 用户已决定：不删**（保留，作为 F2 新增 propose 工具的照抄范本） |

**D 级最终执行范围：仅 D1 + D2**（约 **1,475 行生产代码 + 1,098 行测试**）
**D3 + D4 明确保留**（约 1,032 行生产 + 143 行测试继续留在仓库）

#### 2.4.1 D1 执行明细（skill 子系统，已确认删）

**第一步：删 4 个模块文件**
```
core/skill_catalog.py      104 行   整删
core/skill_discovery.py    335 行   整删
core/skill_models.py       100 行   整删
core/skill_registry.py      91 行   整删
                            630 行
```

**第二步：删 `skills/` 目录里 4 个真技能文件**（保留 `README.md` 作为目录说明或一并删）
```
skills/code-change-delivery.md   6092 B
skills/paper-review.md           1131 B
skills/tool-authoring.md         7560 B
skills/weekly-report.md           778 B
skills/README.md                 2695 B   （撰写指南，可选）
skills/skill-template.md          525 B   （范本，可选）
```

**第三步：改 `agents/agent.py`（49 处 skill 引用，逐行）**
```
删 import（5 行）：
  :16   DEFAULT_SKILLS_ROOT,                       → 删
  :27   SkillCatalogTool,                          → 删
  :28   SkillDiscoveryReport,                      → 删
  :29   SkillRegistry,                             → 删
  :41   from core import discover_skills as discover_skill_packages → 删
改 __init__ 签名与校验（4 处）：
  :222  auto_discover_skills: bool = True,        → 删参数
  :223  skills_root: str = DEFAULT_SKILLS_ROOT,   → 删参数
  :233-234  isinstance 校验                       → 删
  :235-236  skills_root 校验                      → 删
删实例化与注册（4 行）：
  :266  self.skills_root = skills_root           → 删
  :267-269  self.skills / skill_catalog_tool     → 删
  :270  self.skill_discovery_report              → 删
  :271  self.tools.register(self.skill_catalog_tool) → 删
  :272-273  if auto_discover_skills: discover_skills(...) → 删
删方法（2 个）：
  :409-426  def discover_skills(...)             → 整删
  :766-794  def _with_registered_skill_names(...) → 整删
  :877-888  skill_entries 构建                   → 删
改装配（1 处）：
  :817-819  skill_message = ...; prefix.append(skill_message) → 删
  :803-804  注释里"skill 目录作为第二条系统消息"说明      → 改注释
```

**第四步：改 `agents/react.py`（17 处）**
```
删常量（1 处，精确范围）：
  :404-412  SKILL_VIEW_REACT_INSTRUCTIONS = ( ... )   → 整删
            ⚠ 注意：紧接的 :413-415 注释是 UNBOUNDED_ROUND_SAFETY_LIMIT 的说明，
              与 skill 无关，**必须保留**（别顺手删掉）
删方法（1 个）：
  :1155-1174  def _skill_directory_lines(...)      → 整删
改注入（1 处）：
  :1046-1049  skill_entries = ...; lines.extend(...) → 删
```

**第五步：改 `core/__init__.py`（门面导出，精确行号）**
```
删 import 块（14 行）：
  :22-26  from .skill_catalog import (SkillCatalogInput, SkillCatalogOutput, SkillCatalogTool)
  :27-33  from .skill_discovery import (SkillDiscoveryError, SkillDiscoveryRecord,
                                        SkillDiscoveryReport, discover_skills, read_skill_content)
  :34     from .skill_models import SkillSpec
  :35     from .skill_registry import SkillRegistry
删 __all__ 名字（10 个）：
  :46 "SkillCatalogInput",      :47 "SkillCatalogOutput",   :48 "SkillCatalogTool",
  :49 "SkillDiscoveryError",    :50 "SkillDiscoveryRecord", :51 "SkillDiscoveryReport",
  :52 "SkillRegistry",          :53 "SkillSpec",
  :67 "discover_skills",        :71 "read_skill_content",
  ⚠ 注意：:56-58 的 "ToolDiscoveryError/Record/Report" 是**工具发现**（保留），
         别与 skill 的 discovery 名字看混
```

**第六步：改 `constants.py`（比初计多——有 4 个 skill 常量 + 注释块）**
```
:DEFAULT_SKILLS_ROOT = "skills"                    → 删
:72-75  注释块（技能包校验与发现的文件指向）        → 整删
:77-78  SKILL_NAME_PATTERN = re.compile(...)       → 删
:80     MAX_DESCRIPTION_CHARS = 2000               → 删
:81     MAX_VERSION_CHARS = 32                     → 删
```
> ✅ **已实测**：这 4 个常量的引用方只有 `core/skill_models.py`、`core/skill_discovery.py`、`core/skill_catalog.py`、`agents/agent.py:16/223` —— 全是 D1 要删的对象，**无外部使用方**。删 `agents/agent.py:16` 的 `DEFAULT_SKILLS_ROOT,` import 行即可收干净。
> ⚠ 边界提醒：`:70  HISTORY_MAX_MESSAGES = 60` 与 `:82 起` 之后的常量属于**会话历史/其它**模块，别连带删。

**第七步：改测试（3 个文件）**
```
tests/test_skills.py   302 行  → 整删（该子系统专属测试）
tests/test_react.py    :315, :487, :509  → 从工具名硬断言里去掉 "system.skill_catalog"
                       （断言串原为 "All registered tool names: system.skill_catalog, system.tool_catalog, ..."）
tests/test_hot_reload.py  :88  → 换掉用 system.skill_catalog 做指纹基准的那一项
```

**第八步：文档/日志提及**
```
项目代码拓扑与接口解析.md   → 删 4 个模块条目与相关覆盖率行
update_log_readme_first.md / 更新日志 → 记一条"删除 skill 子系统"
```

**风险**：`agents/react.py` 的提示词缓存键会因技能消失而变化（`test_react.py` 有断言），所以**必须同步改那三处断言**，否则测试挂。这属于预期变化，不是缺陷。

#### 2.4.2 D2 执行明细（fs.* 文件工具，已确认删）

**第一步：删 5 个工具模块**
```
tool/fs_read_text.py        150 行
tool/fs_read_dir.py         183 行
tool/fs_read_file_glob.py   177 行
tool/fs_write_text.py       156 行
tool/fs_edit_text.py        179 行
                            845 行
```

**第二步：清理 `tool/_shared.py`（349 行）里变为死代码的 fs 专属辅助**

⚠️ **不能整删 `_shared.py`** —— `tool/rag_tool.py:24` 仍在用它的 `resolve_path, workspace_root`（`rag_tool` 是记忆功能，必须保留）。逐函数判定：

| 函数 | 行 | 删后判定 | 处置 |
|---|---|---|---|
| `workspace_root` | :34 | `rag_tool` 在用 | **保留** |
| `allow_outside_workspace` | :42 | 仅 `resolve_path` 内部用 | 保留（被 `resolve_path` 调用） |
| `resolve_path` | :50 | `rag_tool` 在用 | **保留** |
| `_is_within` | :82 | 仅 `fs_read_file_glob` + `resolve_path` 用 | 保留（`resolve_path` 依赖） |
| `normalize_encoding` | :90 | **仅 fs 三处用** | **删** |
| `_sanitize_surrogates` | :100 | 仅 `cap_text`/`read_text_file` 用 | 随 `cap_text` 删 |
| `cap_text` | :106 | 仅 `fs_read_text` + :349 用 | **删** |
| `read_text_file` | :117 | 仅 `fs_read_text`/`fs_edit_text` 用 | **删** |
| `atomic_write_text` | :140 | 仅 `fs_write_text`/`fs_edit_text` 用 | **删** |
| `run_process` | :199 | 仅 `run_git` 用 | **删** |
| `git_env` | :280 | 仅 `run_git` 用 | **删** |
| `run_git` | :294 | 生产 0 引用（测试里 `test_p0_integration.py:27` 是它**自己另定义**的同名函数） | **删** |
| `_kill_process_tree` | :310 | 仅 `run_process` 用 | **删** |
| `_decode_capped` | :333 | 仅 `run_process` 用 | **删** |
| `MAX_TEXT_FILE_BYTES` | :31 | 仅 `cap_text`/`read_text_file` 用 | **删** |

→ `_shared.py` 预计从 349 行缩到 **约 100 行**（剩 `workspace_root`/`allow_outside_workspace`/`resolve_path`/`_is_within`）。

**第三步：删测试（6 个文件）**
```
tests/test_fs_read_text.py        134 行  整删
tests/test_fs_read_dir.py         144 行  整删
tests/test_fs_read_file_glob.py   137 行  整删
tests/test_fs_write_text.py       172 行  整删
tests/test_fs_edit_text.py        209 行  整删
                                —— 746 行
tests/test_p0_integration.py      需要「换被测工具」而不是删名单（这是本次最需手工的一处）：
  :22-24  删 import EditTextTool / ReadTextTool / WriteTextTool
  :114-145 test_depends_on_read_after_write_in_one_batch —— 全用 fs.write_text + fs.read_text
           → 改用本项目自己的写入-读取替身工具（例如 rag_tool 的 ingest 写、memory.query 读，
             或自建一个 test-only 工具模块），否则该测试失去意义
  :148-167 test_writeout_confirmation_is_rejected —— 用 fs.write_text 验证"未确认的写被拒"
           → 换任一 **write 类工具**（可直接改用 memory.add，它正是聊天里唯一预确认的写工具）
  :27  本地定义的 run_git 与 _shared 无关，保留
```
> ⚠ 这一处**不能机械删除**：它是"side-effect 工具必须确认"以及"同批内 depends_on 顺序"两条核心框架行为的唯一端到端覆盖，必须保留等价测试。建议用 `memory.add`（写）+ `memory.query`（读）重写这两例——它们既是记忆功能、又天然是 write/read 侧效应配对。

**第四步：确认工具面板收缩**

删 D1+D2 后，聊天注册工具应从 **17 个 → 11 个**：
```
保留（11）：memory.add、memory.query、memory.manage、memory.rag、memory.rag_search、
            system.current_time、system.tool_catalog、
            system.update_log、system.read_update_log、system.read_update_logs（D3 保留）
            web.search
删除（6）：fs.read_text、fs.read_dir、fs.read_file_glob、fs.write_text、fs.edit_text、
          system.skill_catalog
```

**风险**：① 若你以后想通过聊天让 LLM 读写本机文件，这个能力就没了（属有意取舍）。② `agents/agent.py:274` 的 `discover_tools` 是按文件 glob 自动注册，**删文件即自动注销**，无需改注册表——但也意味着**不要留下半个文件**，否则会被部分加载。

#### 2.4.3 D1+D2 合计

| 项 | 生产代码 | 测试 | 小计 |
|---|---|---|---|
| D1 skill 子系统 | -630 | -302 | -932 |
| D1 接线改动 | -49（agent.py）/-17（react.py）/-9（__init__）/ -5（constants） | 改 2 个文件 | -80 |
| D2 fs.* 五工具 | -845 | -746 | -1,591 |
| D2 `_shared.py` 瘦身 | -249（349→100） | 改 1 个文件（test_p0 换被测工具） | -249 |
| **D1+D2 合计** | **约 -1,744 ~ -1,824 行**（口径差异来自"引用处"与"实删行"的折算） | **约 -1,048 行** | **约 -2,800 ~ -2,870 行** |

> 口径说明：D1/D2 的**整删文件**行数精确（-630/-845/-302/-746/-249）；**接线改动**行数受"改多少行"影响有 ±40 行浮动，故给区间。执行时以 `git diff --numstat` 实测为准。

**D 级最终范围：仅 D1 + D2 执行删除 ≈ 1,800 行生产 + 1,048 行测试；D3 + D4 保留（1,032 行生产 + 143 行测试留在仓库）。**

**D1 为何不采用"零风险下线"案**

我原本建议先走"`auto_discover_skills=False` + `skills/` 目录改名"的零改动下线案，以规避测试破坏。**用户已明确选择彻底删**（`git` 可回滚，可接受测试改动成本），所以按 §2.4.1 的 8 步执行。保留下线开关亦可作为回滚手段之一，但**不作为替代方案**。

**为什么把它们单列 D 级而不混进 A/B**

A/B 级是"**死代码**"（零引用、不可达），删除是纯增益；D 级是"**活代码但偏离目标**"，删除等于**砍掉一项已实现的能力**。前者我可以直接建议删，后者必须由你决定"这个能力要不要"——这正是 D 级存在的理由。

**同时必须说明：静态可达性分析不可作为删除依据**

我做过一次"从 `web/`+`agents/` 入口的 import 闭包"分析，它报出 45 个模块"不可达，8,116 行"。**这个结果整体不可信**：本项目的工具靠 `agents/agent.py:274` 的 `discover_tools` **按文件 glob 动态注册**、记忆模块靠 `importlib.import_module("...")` **字符串动态导入**、`core/__init__.py` 是**门面导出**——三类都不产生静态 import 边。所以该清单里 `memory/rag/*`、`memory/storage/*`、`tool/memory_*.py` 全部是**误报**（它们是功能核心）。真正可用的判据只有两类：① 精确的符号级 grep（A/B 级用的），② 运行时实测注册表（D 级用的）。

### 2.5 D 级与四大功能的关系（为什么删完不伤功能）

- **F1 边权增强** → 只用 `memory/storage/graph.py` + `semantic.py` + `graph_rag.py` + Neo4j；**不碰** D1/D2。
- **F2 提议删除 + 确认** → 只用 `manager.py`/`memory_tool.py` + 新 propose 工具 + 新路由；新工具会**新增**一个文件（D4 的范本仍有参考价值，已保留）。
- **F3 问句分解** → 只用 `memory/rag/knowledge.py`/`pipeline.py`/`graph_rag.py`/`web/app.py`；**不碰** D1/D2。
- **F4 逐句多元 + 时间状态** → 只用 `memory/rag/document.py`/`pipeline.py`/`knowledge.py`/`semantic.py`；**不碰** D1/D2。

**结论：D1（skill 子系统）+ D2（`fs.*` 五工具）删除后，四大功能一行都不受影响。**

### 2.6 顺手修的几处缺陷（非删除，但同类）

| # | 缺陷 | 位置 | 修法 |
|---|---|---|---|
| D1 | `memory.rag.__all__` 导出 `relation_id_for` 但未导入，`from memory.rag import relation_id_for` 会 `ImportError` | `memory/rag/__init__.py:26` | 补一行 import（或从 `__all__` 移除该名） |
| D1b | **门禁基线其实是红的**：`web/support.py:17`（`noqa: S404`）与 `:134`（`noqa: S603`）挂着两段 noqa 指令，但 `pyproject.toml` 的 `select` 列表并未启用 S404/S603，ruff 据此判「未使用的 noqa」并报 2 条 RUF100 | `web/support.py:17, 134` | 二选一：① `ruff check . --fix` 直接删掉这两段死指令（ruff 自己标为 fixable）；② 若确实想保留 S404/S603 压制，就把这两条规则加进 `pyproject.toml` 的 `select`。**注意**：此前"全绿"是陈旧 `.ruff_cache` 造成的假象——权威跑法是 `ruff check . --no-cache` |
| D1c | **测试变慢且可能超时**：本机 `.env` 已接上真 Qdrant/Neo4j（`HELLOAGENTS_MEMORY_QDRANT_URL`/`NEO4J_URI`），全量 `pytest -q` 从 19s 级涨到超过 120s 超时 | 测试夹具 / `.env` | 方案执行期固定用隔离环境跑测试（离线缓存或清掉 `HELLOAGENTS_MEMORY_QDRANT_*`/`NEO4J_*` 再跑），否则门禁不可用；这也再次说明"离线可跑"必须显式保证 |

> 注意：D 级删除编号（§2.4 的 D1–D4 簇）与本节缺陷编号（D1/D1b/D1c）**是两回事**，前者是"与功能无关的整簇"，后者是"顺手修的实现缺陷"。执行时按 §2.4 称"簇 D1–D4"、本节称"缺陷 D1/D1b/D1c"以区分。
| D2 | `/api/chat` 缺嵌入可用性前置检查：网关断时工具层报 `RuntimeError`，用户拿到的是"工具静默失败 + agent 继续作答"，而不是 `/api/ingest` 那样明确的 503 | `web/app.py` chat 路由 | 在 chat 入口加与 `/api/ingest` 同源的 `embedding_unavailable_detail` 闸门（或至少把工具失败上抬为可见告警） |

### 2.7 删除总账

| 级别 | 行数 | 测试改动 | 风险 | 状态 |
|---|---|---|---|---|
| A 级（20 条） | **-112** | 0 | 极低（全为 0 引用） | 建议执行 |
| B 级（8 条） | **-150** | 3 个测试文件 | 低 | 建议执行 |
| C 级 | 0（不删） | 0 | — | 改为增强 |
| **保守合计** | **约 -262 行** | 3 | — | — |
| **D1 skill 子系统** | **-730**（含接线 -80、常量 -20） | -302 + 改 2 文件 | 中 | ✅ **用户已决定：删** |
| **D2 fs.\* 五工具** | **-1,094**（含 `_shared` 瘦身 -249） | -746 + 改 1 文件 | 中 | ✅ **用户已决定：删** |
| **D1+D2 小计** | **约 -1,744 ~ -1,824 行** | **约 -1,048 行** | — | — |
| D3 update_log 簇 | 0 | 0 | — | ✅ **用户已决定：不删**（保留） |
| D4 tool_template.py | 0 | 0 | — | ✅ **用户已决定：不删**（保留） |
| **本次执行总计（A+B+D1+D2）** | **约 -2,086 行** | 约 6 个测试文件 | — | — |

**删 D1+D2 后的聊天工具面板：17 个 → 11 个**
```
保留 11 个：memory.add、memory.manage、memory.query、memory.rag、memory.rag_search、
            system.current_time、system.tool_catalog、
            system.update_log、system.read_update_log、system.read_update_logs（D3 保留）、
            web.search
删除 6 个：fs.read_text、fs.read_dir、fs.read_file_glob、fs.write_text、fs.edit_text、
          system.skill_catalog
```

**依赖数：0 个可减**（全部实现基于标准库与既有依赖，没有多余三方包）。

---

## 三、需要新增/改造的四大功能

### 3.1 F1 记忆增强：边权重的"回忆即强化"

**目标**：经常被检索到的图边权重更大，下次图检索时相关性更靠前；长期不被回忆的边自然衰减。这就是用户说的"像神经元一样自然连接、有记忆增强"。

**现状（已核实）**：
- Neo4j 边只有 `confidence/active` 等属性，**无 weight/recall_count**（`memory/storage/graph.py:82-90`）。
- 边属性唯一白名单在 `memory/types/semantic.py:104-123`（`memory_id`、`confidence` + 11 个可选键）。
- 写边 Cypher 是 `MERGE (a)-[r:RELATED {kind: $relation}]->(b) SET r += $properties`（`graph.py:89`）——**整体覆盖，没有原子递增**。
- 检索是"BFS 枚举 + confidence 事后排序"（`memory/rag/graph_rag.py:227-229`：`key=(-confidence, len(relations), target)`），边活跃性还要回查 SQLite（`:232-241`）。

**改动设计（6 处，全部为"加字段 + 改排序 + 加递增"）**：

1. **数据模型**：边属性新增三个键
   - `weight: float`（有效权重，默认 1.0）
   - `recall_count: int`（被回忆次数，默认 0）
   - `last_accessed_at: str`（ISO 时间，默认 ""）
   白名单加入 `memory/types/semantic.py:108-120` 的 key 元组，并在 `:104-107` 初始化默认值。

2. **原子递增（关键）**：`Neo4jGraphStore.add_relation`（`graph.py:36-104`）新增可选参数 `bump: bool = False`；当 `bump` 为真时，在 Cypher 里追加
   ```
   ON MATCH SET r.recall_count = r.recall_count + 1,
                r.last_accessed_at = $accessed_at,
                r.weight = r.weight * $grow_factor
   ```
   本地回退分支（`graph.py:56-79`）同步实现同语义（`existing["properties"]` 递增而非覆盖）。
   > 理由：`SET r += $properties` 是覆盖语义，用它做"计数+1"会退化成"每次都被设为 1"。

3. **写入兼容**：`semantic._write_edge`（`semantic.py:141-162`）对新事实写 `weight=1.0/recall_count=0`；对已存在边走 `bump=False` 的普通路径（不破坏 idempotent）。

4. **检索侧排序**：`GraphRAGPipeline._expand`（`graph_rag.py:187-230`）
   - 有效分公式：`effective = confidence * (1 + log(1 + recall_count)) * decae(age)`
     - `decae(age)`：`HELLOAGENTS_MEMORY_EDGE_DECAY_HALFLIFE`（默认 0 = 不衰减，先保证可关）
   - 入队时按 `effective` 降序（把 `queue` 换成 `heapq` 或每层排序），路径最终排序键改为 `(-effective, len(relations), target)`。
   - 命中即"回忆"：对每条被采纳的路径边调用一次 `bump`（同步或通过小批量队列；同步最简，先做同步并在实测里量延迟）。

5. **路径查询接权重**：`Neo4jGraphStore.path_query`（`graph.py:248-281`）Cypher 加 `RETURN ... ORDER BY r.weight DESC`，`_local_paths` 的 BFS 队列改为按 `weight` 优先；这一步同时把 C1 从"test-only"变成**生产可用**（用户点名的"快速检索图关系"）。

6. **接线与防腐**：
   - `RAGPipeline.graph_retrieve`（`pipeline.py:340`）与 `tool/rag_search.py:91-92` 的 `graph_retrieve/context` 动作链路不变（自动享受新排序）。
   - 防"富者越富"：`weight` 上限 `HELLOAGENTS_MEMORY_EDGE_WEIGHT_MAX`（默认 8.0）+ `recall_count` 上限；可选 `supersede` 时权重归 1。
   - 全部开关走 `HELLOAGENTS_MEMORY_*`（与现有 `MemoryConfig.from_env` 一致）。

**验收**：同一查询跑 N 次后，被反复命中的边在 `graph_retrieve` 结果里位置前移；关掉衰减开关后行为与今天完全一致（回归基线）。

---

### 3.2 F2 LLM 驱动删除 + 用户确认（不许任何人直接删）

**目标**：删除只能由 LLM 在"先取到关系"之后提议，且**执行前必须用户确认**；人为直接删除的入口全部关闭；删除留审计痕迹。

**现状（已核实）**：
- 删除路径 3 条：`MemoryManager.delete`（`manager.py:104-112`）、`memory.manage` 工具（`tool/memory_tool.py:27-38`，`action: Literal["delete","clear"]`，`side_effect="write"`）、`RAGPipeline.delete_document`（无路由暴露）。
- 确认机制：`ToolSpec.confirmation_key`（`core/models.py:201-204`）= `name@version#schema_hash`，注册表再拼 `:generation`（`core/registry.py:67-70`）；`core/runtime.py:119-128` 在没有确认键时返回 `CONFIRMATION_REQUIRED` 并**拒绝执行**；世代号不匹配还会 `SCHEMA_MISMATCH`。
- **没有 pending-confirmation 存储、没有过期、没有落盘**（确认是请求内 `ExecutionContext` 的 frozenset）。
- 聊天路径只确认 `memory.add`（`web/support.py:211` `CHAT_CONFIRMED_TOOLS = ("memory.add",)`）→ 所以**在聊天里 LLM 目前根本删不了**（正确方向，但缺"提议-确认"闭环）。
- Web **没有任何删除路由**（符合"不许人为直接删"）。

**改动设计**：

1. **新只读工具 `memory.propose_delete`**（新文件 `tool/memory_propose_delete.py`）
   - `side_effect="read"`（提议本身不写记忆），`permissions=("memory.read",)`
   - 输入：`action: Literal["by_query","by_ids","by_relation"]`、`target`（查询串或 item_id 列表或三元组）、`reason: str`（LLM 必须给理由）
   - 执行：调 `manager.search` / `semantic.facts_for` 取候选 → 写一行**待确认记录**（见 2）→ 返回 `{proposal_id, items:[{item_id, content_preview, memory_type, why}], expires_at}`
   - 注意：它不调用任何删除，因此不需要 write 确认；把它加进 `CHAT_CONFIRMED_TOOLS` 之外的默认可见集合即可（只读工具本来就可见）。

2. **待确认存储（新 SQLite 表）**：在 `memory.sqlite3` 加一张 `delete_proposals` 表
   ```sql
   CREATE TABLE IF NOT EXISTS delete_proposals (
       proposal_id TEXT PRIMARY KEY,
       created_at TEXT NOT NULL,
       expires_at TEXT NOT NULL,      -- 默认 15 分钟
       requested_by TEXT NOT NULL,    -- "llm" / 会话 id
       reason TEXT NOT NULL,
       item_ids TEXT NOT NULL,        -- JSON 数组
       status TEXT NOT NULL,          -- pending / confirmed / rejected / expired
       confirmed_at TEXT,
       approwed_token TEXT            -- 确认时须回传的随机令牌
   )
   ```
   DDL 放 `memory/storage/document_repo.py`（真值源模块，与 `documents/chunks` 同居一个 SQLite 文件，共用连接与锁）。

3. **执行闸门：只在确认后真删**
   - 新增 `DELETE /api/deletions/{proposal_id}`（**新增路由是"加"不是"改前端"**；前端可以永不调用它）
   - 校验链：`proposal_id` 存在 → `status == "pending"` → 未过期 → 带对 `approwed_token` → 才逐条执行
     - `the_manager().delete(item_id, memory_type=...)`（`manager.py:104`）
     - `SemanticMemory.delete` 已自动连带删图边（`semantic.py:174-183` → `graph.delete_memory_relation`）
     - Qdrant：`vector_store` 对应条目删除（新增一个小方法，或复用现有删除；**Qdrant 类本体不动，只加一个 delete-by-id 方法**）
     - 写一条 `episodic.record`（谁、何时、删了什么、为什么）
   - 幂等：重复确认同 `proposal_id` 直接返回既有结果，不重复删。
   - 绝不硬删：语义事实走 `active=False` + `superseded_at` 保留审计（沿用 `supersede/retract` 既有语义，`knowledge.py:580-605`）。

4. **确认交互（关键约束：前端保持不变）** —— 两条路，推荐 A：
   - **A 案（不改一行前端）**：确认走**聊天文本**。LLM 提议后，`/api/chat` 在响应里附带 `pending_deletion: {proposal_id, summary, item_count}`；用户下一句回复含确认意图（如"确认删除"）时，`web/app.py` 在进入 ReAct 之前先做一次**确定性匹配**（不依赖 LLM）：若存在 pending 且用户文本匹配确认词表 → 直接调内部 `execute_deletion(proposal_id)`，再照常回复。前端只用现有聊天面板，零改动。
     - 匹配词表与 `proposal_id` 短码（如 `#A7F2`）由后端下发，用户可回"确认删除 #A7F2"。
   - **B 案（可选，需要前端加一个确认条）**：新增 `GET /api/deletions` + 前端在文档中心加一栏。**与 R1 冲突，默认不做**，仅当用户改主意说"前端可以动"时启用。
   
5. **封死人删入口**：
   - `tool/memory_tool.py`（`memory.manage`）的 `delete`/`clear` 动作**降级为 propose-only**：`action="delete"` 改为"生成提议"，真正执行必须走确认路由；`clear` 动作直接删除（批量清空层）风险最高，建议**移除 `clear` 或限制为仅测试可用**。
   - 加一条测试：断言聊天上下文里 `memory.manage` 仍拿不到确认键（沿用 `web/support.py:211` 的单一白名单，把新工具加为只读即可）。

**验收**：① 聊天里让 LLM 删一条，得到提议而**数据未变**；② 回"确认删除 #码"后才真删且 Qdrant/Neo4j 同步清干净；③ 不带令牌/过期后/重复确认都不产生副作用；④ 直接调 `memory.manage{action:"delete"}` 不再能绕过确认。

---

### 3.3 F3 问句分解：一句话 → 多种待测查询 → 分别查 → 融合

**目标**：用户问"小红的亲戚是谁"，LLM 先把它拆成若干**待测查询**（如 `小红`、`小红 亲戚`、`小红 亲属 关系`、`小红的父母`），交给查询工具分别检索，再融合排名。

**现状（已核实）**：
- 检索只有一层融合（向量 × FTS5 的 RRF，`memory/rag/pipeline.py:93-100 _rrf_fuse`），**没有问句分解**。
- ReAct 提示词（`agents/react.py:365-387`）已允许多轮多工具调用，但没有引导拆句；`memory.query` 的 schema 是"一次一个 query"（`tool/memory_query.py:32-47`）。
- `_rrf_fuse` **天然接受 N 路 rank 列表**（`pipeline.py:93-100`）——这是最大的顺风。

**改动设计**：

1. **新增 `QuerieDecomposer`**（放 `memory/rag/knowledge.py`，与 `LLMKnowledgeExtractor` 同构）
   - 复用 complete 注入模式（参照 `knowledge.py:193-204` 与 `tool/_memory.py:58-82 build_default_pipeline` 的三级降级）
   - 输出 schema：`DecomposedQueries{sub_queries: list[{query: str, weght: float, intent: str}], entities: list[str]}`
   - 提示词要点：中文、`temperature=0`、要求"只输出合法 JSON"、最多 6 条、去重、必须包含原句
   - 降级：LLM 不可用/解析失败 → `NullDecomposer` 返回 `[原句]`（与 `NullKnowledgeExtractor` 同思路），**永不因分解失败而答不出来**。

2. **多路检索**
   - `RAGPipeline.hybrid_retrieve`（`pipeline.py:297-333`）→ 加 `hybrid_retrieve_multi(queries)`：每条子查询各跑 `_vector_hits` + `search_keywords`，把 N 路 rank 列表一起丢给现有 `_rrf_fuse`（无需改融合器）。
   - `GraphRAGPipeline`（`graph_rag.py:87-122`）→ 加 `retrieve_multi(queries)`：每条各做 `_find_seed_entities` + `_expand`，路径按 `(effective, -子查询权重)` 合并去重（`GraphPath` 直接复用）。
   - `RAGPipeline.graph_retrieve`（`pipeline.py:340`）保持单查询签名不变，新增 `graph_retrieve_multi` 供分解路径使用（**不动现有调用方**）。

3. **接入点（两处，都是"加一步"而非改架构）**
   - `/api/chat` 检索段（`web/app.py:298`）：先分解 → 多路检索 → `retrieval` 报告每路命中（前端 `appendRetrieval` 已能渲染 hit 列表，只在 meta 上多一个"子查询"标签即可——**这属于数据字段变多变，不要求前端改代码**）。
   - `/api/graph-rag`（`app.py:247-254`）：同样先分解（前端已在调用它，请求体字段不变，内部多路）。
   - `tool/memory_query.py` / `tool/rag_search.py`：**schema 不变**（仍单 query），避免改动工具契约导致确认键/世代号变化。

4. **可观测**：`retrieval` 报告加 `decomposed: [{query, weight, source}]`，让用户看到"LLM 把我这句话拆成了哪几路"。

**验收**：对"小红的亲戚是谁"这类需要两跳/多词的问题，多路检索的 recall 高于单路（用 `tests/test_qdrant_hybrid.py` 的现有夹具做 A/B 对比）；LLM 关掉时结果与今天一致。

---

### 3.4 F4 导入记忆：逐句多元关系 + 时间/状态分类

**目标**：导入文档/记忆时，LLM **一句话一句话**地生成**多元关系**（一句可出多组三元组），并按**时间与状态**分类，最终：块写 SQLite → 生成向量写 Qdrant → 关系写 Neo4j。

**现状（已核实）**：
- `pipeline.ingest`（`pipeline.py:147-245`）按**字符窗口**切块（`RAG_CHUNK_SIZE`，`web/app.py:410`），不是按句。
- `ExtractionResult.relations` 已支持**任意多条**（上限 80，`knowledge.py:127`），`RelationCandidate` 已有 `action: assert/supersede/retract` 与 `cardinality: single/multi`（`knowledge.py:102-118`）——"多元"的骨架已有，卡在 ① 提示词是"补丁式"单组导向（`knowledge.py:159-191`）② 没有时间维度字段。
- 语义事实**没有** `valid_from/valid_to/status/event_time`；时间语义只有 `superseded_at`（退休时刻）与 `active` 布尔。
- 三库落地已是一跳到位：`materialize_extraction`（`knowledge.py:455-676`）→ `semantic.add_fact`（`semantic.py:28-97`）→ `BaseMemory.add`（写 `memories` + 向量）+ `_write_edge`（写 Neo4j）。
- `/api/import`（`app.py:462-551`）**不跑抽取**，只做幂等回放——所以新字段对它是天然兼容的。

**改动设计**：

1. **按句切分**：`DocumentProcessor`（`memory/rag/document.py`）加 `sentences_with_spans(document)`（在中英句读号 `。！？!?.;\n` 上切，保留 `char_start/char_end`，与现有 `ChunkSpan` 同构）；`pipeline.ingest` 加参数 `granularity: Literal["chunk","sentences"]`（默认 `"chunk"` 保持向后兼容）。
   - 真值源：句级也写 `chunks`（`chunk_index` 连续、`char_start/char_end` 精确），**不新建表**——避免与 R2 的 `chunks_fts` 触发器双写；句与块用 `metadata["granularity"]` 区分。

2. **LLM 逐句多元抽取**：
   - `LLMKnowledgeExtractor.SYSTEM_PROMPT`（`knowledge.py:159-191`）改为明确要求：**每句可输出多条关系**（`relations` 上限不变），且**每条关系必须带时间/状态**；提示词加小示例（中文）。
   - `RelationCandidate` 加字段：`valid_from: str | None`、`valid_to: str | None`、`status: Literal["fact","plan","expired","uncertain"] | None`、`event_at: str | None`。
   - `materialize_extraction` 的 `fact_metadata` 白名单（`knowledge.py:636-654`）同步透传这四个键。

3. **边属性白名单同步**：`semantic._graph_properties`（`semantic.py:108-120`）加入 `valid_from/valid_to/status/event_at`（Neo4j 边因 `SET r += $properties` 自动落地，无需改 Cypher）。

4. **时间/状态分类的检索语义**：
   - `GraphRAGPipeline._expand`（`graph_rag.py:194`）现在只看 `active`；扩展为：`active == True` **且** 时间段过滤（`valid_from <= now <= valid_to`，空值视为无界）**且** `status` 按查询意图过滤（默认只要 `fact`；`status="uncertain"` 的边降权而非排除）。
   - `retrieval` 报告暴露 `status/event_at`，前端依据面板自动多显示两个字段（**不要求前端改代码**）。
   - 可选：`memories` 表已有通用 `created_at/expires_at/timestamp`，新字段走 `metadata` 即可，**不改表结构**。

5. **导入链路不变**：`/api/ingest`（文件上传）与 `/api/knowledge`（一句话）继续走 `pipeline.ingest`，只多一个 `granularity="sentences"` 的开关（默认给 `/api/knowledge` 开、给大文件保持 `chunk`，避免逐句调用 LLM 的代价）。

**验收**：导入一段含时间信息的文本（如"小红 2024 年结婚"），`chunks` 表出现句级块；Neo4j 对应边带 `valid_from/event_at`；用过期时间段检索时该边不再命中；`active=False` 语义不变。

---

## 四、实施顺序（每阶段独立可验证、可回滚）

| 阶段 | 内容 | 验证 | 预期风险 |
|---|---|---|---|
| **P0** | 基线锁定：修 D1（`relation_id_for` 悬空）、D1b（两条死 noqa 指令）、D2（chat 闸门）；用 `ruff check . --no-cache` + 隔离环境的 `pytest` 确认全绿并记录 | **先有可复现的绿色基线**才允许后续删改（注意 D1c：接上真 Qdrant/Neo4j 后全量测试会超时，须显式隔离） | 无 |
| **P1** | 删除 **A 级 20 条**（§2.0） | `check.py` 仍全绿；`/api/health`、`/api/graph` 冒烟 | 极低 |
| **P2** | 删除 **B 级 8 条**（§2.1）+ 同步改 3 个测试文件 | `check.py` 全绿；`tests/test_memory.py`、`test_memory_rag.py`、`test_react.py` 单跑通过 | 低 |
| **P2.5a** | **删除 D1 skill 子系统**（§2.4.1，8 步）：删 4 模块 + 4 技能 md → 改 `agent.py`(5 处 import + 签名/实例化/2 方法/装配) → 改 `react.py`(常量 + 方法 + 注入，**保留 :413-415 那条注释**) → 改 `core/__init__.py`(14 行 import + 10 个导出名) → 改 `constants.py`(4 常量 + 注释块) → 删 `tests/test_skills.py` → 修 `test_react.py`:315/487/509 与 `test_hot_reload.py`:88 | 隔离环境 `pytest`；`ruff --no-cache`；聊天工具面板 17→**16**（只少 skill_catalog）；`python -c "import agents.agent"` 可导入；提示词缓存键变化导致的断言失败即预期 | 中 |
| **P2.5b** | **删除 D2 fs.\* 五工具**（§2.4.2，4 步）：删 5 模块 → 瘦身 `_shared.py`(349→约 100，**留 `workspace_root`/`allow_outside_workspace`/`resolve_path`/`_is_within`**，因 `rag_tool.py:24` 在用) → 删 5 个 `test_fs_*.py` → 重写 `test_p0_integration.py` 两个用例（用 `memory.add` 写 + `memory.query` 读 取代 fs 写读） | 隔离环境 `pytest`；`ruff --no-cache`；工具面板 16→**11**；`/api/chat` 冒烟问一次记忆类问题仍正确作答 | 中 |
| **P3** | **F1 边权重的记忆增强**（§3.1）：白名单 → 原子递增 → 检索排序 → `path_query` 接权重 → 开关与上限 | 新增 `tests/test_semantic_graph.py` 断言"回忆后排名前移"；关衰减开关时与 P2 结果 dict 级一致 | 中（Neo4j 递增语义需实测） |
| **P4** | **F4 逐句多元 + 时间状态**（§3.4）：句切分 → prompt/schema → 白名单透传 → 检索过滤 | 新增句级切分测试 + 带时间文本的端到端；旧数据（无新字段）仍能检索（向后兼容） | 中 |
| **P5** | **F3 问句分解**（§3.3）：`QuerieDecomposer` → `*_multi` 检索 → 接入 `/api/chat` 与 `/api/graph-rag` | A/B 对比 recall；LLM 关掉时降级为单路，输出结构不变 | 中 |
| **P6** | **F2 LLM 提议删除 + 确认闸门**（§3.2）：表 DDL → propose 只读工具 → 确认路由 → `memory.manage` 降级 → 聊天内确认（A 案） | 四条验收（提议不改数据 / 确认后三库同步清 / 令牌与过期 / 绕过被阻断） | **高**（唯一的写入-删除闸门，需最严测试） |
| **P7** | **执行未做的数据迁移**：`python scripts/migrate_storage.py --dry-run` 先看映射，再 `--backup` 真跑（当前 37 条 `memories` → `documents/chunks`） | 迁移后 `documents/chunks` 非零；`/api/reconcile` 漂移为 0；`/api/documents` 列表可见 | 中（**先备份**，脚本自带 `.bak.pre_migration`） |
| **P8** | 文档同步：更新 `存储架构规划方案.md` 状态行、`update_log_readme_first.md` 日志 ID、`项目代码拓扑与接口解析.md` 的删除/新增条目 | 覆盖率自检 `COVERAGE OK` | 低 |

**回滚策略**：P1–P2 是纯删除，`git revert` 即回；P3–P6 每项都带开关（`HELLOAGENTS_MEMORY_EDGE_*`、`granularity`、`decompose` 开关），关掉即刻退回旧行为；P7 依赖脚本自带备份。

---

## 五、风险与对策

| 风险 | 说明 | 对策 |
|---|---|---|
| **迁移未执行** | `memory.sqlite3` 实测 `documents=0/chunks=0`、`memories=37`（31 条为种子）——F4 所依赖的真值源目前是空的；同时`scripts/migrate_storage.py` 自称一次性却从未跑过 | 把 P7 提到 F4 之前或紧随其后；先 `--dry-run`，再 `--backup`；迁移不动 `memories`（只新增行） |
| **权重的"富者越富"** | 边权随回忆单调上升会把检索锁死在旧路径 | 上限 `EDGE_WEIGHT_MAX`；`decae` 半衰期可配；`uncertain/expired` 边降权不排除；定期 `reconcile` 后可选重算 |
| **Neo4j 递增原子性** | 现在 `SET r += $properties` 是覆盖；并发回忆可能丢计数 | 改成 Cypher 内 `SET r.recall_count = r.recall_count + 1`（Neo4j `MERGE ... ON MATCH SET` 原生支持）；本地回退分支同步实现 |
| **确认机制无落盘** | 现确认是请求内 frozenset，无 pending/无过期；F2 需要跨请求 | 用新 `delete_proposals` 表自带 `expires_at` 与随机令牌；**不改 `core/runtime.py` 与 `core/registry.py`**（避免影响所有工具的确认语义） |
| **前端不能动** | 用户明确"前端保持不变" | F2 用 A 案（聊天内文本确认）；F3/F4 只改响应**字段**（前端已有 `retrieval` 渲染与字段容错），不加 UI 元素 |
| **向量空间不互转** | 换嵌入提供方必须重索引 | 任何涉及 embedding 的变更都走 `scripts/reindex_embeddings.py`，不在方案内切换 provider |
| **误删 test-only 方法** | `path_query`/`entity()` 判"test-only"但正是新功能载体 | C 级明确"不删，改为增强"；删除前先跑 `check.py` |
| **LLM 不可用时的稳定性** | 分解/抽取/提议都依赖 LLM | 三处都有 `Null*` 降级（`NullDecomposer`/`NullKnowledgeExtractor`/提议失败只返回空候选），永不因 LLM 缺失而阻塞 |

---

## 六、测试计划（新增与改动）

| 测试 | 覆盖 |
|---|---|
| `tests/test_semantic_graph.py`（扩） | 边权初始值、`bump` 递增、`path_query` 按权重排序、衰减开关关闭时与基线一致 |
| `tests/test_memory_rag.py`（扩） | `granularity="sentences"` 的句级块与 char span、`retrieve_multi` 融合去重 |
| `tests/test_document_repo.py`（扩） | `delete_proposals` 表 DDL 与状态流转（pending→confirmed/rejected/expired） |
| `tests/test_web_api.py`（扩） | `DELETE /api/deletions/{id}` 四道校验；聊天内确认匹配；绕过 `memory.manage` 被阻断 |
| `tests/test_knowledge_patch.py`（扩） | 每句多条关系 + `valid_from/status` 透传到 Neo4j 边 |
| 新增 `tests/test_decompose.py` | 分解输出的 schema、去重、上限、降级 |
| `tests/conftest.py` | 沿用已加的空串隔离（`EMBEDDING_BASE_URL=""`），保证全套离线可跑 |

**门禁**：`ruff check . --no-cache`（避开陈旧 `.ruff_cache` 假象）+ 隔离环境下的全量 `pytest`，每阶段必绿；`/api/graph`、`/api/chat` 离线冒烟。接上真 Qdrant/Neo4j 时全量测试会超时（见 D1c），须在隔离环境跑。

---

## 七、明确"不做"（防止方案膨胀）

- 不新增任何三方依赖（依赖数变化 = 0）。
- 不动前端 `web/static/index.html`（一行不改）。
- 不改 `core/runtime.py` / `core/registry.py` 的确认机制（新功能只借用）。
- 不新建第四种存储，不换向量空间。
- 不做硬删除（一律留 `active=False` + `superseded_at` 审计痕）。
- 不为"分解/权得"新增 UI 面板，只扩展已有响应字段。
- `tool/tool_template.py` 保留（D4，用户已定）——它同时是 F2 新增 propose 工具的照抄范本。
- `update_log` 开发日志簇保留（D3，用户已定）——它虽与产品功能无关，但保留了这个工作流；**执行 D1/D2 时切勿误删** `agents/agent.py:43-45`、`core/update_log.py`、`tool/update_log.py`、`tool/read_update_log*.py`、`core/activity_log.py` 与 `update_log.sqlite3`。

---

## 附录 A：删除清单速查表（复制给执行者）

```
A 级（-112 行，零测试改动）
  constants.py        删 DEFAULT_EMBEDDING_TUNNEL_REMOTE_PORT(132) / DEFAULT_NEO4J_USERNAME(144)
                         / DEFAULT_NEO4J_PASSWORD(145) / DOC_DOMAIN(308) / RAG_CHUNK_MAX(216)
  knowledge.py        删 EntityResolver._alias(318-325) + or 链(382, 401)
  base.py             删 MemoryItem.from_dict(200-204) / event_time(177-179) / MemoryConfig.to_dict(348-352)
                         / MemoryConfig.extra(280,312-313,339-340) / to_dict 的 include_payload 参数(181,196-197)
                         / add 的 ttl/expires_at 参数(387-388,397-402)
  episodic.py         删 EpisodicMemory.timeline(18-26)
  manager.py          删 cleanup_expired(132-137) / stats(149-150)
  storage/document.py 删 BaseDocumentStore.save(30-31) / clear(33-36) / SQLiteDocumentStore.clear(149-155)
  storage/vector.py   删 BaseVectorStore.clear(22-23) / InMemoryVectorStore.clear(68-70)
  storage/qdrant.py   删 QdrantVectorStore.clear(181-184)   ← 仅 clear 方法，类本体保留
  storage/graph.py    删 delete_relation(225-246) / upsert_relation 别名(177-178)

B 级（-150 行，改 3 个测试）
  types/working.py    删 set/get_by_key/get_value(25-46)          → 改 tests/test_memory.py
  rag/document.py     删 DocumentProcessor.chunks(87-88)           → 改 tests/test_memory_rag.py
  types/perceptual.py 删 store/get_data/remember(14-24)，留枚举     → 无需改测试
  agents/agent.py     删原型兼容 shim(297, 922-961, 1038-1052)      → 改 2 个测试
  agents/react.py     删 arun/run_react(586-591)（或标注保留）
  agents/llm.py       删 get_query(249)
  core/registry.py    删 summaries(171)                            → 先确认无动态取用
  tool/search.py      删 normalize_*/reject_blank_text(99-148)      ← SearchTool 本体保留

明确不删：path_query/_neighbours/_local_paths（C1，改为接权重）、Neo4jGraphStore.entity（C2）、
        memory.* 五个工具、system.current_time/tool_catalog、tool/search.py 本体、APIEmbedding、
        HashEmbedding、SQLiteDocumentStore、DocumentRepository、QdrantVectorStore/Neo4jGraphStore 类本体、
        agents/providers.py（LLM 接入层）、core/ 框架主干（models/registry/runtime/repository/parser/
        catalog/discovery/tool_loop）、update_log.sqlite3、web/static/index.html

D 级（与目标功能无关的活代码簇，见 §2.4）
  D1 skill 子系统   core/skill_{catalog,discovery,models,registry}.py=630行   ✅ 用户已决定：删
                    改 agents/agent.py(:16 import + 5 import + :222/:223 参数 + :233-236 校验 + :266-273 实例化
                       + :409-426 discover_skills + :766-794 _with_registered_skill_names + :817-819 装配 + :877-888 entries)
                    改 agents/react.py(:404-412 SKILL_VIEW 常量 + :1155-1174 _skill_directory_lines + :1046-1049 注入；
                       ⚠ 保留 :413-415 注释，那是 UNBOUNDED_ROUND_SAFETY_LIMIT 的)
                    改 core/__init__.py(:22-35 四条 import + __all__ 10 个名：46,47,48,49,50,51,52,53,67,71)
                    改 constants.py(:47 DEFAULT_SKILLS_ROOT + :72-75 注释块 + :78 SKILL_NAME_PATTERN
                       + :80 MAX_DESCRIPTION_CHARS + :81 MAX_VERSION_CHARS；均已实测为 skill 独占)
                    删 tests/test_skills.py(302) + 改 tests/test_react.py:315/487/509 + test_hot_reload.py:88
                    明细见 §2.4.1；小计 约 -730 生产 / -302 测试
  D2 fs.* 文件工具   tool/fs_{read_text,read_dir,read_file_glob,write_text,edit_text}.py=845行   ✅ 用户已决定：删
                    + 瘦身 tool/_shared.py 349→约100行（删 normalize_encoding/_sanitize_surrogates/cap_text/
                       read_text_file/atomic_write_text/run_process/git_env/run_git/_kill_process_tree/
                       _decode_capped/MAX_TEXT_FILE_BYTES；⚠ 必留 workspace_root/allow_outside_workspace/
                       resolve_path/_is_within —— rag_tool.py:24 在用）
                    + 删 tests/test_fs_*.py 5 个(746 行)
                    + 重写 tests/test_p0_integration.py 两个用例（原用 fs 写读；改用 memory.add 写 + memory.query 读）
                       ⚠ 不可机械删：这是"side-effect 须确认"与"同批 depends_on"的唯一端到端覆盖
                    明细见 §2.4.2；小计 约 -1,094 生产 / -746 测试
  D3 update_log 簇   core/update_log.py(246)、core/activity_log.py(123)、tool/update_log.py(159)、
                     tool/read_update_log.py(119)、tool/read_update_logs.py(133) = 780行
                     ✅ 用户已决定：不删 —— 保留，勿动 agents/agent.py:43-45、core/__init__.py 的相关导出、
                     tool/update_log.py、tool/read_update_log*.py 与 update_log.sqlite3
  D4 tool_template.py 252行
                     ✅ 用户已决定：不删 —— 保留，作为 F2 新增 propose 工具的照抄范本
  最终执行范围：仅 D1+D2 ≈ 1,744~1,824 行生产 + 1,048 行测试；D3+D4 共 1,032 行生产 + 143 行测试保留

判定依据说明：静态 import 闭包分析在本项目不可用（工具靠 glob 动态注册、memory 靠字符串动态导入、
core/__init__.py 是门面）。只用 ① 符号级 grep（A/B 级）② 运行时注册表实测（D 级）。
```

## 附录 B：四大功能的改动锚点速查表

```
F1 边权得/记忆增强
  memory/types/semantic.py:104-120   白名单 + weight/recall_count/last_accessed_at 默认
  memory/storage/graph.py:36-104     add_relation 加 bump 参数 + Cypher 原子递增（+ 本地回退同语义）
  memory/storage/graph.py:248-281    path_query 加 ORDER BY r.weight（C1 由此进入生产）
  memory/rag/graph_rag.py:187-230    _expand 按 effective 排序 + 命中 bumper
  constants.py                       HELLOAGENTS_MEMORY_EDGE_* 开关（走 MemoryConfig.from_env）

F2 LLM 提议删除 + 确认
  tool/memory_propose_delete.py      新增只读工具（action: by_query/by_ids/by_relation + reason）
  memory/storage/document_repo.py    + delete_proposals 表 DDL
  web/app.py                         + DELETE /api/deletions/{id}
  web/app.py chat 入口               聊天内确认匹配（A 案，不改前端）
  tool/memory_tool.py:27-38          delete/clear 降级为 propose-only
  web/support.py:211                 确认白名单不放删（保持 LLM 不能直接删）

F3 问句分解多路查询
  memory/rag/knowledge.py            新增 QuerieDecomposer + NullDecomposer（仿 193-204 构造器）
  memory/rag/pipeline.py:297-333     + hybrid_retrieve_multi（复用 _rrf_fuse 93-100）
  memory/rag/graph_rag.py:87-122     + retrieve_multi（GraphPath 复用 21-28）
  web/app.py:298 /app.py:247-254     两处接入分解步骤；响应加 decomposed 字段

F4 逐句多元关系 + 时间状态
  memory/rag/document.py             + sentences_with_spans（仿 ChunkSpan）
  memory/rag/pipeline.py:147-245     ingest 加 granurality 开关
  memory/rag/knowledge.py:159-191    提示词改为多元 + 带时间/状态
  memory/rag/knowledge.py:102-118    RelationCandidate + valid_from/valid_to/status/event_at
  memory/rag/knowledge.py:636-654    fact_metadata 白名单透传四个新键
  memory/types/semantic.py:108-120   边白名单加四个新键（Neo4j 自动落地）
  memory/rag/graph_rag.py:194        检索加时间段 + status 过滤
```

---

> 本方案书为**纯方案**，未对代码做任何修改。执行前请先确认 P0 的绿色基线，并按 §4 的阶段顺序推进。

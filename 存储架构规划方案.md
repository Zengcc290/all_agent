# 存储架构规划方案（v2 实现精修版）

> 状态：待实施
> 目标：在不丢失任何现有功能的前提下，把存储层演进为三库各司其职——
> **SQLite = 文档真值源（Source of Truth）**，**Qdrant = 纯向量检索索引**，**Neo4j = 实体关系知识图谱**。
> 本版所有结论均锚定当前真实代码（文件 + 行号 + 关键代码），可直接按 Phase 增量化合入。

---

## 〇、设计总原则与决策记录（先统一口径）

| # | 决策 | 理由 |
|---|---|---|
| D1 | **SQLite 是唯一不可丢失的库**；Qdrant 与 Neo4j 都是可从 SQLite 重放的「投影」 | 任何投影库损坏都能全量重建；SQLite 单文件备份即可 |
| D2 | **保留 `memories` 表不变（新建 `documents`/`chunks` 表）** | `working/episodic/perceptual` 是持有型业务数据；`semantic` 事实条目是星云图分类、`_edge_is_active` 回查、导出的依赖。整体翻新收益低、回归风险高 |
| D3 | **向量的真值只在向量库**；`memories.embedding` 列停止新增（列保留，旧值 P5 置空） | 消除 SQLite/Qdrant 双写；旧 reader 兼容由列保留保证 |
| D4 | **图边只是投影，「active 真值」仍以 SQLite 事实条目为准**（`_edge_is_active` 逻辑不变） | 即使 Neo4j 短暂不一致，检索结果也正确；Neo4j 修复可从 SQLite 重放 |
| D5 | **一致性靠状态位协调，不靠跨库事务**：任一投影写入失败 → 置 `failed`，幂等重试 | Qdrant/Neo4j 不支持与 SQLite 的分布式事务 |
| D6 | **幂等键沿用现状**：`document.id`（uuid4）、`chunk.id = f"{document.id}:{index}"`、事实用 `relation_id_for / predicate_key_for` | 已存在且被全链路使用，见 `memory/ids.py` |

---

## 一、现状精确盘点（源码锚点）

### 1.1 装配链现状

`memory/manager.py:25-59` 是唯一选型点：

```python
document_store = 注入 ? : SQLiteDocumentStore(config.sqlite_path)   # 永远是 SQLite
vector_store   = 注入 ? : config.qdrant_url ? QdrantVectorStore(...) : InMemoryVectorStore()
graph_store    = 注入 ? : Neo4jGraphStore(config.neo4j_uri, user, pwd)  # uri 为空 → 进程内字典
```

四层记忆共享同一套三件套（`manager.py:60-75`）。

### 1.2 关键事实与缺口（逐条已核读）

| # | 现状（锚点） | 与目标差距 |
|---|---|---|
| F1 | 文档入库只存 chunk：`RAGPipeline.ingest` → `DocumentProcessor.chunks`（`memory/rag/document.py:62-81`，`id=f"{doc.id}:{index}"`）→ `manager.add(chunk.content, SEMANTIC, item_id=chunk.id)`（`memory/rag/pipeline.py:77-100`） | 文档原文不落库；无 `documents`/`chunks` 表、无状态/标签/权限/分页/统计 |
| F2 | `memories` 表 13 列（含 `embedding TEXT` 向量快照列），`memory/storage/document.py:78-96` | 向量双写（SQLite + 向量库各一份） |
| F3 | Qdrant 适配器完整但**无激活路径**：`MemoryConfig.from_env` 全仓库仅 `make_default_embedding` 内部使用（`memory/base.py:73`）；`web/support.py:133-136` 与 `tool/_memory.py:51` 硬构造 `MemoryConfig(sqlite_path=...)` | `HELLOAGENTS_MEMORY_QDRANT_URL/_NEO4J_URI` 写了不生效 |
| F4 | Qdrant payload 存**完整 `item.to_dict()`**（`memory/storage/qdrant.py:61-68`），`delete()` 在 collection 未 ready 时静默返回 False（`qdrant.py:70-75`） | 非「轻量 payload」；语义与目标不符 |
| F5 | 图回退在 `Neo4jGraphStore.__init__`（`memory/storage/graph.py:23-32`）；Neo4j 持久化时 `MERGE (a:MemoryEntity {name})...MERGE (a)-[r:RELATED {kind}]->(b) SET r += props`（`graph.py:70-74`） | 实体只有 `name`，无 `domain/aliases/importance`；无路径查询 |
| F6 | **`add_fact` 合并路径不刷图边**：`memory/types/semantic.py:62-76` 走合并时只 `self.add(...)`，不调 `graph_store.add_relation`（只有新建路径 77-114 才写边） | retract/supersede 后投影边 `active` 陈旧；Neo4j 持久化后成为正确性 bug |
| F7 | 启动仅重建 `InMemoryVectorStore`（`manager.py:79-81`） | 切到 Qdrant/Neo4j 后无回填：向量缺、图为空 |
| F8 | 星云图 `build_graph(manager.list())` **只读 SQLite**（`web/graph_builder.py:63-64`）；Neo4j 唯一消费者是 `GraphRAGPipeline._expand`（经 `semantic.related()`） | Neo4j 开了也只服务多跳，不影响 UI |
| F9 | 嵌入优先级已实现：`EMBEDDING_BASE_URL` → `DASHSCOPE_API_KEY` → `HashEmbedding`（`memory/base.py:61-89`、`web/support.py:56-78`） | 无需改动 |

---

## 二、目标数据模型（含完整 DDL / Cypher / Payload）

### 2.1 SQLite（`memory.sqlite3`，真值源）

在 `memory/storage/document_repo.py`（新文件）中建两表，与 `memories` 同库：

```sql
-- ① 文档主数据（真值源）
CREATE TABLE IF NOT EXISTS documents (
  document_id TEXT PRIMARY KEY,                -- 稳定派生，重复导入幂等
  title       TEXT NOT NULL DEFAULT '',
  raw_text    TEXT NOT NULL,                   -- 归一化后的全文（与分块同源，见 2.3）
  source      TEXT NOT NULL DEFAULT '',        -- 文件名/URL/一句话入库
  tags        TEXT NOT NULL DEFAULT '[]',      -- JSON 数组：权限/分类标签
  permission  TEXT NOT NULL DEFAULT 'private', -- private|shared|public
  status      TEXT NOT NULL DEFAULT 'uploaded',-- uploaded|parsed|vectorized|extracted|failed
  error       TEXT,                            -- failed 时的异常摘要
  created_at  TEXT NOT NULL,
  updated_at  TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_documents_status ON documents(status);

-- ② 分段信息（原文在 documents，这里存边界与切片文本）
CREATE TABLE IF NOT EXISTS chunks (
  chunk_id      TEXT PRIMARY KEY,              -- f"{document_id}:{index}"
  document_id   TEXT NOT NULL,
  chunk_index   INTEGER NOT NULL,
  char_start    INTEGER NOT NULL,              -- 相对 raw_text 的字符区间
  char_end      INTEGER NOT NULL,
  text          TEXT NOT NULL,
  vector_status TEXT NOT NULL DEFAULT 'pending'-- pending|indexed|failed
);
CREATE INDEX IF NOT EXISTS idx_chunks_document ON chunks(document_id);
```

标签过滤查询（SQLite 内建 `json_each`，无需新索引）：

```sql
SELECT * FROM documents
WHERE (?1 = '' OR EXISTS (SELECT 1 FROM json_each(documents.tags) WHERE json_each.value = ?1))
  AND (?2 = '' OR status = ?2)
ORDER BY updated_at DESC
LIMIT ?3 OFFSET ?4;
```

### 2.2 Qdrant（纯向量索引）

- collection：沿用 `MEMORY_QDRANT_COLLECTION`（常量值 `helloagents_memory`）
- 向量：1024 维（`MEMORY_EMBEDDING_DIMENSION`），距离 `COSINE`（懒建，`qdrant.py:36-59` 保留）
- point id：`_point_id(chunk_id)`（沿用 uuid5 方案，`qdrant.py:93-100`）
- **轻量 payload**（替代现在整条 `item.to_dict()`）：

```json
{
  "chunk_id": "5a85fc13-...:3",
  "document_id": "5a85fc13-...",
  "chunk_index": 3,
  "source": "file.pdf",
  "memory_type": "semantic"
}
```

检索回查链路（行为等价改造）：`Qdrant 命中 chunk_id` → `DocumentRepository.get_chunk(chunk_id)` → 返回 `chunk.text`。现有 `BaseMemory.search` 已是「向量索引 → document_store 回查 item」模式，把回查目标从 `memories` 换成 `chunks` 即可。

### 2.3 全文与分块的同源保证（关键实现点）

`DocumentProcessor.chunks` 现在先做 `text = re.sub(r"\s+", " ", document.content).strip()`（`memory/rag/document.py:67`），再做切片。为保证 `char_start/char_end` 能正确定位，**分块与 `documents.raw_text` 必须消费同一份归一化文本**。给 `DocumentProcessor` 增加一个方法，`chunks` 与仓库共用：

```python
def normalized_text(self, document: Document) -> str:
    return re.sub(r"\s+", " ", document.content).strip()  # document.py:67 的现行逻辑抽出
```

### 2.4 Neo4j（实体关系图谱）

```
(:MemoryEntity {name, domain, aliases:[], importance:float})
-[:RELATED {kind, confidence:float, active:bool, superseded_at, superseded_by:[], source_document, chunk_id, memory_id}]->
(:MemoryEntity)
```

实体属性通过扩展 `graph.py:70` 的 MERGE 写入；边属性沿用 `semantic.py:92-110` 目前抽取的字段集合。

---

## 三、Phase 0：激活接线（最小，打通 Qdrant/Neo4j 开关）

### 改什么

**`web/support.py:127-137`** `get_manager()`：

```python
def get_manager() -> MemoryManager:
    global _manager
    if _manager is None:
        with _manager_lock:
            if _manager is None:
                cfg = MemoryConfig.from_env()      # 新增：读 HELLOAGENTS_MEMORY_* 全套
                cfg.sqlite_path = str(DB_PATH)     # 保留：MEMORY_DB_PATH 优先级不变
                _manager = MemoryManager(cfg, embedding=build_embedding())
    return _manager
```

**`tool/_memory.py:48-51`** `build_default_manager()`：

```python
def build_default_manager() -> MemoryManager:
    cfg = MemoryConfig.from_env()
    cfg.sqlite_path = default_sqlite_path()   # MEMORY_DB_PATH 优先，无则回退常量
    return MemoryManager(cfg)
```

> 说明：`MemoryConfig.from_env()`（`memory/base.py:308-334`）已完整支持 `QDRANT_URL/QDRANT_COLLECTION/NEO4J_URI/NEO4J_USERNAME/NEO4J_PASSWORD` 等字段。默认（都不配置）时行为与现在完全一致（内存向量 + 内存图）。

### 测试用例（`tests/test_memory.py` 追加）

| 用例 | 断言 |
|---|---|
| `test_manager_from_env_selects_qdrant_when_url_set` | monkeypatch `HELLOAGENTS_MEMORY_QDRANT_URL=http://localhost:6333`，构造 `MemoryManager(MemoryConfig.from_env())`，断言 `type(vector_store) is QdrantVectorStore`（注入假 client 避免联网）|
| `test_manager_from_env_selects_neo4j_when_uri_set` | 同上 NEO4J_URI 分支，断言 `graph_store.driver is not None`（或不抛 `ValueError`）|
| `test_manager_default_backends_unchanged` | 无 env 时 `vector_store is InMemoryVectorStore` 且 `graph_store.driver is None` |

---

## 四、Phase 1：SQLite 文档仓储（`memory/storage/document_repo.py`）

### 新增文件：`memory/storage/document_repo.py`

```python
from dataclasses import dataclass, field
@dataclass
class DocumentRecord:
    document_id: str
    title: str = ""
    raw_text: str = ""
    source: str = ""
    tags: list[str] = field(default_factory=list)
    permission: str = "private"
    status: str = "uploaded"
    error: str | None = None
    created_at: str = ""          # utc_now() 填充
    updated_at: str = ""

@dataclass
class ChunkRecord:
    chunk_id: str
    document_id: str
    chunk_index: int
    char_start: int
    char_end: int
    text: str
    vector_status: str = "pending"

class DocumentRepository:
    def __init__(self, path: str | Path) -> None: ...   # 同 SQLiteDocumentStore 的连接/锁/线程模式
    def _initialize(self) -> None: ...                  # 建 2.1 的两张表 + 索引
    def upsert_document(self, doc: DocumentRecord) -> None
    def get_document(self, document_id: str) -> DocumentRecord | None
    def list_documents(self, *, tag: str = "", status: str = "", page: int = 1, page_size: int = 20) -> tuple[list[DocumentRecord], int]  # (items, total)
    def count_documents(self) -> int
    def set_status(self, document_id: str, status: str, *, error: str | None = None) -> None
    def upsert_chunk(self, chunk: ChunkRecord) -> None
    def upsert_chunks(self, chunks: list[ChunkRecord]) -> None
    def get_chunk(self, chunk_id: str) -> ChunkRecord | None
    def set_chunk_vector_status(self, chunk_id: str, status: str) -> None
    def delete_document(self, document_id: str) -> int   # 先删 chunks 再删 documents，返回删除块数
    def stats(self) -> dict[str, int]                    # documents/chunks/chunks_indexed
    def close(self) -> None
```

- 复用 `memory/base.py` 的 `utc_now` 与 JSON 序列化策略（`document.py:98-120` 已有 `_json_safe` 先例）。
- `upsert_document` 用 `INSERT ... ON CONFLICT(document_id) DO UPDATE` 实现幂等。
- `stats` 的 `chunks_indexed = SELECT count(*) FROM chunks WHERE vector_status='indexed'`。

### 测试用例（`tests/test_document_repo.py` 新增）

| 用例 | 断言 |
|---|---|
| `test_upsert_document_idempotent` | 同 id upsert 两次，`list_documents` 只有 1 条且字段被覆盖 |
| `test_list_with_tag_filter` | 3 条不同 tags，`tag="权限A"` 只回 1 条 |
| `test_list_pagination_and_total` | total 正确，page 边界不越界 |
| `test_status_transition` | `set_status` 到 `failed` 时 error 一并落库 |
| `test_delete_document_cascades_chunks` | 删 document 后 `get_chunk` 全部 None |
| `test_stats_counts` | 写入 2 文档 5 块（3 indexed）→ `chunks_indexed==3` |

---

## 五、Phase 2：摄取管道双写（原文/分段落库 + 状态机）

### 改什么

**`memory/rag/document.py`**：抽出 `normalized_text` 并让 `chunks()` 可选返回偏移，新增：

```python
@dataclass(frozen=True)
class ChunkSpan:
    chunk: Document
    char_start: int
    char_end: int

def chunks_with_spans(self, document, *, chunk_size=RAG_CHUNK_SIZE, overlap=RAG_CHUNK_OVERLAP) -> list[ChunkSpan]:
    text = self.normalized_text(document)
    step = chunk_size - overlap
    spans = []
    for index, start in enumerate(range(0, len(text), step)):
        end = min(start + chunk_size, len(text))
        chunk_text = text[start:end]
        if not chunk_text:
            break
        metadata = {**document.metadata, "document_id": document.id, "chunk_index": index}
        spans.append(ChunkSpan(Document(chunk_text, id=f"{document.id}:{index}", metadata=metadata), start, end))
        if end >= len(text):
            break
    return spans
```

> `chunks()` 改为 `[s.chunk for s in self.chunks_with_spans(...)]`，**现有调用零破坏**。

**`memory/rag/pipeline.py:94-100`** `ingest()` 循环内，对每个 chunk 增加仓储双写：

```python
repo = DocumentRepository(self.manager.document_store.path)   # 同一个 sqlite 文件
for document in values:
    repo.upsert_document(DocumentRecord(
        document_id=document.id,
        title=document.metadata.get("title", ""),
        raw_text=self.processor.normalized_text(document),
        source=document.metadata.get("source", document.id),
        tags=list(document.metadata.get("tags", [])),
        permission=document.metadata.get("permission", "private"),
        status="parsed",
    ))
    for span in self.processor.chunks_with_spans(document, ...):
        repo.upsert_chunk(ChunkRecord(
            chunk_id=span.chunk.id, document_id=document.id, chunk_index=span.chunk.metadata["chunk_index"],
            char_start=span.char_start, char_end=span.char_end, text=span.chunk.content,
        ))
        item = self.manager.add(span.chunk.content, memory_type=SEMANTIC, metadata=span.chunk.metadata, item_id=span.chunk.id)
        repo.set_chunk_vector_status(span.chunk.id, "indexed")
        ...
    repo.set_status(document.id, "vectorized")
```

- 抽提完成后 `repo.set_status(document.id, "extracted")`；extractor 异常走既有 `report["errors"]` 分支时置 `failed`（带 error）。
- 失败重试靠幂等 upsert（`upsert_document/upsert_chunk` 均 ON CONFLICT 覆盖）。

### 测试用例（`tests/test_memory_rag.py` 追加）

| 用例 | 断言 |
|---|---|
| `test_ingest_persists_document_and_chunks` | ingest 后仓库 `get_document` 非 None，`list_documents.total==1`，chunks 数与分块一致，`raw_text` 与 `DocumentProcessor.normalized_text` 相同 |
| `test_ingest_idempotent` | 同文档 ingest 两次，documents/chunks 不翻倍 |
| `test_chunk_text_matches_repo` | 每个 `ChunkRecord.text == 对应 MemoryItem.content` |

---

## 六、Phase 3：Qdrant 轻量化 + 命中回查 chunks

### 改什么（`memory/storage/qdrant.py`）

**新增 `upsert_chunk`**（窄接口，只放轻量 payload）：

```python
def upsert_chunk(self, chunk_id: str, vector: list[float], *, document_id: str = "",
                 chunk_index: int = 0, source: str = "", memory_type: str = "semantic") -> None:
    self._ensure_collection(len(vector))
    from qdrant_client.models import PointStruct
    payload = {
        "chunk_id": chunk_id,
        "document_id": document_id,
        "chunk_index": chunk_index,
        "source": source,
        "memory_type": memory_type,
    }
    self.client.upsert(
        collection_name=self.collection_name,
        points=[PointStruct(id=self._point_id(chunk_id), vector=vector, payload=payload)],
    )
```

**`search` 调整返回键**（`qdrant.py:77-91`）：payload 里优先取 `chunk_id`，否则退回老逻辑：

```python
p_id = (point.payload or {}).get("chunk_id") or (point.payload or {}).get("id") or point.id
return [(str(p_id), float(point.score)) for point in points]
```

**`upsert(MemoryItem)` 瘦身**（兼容四层记忆的非 chunk 条目）：payload 过滤掉重字段 `embedding`，只留 `{id, memory_type, created_at, expires_at, namespace, metadata中必要键}`。

### 回查改造

- `RAGPipeline.retrieve`（`pipeline.py:161-163`）命中后优先 `repo.get_chunk(result.item.id)` 取文本；查不到再退回 `result.item.content`（灰度兼容）。
- `BaseMemory.search` 保持「向量库 → document_store.get(item_id)」不变（working/episodic/perceptual 仍在 memories 真值源）。

### 3.3 混合检索：SQLite FTS5 × Qdrant，RRF 融合（优化 A）

**动机**：纯向量检索对**精确词**（型号、人名、编号、代码标识符）区分度差；纯关键词检索对**转述**无能为力。两者恰好互补，这是三库「同场协作」最直接的外显——一次查询里 SQLite 与 Qdrant 并肩工作。

**建索引**（SQLite 内建，零新依赖；中文用 `trigram` 分词器以支持子串匹配）：

```sql
CREATE VIRTUAL TABLE IF NOT EXISTS chunks_fts USING fts5(
  text, content='chunks', content_rowid='rowid', tokenize='trigram'
);
CREATE TRIGGER IF NOT EXISTS chunks_ai AFTER INSERT ON chunks BEGIN
  INSERT INTO chunks_fts(rowid, text) VALUES (new.rowid, new.text);
END;
CREATE TRIGGER IF NOT EXISTS chunks_ad AFTER DELETE ON chunks BEGIN
  INSERT INTO chunks_fts(chunks_fts, rowid, text) VALUES ('delete', old.rowid, old.text);
END;
CREATE TRIGGER IF NOT EXISTS chunks_au AFTER UPDATE ON chunks BEGIN
  INSERT INTO chunks_fts(chunks_fts, rowid, text) VALUES ('delete', old.rowid, old.text);
  INSERT INTO chunks_fts(rowid, text) VALUES (new.rowid, new.text);
END;
```

> `trigram` 需 SQLite ≥ 3.34（Python 3.12 自带满足）；不支持时退回 `unicode61` 并在文档标注中文召回降级。

**仓储补关键词检索**（`DocumentRepository.search_keywords`）：

```python
def search_keywords(self, query: str, *, limit: int = 10) -> list[tuple[str, float]]:
    rows = self._conn().execute(
        "SELECT c.chunk_id AS chunk_id, bm25(chunks_fts) AS rank "
        "FROM chunks_fts JOIN chunks c ON c.rowid = chunks_fts.rowid "
        "WHERE chunks_fts MATCH ? ORDER BY rank LIMIT ?", (query, limit)).fetchall()
    return [(r["chunk_id"], -float(r["rank"])) for r in rows]   # bm25 越小越相关 → 取负
```

**RRF 融合 + 入口改造**（`memory/rag/pipeline.py`）：

```python
def _rrf_fuse(rank_lists: list[list[str]], *, k: int = 60) -> list[tuple[str, float]]:
    scores: dict[str, float] = {}
    for hits in rank_lists:
        for rank, chunk_id in enumerate(hits, start=1):
            scores[chunk_id] = scores.get(chunk_id, 0.0) + 1.0 / (k + rank)
    return sorted(scores.items(), key=lambda kv: -kv[1])

def hybrid_retrieve(self, query, *, limit=RAG_RETRIEVE_LIMIT, threshold=None, metadata=None):
    if not _hybrid_enabled():                      # 配置项 HELLOAGENTS_MEMORY_HYBRID，默认开
        return self.retrieve(query, limit=limit, threshold=threshold, metadata=metadata)
    vec = [r.item.id for r in self.manager.search(
        query, memory_type=MemoryType.SEMANTIC, limit=limit * 2, threshold=threshold, metadata=metadata)]
    kw = [cid for cid, _ in self.repository.search_keywords(query, limit=limit * 2)]
    fused = _rrf_fuse([vec, kw])[:limit]
    return [RetrievedChunk(*self._chunk_payload(cid)) for cid in fused]   # 正文统一从 chunks 回查
```

- 旧 `retrieve()` 保留不删：配置关闭即完全回到纯向量路径（灰度与回归安全）。

### 测试用例（`tests/test_embedding_qdrant_chunk.py` 或并入现有 qdrant 测试）

| 用例 | 断言 |
|---|---|
| `test_upsert_chunk_payload_lightweight` | 假 client 收到的 payload 仅含 5 个键，且不含 embedding |
| `test_search_prefers_chunk_id` | payload 带 `chunk_id` 时 search 返回该值 |
| `test_dimension_mismatch_still_raises` | `_ensure_collection` 原校验不变 |
| `test_fts_index_syncs_on_insert_update_delete` | chunks 增/改/删后 `search_keywords` 结果与表内容一致 |
| `test_keyword_search_finds_exact_identifier` | 精确编号类 query（向量弱、关键词强）能命中目标 chunk |
| `test_rrf_fuse_prefers_common_hits` | 两路都命中的 chunk 排名高于仅单路命中 |
| `test_hybrid_falls_back_to_vector_when_disabled` | 关闭配置后 `hybrid_retrieve` 等价于原 `retrieve` |

---

## 七、Phase 4：Neo4j 实体属性 + 路径查询 + 修复边一致性

### 7.1 `memory/storage/graph.py`

**实体属性 MERGE**（替换 `graph.py:70` 的查询）：

```cypher
MERGE (a:MemoryEntity {name: $source})
ON CREATE SET a.domain = $source_domain, a.aliases = $source_aliases, a.importance = $source_importance
ON MATCH  SET a.aliases = CASE WHEN $source_aliases = [] THEN a.aliases ELSE $source_aliases END
MERGE (b:MemoryEntity {name: $target})
ON CREATE SET b.domain = $target_domain, b.aliases = $target_aliases, b.importance = $target_importance
ON MATCH  SET b.aliases = CASE WHEN $target_aliases = [] THEN b.aliases ELSE $target_aliases END
MERGE (a)-[r:RELATED {kind: $relation}]->(b)
SET r += $properties
```

对应新增方法（可选参数，不破坏现有签名）：

```python
def add_relation(self, source, relation, target, *, properties=None,
                 source_domain: str = "", target_domain: str = "",
                 source_aliases: list[str] | None = None, target_aliases: list[str] | None = None,
                 source_importance: float = 0.5, target_importance: float = 0.5) -> None
```

**新增路径查询**（服务「关联实体挖掘」，内存回退给等价 BFS 实现）：

```python
def path_query(self, start: str, target: str, *, max_depth: int = 3,
               limit: int = 50) -> list[dict[str, Any]]:
    # Neo4j: MATCH p=(a:MemoryEntity {name:$start})-[*1..N]-(b:MemoryEntity {name:$target})
    #        RETURN nodes(p) 与 relationships(p)，包装为 {entities:[...], relations:[...]}
    # 内存: 在 _local/_reverse 上做限定深度 BFS
```

### 7.2 `memory/types/semantic.py`：修复合并路径不刷边（F6）

把 92-110 的属性抽取提取为私有方法复用，并在合并分支补写边：

```python
def _graph_properties(self, item: MemoryItem, metadata: Mapping[str, Any]) -> dict[str, Any]:
    props = {"memory_id": item.id, "confidence": metadata.get("confidence", 1.0)}
    for key in ("evidence","source","source_document","chunk_id","predicate_key",
                "action","cardinality","active","superseded_by","superseded_at","supersedes"):
        if key in item.metadata:
            props[key] = item.metadata[key]
    return props
```

合并分支（`semantic.py:62-76`）末尾追加：

```python
    updated = self.add(f"{subject} {predicate} {object}", metadata=merged_metadata,
                       importance=max(existing.importance, confidence), item_id=fact_id)
    self.graph_store.add_relation(subject, predicate, object,
                                  properties=self._graph_properties(updated, merged_metadata))
    return updated
```

新建分支把 92-113 换成 `self._graph_properties(item, item_metadata)`。

### 测试用例（`tests/test_semantic_graph.py` 追加）

| 用例 | 断言 |
|---|---|
| `test_retract_refreshes_graph_edge` | `add_fact` 建边后 `add_fact(..., metadata={"active": False})`，断言内存图的边 `properties["active"] is False` |
| `test_supersede_writes_edge_markers` | 合并后边含 `superseded_by`/`superseded_at` |
| `test_path_query_neo4j_equivalence` | 内存回退的 `path_query` 对已知两条路径返回正确 entities/relations |

---

## 八、Phase 5：迁移与回填脚本（`scripts/migrate_storage.py`）

### 迁移映射（针对现库 37 条）

| 现有数据 | 迁移去向 |
|---|---|
| `semantic` 且 `metadata.document_id + chunk_index` | 按 `document_id` 分组、按 `chunk_index` 排序 → `documents.raw_text = "\n".join(chunk.content)`（或按 char 区间拼接）→ `chunks` 表；`chunks.text = 原 chunk.content` |
| `semantic` 且 `metadata.subject/predicate/object`（事实条目） | **保留在 `memories`**（审计副本）；若 Neo4j 开启则重放 `graph_store.add_relation` 回填图（属性照 `_graph_properties`）|
| `working/episodic/perceptual` | **保留在 `memories`**，不动 |
| `memories.embedding` 列 | 置空（向量真值移入 Qdrant）；Qdrant 开启时用现有 `scripts/reindex_embeddings.py` 的思路对全部 chunk 重嵌入 upsert |

### 脚本接口（伪代码级）

```python
# scripts/migrate_storage.py
# 用法: python scripts/migrate_storage.py [--db path] [--dry-run] [--backup]
#  - 迁移前默认备份 {db}.bak.pre_migration
#  - idempotent: documents/chunks 按 document_id/chunk_id 存在即跳过
#  - 结束输出 {"documents": n, "chunks": m, "facts_kept": k, "graph_replayed": g}
```

### 回滚

- P5 只新增 `documents`/`chunks` 表 + 清空 `memories.embedding`，不改 `memories` 其它字段 → 回滚 = 恢复备份 sqlite 文件 + 清空 Qdrant collection（新索引可随时重建）+ Neo4j 全量重放即可。

---

## 九、Phase 6：API 与健康检查（`web/app.py`）

### 新端点（Pydantic 响应模型）

| 端点 | 请求 | 响应 |
|---|---|---|
| `GET /api/documents` | `?tag=&status=&page=1&page_size=20` | `{total, page, page_size, items:[{document_id,title,source,tags,status,chunk_count,created_at}]}` |
| `GET /api/documents/{id}` | — | `{document_id,title,raw_text,source,tags,permission,status,error,chunks:[{chunk_id,chunk_index,char_start,char_end,text,vector_status}]}` |
| `POST /api/documents/{id}/revectorize` | — | `{document_id, chunks_reindexed, status}`（重嵌入全部 chunk → upsert_chunk → 置 indexed/extracted）|
| `GET /api/stats` | — | `{documents, chunks, chunks_indexed, facts, memories_total}` |
| `GET /api/reconcile` | — | `{counts:{...}, drift:[{kind, count, ids[]}]}`（三库计数对账，只看不改）|
| `POST /api/reconcile` | `{"repair":["missing_vector","missing_edge"]}` | `{repaired:{missing_vector:n, missing_edge:m}}`（复用 P5 回填函数自愈）|

### `/api/health` 增补

在现有 `embedding_mode` 旁加 `store_modes`：

```json
"store_modes": {
  "document": "sqlite|memory",
  "vector": "inmemory|qdrant",
  "graph": "inmemory|neo4j"
}
```

取值直接读 `get_manager().vector_store / graph_store` 的类名或 base_url（鸭子判断，与现有 `embedding_mode` 同风格）。

### 对账与自愈（优化 B）

把「协调配合」落到运维面：健康检查从「看模式」升级为「看一致性」。

```json
GET /api/reconcile
{
  "counts": {"chunks": 120, "chunks_indexed_sqlite": 120, "qdrant_points": 118,
             "facts": 45, "neo4j_edges": 45},
  "drift": [{"kind": "missing_vector", "count": 2, "ids": ["...:7", "...:9"]}]
}
```

- 对账口径：`chunks WHERE vector_status='indexed'` ↔ Qdrant `count()`／按 `scroll` 抽样比对；`memories` 事实行 ↔ Neo4j `RELATED` 边数。
- 漂移种类：`missing_vector`（真值有、向量无）、`orphan_vector`（向量有、真值无）、`missing_edge`（事实有、边无）。
- 修复：`POST` 复用 P5 的回填函数（重嵌入缺失 chunk / 重放缺失事实边），全程幂等，可重复执行。

### 测试用例（`tests/test_web_api.py` 追加）

| 用例 | 断言 |
|---|---|
| `test_list_documents_pagination` | 3 文档 page_size=2 → total=3、items=2、第二页 1 条 |
| `test_get_document_raw_text` | 返回 `raw_text` 与 chunks 边界一致 |
| `test_stats_endpoint` | 键齐全、数值与仓库 `stats()` 一致 |
| `test_reconcile_reports_drift` | 造 1 块 `vector_status='indexed'` 但不在假向量库中 → `drift[0].kind=="missing_vector"` |
| `test_reconcile_repair_is_idempotent` | 连续两次 `POST /api/reconcile` 第二次 `repaired` 计数为 0 |

---

## 十、Phase 7：UI 架构改进（`web/static/index.html`）

### 10.1 前端现状（已核读）

| 项 | 现状 |
|---|---|
| 形态 | 单文件 `web/static/index.html`（67.9 KB / 1753 行），**零构建、原生 JS**，Canvas 2D 自绘星空 |
| 渲染函数族 | `loadGraph / computeOverview / buildStarSystem / project2D / drawCelestialBody / drawEdges / zoomToLevel / drillIntoNode / updateBreadcrumb / renderDrawer / renderTourPanel / render` |
| 已接端点 | `/api/graph`(3 处) · `/api/chat` · `/api/facts`(2) · `/api/knowledge`(2) · `/api/ingest` · `/api/export` · `/api/health` |
| **未接端点（后端已实现，前端零入口）** | **`/api/graph-rag`（多跳推理）· `/api/import`（导入）· `/api/seed`（种子）** |
| 现有组件 | 星云图 `#universe` · 问答面板 `#chat-panel`（含 `#chat-mode-select`）· 详情抽屉 `#detail-drawer`（breadcrumb/parent-uplink/subnodes/relations）· 搜索 · 知识/事实录入 · `#stats-bar` · toast · 引导 `#tour-panel` |

> **结论：UI 最大的浪费是「能力已具备，用户看不见」**——多跳图推理与导入能力后端都已完成，前端没有任何入口；同时新的 `documents/chunks`、`store_modes`、`reconcile` 一旦落地也没有呈现面。

### 10.2 UI ↔ 三库协作映射（新增界面必须明确数据来源）

| UI 区域 | 数据来源 | 状态 |
|---|---|---|
| 星云图 | SQLite `memories`（经 `web/graph_builder.build_graph`） | 现有 |
| 问答回答 | Qdrant + SQLite FTS5 混合检索 → LLM | 优化 A 后 |
| 多跳推理路径 | Neo4j 扩边 + SQLite 事实活性校验 | 新增 U1 |
| 文档中心 | SQLite `documents` / `chunks` | 新增 U2 |
| 健康与一致性面板 | 三库计数对账（`/api/health` + `/api/reconcile`） | 新增 U3 |

### 10.3 改进项（按价值排序）

#### U1 多跳图推理可视化（接 `/api/graph-rag`）★★★

- 问题：后端多跳推理零入口，图谱的推理价值对用户不可见。
- 落点：`#chat-mode-select` 增加「图谱推理」模式 → 走 `/api/graph-rag`；返回的实体/关系链交给星云图**高亮路径 + 连线流光**，抽屉逐条列出证据 chunk。
- 实现：`drawEdges` 增加 `highlight: Set<edgeKey>` 参数；新增 `highlightPath(entities, relations)`；`loadGraph` 完成后套用高亮态。
- 验收：一次多跳提问后，图上可见 ≥2 跳实体链高亮，抽屉可点开每个证据 chunk。

#### U2 文档中心（对接 `/api/documents`）★★★

- 列表：分页 + 标签 + 状态筛选；每行显示 `chunk_count` 与状态徽章（`uploaded→parsed→vectorized→extracted`，`failed` 显示 error 与重试按钮）。
- 详情：`raw_text` 全文 + 按 `char_start/char_end` 高亮分块；点块看 `vector_status`。
- 操作：`POST /api/documents/{id}/revectorize` 重索引；补上未接的 `/api/import` 做文件导入。
- 实现：复用现有 drawer / panel / toast 组件，**不引入构建链**。

#### U3 存储健康与一致性面板（对接 `/api/health` + `/api/reconcile`）★★★

- 展示 `store_modes` 三格（document / vector / graph）+ 三库计数 + drift 列表。
- 出现漂移时给「一键修复」按钮（调 `POST /api/reconcile`）。
- 位置：`#stats-bar` 扩展为可展开面板。

#### U4 检索溯源面板 ★★

- 回答气泡下方可展开「依据」：命中的 chunks（向量分 / 关键词分 / RRF 融合分）、来源文档、涉及实体。
- 价值：让混合检索的贡献可见，便于调试与课堂演示。

#### U5 摄取进度与失败重试 ★★

- 上传后按 `documents.status` 展示状态机进度；失败可一键重试。
- 端点：轮询 `GET /api/documents/{id}`（后续可升级 SSE）。

#### U6 星云图大图可扩展性 ★★

- 现状：`loadGraph` 全量拉取、每帧全量重绘，节点上千必卡。
- 做法：
  - **增量图**：`GET /api/graph?since=<revision>`（复用已有 `GRAPH_REVISION`）→ 前端差量合并（本章唯一需要的后端改动）。
  - **LOD**：按 `zoomToLevel` 当前级别决定绘制细节（领域 → 实体 → 事实/块），低级别不画卫星与文字。
  - **视口裁剪 + 静态层离屏缓存**（`OffscreenCanvas`），文字宽度结果缓存。
  - **前端筛选**：按领域/标签/时间过滤，纯前端，不改后端。
- 验收：500 节点下交互不掉帧。

#### U7 实体侧栏（别名 / 领域 / 关联）★

- 点实体 → 别名、领域、重要度、关联事实列表（Neo4j + SQLite）。
- 为 `EntityResolver` 的别名归并提供人工修正入口。

#### U8 可访问性与移动端 ★

- 键盘可达、`aria-label`、抽屉手势、小屏布局。

### 10.4 实施顺序与前端原则

1. 顺序：**U1**（后端已就绪、纯前端、收益最大）→ **U3**（只读展示）→ **U2**（依赖 P1/P2）→ **U4/U5** → **U6/U7/U8**。
2. **契约先行**：前端只依赖 `/api/*` JSON；新能力一律先定响应模型再接 UI。
3. **不引构建链**：维持单文件零依赖，新面板复用既有样式与交互范式。
4. **状态单一来源**：图状态来自 `/api/graph`、文档状态来自 `/api/documents`，前端不本地双写。

### 10.5 验收

| 检查 | 断言 |
|---|---|
| 静态冒烟 | `GET /` 200 且含 `#universe`；`/api/graph` 返回可渲染 nodes/edges |
| U1 | 注入一次 graph-rag 响应后，`highlightPath` 置位的边键被 `drawEdges` 消费（手工冒烟为主） |
| U3 | health + reconcile 响应能渲染出三格 `store_modes` 与 drift 计数 |
| U6 | `?since=` 差量合并后节点总数与全量一致（后端参数单测覆盖） |

---

## 十一、全阶段回归与验收标准

1. 每个 Phase 结束：`ruff check .` 全绿 + `pytest -q` 全绿（基线 332 passed + 1 skipped 之上增量）。
2. 存量 332 用例**零删改**（只在扩展点追加），保证行为等价。
3. 实网冒烟（Phase 2/3 后）：经 `127.0.0.1:10800` 网关走一遍 `ingest → retrieve → graph_retrieve`，区分度与改造前相当；`/api/health` 三 store 模式正确。
4. P5 在副本库跑 `--dry-run` 先行，确认映射数量后再写生产库。
5. UI 冒烟（Phase 7）：首页可加载、星云图正常渲染、U1/U3 面板可用、`?since=` 增量刷新不丢节点。

---

## 十二、风险与应对

| 风险 | 应对 |
|---|---|
| 文档原文不可逆（F1 只存过 chunk） | 迁移时 `raw_text = join(chunk.text)`（按索引序），归一化空白丢失是既有限制，标注于原始 `Document.content` 的 guaranteed loss |
| 跨库最终一致 | 状态位（`documents.status`/`chunks.vector_status`/`memories` active 标志）为唯一协调者；任意投影失败置 `failed`，重试幂等（D5）；`/api/reconcile` 提供对账与自愈 |
| Qdrant/Neo4j 需要外部服务 | 默认内存回退不变；仅 `HELLOAGENTS_MEMORY_*` 显式配置才启用；测试用假 client/driver |
| 向量空间切换 | `scripts/reindex_embeddings.py` 思路复用为按 chunk 重索引，`vector_status` 全部重置 `pending`；维度由 `_ensure_collection` 校验 |
| FTS5 中文分词 | 优先 `trigram`（支持子串）；环境不支持时退回 `unicode61` 并在文档标注中文召回降级；`HELLOAGENTS_MEMORY_HYBRID` 可一键关闭混合检索回退纯向量 |
| 回归面大 | 每 Phase 独立提交 + 独立日志；P1-P4 不触碰四层记忆对外 API，只新增仓储/接口 |
| 前端单文件持续膨胀 | 维持零构建但按 U1-U8 分块组织（每块自带注释边界与独立函数族）；U6 的 LOD 与视口裁剪先于继续加功能，避免 1753 行继续线性增长 |
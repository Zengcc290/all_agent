# 存储架构规划方案：SQLite 真值源 + Qdrant 向量索引 + Neo4j 图谱

> 状态：规划文档（尚未实施）
> 目标：把当前「SQLite 单表存一切 + 内存向量/图回退」的架构，演进为三库各司其职：
> **SQLite = 文档真值源（Source of Truth）**，**Qdrant = 纯向量检索索引**，**Neo4j = 实体关系知识图谱**。

---

## 一、背景与现状

当前项目（all-agent / 知识星云）的记忆层现状：

- **SQLite**（`memory/storage/document.py`）：一张 `memories` 表存所有内容——正文、metadata、**embedding 向量快照**、relations。文档被打散成 chunk 行，**没有原始文档表、没有状态标记、没有标签/分页能力**。
- **Qdrant**（`memory/storage/qdrant.py`）：适配器完整（懒建 collection、维度校验、namespace/memory_type 过滤），但**代码里没有任何激活路径**——`MemoryConfig.from_env()` 全仓库只有 `make_default_embedding` 内部调用，22 处 `MemoryConfig(...)` 构造点都只传 `sqlite_path`。payload 存的是**完整 `item.to_dict()`**（qdrant.py:66-68），不符合「轻量 payload」定位。
- **Neo4j**（`memory/storage/graph.py`）：适配器完整（MERGE Entity/RELATED、内存回退），同样**无激活路径**。唯一消费者是 `GraphRAG._expand` 多跳扩展；星云图（`web/graph_builder.py`）反而从 SQLite 现算边，完全不读图库。
- **MySQL**：与项目**零关系**（源码/依赖/配置均无引用）。

已核实的关键缺口（详见 `代码审查与修复规划.md` 与本仓库历次审计）：

1. 环境变量 `HELLOAGENTS_MEMORY_QDRANT_URL` / `_NEO4J_URI` 无任何效果（`from_env` 未被 Manager 装配使用）。
2. 图边写入与 SQLite 非事务：`semantic.add_fact` 先写库再写图，图失败不回收 → 孤立事实。
3. `add_fact` 合并路径（retract/supersede 走的分支）**不刷新图边属性**（semantic.py:62-76），图边 `active` 停留旧值；现有消费者靠 `_edge_is_active` 回查 SQLite 兜住。
4. 切换后端不迁移数据：Qdrant 需按当前 1024 维重索引；Neo4j 不会自动回填历史事实。
5. 向量快照双写：SQLite `memories.embedding` 列与向量索引重复存储。

---

## 二、职责对照（期望 → 现状 → 差距）

| 期望职责 | 现状 | 差距 |
|---|---|---|
| **SQLite**：文档原文、文档 ID、创建时间、来源、权限标签、分段信息、状态标记（是否解析/是否向量化）；按 ID 查原文、按标签过滤、分页、事务增删改、统计文档数量 = **真值源** | SQLite 是「记忆条目」库，文档被打散成 chunk 行，无原文表/状态机/标签/分页 | 新增 documents/chunks 表、状态机、标签过滤、分页统计；向量从 SQLite 剥离 |
| **Qdrant**：只存 embedding 向量 + 轻量 payload（chunk ID 等少量附属信息）；语义相似度检索 = **检索加速索引，非原始文档库** | 适配器存在但永不激活；payload 是完整 item.to_dict() | 激活接线 + payload 瘦身 |
| **Neo4j**：实体节点、实体间关系、关系属性；多跳路径、关系推理、关联实体挖掘 = **知识图谱** | 适配器存在但永不激活；唯一消费者是 GraphRAG 多跳；星云图反而读 SQLite | 激活接线 + 实体属性增强 + 多跳查询 + 退休路径刷边（修复 P2） |
| **MySQL** | 与项目零关系 | **不引入**——SQLite 已承担业务主数据职责 |

---

## 三、目标数据模型

### 3.1 SQLite（`memory.sqlite3`，事实源）—— 三类表分工

```sql
-- ① 文档主数据（真值源）
CREATE TABLE documents (
  document_id TEXT PRIMARY KEY,      -- 稳定派生（同文件重复导入幂等）
  title        TEXT NOT NULL,
  raw_text     TEXT NOT NULL,        -- 文档原文
  source       TEXT,                 -- 来源：文件名/URL/一句话入库
  tags         TEXT NOT NULL DEFAULT '[]',      -- 权限/分类标签 JSON（json_each 过滤）
  permission   TEXT NOT NULL DEFAULT 'private',
  status       TEXT NOT NULL DEFAULT 'uploaded',-- uploaded→parsed→vectorized→extracted/failed
  error        TEXT,                 -- failed 时的原因
  created_at   TEXT NOT NULL,
  updated_at   TEXT NOT NULL
);
CREATE INDEX idx_documents_status ON documents(status);

-- ② 分段信息（原文在 documents，这里存边界与切片文本）
CREATE TABLE chunks (
  chunk_id      TEXT PRIMARY KEY,    -- 稳定派生（document_id + index）
  document_id   TEXT NOT NULL REFERENCES documents(document_id),
  chunk_index   INTEGER NOT NULL,
  char_start    INTEGER NOT NULL,
  char_end      INTEGER NOT NULL,
  text          TEXT NOT NULL,       -- 切片文本（检索命中的原文出处）
  vector_status TEXT NOT NULL DEFAULT 'pending'  -- pending|indexed|failed
);

-- ③ 业务主数据（四层记忆里 non-semantic 的：working/episodic/perceptual）
-- 沿用现有 memories 表结构，去掉 embedding 列（向量只在 Qdrant）

-- ④ semantic 事实审计副本（供 _edge_is_active 回查与导出）
CREATE TABLE fact_history (
  fact_id TEXT PRIMARY KEY,
  subject TEXT NOT NULL, predicate TEXT NOT NULL, object TEXT NOT NULL,
  active INTEGER NOT NULL DEFAULT 1,
  superseded_at TEXT, superseded_by TEXT
);
```

### 3.2 Qdrant（纯向量索引）—— 轻量 payload

```json
{
  "chunk_id": "doc1:chunk:3",
  "document_id": "doc1",
  "chunk_index": 3,
  "memory_type": "semantic",
  "source": "file.pdf",
  "created_at": "…"
}
```

检索命中 → 拿 `chunk_id` 回 SQLite `chunks` 表取回切片文本。现有 `BaseMemory.search` 已经是「向量索引 → SQLite 回查」模式，只需把回查目标换成 chunks 表。

### 3.3 Neo4j（图谱）

```
(:Entity {name, domain, aliases:[], importance})
-[:RELATED {kind, confidence, active, superseded_at, superseded_by:[], source_document}]-> (:Entity)
```

查询面：
- `GraphRAG._expand` 多跳（现有 BFS 保留，可加 cypher 路径查询做关联实体挖掘）；
- 星云图节点/边（`graph_builder.py` 改为读图库，或保留「SQLite 派生 / Neo4j」双模式）。

---

## 四、关键机制

1. **文档状态机（一致性协调者）**：`uploaded → parsed（原文+分块落库）→ vectorized（块向量入 Qdrant）→ extracted（实体/关系入 Neo4j）`；任一步失败 → `failed` + error 字段，可幂等重试（document_id/chunk_id 稳定派生，重跑不产生重复）。
2. **嵌入模式切换**：全局 embedding 版本号（复用现有 GRAPH_REVISION 思路）→ 检测到向量空间变更 → 相关文档 `vectorized` 置 false → 一键重向量化（复用 `scripts/reindex_embeddings.py` 思路改造成文档级）。
3. **图边一致性**：修复 `semantic.add_fact` 合并路径不刷边的 P2（semantic.py:62-76）——Neo4j 成为图谱真值后，retract/supersede 必须同步边属性；`_edge_is_active` 保留作双保险。
4. **激活接线**：`web/support.py:134` 与 `tool/_memory.py:51` 改用 `MemoryConfig.from_env()` 为基底再覆盖 `sqlite_path`，让 `HELLOAGENTS_MEMORY_QDRANT_URL` / `_NEO4J_URI` 真正生效；**默认不配置时行为完全不变**（内存回退），课设演示无需外部服务。

---

## 五、分阶段实施计划

> 每阶段：ruff + pytest 全绿 → `system.update_log` → commit `update-log-<id>: <标题>` → push。

| 阶段 | 内容 | 涉及文件 | 验证 |
|---|---|---|---|
| **P1 激活接线** | from_env 装配 + 测试 | `web/support.py`、`tool/_memory.py` | 单测：env 配置 → Qdrant/Neo4j 选中；无配置 → 原行为 |
| **P2 SQLite 文档仓储** | 新表 + `DocumentRepository`（CRUD/分页/标签过滤/统计/状态迁移），`Document`/`Chunk` 数据类 | 新增 `memory/storage/document_repo.py` | 单测：事务、分页、json_each 标签过滤、状态机 |
| **P3 Qdrant 瘦身** | payload 改为轻量字段；保留 `InMemoryVectorStore` 兼容 | `memory/storage/qdrant.py` | 单测：payload 形状、维度校验、回查映射 |
| **P4 Neo4j 图谱** | 实体属性增强、多跳路径查询、修复合并路径刷边 | `memory/storage/graph.py`、`memory/types/semantic.py` | 单测 + 定向回归（supersede/retract 后边状态） |
| **P5 摄取管道改造** | `ingest_source` → 文档入库→分块→向量化→抽取，全流程走状态机 | `memory/rag/pipeline.py`、`memory/rag/document.py` | 集成测试：重复导入幂等、失败重试 |
| **P6 API 层** | `GET /api/documents`（分页/标签/状态）、`GET /api/documents/{id}`、`POST /api/documents/{id}/revectorize`、`GET /api/stats`；既有端点适配 | `web/app.py` | FastAPI 测试 |
| **P7 迁移脚本** | 现有 37 条数据 → 文档记录 + Qdrant 回填 + Neo4j 回填，dry-run 模式 | `scripts/migrate_to_stores.py` | 实跑迁移 + 前后一致性对比 |
| **P8 收尾** | 文档更新（README / memory/README、项目代码拓扑文档）、全量回归 | 文档 | ruff + pytest 全绿 |

---

## 六、风险与对策

- **兼容性**：记忆层（working/episodic/perceptual）保留 `MemoryItem` 路径不动，文档层新增并存 → 既有 332 测试不受破坏，只新增。
- **双写一致**：SQLite 状态位是唯一协调者；Qdrant/Neo4j 失败只置 `failed` 不假报成功。
- **外部服务依赖**：Qdrant/Neo4j 仅在 env 显式配置时启用；测试一律用假客户端（沿用现有风格）。
- **迁移可逆**：迁移脚本支持 dry-run，实际写入前自动备份 `memory.sqlite3`。

---

## 七、MySQL 定位

本方案不再引入 MySQL：SQLite 承担「业务主数据」职责后，MySQL 无必要（省去驱动与方言适配）。若课设评审硬性要求 MySQL，可在 P2 阶段增加一个可选的 `mysql` 后端实现同一仓储接口，默认不启用。

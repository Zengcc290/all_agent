# Credentials only. Provider URLs, models, and default selection are in
# config/providers.toml. Leave unused provider keys empty.
LLM_OPENAI_API_KEY=replace-me
LLM_DEEPSEEK_API_KEY=
SEARCH_API=
SEARCH_BASE_URL=https://example.invalid/search
SEARCH_TOOL_ENABLED=true

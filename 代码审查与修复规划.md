# 知识星云 / all-agent 代码审查与修复深度规划

> 依据：对全项目（agents / core / tool / memory / web / tests / docs）逐文件审查，加上 6 组运行时实证复现。
> 审查基线：`python -m pytest -q` → 275 passed / 1 skipped；`python -m ruff check .` → 97 项。
> 本文档只描述问题与对应修法，尚未改动任何源码。
> 另：`verify_hit.js`（未跟踪、未被忽略）在审查会话期间从磁盘消失，非审查方删除，无法纳入审查。

---

## 问题总览（问题 ↔ 修复条目一一对应）

| # | 严重度 | 问题 | 位置 | 修复条目 |
|---|--------|------|------|----------|
| P0-1 | 严重 | 只读记忆动作被副作用确认拦截，网问答"先检索再回答"实际失效 | `tool/memory_tool.py:72`、`tool/rag_tool.py:55`、`core/runtime.py:119` | A1 |
| P0-2 | 严重 | supersede 只退役一个旧值，单值槽残留多个"当前值" | `memory/rag/knowledge.py:534-557` | A2 |
| P0-3 | 严重 | 同一三元组两套主键 → 重复事实 / 重复边 | `memory/types/semantic.py:47-49` | A3 |
| P0-4 | 严重 | 文档解析无路径沙箱，可读工作区外任意文件 | `memory/rag/document.py:34-46` | A4 |
| P1-1 | 中 | `MemoryItem` 默认 id 引用未导入的 `uuid`（NameError） | `memory/base.py:107` | B1 |
| P1-2 | 中 | 离线默认不可用；README 嵌入描述不实；启动全量重建向量索引 | `memory/base.py:60-75`、`memory/manager.py:68-69` | B2 |
| P1-3 | 中 | `/api/import` 无上传大小限制且逐条静默吞错 | `web/app.py:358-428` | B3 |
| P1-4 | 中 | fs 写/编辑工具并发与幂等标注失真（同文件并发丢更新） | `tool/fs_edit_text.py:107-108`、`tool/fs_write_text.py:104-105` | B4 |
| P1-5 | 中 | NaN / Infinity 参数抛错类型不一致 | `core/parser.py:92-107` | B5 |
| P2-1..8 | 低 | JSON 工具三处重复；react 跨模块私有导入；activity_log 空实现与 README 矛盾；每 chunk 全量重载实体；support.py 注释与实现相反；根 README 缺 web 章节；打包缺数据文件；ruff 基线 97 项 | 见阶段 C | C1–C8 |

---

## 目标与验收标准

修复上表全部 P0/P1 问题与 P2 可维护性问题。验收标准：

- 每个 P0 缺陷有回归测试证明已修复（Agent 链路调用记忆检索返回 `ok:true`；supersede 后单值槽只剩一个 active 值；同一三元组只落一条记录；越界 `source` 被拒绝）。
- `python -m pytest -q` 全绿；`python -m ruff check .` 零输出。
- README 与实现一致；`python -m web.app` 冒烟通过。
- 遵守项目自身协议（`update_log_readme_first.md`：update_log → 索引更新 → 提交推送）。

不改变项目既有约定：permissions 仍为元数据不设权限门禁；工具仍走"单文件 + `TOOL_ENABLED` + `create_tool()`"发现协议；`side_effect` 仍为 ToolSpec 静态契约（不引入按 action 判定副作用的运行时改动）。

---

## 阶段 A：P0 功能正确性（4 项）

### A1. 记忆工具只读动作被副作用确认拦截（最严重）

**问题与实证**：`memory.manage` 与 `memory.rag` 整体标为 `side_effect="write"`，而 search/get/list、retrieve/context/graph_* 均为只读。运行时对非 read 工具强制 `ExecutionContext.confirmed_side_effects`，但 `agents/`、`web/` 无任何地方填充（全仓库只有测试填充）。实测：catalog resolve 成功后真实调用返回 `CONFIRMATION_REQUIRED`。`web/support.py:158-169` 系统提示词第 1 条要求先调 `memory.rag`/`memory.manage` 检索、第 4 条要求用 add 记事——全部失效，模型只能凭记忆回答。

**修法：按读/写拆分工具 + web 层只为"增量写"提供确认**

1. 新建 `tool/memory_query.py` — `memory.query` v1.0.0（`side_effect="read"`）：action 为 `Literal["search","get","list"]`，输入/输出模型从现 `MemoryToolInput/Output` 拆出；构造器 `manager: MemoryManager | None = None` 懒创建（沿用现懒加载模式）。
2. 新建 `tool/memory_add.py` — `memory.add` v1.0.0（`side_effect="write"`、`idempotent=False`、`parallel_safe=False`）：只保留 add 动作字段（content/memory_type/metadata/importance/ttl_seconds/item_id）。
3. 改 `tool/memory_tool.py` — `memory.manage` v2.0.0：action 收窄为 `Literal["delete","clear"]`（管理员语义，Web 聊天不自动确认）。
4. 新建 `tool/rag_search.py` — `memory.rag_search` v1.0.0（`side_effect="read"`）：action 为 `Literal["retrieve","context","graph_retrieve","graph_context"]`；构造器 `pipeline: RAGPipeline | None` 懒创建（复用 `rag_tool.py:67-87` 的降级逻辑，抽成共享小函数）。
5. 改 `tool/rag_tool.py` — `memory.rag` v2.0.0：只保留 ingest。
6. 改 `web/support.py`：
   - `SYSTEM_PROMPT`（158-169 行）规则 1 改为"先用 `memory.rag_search` 的 graph_retrieve/context 检索，再用 `memory.query` 的 search 补充"；规则 4 改为"用 `memory.add` 写入 episodic"。
   - `get_agent()`（185-199 行）在 `RAGTool(pipeline=get_pipeline())` 之外，再以 `replace=True` 注册 `MemoryQueryTool(manager=get_manager())`、`MemoryAddTool(manager=get_manager())`、`RagSearchTool(pipeline=get_pipeline())`，共享 Web 单例后端。
7. 改 `web/app.py` `/api/chat`（168-185 行）：构造 `ExecutionContext(confirmed_side_effects=frozenset({agent.tools.confirmation_key("memory.add")}))`，改为 `agent.run(body.message, tool_names=tool_names, context=ctx)`（`ReActAgent.run(query, **kwargs)` 已透传给 `run_with_react` 的 `context` 形参，见 `agents/react.py:642,693`）。只确认 `memory.add`：即使提示词注入，模型也只能"记住"，不能 delete/clear/ingest。
8. 同步文档与测试中的工具名：`README.md:110`、`memory/README.md:130`、`web/README.md:50`、`tests/test_web_api.py:197-198,241,295`、`tests/test_react.py:916`。

### A2. supersede 只退役一个旧值

**问题与实证**：`knowledge.py:534-557` 的 `known_items` 用 `setdefault` 每个 (subject,predicate) 槽只缓存一条，`facts_for` 命中即 `return [cached]`。实测槽内两个 active 值经 supersede 后仍剩一个旧值 active，违背 506 行 docstring 与系统提示词"只采信当前有效值"。

**修法**：
- `known_items` 改为 `dict[str, list[MemoryItem]]`；`facts_for(subject, predicate)` 返回该槽**全部 active** 项。
- cache miss 时不再按原始字符串相等过滤，而是对 `manager.semantic.facts()` 做一次全量扫描、按规范化键把**所有**槽补进索引（后续 miss 直接命中），再返回目标槽。
- 624-649 行 retire 循环每执行一次 `add_fact(..., item_id=stale.id)` 后，用返回的新 item 替换缓存中的旧对象（否则同一 chunk 内后续 candidate 会看到陈旧的 `active=True`）；670-679 行写入新事实后同样把返回 item 并入缓存（`stale.id == relation_id: continue` 守卫已能跳过自身）。

### A3. 同一三元组两套主键导致重复事实/边

**问题与实证**：`semantic.py:47-49` 默认 id 为 `fact:{s}|{p}|{o}`（`/api/facts`、`web/seed.py:63`、`web/app.py:408` 走此路径），抽取链路用 `relation_id_for` 哈希 id。实测同一三元组落两条记录（`fact:Alice|余额|1元` 与 `relation:2baa…`）；`web/app.py:370-407` 的手工三元组去重正是此问题的局部补丁。

**修法**：
- 新建 `memory/ids.py`（叶子模块，仅依赖 hashlib/re）：把 `normalize_entity_name`、`entity_id_for`、`relation_id_for`、`predicate_key_for`、`_clean_text` 从 `knowledge.py` 迁入；`knowledge.py` 改 `from ..ids import ...` 并 re-export 保持兼容。（不能让 semantic.py 直接 import knowledge.py——会经 manager.py 形成环。）
- `semantic.py` `add_fact` 默认 id 改为 `relation_id_for(subject, predicate, object)`；并做存量兼容：canonical id 在 `document_store` 未命中时，回查旧 id `f"fact:{subject.strip()}|{predicate.strip()}|{object.strip()}"`，命中则沿用旧记录原 id 更新（老库不产生新重复）。
- `/api/import` 的三元组去重保留（对旧导出文件仍有效），不需改动。

### A4. `DocumentProcessor.parse` 无沙箱 + 字符串误判为路径

**问题与实证**：`document.py:34-46` 对任何"能当路径打开"的字符串直接 `read_bytes()`。实测能读工作区外任意文件（`memory.rag` 的 `source` 由模型提供，等于绕开 `fs.*` 工具的沙箱）；普通短文本恰好与现存文件同名也会被当文件读。

**修法**：
- `document.py` `parse` 语义收窄：`str` → 一律纯文本；`Path`/`os.PathLike` → 文件；**删除** `Path(source).exists()` 探测。新增模块内小函数 `_resolve_within(base_dir, path)`（resolve + 前缀包含检查，参照 `tool/_shared.py:50-87`，但 memory 层不依赖 tool 包）。
- `pipeline.py:135-136` `ingest_source(source, *, base_dir=None)`：`str` 先转 `Path`；`base_dir` 提供时经 `_resolve_within` 强制包含，越界抛 `ValueError`（带可读信息）。
- `tool/rag_tool.py:99` ingest 分支：`from ._shared import resolve_path, workspace_root`，先 `resolve_path(workspace_root(), arguments.source)` 再 `ingest_source(resolved_path)`——与 fs 工具同一套约束与报错口径。
- `web/app.py:249` `/api/ingest` 传 `tmp_path`（服务端自建的受信文件，`base_dir=None` 不变）；`tests/test_memory_rag.py:201` 传 `str(path)` 的用法由 `ingest_source` 内部转 `Path` 保持兼容。

---

## 阶段 B：P1 健壮性（5 项）

### B1. `MemoryItem` 默认 id 引用未导入的 `uuid`
`memory/base.py:107` 改为 `str(uuid4())`（`uuid4` 已在第 20 行导入）；新增直接构造测试。这是 ruff F821，实测 `MemoryItem("hello")` 抛 `NameError`（内部调用都显式传 id，故被掩盖）。

### B2. 离线默认不可用 + 文档不符 + 启动全量重建索引
- `memory/embedding.py` 新增 `HashEmbedding(BaseEmbedding)`（从 `web/support.py:52-73` 迁入，dimension 参数化）。
- `memory/base.py:60-75` `make_default_embedding`：`config.embedding_api_key` 为空（`from_env` 已回退 `DASHSCOPE_API_KEY`，见 base.py:309-310）→ 返回 `HashEmbedding(dimension=config.embedding_dimension)`；否则 `APIEmbedding`。
- `memory/manager.py:68-69`：启动重建索引仅当 `isinstance(self.vector_store, InMemoryVectorStore)`（顺带解决 Qdrant 后端每次启动全量重灌的问题）。
- `web/support.py` 删除本地 `HashEmbedding`，`build_embedding` 改为薄包装；`web/app.py:436` `/api/health` 的 `embedding_mode` 改报实际类名（`type(app.state.manager.embedding).__name__ == "APIEmbedding"` → `"api"`，否则 `"local-hash"`），顺带修掉"只看 `DASHSCOPE_API_KEY`、忽略 `HELLOAGENTS_MEMORY_EMBEDDING_API_KEY`"的问题。
- `tests/conftest.py:22` 测试版 `HashEmbedding` 改为继承 `memory.HashEmbedding`（保留 128 维差异）。
- `tool/rag_tool.py:81-82` 的 `except Exception: pass` 加 `LOGGER.warning(..., exc_info=True)`（保留降级行为，顺带消除 ruff S110/BLE001）。
- 文档：`README.md:109-110` 删去"local-transformer/TF-IDF"的不实描述，写明"APIEmbedding（OpenAI 兼容）+ 离线 HashEmbedding 降级"；`memory/README.md` 同步，并注明两种嵌入向量空间不兼容、中途换 key 需重建索引。

### B3. `/api/import` 无大小限制且静默吞错
`web/app.py:358-428`：把 `/api/ingest:226-247` 的分块读取 + `MAX_UPLOAD_BYTES` → 413 逻辑抽成共享函数 `_save_upload(file) -> Path` 供两个端点使用；导入循环内 `except Exception` 改为记录 `f"{item_id}: {type(exc).__name__}: {exc}"` 到 `errors` 列表（截取前 20 条）随响应返回。

### B4. fs 写/编辑工具的并发与幂等标注失真
`tool/fs_write_text.py:104-105` 与 `tool/fs_edit_text.py:107-108`：`parallel_safe=False`（运行时会把此类调用隔离进单调用层，消除同文件读-改-写丢更新）；`fs.edit_text` 另改 `idempotent=False`（超时重试会因 old_string 已被替换而报错，不符合幂等语义）；两者 `version` 升 1.1.0（模板要求可观察元数据变更必须升版本）。同步更新 `tests/test_fs_write_text.py` / `test_fs_edit_text.py` 中对 spec 字段的断言（若有）。

### B5. 非有限 JSON 常量抛错类型不一致
`core/parser.py:92-107` 三处 `except json.JSONDecodeError` 改为 `except (json.JSONDecodeError, ValueError)`，使 `{"a": NaN}` 这类载荷最终统一以 `JSONDecodeError("invalid JSON arguments")` 结束（现被 `_reject_json_constant` 的裸 `ValueError` 逃逸）；审查 `tool/search.py:305-315` 的同类调用并保持口径一致。`agents/react.py:249-279` 已捕获 `ValueError`，无需改。

---

## 阶段 C：P2 可维护性 / 文档 / 打包

- **C1 JSON 容错工具去重（三处重复）**：`core/parser.py` 将 `_load_json/_balanced_json_substring/_repair_single_quoted_object/_reject_json_constant` 升为公开 API（`loads_model_json` 等）；`agents/react.py:249-340` 的 `_decode_action_input` 改调 `loads_model_json`（保留"结果必须是 dict，否则返回纠错提示"的语义）；`tool/search.py:435` 改导入。注意两版 `_balanced_json_substring` 的差异（core 版额外支持 `[...]`）——统一用 core 超集，靠全量测试确认无回归。
- **C2 react.py 跨模块私有导入**：新建 `agents/message_utils.py`，把 `agent.py` 中的 `_field/_message_dict/_result_json/_safe_tool_call_error/_safe_tool_name` 公共化迁入；`agent.py` 内部改引用并保留私有别名，`react.py:53-60` 改导入公共名。
- **C3 activity_log 空实现与 README 矛盾**：保留静默实现（是有意为之），改写 `README.md:26-35` "Runtime logs" 一节，如实描述当前输出（Thought、工具名与耗时、模型耗时、协议告警）。
- **C4 每次抽取全量重载实体（性能）**：`knowledge.py` `build_graph_context`（694 行）增加 `resolver: EntityResolver | None = None` 参数；`pipeline.py:105` 调用时传入 ingest 级 resolver（`pipeline.py:90` 已建），消除每 chunk 一次的全库实体扫描。`facts()` 的多次全表扫描本轮不动（正确性优先），在代码注释标注为后续优化点。
- **C5 support.py 注释与实现相反**：`web/support.py:44-45` 注释改为"环境变量优先于 .env（load_dotenv 默认 override=False）"，行为不动。
- **C6 根 README 补全**：新增「知识星云 Web 应用」章节（`.venv\Scripts\python.exe -m web.app` → http://127.0.0.1:8765、功能一览、链接 `web/README.md`）；修正 `README.md:17` 技能描述为扁平 `skills/<name>.md`（旧目录布局会被报错）；随 A1 更新工具名。
- **C7 打包缺数据文件**：`pyproject.toml` 增加 `[tool.setuptools.package-data]`，`web = ["static/*", "seed_data.json"]`；README 说明 `skills/`、`config/` 按仓库根运行（技能根与 provider 配置路径均相对项目根解析），不做 wheel 内嵌。
- **C8 ruff 基线（现 97 项）**：`pyproject.toml` 增加 `[tool.ruff]`（line-length 100，select 至少 E/F/I/UP/B/SIM，豁免项显式列出）；先 `ruff check --fix` 清掉 69 项可自动修复（I001/UP035/UP017/UP037/F401/RUF100/RUF022）；剩余人工处理：DTZ005（`web/app.py:350` 加 `tz=`）、TRY004（逐个判断：改 `TypeError` 需同步测试断言，否则 `# noqa: TRY004` 保留语义）、BLE001（逐个加日志或 noqa+理由）、PYI034/036（dunder 注解）、RUF012（`tests/test_embedding_http.py:24` 加 ClassVar）、FURB162、ISC004（加括号）。目标 `ruff check .` 零输出。
- **C9 记录不做处理的事项**：`verify_hit.js` 消失事件如实向用户报告，不纳入本规划；`update_log.sqlite3` 入库策略遵循项目既有协议不动。

---

## 测试计划（新增/修改清单）

| 文件 | 用例 |
|---|---|
| `tests/test_memory.py` | `MemoryItem("x")` 默认 id 为合法 UUID；无任何 key 时 `MemoryManager()` 构造成功且 embedding 为 HashEmbedding |
| `tests/test_knowledge_patch.py` | 槽内两个 active 值 → supersede 后仅新值 active；同一三元组经 `add_fact`（不传 id）与 `materialize_extraction` 两条路径 → 仅一条记录/一条边；`build_graph_context` 复用传入 resolver（可用计数桩验证只加载一次） |
| `tests/test_memory_rag.py` | `parse("同名文件名")` 恒按文本处理（预先创建同名文件）；`ingest_source(path, base_dir=...)` 越界抛 ValueError；`str` 路径仍可用（兼容 201 行现有用例） |
| 新 `tests/test_memory_tools.py` | `memory.query`/`memory.rag_search` 无确认执行 `ok:true`；`memory.add`/`memory.manage`/`memory.rag` 无确认被拒、经 `registry.confirmation_key` 确认后通过（复用 `tests/test_p0_integration.py:88-108` 的 confirmer 模式） |
| `tests/test_web_api.py` | `/api/import` 超限 → 413、响应含 `errors`；`/api/chat` 把带确认的 `context` 传给 agent（fake agent 断言 kwargs）；`/api/health` 按实际嵌入类报告；现有 197/241/295 行用例改用新工具名 |
| `tests/test_parser.py` | `NaN`/`Infinity` 参数经 `parse_openai_tool_calls` 归一为 "invalid JSON arguments" 错误 |
| `tests/test_fs_*.py` | spec 元数据断言更新（parallel_safe/idempotent/version） |
| 全量 | `python -m pytest -q` 与 `python -m ruff check .` 双绿 |

审查期间用于复现问题的 6 组探针（catalog resolve → CONFIRMATION_REQUIRED、supersede 残留、双主键、路径逃逸、MemoryItem NameError、离线默认崩溃）全部转成上述回归用例。

---

## 执行顺序、验证与收尾

1. **A 阶段**（A1 → A2 → A3 → A4，A1 触点最多放最前）→ 全量测试 + 新增回归用例全绿。
2. **B 阶段**（B1 → B5）→ 同上。
3. **C 阶段**（C1 → C8，纯重构/文档项放后）→ ruff 与测试双绿。
4. **手工冒烟**：`.venv\Scripts\python.exe -m web.app` → 验证 `/api/health`、`/api/graph`；配置真实 provider 后走 `/api/chat`，确认模型能成功调用 `memory.rag_search`/`memory.query`（`ok:true`，答案带来源），"记住某事"经 `memory.add` 落库。
5. **项目协议收尾**（`update_log_readme_first.md`）：调用 `system.update_log` 写入 ID 77（需按协议提供写确认；或以相同字段经 `core.update_log.UpdateLogRepository.append` 等价写入并在记录中注明）→ 用返回的 `next_update_id` 更新导引索引（76→77 已写入，索引更新为 77→78）→ git 提交，格式 `update-log-77: <简短标题>`，必须包含 `update_log.sqlite3`、导引更新与本规划文档 `代码审查与修复规划.md` → 推送远端。

---

## 风险与回滚

- **A3 主键变更**：存量库靠 legacy-id 回查兜底；若历史数据已存在双记录，属遗留数据问题，本轮不清洗（可选后续：一次性合并脚本）。
- **A1 工具拆分**：提示词、注册、测试、文档四处必须同步，缺一处即行为回退——触点已在 A1 第 8 条列全。
- **B2 离线哈希默认**：哈希与 API 向量空间不兼容，已在文档注明；中途切换 key 的索引重建脚本列为可选后续。
- **B4** `parallel_safe=False` 使批量写串行化，吞吐略降——正确性优先，可接受。
- 每阶段独立提交，可单独 `git revert` 回滚。

---

## 明确不做（控制范围）

- 不引入 permissions 权限门禁（项目定位为兼容性元数据）。
- 不重构 `agent.py`/`react.py` 整体结构（仅 C1/C2 最小去重）；不动 `agents/llm.py` 双流式路径（另立后续）。
- 不改 update_log 协议、`.gitignore` 策略与 sqlite 入库方式。
- 不做 skills/config 的 wheel 打包（以文档说明运行方式替代）。

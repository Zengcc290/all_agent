# AI 必读：更新日志导引

> 本文件只保存规则和极少量索引，不保存详细历史内容。详细更新记录位于 SQLite 数据库 `update_log.sqlite3`，每次修改一行、一个递增 ID，因此不会随着项目迭代把全部历史塞进 AI 上下文。

## 当前索引

- **日志数据库**：`update_log.sqlite3`
- **数据库表**：`update_logs`
- **已写入的最后更新 ID**：`47`
- **下一次必须写入的更新 ID**：`48`
- **写入工具**：`system.update_log`（实现：`tool/update_log.py`）

## 所有 AI 必须遵守的协议

1. **开始任何项目修改前，先阅读本文件。** 只需阅读本导引，不要把 SQLite 历史记录全部加载到上下文；只有审计或排查特定问题时才按 ID 查询所需记录。
2. **完成任何实际修改后，主动调用 `system.update_log` 一次。** 代码、配置、依赖、接口、数据结构、脚本、资源、文档或行为发生变化都算实际修改。
3. 调用工具时必须如实提供完整信息：执行者、更新类型、标题、任务背景、具体实现细节、新增功能、每个涉及文件、兼容性/行为影响、实际验证结果、风险/回滚说明和后续事项。
4. **不要手写 SQL、直接改数据库或伪造 ID。** 工具会在事务中生成下一个 ID（从 1 开始，SQLite `AUTOINCREMENT`），并自动记录实际 UTC 时间和当前操作系统名。
5. 工具成功后只返回本次 `update_id`、`next_update_id` 等简短确认，不返回历史正文。将返回的 `next_update_id` 更新到本文件“当前索引”中；这次索引更新属于刚刚记录的同一任务，不需要再次记录。
6. 如果工具调用失败，**不得假装完成**：当前 `database.write` 仅为元数据，不会导致工具不可见；请提供该工具当前注册代次的写入确认后重试，或者向用户报告日志阻塞原因。
7. 日志是交付的一部分。没有成功写入日志，不能向用户声称任务已完成。仅查看、分析、解释且没有任何文件或外部状态变化时无需记录。
8. 数据库记录只追加，不删除、不覆盖、不重排。发现历史错误时追加一条“日志更正”，保留原记录。

## 完成后的远端同步（强制）

凡是产生了实际项目修改的任务，只有在以下流程全部成功后才算完成：

1. 先按上面的协议成功调用一次 `system.update_log`。
2. 用工具返回的 `next_update_id` 更新本文件的当前索引。
3. 检查工作区，提交本次任务的全部相关变更；提交内容必须包含 `update_log.sqlite3`，以及记录本次日志所需的 `update_log_readme_first.md` 索引更新，不得把日志数据库加入忽略或漏提交。Git 提交名称（commit message）必须使用本次工具返回的最后更新编号，统一格式为 `update-log-<update_id>: <简短标题>`（例如 `update-log-23: 修复运行时校验`），不得使用下一次编号或自行编造编号。
4. 将该提交推送到当前分支已配置的远端（通常是 `origin`）。如当前分支尚未设置上游，先设置正确的上游后再推送。

提交或推送任一步失败，都必须如实报告失败原因并继续处理或请求用户介入；在远端同步成功前，不得向用户声称任务已完成。只读、分析或解释且没有任何文件/外部状态变化的请求不适用本节。

## 工具调用要点

`system.update_log` 是自动发现的单文件工具，具有以下固定约束：

- `side_effect="write"`、单并发，专门追加日志，不能执行其他业务；权限字段不参与访问控制，但写入确认仍是必需的。
- `system_name`、时间和 ID 由工具/数据库生成；AI 不应自行编造这些值。
- `files` 至少包含一项，每项包含 `path`、`action`（added/modified/deleted/renamed/generated 等）和具体 `description`。
- 没有新增功能、风险或后续事项时，填写明确的 `none`（或中文“无”），不要省略字段。
- 详细字段和长度约束以工具的 Pydantic Schema 为准；不要传入未声明字段。

标准调用顺序：

```text
阅读本导引
  -> 修改项目
  -> 运行适当验证
  -> 调用 system.update_log（一次）
  -> 用返回的 next_update_id 更新本文件索引
  -> 提交全部变更（必须包含 update_log.sqlite3）
  -> 推送到当前分支的远端
  -> 向用户报告完成
```

## 数据库字段

每条记录包含：`update_id`、`timestamp`、`system_name`、`executor`、`update_type`、`title`、`task_background`、`update_details`、`added_features`、`files`、`behavior_impact`、`validation`、`risks`、`follow_up`。

需要审计时，使用项目的 `core.update_log.UpdateLogRepository` 按 ID 查询单条记录；不要把整张表读入 AI 上下文。数据库文件可备份，删除或移动它会破坏连续索引，应先获得用户明确授权。

## 索引维护规则

- “已写入的最后更新 ID”必须等于工具返回的 `update_id`。
- “下一次必须写入的更新 ID”必须等于工具返回的 `next_update_id`。
- 若数据库实际 ID 与本文件不一致，以数据库为准，并追加一条日志更正后修复索引。
- 修改本文件的索引时，不要改动本文件其他规则，也不要复制数据库历史正文。
